// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Curry = require("rescript/lib/js/curry.js");
var Js_exn = require("rescript/lib/js/js_exn.js");
var Assert = require("assert");
var Core__Option = require("@rescript/core/lib/js/src/Core__Option.bs.js");
var Caml_js_exceptions = require("rescript/lib/js/caml_js_exceptions.js");
var Util$AgdaModeVscode = require("../../../src/Util/Util.bs.js");
var Connection__Transport__Process$AgdaModeVscode = require("../../../src/Connection/Transport/Connection__Transport__Process.bs.js");

describe("Process Interface", (function () {
        describe("Use `echo` as the testing subject", (function () {
                it.skip("should trigger `close`", (async function () {
                        var $$process = Connection__Transport__Process$AgdaModeVscode.make(undefined, "echo", ["hello"]);
                        var match = Util$AgdaModeVscode.Promise_.pending();
                        var reject = match[2];
                        var resolve = match[1];
                        var destructor = Connection__Transport__Process$AgdaModeVscode.onOutput($$process, (function (output) {
                                switch (output.TAG) {
                                  case "Stdout" :
                                      var output$1 = output._0;
                                      switch (output$1) {
                                        case "hello\n" :
                                        case "hello\r\n" :
                                            return ;
                                        default:
                                          return reject(Js_exn.raiseError("wrong output: " + output$1));
                                      }
                                  case "Stderr" :
                                      return resolve(Js_exn.raiseError("Stderr: " + output._0));
                                  case "Event" :
                                      var $$event = output._0;
                                      if (typeof $$event === "object" && $$event.TAG !== "OnError" && $$event._0 === 0) {
                                        return resolve();
                                      }
                                      return resolve(Js_exn.raiseError("Event: " + Connection__Transport__Process$AgdaModeVscode.$$Event.toString($$event)));
                                  
                                }
                              }));
                        await match[0];
                        return destructor();
                      }));
              }));
        describe("Use a non-existing command as the testing subject", (function () {
                it("should trigger receive something from stderr", (async function () {
                        var $$process = Connection__Transport__Process$AgdaModeVscode.make(undefined, "echooo", ["hello"]);
                        var match = Util$AgdaModeVscode.Promise_.pending();
                        var reject = match[2];
                        var resolve = match[1];
                        var destructor = Connection__Transport__Process$AgdaModeVscode.onOutput($$process, (function (output) {
                                switch (output.TAG) {
                                  case "Stdout" :
                                      return reject(Js_exn.raiseError("wrong output: " + output._0));
                                  case "Stderr" :
                                      return resolve();
                                  case "Event" :
                                      return reject(Js_exn.raiseError("Event: " + Connection__Transport__Process$AgdaModeVscode.$$Event.toString(output._0)));
                                  
                                }
                              }));
                        await match[0];
                        return destructor();
                      }));
              }));
        it("destroy should wait for process close before resolving", (async function () {
                var restoreSpawn = ((() => {
        const cp = require("node:child_process");
        const originalSpawn = cp.spawn;
        globalThis.__agdaModeProcessCloseFired = false;

        cp.spawn = function () {
          const handlers = {};
          const mkStream = () => ({
            on: function (_event, _cb) {
              return this;
            },
          });

          return {
            stdout: mkStream(),
            stderr: mkStream(),
            stdin: {
              write: function () {
                return true;
              },
            },
            pid: 424242,
            on: function (event, cb) {
              handlers[event] = cb;
              return this;
            },
            kill: function (_signal) {
              setTimeout(() => {
                globalThis.__agdaModeProcessCloseFired = true;
                if (handlers["close"]) {
                  handlers["close"](137);
                }
              }, 50);
              return true;
            },
          };
        };

        return () => {
          cp.spawn = originalSpawn;
          delete globalThis.__agdaModeProcessCloseFired;
        };
      })());
                var closeFiredWhenDestroyResolved = {
                  contents: false
                };
                var error;
                try {
                  await (async function () {
                        var $$process = Connection__Transport__Process$AgdaModeVscode.make(false, "fake-process", []);
                        await Connection__Transport__Process$AgdaModeVscode.destroy($$process);
                        closeFiredWhenDestroyResolved.contents = (globalThis.__agdaModeProcessCloseFired === true);
                      })();
                }
                catch (raw_exn){
                  var exn = Caml_js_exceptions.internalToOCamlException(raw_exn);
                  error = exn;
                }
                restoreSpawn();
                Core__Option.forEach(error, (function (exn) {
                        throw exn;
                      }));
                return Curry._3(Assert.deepStrictEqual, closeFiredWhenDestroyResolved.contents, true, undefined);
              }));
      }));

var Process;

exports.Process = Process;
/*  Not a pure module */
