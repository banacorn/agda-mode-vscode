// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Curry = require("rescript/lib/js/curry.js");
var Assert = require("assert");
var Vscode = require("vscode");
var Nodefs = require("node:fs");
var Nodeos = require("node:os");
var Process = require("process");
var Nodepath = require("node:path");
var Core__Option = require("@rescript/core/lib/js/src/Core__Option.bs.js");
var Log$AgdaModeVscode = require("../../../src/State/Log.bs.js");
var Chan$AgdaModeVscode = require("../../../src/Util/Chan.bs.js");
var Mock$AgdaModeVscode = require("../../../src/Main/Mock.bs.js");
var State$AgdaModeVscode = require("../../../src/State/State.bs.js");
var Config$AgdaModeVscode = require("../../../src/Config.bs.js");
var Desktop$AgdaModeVscode = require("../../../src/Main/Desktop.bs.js");
var Memento$AgdaModeVscode = require("../../../src/Memento.bs.js");
var Connection$AgdaModeVscode = require("../../../src/Connection/Connection.bs.js");
var Test__Util$AgdaModeVscode = require("../Test__Util.bs.js");
var Registry__Connection$AgdaModeVscode = require("../../../src/Registry__Connection.bs.js");
var State__SwitchVersion$AgdaModeVscode = require("../../../src/State/State__SwitchVersion.bs.js");

describe("Config.Connection paths", (function () {
        this.retries(2);
        var userAgda = {
          contents: ""
        };
        var systemAgda = {
          contents: ""
        };
        var brokenAgda = {
          contents: "/broken/agda"
        };
        var alternativeAgda = {
          contents: ""
        };
        var downloadedALS = {
          contents: ""
        };
        var logChannel = Chan$AgdaModeVscode.make();
        before(async function () {
              userAgda.contents = await Test__Util$AgdaModeVscode.Candidate.Agda.mock("2.7.0.1", "agda-mock-user");
              systemAgda.contents = await Test__Util$AgdaModeVscode.Candidate.Agda.mock("2.7.0.1", "agda-mock-system");
              alternativeAgda.contents = await Test__Util$AgdaModeVscode.Candidate.Agda.mock("2.7.0.1", "agda-mock-alt");
              downloadedALS.contents = await Test__Util$AgdaModeVscode.Candidate.ALS.mock("1.3.1", "2.6.3", "als-make-mock");
            });
        after(async function () {
              await Test__Util$AgdaModeVscode.Candidate.Agda.destroy(userAgda.contents);
              await Test__Util$AgdaModeVscode.Candidate.Agda.destroy(systemAgda.contents);
              await Test__Util$AgdaModeVscode.Candidate.Agda.destroy(alternativeAgda.contents);
              return await Test__Util$AgdaModeVscode.Candidate.ALS.destroy(downloadedALS.contents);
            });
        beforeEach(function () {
              Registry__Connection$AgdaModeVscode.status.contents = "Empty";
            });
        var findCommand = function (_command, _timeoutOpt) {
          return Promise.resolve({
                      TAG: "Ok",
                      _0: systemAgda.contents
                    });
        };
        var platformWithDiscovery_determinePlatform = Desktop$AgdaModeVscode.Desktop.determinePlatform;
        var platformWithDiscovery_alreadyDownloaded = Desktop$AgdaModeVscode.Desktop.alreadyDownloaded;
        var platformWithDiscovery_resolveDownloadChannel = Desktop$AgdaModeVscode.Desktop.resolveDownloadChannel;
        var platformWithDiscovery_download = Desktop$AgdaModeVscode.Desktop.download;
        var platformWithDiscovery_askUserAboutDownloadPolicy = Desktop$AgdaModeVscode.Desktop.askUserAboutDownloadPolicy;
        var platformWithDiscovery = {
          determinePlatform: platformWithDiscovery_determinePlatform,
          findCommand: findCommand,
          alreadyDownloaded: platformWithDiscovery_alreadyDownloaded,
          resolveDownloadChannel: platformWithDiscovery_resolveDownloadChannel,
          download: platformWithDiscovery_download,
          askUserAboutDownloadPolicy: platformWithDiscovery_askUserAboutDownloadPolicy
        };
        var findCommand$1 = function (_command, _timeoutOpt) {
          return Promise.resolve({
                      TAG: "Error",
                      _0: "NotFound"
                    });
        };
        var platformNoDiscovery_determinePlatform = Desktop$AgdaModeVscode.Desktop.determinePlatform;
        var platformNoDiscovery_alreadyDownloaded = Desktop$AgdaModeVscode.Desktop.alreadyDownloaded;
        var platformNoDiscovery_resolveDownloadChannel = Desktop$AgdaModeVscode.Desktop.resolveDownloadChannel;
        var platformNoDiscovery_download = Desktop$AgdaModeVscode.Desktop.download;
        var platformNoDiscovery_askUserAboutDownloadPolicy = Desktop$AgdaModeVscode.Desktop.askUserAboutDownloadPolicy;
        var platformNoDiscovery = {
          determinePlatform: platformNoDiscovery_determinePlatform,
          findCommand: findCommand$1,
          alreadyDownloaded: platformNoDiscovery_alreadyDownloaded,
          resolveDownloadChannel: platformNoDiscovery_resolveDownloadChannel,
          download: platformNoDiscovery_download,
          askUserAboutDownloadPolicy: platformNoDiscovery_askUserAboutDownloadPolicy
        };
        var makeConnection = async function (configPaths, platform) {
          var memento = Memento$AgdaModeVscode.make(undefined);
          await Config$AgdaModeVscode.Connection.setAgdaPaths(logChannel, configPaths);
          var listener = Log$AgdaModeVscode.collect(logChannel);
          var result = await Connection$AgdaModeVscode.makeWithFallback(platform, memento, Vscode.Uri.file("/tmp/test"), configPaths, ["agda"], logChannel);
          var logs = listener(Log$AgdaModeVscode.isConfig);
          return [
                  logs,
                  result
                ];
        };
        describe("Working config paths", (function () {
                it("should not modify config with single working path", (async function () {
                        var configPaths = [userAgda.contents];
                        var match = await makeConnection(configPaths, platformWithDiscovery);
                        var result = match[1];
                        Curry._3(Assert.deepStrictEqual, match[0], [], undefined);
                        Curry._3(Assert.deepStrictEqual, Config$AgdaModeVscode.Connection.getAgdaPaths(), configPaths, undefined);
                        if (result.TAG === "Ok") {
                          return Curry._3(Assert.deepStrictEqual, Connection$AgdaModeVscode.getPath(result._0), userAgda.contents, undefined);
                        }
                        Assert.fail("Connection should succeed");
                      }));
                it("should not modify config with multiple working paths", (async function () {
                        var configPaths = [
                          userAgda.contents,
                          alternativeAgda.contents
                        ];
                        var match = await makeConnection(configPaths, platformWithDiscovery);
                        var result = match[1];
                        Curry._3(Assert.deepStrictEqual, match[0], [], undefined);
                        Curry._3(Assert.deepStrictEqual, Config$AgdaModeVscode.Connection.getAgdaPaths(), configPaths, undefined);
                        if (result.TAG === "Ok") {
                          return Curry._3(Assert.deepStrictEqual, Connection$AgdaModeVscode.getPath(result._0), alternativeAgda.contents, undefined);
                        }
                        Assert.fail("Connection should succeed");
                      }));
                it("should not modify config with mixed working/broken paths", (async function () {
                        var configPaths = [
                          brokenAgda.contents,
                          userAgda.contents,
                          "/another/broken"
                        ];
                        var match = await makeConnection(configPaths, platformWithDiscovery);
                        var result = match[1];
                        Curry._3(Assert.deepStrictEqual, match[0], [], undefined);
                        Curry._3(Assert.deepStrictEqual, Config$AgdaModeVscode.Connection.getAgdaPaths(), configPaths, undefined);
                        if (result.TAG === "Ok") {
                          return Curry._3(Assert.deepStrictEqual, Connection$AgdaModeVscode.getPath(result._0), userAgda.contents, undefined);
                        }
                        Assert.fail("Connection should succeed");
                      }));
              }));
        describe("Bare command in config paths (#277)", (function () {
                it("should resolve bare command via PATH lookup", (async function () {
                        var result = await Connection$AgdaModeVscode.fromPathsOrCommands(platformWithDiscovery, [[
                                "agda",
                                "FromConfig"
                              ]]);
                        if (result.TAG === "Ok") {
                          return Curry._3(Assert.deepStrictEqual, Connection$AgdaModeVscode.getPath(result._0), systemAgda.contents, undefined);
                        }
                        Assert.fail("fromPathsOrCommands should succeed with bare command 'agda'");
                      }));
                it("should fail when bare command is not found in PATH", (async function () {
                        var result = await Connection$AgdaModeVscode.fromPathsOrCommands(platformNoDiscovery, [[
                                "agda",
                                "FromConfig"
                              ]]);
                        if (result.TAG !== "Ok") {
                          return ;
                        }
                        Assert.fail("should fail when bare command is not in PATH");
                      }));
                it("should still resolve absolute paths directly", (async function () {
                        var result = await Connection$AgdaModeVscode.fromPathsOrCommands(platformNoDiscovery, [[
                                userAgda.contents,
                                "FromConfig"
                              ]]);
                        if (result.TAG === "Ok") {
                          return Curry._3(Assert.deepStrictEqual, Connection$AgdaModeVscode.getPath(result._0), userAgda.contents, undefined);
                        }
                        Assert.fail("absolute paths should work without PATH lookup");
                      }));
              }));
        describe("Broken config paths", (function () {
                it("should fail without modifying config when all config paths are broken and auto discovery fails", (async function () {
                        await Config$AgdaModeVscode.Connection.DownloadPolicy.set("No");
                        var configPaths = [
                          brokenAgda.contents,
                          "/another/broken"
                        ];
                        var match = await makeConnection(configPaths, platformNoDiscovery);
                        Curry._3(Assert.deepStrictEqual, match[0], [], undefined);
                        Curry._3(Assert.deepStrictEqual, Config$AgdaModeVscode.Connection.getAgdaPaths(), configPaths, undefined);
                        if (match[1].TAG !== "Ok") {
                          return ;
                        }
                        Assert.fail("Connection should fail when all paths are broken and no discovery");
                      }));
              }));
        describe("Empty config paths", (function () {
                it("should fail without modifying config when config is empty and auto discovery fails", (async function () {
                        await Config$AgdaModeVscode.Connection.DownloadPolicy.set("No");
                        var configPaths = [];
                        var match = await makeConnection(configPaths, platformNoDiscovery);
                        Curry._3(Assert.deepStrictEqual, match[0], [], undefined);
                        Curry._3(Assert.deepStrictEqual, Config$AgdaModeVscode.Connection.getAgdaPaths(), configPaths, undefined);
                        if (match[1].TAG !== "Ok") {
                          return ;
                        }
                        Assert.fail("Connection should fail when config is empty and no discovery");
                      }));
              }));
        describe("Default value", (function () {
                it("connection.paths default value should be [\"agda\", \"als\"] in package.json", (async function () {
                        var packageJsonPath = Nodepath.join(Process.cwd(), "package.json");
                        var content = Nodefs.readFileSync(packageJsonPath).toString();
                        var json = JSON.parse(content);
                        var $$default;
                        $$default = !Array.isArray(json) && (json === null || typeof json !== "object") && typeof json !== "number" && typeof json !== "string" && typeof json !== "boolean" || !(typeof json === "object" && !Array.isArray(json)) ? undefined : Core__Option.flatMap(Core__Option.flatMap(Core__Option.flatMap(Core__Option.flatMap(json["contributes"], (function (v) {
                                              if (!Array.isArray(v) && (v === null || typeof v !== "object") && typeof v !== "number" && typeof v !== "string" && typeof v !== "boolean" || !(typeof v === "object" && !Array.isArray(v))) {
                                                return ;
                                              } else {
                                                return v["configuration"];
                                              }
                                            })), (function (v) {
                                          if (!Array.isArray(v) && (v === null || typeof v !== "object") && typeof v !== "number" && typeof v !== "string" && typeof v !== "boolean" || !(typeof v === "object" && !Array.isArray(v))) {
                                            return ;
                                          } else {
                                            return v["properties"];
                                          }
                                        })), (function (v) {
                                      if (!Array.isArray(v) && (v === null || typeof v !== "object") && typeof v !== "number" && typeof v !== "string" && typeof v !== "boolean" || !(typeof v === "object" && !Array.isArray(v))) {
                                        return ;
                                      } else {
                                        return v["agdaMode.connection.paths"];
                                      }
                                    })), (function (v) {
                                  if (!Array.isArray(v) && (v === null || typeof v !== "object") && typeof v !== "number" && typeof v !== "string" && typeof v !== "boolean" || !(typeof v === "object" && !Array.isArray(v))) {
                                    return ;
                                  } else {
                                    return v["default"];
                                  }
                                }));
                        return Curry._3(Assert.deepStrictEqual, $$default, [
                                    "agda",
                                    "als"
                                  ], undefined);
                      }));
              }));
        describe("Duplicate candidates", (function () {
                it("addAgdaPath should not add duplicate path", (async function () {
                        var initialPaths = [
                          "/usr/bin/agda",
                          "/opt/homebrew/bin/agda"
                        ];
                        await Config$AgdaModeVscode.Connection.setAgdaPaths(logChannel, initialPaths);
                        await Config$AgdaModeVscode.Connection.addAgdaPath(logChannel, "/usr/bin/agda");
                        return Curry._3(Assert.deepStrictEqual, Config$AgdaModeVscode.Connection.getAgdaPaths(), [
                                    "/usr/bin/agda",
                                    "/opt/homebrew/bin/agda"
                                  ], undefined);
                      }));
                it("addAgdaPath should not add semantically duplicate candidate", (async function () {
                        var initialPaths = ["/usr/bin/agda"];
                        await Config$AgdaModeVscode.Connection.setAgdaPaths(logChannel, initialPaths);
                        await Config$AgdaModeVscode.Connection.addAgdaPath(logChannel, "file:///usr/bin/agda");
                        return Curry._3(Assert.deepStrictEqual, Config$AgdaModeVscode.Connection.getAgdaPaths(), ["/usr/bin/agda"], undefined);
                      }));
                it("setAgdaPaths should deduplicate paths", (async function () {
                        var pathsWithDuplicates = [
                          "/usr/bin/agda",
                          "/opt/homebrew/bin/agda",
                          "/usr/bin/agda"
                        ];
                        await Config$AgdaModeVscode.Connection.setAgdaPaths(logChannel, pathsWithDuplicates);
                        return Curry._3(Assert.deepStrictEqual, Config$AgdaModeVscode.Connection.getAgdaPaths(), [
                                    "/usr/bin/agda",
                                    "/opt/homebrew/bin/agda"
                                  ], undefined);
                      }));
                it("setAgdaPaths should deduplicate semantically equal candidates while preserving first raw spelling", (async function () {
                        var pathsWithDuplicates = [
                          "/usr/bin/agda",
                          "file:///usr/bin/agda",
                          "agda",
                          "agda"
                        ];
                        await Config$AgdaModeVscode.Connection.setAgdaPaths(logChannel, pathsWithDuplicates);
                        return Curry._3(Assert.deepStrictEqual, Config$AgdaModeVscode.Connection.getAgdaPaths(), [
                                    "/usr/bin/agda",
                                    "agda"
                                  ], undefined);
                      }));
                it("setAgdaPaths then getAgdaPaths should round-trip without reordering (production path)", (async function () {
                        Config$AgdaModeVscode.inTestingMode.contents = false;
                        var paths = [
                          "/first",
                          "/second",
                          "/third"
                        ];
                        await Config$AgdaModeVscode.Connection.setAgdaPaths(logChannel, paths);
                        var retrieved = Config$AgdaModeVscode.Connection.getAgdaPaths();
                        Config$AgdaModeVscode.inTestingMode.contents = true;
                        return Curry._3(Assert.deepStrictEqual, retrieved, paths, undefined);
                      }));
                it("addAgdaPath should preserve existing order (production path)", (async function () {
                        Config$AgdaModeVscode.inTestingMode.contents = false;
                        var initialPaths = [
                          "/usr/bin/agda",
                          "/opt/homebrew/bin/agda"
                        ];
                        await Config$AgdaModeVscode.Connection.setAgdaPaths(logChannel, initialPaths);
                        await Config$AgdaModeVscode.Connection.addAgdaPath(logChannel, "/new/path/als");
                        var retrieved = Config$AgdaModeVscode.Connection.getAgdaPaths();
                        Config$AgdaModeVscode.inTestingMode.contents = true;
                        return Curry._3(Assert.deepStrictEqual, retrieved, [
                                    "/usr/bin/agda",
                                    "/opt/homebrew/bin/agda",
                                    "/new/path/als"
                                  ], undefined);
                      }));
              }));
        describe("UI-triggered additions", (function () {
                var createTestState = function () {
                  var channels_inputMethod = Chan$AgdaModeVscode.make();
                  var channels_responseHandled = Chan$AgdaModeVscode.make();
                  var channels_commandHandled = Chan$AgdaModeVscode.make();
                  var channels = {
                    inputMethod: channels_inputMethod,
                    responseHandled: channels_responseHandled,
                    commandHandled: channels_commandHandled,
                    log: logChannel
                  };
                  var mockEditor = {
        document: { fileName: "test.agda" }
      };
                  var mockStorageUri = Vscode.Uri.file(Nodeos.tmpdir());
                  var mockExtensionUri = Vscode.Uri.file(Process.cwd());
                  return State$AgdaModeVscode.make("test-id", platformWithDiscovery, channels, mockStorageUri, mockExtensionUri, Memento$AgdaModeVscode.make(undefined), mockEditor, undefined);
                };
                var setupUITest = async function (initialConfig) {
                  await Config$AgdaModeVscode.Connection.setAgdaPaths(logChannel, initialConfig);
                  var listener = Log$AgdaModeVscode.collect(logChannel);
                  var mockState = createTestState();
                  return [
                          listener,
                          mockState
                        ];
                };
                var executeUITest = async function (mockState, platform, mockSelectedItem) {
                  var view = State__SwitchVersion$AgdaModeVscode.View.make(logChannel);
                  var manager = State__SwitchVersion$AgdaModeVscode.SwitchVersionManager.make(mockState);
                  var onOperationComplete = Log$AgdaModeVscode.on(logChannel, (function (log) {
                          if (log.TAG !== "SwitchVersionUI") {
                            return false;
                          }
                          var tmp = log._0;
                          if (typeof tmp !== "object" && tmp === "SelectionCompleted") {
                            return true;
                          } else {
                            return false;
                          }
                        }));
                  State__SwitchVersion$AgdaModeVscode.Handler.onSelection(mockState, platform, manager, {
                        contents: ["Hardcoded"]
                      }, {
                        contents: "Hardcoded"
                      }, (function (_downloadInfo) {
                          return Promise.resolve();
                        }), view, [mockSelectedItem]);
                  await onOperationComplete;
                  return State__SwitchVersion$AgdaModeVscode.View.destroy(view);
                };
                var simulatePathSelection = async function (initialConfig, selectedPath) {
                  var match = await setupUITest(initialConfig);
                  var mockState = match[1];
                  var mockSelectedItem = State__SwitchVersion$AgdaModeVscode.Item.fromItemData({
                        TAG: "Candidate",
                        _0: selectedPath,
                        _1: selectedPath,
                        _2: {
                          kind: {
                            TAG: "Agda",
                            _0: "2.7.0.1"
                          },
                          timestamp: new Date(),
                          error: undefined
                        },
                        _3: false
                      }, mockState.extensionUri);
                  await executeUITest(mockState, platformWithDiscovery, mockSelectedItem);
                  var logs = match[0](Log$AgdaModeVscode.isConfig);
                  var finalConfig = Config$AgdaModeVscode.Connection.getAgdaPaths();
                  var preferredCandidate = Memento$AgdaModeVscode.Module.PreferredCandidate.get(mockState.memento);
                  return [
                          logs,
                          finalConfig,
                          preferredCandidate
                        ];
                };
                var simulateDownloadAction = async function (initialConfig, isAlreadyDownloadedOpt) {
                  var isAlreadyDownloaded = isAlreadyDownloadedOpt !== undefined ? isAlreadyDownloadedOpt : false;
                  var match = await setupUITest(initialConfig);
                  var mockState = match[1];
                  var expectedDownloadedPath = isAlreadyDownloaded ? State__SwitchVersion$AgdaModeVscode.Download.expectedPathForVariant(mockState.globalStorageUri, "Native", undefined) : downloadedALS.contents;
                  var mockSelectedItem = State__SwitchVersion$AgdaModeVscode.Item.fromItemData({
                        TAG: "DownloadAction",
                        _0: isAlreadyDownloaded,
                        _1: "ALS v0.2.10, Agda v2.7.0.1",
                        _2: "native"
                      }, mockState.extensionUri);
                  await executeUITest(mockState, Mock$AgdaModeVscode.Platform.makeWithSuccessfulDownload(downloadedALS.contents), mockSelectedItem);
                  var logs = match[0](Log$AgdaModeVscode.isConfig);
                  var finalConfig = Config$AgdaModeVscode.Connection.getAgdaPaths();
                  var preferredCandidate = Memento$AgdaModeVscode.Module.PreferredCandidate.get(mockState.memento);
                  return [
                          logs,
                          finalConfig,
                          preferredCandidate,
                          expectedDownloadedPath
                        ];
                };
                var simulateEmptySelection = async function (initialConfig) {
                  var match = await setupUITest(initialConfig);
                  var mockState = match[1];
                  var view = State__SwitchVersion$AgdaModeVscode.View.make(logChannel);
                  var manager = State__SwitchVersion$AgdaModeVscode.SwitchVersionManager.make(mockState);
                  var onOperationComplete = Log$AgdaModeVscode.on(logChannel, (function (log) {
                          if (log.TAG !== "SwitchVersionUI") {
                            return false;
                          }
                          var tmp = log._0;
                          if (typeof tmp !== "object" && tmp === "SelectionCompleted") {
                            return true;
                          } else {
                            return false;
                          }
                        }));
                  State__SwitchVersion$AgdaModeVscode.Handler.onSelection(mockState, platformWithDiscovery, manager, {
                        contents: ["Hardcoded"]
                      }, {
                        contents: "Hardcoded"
                      }, (function (_downloadInfo) {
                          return Promise.resolve();
                        }), view, []);
                  await onOperationComplete;
                  State__SwitchVersion$AgdaModeVscode.View.destroy(view);
                  var logs = match[0](Log$AgdaModeVscode.isConfig);
                  var finalConfig = Config$AgdaModeVscode.Connection.getAgdaPaths();
                  var preferredCandidate = Memento$AgdaModeVscode.Module.PreferredCandidate.get(mockState.memento);
                  return [
                          logs,
                          finalConfig,
                          preferredCandidate
                        ];
                };
                describe("Switch version UI selection", (function () {
                        it("should update PreferredCandidate without modifying config when user selects existing path not in config", (async function () {
                                this.retries(2);
                                var initialConfig = [userAgda.contents];
                                var selectedPath = alternativeAgda.contents;
                                var match = await simulatePathSelection(initialConfig, selectedPath);
                                Curry._3(Assert.deepStrictEqual, match[0], [], undefined);
                                Curry._3(Assert.deepStrictEqual, match[1], initialConfig, undefined);
                                return Curry._3(Assert.deepStrictEqual, match[2], selectedPath, undefined);
                              }));
                        it("should update PreferredCandidate without modifying config when user selects path already in config", (async function () {
                                var initialConfig = [
                                  userAgda.contents,
                                  alternativeAgda.contents
                                ];
                                var selectedPath = userAgda.contents;
                                var match = await simulatePathSelection(initialConfig, selectedPath);
                                Curry._3(Assert.deepStrictEqual, match[0], [], undefined);
                                Curry._3(Assert.deepStrictEqual, match[1], initialConfig, undefined);
                                return Curry._3(Assert.deepStrictEqual, match[2], selectedPath, undefined);
                              }));
                        it("should update PreferredCandidate when user selects different path", (async function () {
                                var initialConfig = [userAgda.contents];
                                var selectedPath = alternativeAgda.contents;
                                var match = await simulatePathSelection(initialConfig, selectedPath);
                                Curry._3(Assert.deepStrictEqual, match[0], [], undefined);
                                Curry._3(Assert.deepStrictEqual, match[1], initialConfig, undefined);
                                return Curry._3(Assert.deepStrictEqual, match[2], selectedPath, undefined);
                              }));
                        it("should preserve bare agda command in PreferredCandidate without modifying config", (async function () {
                                var initialConfig = ["agda"];
                                var match = await simulatePathSelection(initialConfig, "agda");
                                Curry._3(Assert.deepStrictEqual, match[0], [], undefined);
                                Curry._3(Assert.deepStrictEqual, match[1], initialConfig, undefined);
                                return Curry._3(Assert.deepStrictEqual, match[2], "agda", undefined);
                              }));
                        it("should preserve bare als command in PreferredCandidate without modifying config", (async function () {
                                var initialConfig = ["als"];
                                var match = await simulatePathSelection(initialConfig, "als");
                                Curry._3(Assert.deepStrictEqual, match[0], [], undefined);
                                Curry._3(Assert.deepStrictEqual, match[1], initialConfig, undefined);
                                return Curry._3(Assert.deepStrictEqual, match[2], "als", undefined);
                              }));
                      }));
                describe("Download action selection", (function () {
                        it("should add downloaded path to config when user downloads new ALS", (async function () {
                                var initialConfig = [userAgda.contents];
                                var match = await simulateDownloadAction(initialConfig, false);
                                var expectedDownloadedPath = match[3];
                                var expectedConfig = initialConfig.concat([expectedDownloadedPath]);
                                Curry._3(Assert.deepStrictEqual, match[0], [{
                                        TAG: "Config",
                                        _0: {
                                          TAG: "Changed",
                                          _0: initialConfig,
                                          _1: expectedConfig
                                        }
                                      }], undefined);
                                Curry._3(Assert.deepStrictEqual, match[1], expectedConfig, undefined);
                                return Curry._3(Assert.deepStrictEqual, match[2], expectedDownloadedPath, undefined);
                              }));
                        it("should add already downloaded path to config when user selects already downloaded ALS", (async function () {
                                var initialConfig = [userAgda.contents];
                                var match = await simulateDownloadAction(initialConfig, true);
                                var expectedDownloadedPath = match[3];
                                var expectedConfig = initialConfig.concat([expectedDownloadedPath]);
                                Curry._3(Assert.deepStrictEqual, match[0], [{
                                        TAG: "Config",
                                        _0: {
                                          TAG: "Changed",
                                          _0: initialConfig,
                                          _1: expectedConfig
                                        }
                                      }], undefined);
                                Curry._3(Assert.deepStrictEqual, match[1], expectedConfig, undefined);
                                return Curry._3(Assert.deepStrictEqual, match[2], expectedDownloadedPath, undefined);
                              }));
                      }));
                describe("Non-candidate selections", (function () {
                        it("should handle empty selection correctly", (async function () {
                                var initialConfig = [userAgda.contents];
                                var match = await simulateEmptySelection(initialConfig);
                                Curry._3(Assert.deepStrictEqual, match[0], [], undefined);
                                Curry._3(Assert.deepStrictEqual, match[1], initialConfig, undefined);
                                return Curry._3(Assert.deepStrictEqual, match[2], undefined, undefined);
                              }));
                      }));
              }));
      }));

/*  Not a pure module */
