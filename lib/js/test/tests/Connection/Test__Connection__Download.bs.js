// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Curry = require("rescript/lib/js/curry.js");
var Js_exn = require("rescript/lib/js/js_exn.js");
var Assert = require("assert");
var Vscode = require("vscode");
var Nodefs = require("node:fs");
var Nodeos = require("node:os");
var Nodepath = require("node:path");
var Caml_option = require("rescript/lib/js/caml_option.js");
var Nodestream = require("node:stream");
var Core__Option = require("@rescript/core/lib/js/src/Core__Option.bs.js");
var Core__Result = require("@rescript/core/lib/js/src/Core__Result.bs.js");
var Core__Promise = require("@rescript/core/lib/js/src/Core__Promise.bs.js");
var FS$AgdaModeVscode = require("../../../src/FS.bs.js");
var Caml_js_exceptions = require("rescript/lib/js/caml_js_exceptions.js");
var Memento$AgdaModeVscode = require("../../../src/Memento.bs.js");
var Connection__Download$AgdaModeVscode = require("../../../src/Connection/Download/Connection__Download.bs.js");
var Connection__LatestALS$AgdaModeVscode = require("../../../src/Connection/Download/Connection__LatestALS.bs.js");
var Connection__Download__Unzip$AgdaModeVscode = require("../../../src/Connection/Download/Connection__Download__Unzip.bs.js");
var Connection__Download__GitHub$AgdaModeVscode = require("../../../src/Connection/Download/Connection__Download__GitHub.bs.js");

describe("Download", (function () {
        this.timeout(10000);
        describe("alreadyDownloaded", (function () {
                it("should return None when dev ALS not downloaded", (async function () {
                        var nonExistentDir = Nodepath.join(Nodeos.tmpdir(), "devals-test-nonexistent-" + String(Date.now() | 0));
                        var globalStorageUri = Vscode.Uri.file(nonExistentDir);
                        var result = await Connection__Download$AgdaModeVscode.alreadyDownloaded(globalStorageUri, "DevALS");
                        return Curry._3(Assert.deepStrictEqual, result, undefined, undefined);
                      }));
                it("should return Some(path) when dev ALS executable exists", (async function () {
                        var tempDir = Nodepath.join(Nodeos.tmpdir(), "devals-test-exists-" + String(Date.now() | 0));
                        var devAlsDir = Nodepath.join(tempDir, "dev-als");
                        var alsExecutable = Nodepath.join(devAlsDir, "als");
                        await Nodefs.promises.mkdir(devAlsDir, {
                              recursive: true,
                              mode: 511
                            });
                        Nodefs.writeFileSync(alsExecutable, Buffer.from("mock executable"));
                        var globalStorageUri = Vscode.Uri.file(tempDir);
                        var result = await Connection__Download$AgdaModeVscode.alreadyDownloaded(globalStorageUri, "DevALS");
                        Curry._3(Assert.deepStrictEqual, result, Vscode.Uri.file(alsExecutable).fsPath, undefined);
                        Nodefs.unlinkSync(alsExecutable);
                        Nodefs.rmdirSync(devAlsDir);
                        Nodefs.rmdirSync(tempDir);
                      }));
                it("should return None when dev-als directory exists but als executable missing", (async function () {
                        var tempDir = Nodepath.join(Nodeos.tmpdir(), "devals-test-noals-" + String(Date.now() | 0));
                        var devAlsDir = Nodepath.join(tempDir, "dev-als");
                        await Nodefs.promises.mkdir(devAlsDir, {
                              recursive: true,
                              mode: 511
                            });
                        var globalStorageUri = Vscode.Uri.file(tempDir);
                        var result = await Connection__Download$AgdaModeVscode.alreadyDownloaded(globalStorageUri, "DevALS");
                        Curry._3(Assert.deepStrictEqual, result, undefined, undefined);
                        Nodefs.rmdirSync(devAlsDir);
                        Nodefs.rmdirSync(tempDir);
                      }));
                it("should return None when global storage exists but no dev-als directory", (async function () {
                        var tempDir = Nodepath.join(Nodeos.tmpdir(), "devals-test-nostorage-" + String(Date.now() | 0));
                        await Nodefs.promises.mkdir(tempDir, {
                              recursive: true,
                              mode: 511
                            });
                        var globalStorageUri = Vscode.Uri.file(tempDir);
                        var result = await Connection__Download$AgdaModeVscode.alreadyDownloaded(globalStorageUri, "DevALS");
                        Curry._3(Assert.deepStrictEqual, result, undefined, undefined);
                        Nodefs.rmdirSync(tempDir);
                      }));
              }));
        describe("alreadyDownloaded with Hardcoded", (function () {
                it("should return Some(path) when native binary exists at hardcoded-als/als", (async function () {
                        var tempDir = Nodepath.join(Nodeos.tmpdir(), "hardcoded-als-native-test-" + String(Date.now() | 0));
                        var hardcodedAlsDir = Nodepath.join(tempDir, "hardcoded-als");
                        var alsExecutable = Nodepath.join(hardcodedAlsDir, "als");
                        await Nodefs.promises.mkdir(hardcodedAlsDir, {
                              recursive: true,
                              mode: 511
                            });
                        Nodefs.writeFileSync(alsExecutable, Buffer.from("mock executable"));
                        var globalStorageUri = Vscode.Uri.file(tempDir);
                        var result = await Connection__Download$AgdaModeVscode.alreadyDownloaded(globalStorageUri, "Hardcoded");
                        Curry._3(Assert.deepStrictEqual, result, Vscode.Uri.file(alsExecutable).fsPath, undefined);
                        Nodefs.unlinkSync(alsExecutable);
                        Nodefs.rmdirSync(hardcodedAlsDir);
                        Nodefs.rmdirSync(tempDir);
                      }));
                it("should return Some(uri) when WASM exists at hardcoded-als/als.wasm", (async function () {
                        var tempDir = Nodepath.join(Nodeos.tmpdir(), "hardcoded-als-wasm-test-" + String(Date.now() | 0));
                        var hardcodedAlsDir = Nodepath.join(tempDir, "hardcoded-als");
                        var wasmFile = Nodepath.join(hardcodedAlsDir, "als.wasm");
                        await Nodefs.promises.mkdir(hardcodedAlsDir, {
                              recursive: true,
                              mode: 511
                            });
                        Nodefs.writeFileSync(wasmFile, Buffer.from("mock wasm"));
                        var globalStorageUri = Vscode.Uri.file(tempDir);
                        var result = await Connection__Download$AgdaModeVscode.alreadyDownloaded(globalStorageUri, "Hardcoded");
                        var wasmUri = Vscode.Uri.joinPath(globalStorageUri, "hardcoded-als", "als.wasm");
                        Curry._3(Assert.deepStrictEqual, result, wasmUri.toString(), undefined);
                        Nodefs.unlinkSync(wasmFile);
                        Nodefs.rmdirSync(hardcodedAlsDir);
                        Nodefs.rmdirSync(tempDir);
                      }));
                it("should return None when nothing exists at hardcoded-als", (async function () {
                        var nonExistentDir = Nodepath.join(Nodeos.tmpdir(), "hardcoded-als-test-nonexistent-" + String(Date.now() | 0));
                        var globalStorageUri = Vscode.Uri.file(nonExistentDir);
                        var result = await Connection__Download$AgdaModeVscode.alreadyDownloaded(globalStorageUri, "Hardcoded");
                        return Curry._3(Assert.deepStrictEqual, result, undefined, undefined);
                      }));
                it("should prefer native binary over WASM when both exist", (async function () {
                        var tempDir = Nodepath.join(Nodeos.tmpdir(), "hardcoded-als-both-test-" + String(Date.now() | 0));
                        var hardcodedAlsDir = Nodepath.join(tempDir, "hardcoded-als");
                        var alsExecutable = Nodepath.join(hardcodedAlsDir, "als");
                        var wasmFile = Nodepath.join(hardcodedAlsDir, "als.wasm");
                        await Nodefs.promises.mkdir(hardcodedAlsDir, {
                              recursive: true,
                              mode: 511
                            });
                        Nodefs.writeFileSync(alsExecutable, Buffer.from("mock executable"));
                        Nodefs.writeFileSync(wasmFile, Buffer.from("mock wasm"));
                        var globalStorageUri = Vscode.Uri.file(tempDir);
                        var result = await Connection__Download$AgdaModeVscode.alreadyDownloaded(globalStorageUri, "Hardcoded");
                        Curry._3(Assert.deepStrictEqual, result, Vscode.Uri.file(alsExecutable).fsPath, undefined);
                        Nodefs.unlinkSync(alsExecutable);
                        Nodefs.unlinkSync(wasmFile);
                        Nodefs.rmdirSync(hardcodedAlsDir);
                        Nodefs.rmdirSync(tempDir);
                      }));
              }));
        describe("isDownloading", (function () {
                it("should return false and create directory when global storage directory doesn't exist", (async function () {
                        var nonExistentDir = Nodepath.join(Nodeos.tmpdir(), "agda-isdownloading-test-" + String(Date.now() | 0));
                        Assert.ok(!Nodefs.existsSync(nonExistentDir));
                        var globalStorageUri = Vscode.Uri.file(nonExistentDir);
                        var result = await Connection__Download__GitHub$AgdaModeVscode.isDownloading(globalStorageUri);
                        Assert.ok(!result);
                        Assert.ok(Nodefs.existsSync(nonExistentDir));
                        Nodefs.rmdirSync(nonExistentDir);
                      }));
                it("should return false when directory exists but no in-flight download file is present", (async function () {
                        var tempDir = Nodepath.join(Nodeos.tmpdir(), "agda-isdownloading-exists-" + String(Date.now() | 0));
                        await Nodefs.promises.mkdir(tempDir, {
                              recursive: true,
                              mode: 511
                            });
                        Assert.ok(Nodefs.existsSync(tempDir));
                        var inFlightFile = Nodepath.join(tempDir, "in-flight.download");
                        Assert.ok(!Nodefs.existsSync(inFlightFile));
                        var globalStorageUri = Vscode.Uri.file(tempDir);
                        var result = await Connection__Download__GitHub$AgdaModeVscode.isDownloading(globalStorageUri);
                        Assert.ok(!result);
                        Nodefs.rmdirSync(tempDir);
                      }));
                it("should return true when in-flight download file exists", (async function () {
                        var tempDir = Nodepath.join(Nodeos.tmpdir(), "agda-isdownloading-inflight-" + String(Date.now() | 0));
                        var inFlightFile = Nodepath.join(tempDir, "in-flight.download");
                        await Nodefs.promises.mkdir(tempDir, {
                              recursive: true,
                              mode: 511
                            });
                        Nodefs.writeFileSync(inFlightFile, Buffer.from("downloading..."));
                        Assert.ok(Nodefs.existsSync(tempDir));
                        Assert.ok(Nodefs.existsSync(inFlightFile));
                        var globalStorageUri = Vscode.Uri.file(tempDir);
                        var result = await Connection__Download__GitHub$AgdaModeVscode.isDownloading(globalStorageUri);
                        Assert.ok(result);
                        Nodefs.unlinkSync(inFlightFile);
                        Nodefs.rmdirSync(tempDir);
                      }));
                it("should return false when directory exists with other files but no in-flight download file", (async function () {
                        var tempDir = Nodepath.join(Nodeos.tmpdir(), "agda-isdownloading-otherfiles-" + String(Date.now() | 0));
                        var otherFile = Nodepath.join(tempDir, "some-other-file.txt");
                        var inFlightFile = Nodepath.join(tempDir, "in-flight.download");
                        await Nodefs.promises.mkdir(tempDir, {
                              recursive: true,
                              mode: 511
                            });
                        Nodefs.writeFileSync(otherFile, Buffer.from("other content"));
                        Assert.ok(Nodefs.existsSync(tempDir));
                        Assert.ok(Nodefs.existsSync(otherFile));
                        Assert.ok(!Nodefs.existsSync(inFlightFile));
                        var globalStorageUri = Vscode.Uri.file(tempDir);
                        var result = await Connection__Download__GitHub$AgdaModeVscode.isDownloading(globalStorageUri);
                        Assert.ok(!result);
                        Nodefs.unlinkSync(otherFile);
                        Nodefs.rmdirSync(tempDir);
                      }));
                it("should handle multiple calls consistently when no download is in progress", (async function () {
                        var tempDir = Nodepath.join(Nodeos.tmpdir(), "agda-isdownloading-multiple-" + String(Date.now() | 0));
                        await Nodefs.promises.mkdir(tempDir, {
                              recursive: true,
                              mode: 511
                            });
                        var globalStorageUri = Vscode.Uri.file(tempDir);
                        var result1 = await Connection__Download__GitHub$AgdaModeVscode.isDownloading(globalStorageUri);
                        var result2 = await Connection__Download__GitHub$AgdaModeVscode.isDownloading(globalStorageUri);
                        var result3 = await Connection__Download__GitHub$AgdaModeVscode.isDownloading(globalStorageUri);
                        Assert.ok(!result1);
                        Assert.ok(!result2);
                        Assert.ok(!result3);
                        Nodefs.rmdirSync(tempDir);
                      }));
                it("should handle multiple calls consistently when download is in progress", (async function () {
                        var tempDir = Nodepath.join(Nodeos.tmpdir(), "agda-isdownloading-multiple-progress-" + String(Date.now() | 0));
                        var inFlightFile = Nodepath.join(tempDir, "in-flight.download");
                        await Nodefs.promises.mkdir(tempDir, {
                              recursive: true,
                              mode: 511
                            });
                        Nodefs.writeFileSync(inFlightFile, Buffer.from("downloading..."));
                        var globalStorageUri = Vscode.Uri.file(tempDir);
                        var result1 = await Connection__Download__GitHub$AgdaModeVscode.isDownloading(globalStorageUri);
                        var result2 = await Connection__Download__GitHub$AgdaModeVscode.isDownloading(globalStorageUri);
                        var result3 = await Connection__Download__GitHub$AgdaModeVscode.isDownloading(globalStorageUri);
                        Assert.ok(result1);
                        Assert.ok(result2);
                        Assert.ok(result3);
                        Nodefs.unlinkSync(inFlightFile);
                        Nodefs.rmdirSync(tempDir);
                      }));
                it("should transition from not downloading to downloading when in-flight file is created", (async function () {
                        var tempDir = Nodepath.join(Nodeos.tmpdir(), "agda-isdownloading-transition-" + String(Date.now() | 0));
                        var inFlightFile = Nodepath.join(tempDir, "in-flight.download");
                        await Nodefs.promises.mkdir(tempDir, {
                              recursive: true,
                              mode: 511
                            });
                        var globalStorageUri = Vscode.Uri.file(tempDir);
                        var result1 = await Connection__Download__GitHub$AgdaModeVscode.isDownloading(globalStorageUri);
                        Assert.ok(!result1);
                        Nodefs.writeFileSync(inFlightFile, Buffer.from("downloading..."));
                        var result2 = await Connection__Download__GitHub$AgdaModeVscode.isDownloading(globalStorageUri);
                        Assert.ok(result2);
                        Nodefs.unlinkSync(inFlightFile);
                        var result3 = await Connection__Download__GitHub$AgdaModeVscode.isDownloading(globalStorageUri);
                        Assert.ok(!result3);
                        Nodefs.rmdirSync(tempDir);
                      }));
                it("should handle edge case with empty in-flight file", (async function () {
                        var tempDir = Nodepath.join(Nodeos.tmpdir(), "agda-isdownloading-empty-" + String(Date.now() | 0));
                        var inFlightFile = Nodepath.join(tempDir, "in-flight.download");
                        await Nodefs.promises.mkdir(tempDir, {
                              recursive: true,
                              mode: 511
                            });
                        Nodefs.writeFileSync(inFlightFile, Buffer.from(""));
                        Assert.ok(Nodefs.existsSync(tempDir));
                        Assert.ok(Nodefs.existsSync(inFlightFile));
                        var globalStorageUri = Vscode.Uri.file(tempDir);
                        var result = await Connection__Download__GitHub$AgdaModeVscode.isDownloading(globalStorageUri);
                        Assert.ok(result);
                        Nodefs.unlinkSync(inFlightFile);
                        Nodefs.rmdirSync(tempDir);
                      }));
                it("should create directory with proper permissions when it doesn't exist", (async function () {
                        var nonExistentDir = Nodepath.join(Nodeos.tmpdir(), "agda-isdownloading-permissions-" + String(Date.now() | 0));
                        Assert.ok(!Nodefs.existsSync(nonExistentDir));
                        var globalStorageUri = Vscode.Uri.file(nonExistentDir);
                        var result = await Connection__Download__GitHub$AgdaModeVscode.isDownloading(globalStorageUri);
                        Assert.ok(!result);
                        Assert.ok(Nodefs.existsSync(nonExistentDir));
                        var testFile = Nodepath.join(nonExistentDir, "test.txt");
                        Nodefs.writeFileSync(testFile, Buffer.from("test"));
                        Assert.ok(Nodefs.existsSync(testFile));
                        Nodefs.unlinkSync(testFile);
                        Nodefs.rmdirSync(nonExistentDir);
                      }));
              }));
        describe("chmodExecutable", (function () {
                it("should set the executable bit on a file", (async function () {
                        var tempFile = Nodepath.join(Nodeos.tmpdir(), "chmod-test-" + String(Date.now() | 0));
                        Nodefs.writeFileSync(tempFile, Buffer.from("mock binary"));
                        await Nodefs.promises.chmod(tempFile, 420);
                        var statsBefore = Nodefs.lstatSync(tempFile);
                        Curry._3(Assert.deepStrictEqual, statsBefore.mode & 73, 0, undefined);
                        var result = await Connection__Download__GitHub$AgdaModeVscode.chmodExecutable(tempFile);
                        Curry._3(Assert.deepStrictEqual, result, {
                              TAG: "Ok",
                              _0: undefined
                            }, undefined);
                        var statsAfter = Nodefs.lstatSync(tempFile);
                        Assert.ok((statsAfter.mode & 73) !== 0);
                        Nodefs.unlinkSync(tempFile);
                      }));
              }));
        describe("download — directory vs file cache check", (function () {
                it("should NOT treat existing directory as cached when binary file is missing", (async function () {
                        var tempDir = Nodepath.join(Nodeos.tmpdir(), "agda-dl-cache-test-" + String(Date.now() | 0));
                        var devAlsDir = Nodepath.join(tempDir, "dev-als");
                        await Nodefs.promises.mkdir(devAlsDir, {
                              recursive: true,
                              mode: 511
                            });
                        var globalStorageUri = Vscode.Uri.file(tempDir);
                        var fakeAsset = {
                          url: "https://invalid.example.com/fake-asset.zip",
                          id: 0,
                          node_id: "",
                          name: "als-dev-Agda-2.8.0-macos-arm64.zip",
                          label: undefined,
                          content_type: "application/zip",
                          state: "uploaded",
                          size: 0,
                          created_at: "",
                          updated_at: "",
                          browser_download_url: "https://invalid.example.com/fake-asset.zip"
                        };
                        var fakeRelease_assets = [fakeAsset];
                        var fakeRelease = {
                          url: "",
                          assets_url: "",
                          upload_url: "",
                          html_url: "",
                          id: 0,
                          node_id: "",
                          tag_name: "dev",
                          target_commitish: "",
                          name: "dev",
                          draft: false,
                          prerelease: true,
                          created_at: "",
                          published_at: "",
                          assets: fakeRelease_assets,
                          tarball_url: "",
                          zipball_url: "",
                          body: undefined
                        };
                        var descriptor = {
                          release: fakeRelease,
                          asset: fakeAsset,
                          saveAsFileName: "dev-als"
                        };
                        var result = await Connection__Download__GitHub$AgdaModeVscode.download(descriptor, globalStorageUri, (function (param) {
                                
                              }), undefined, undefined, undefined, undefined, undefined);
                        Curry._3(Assert.deepStrictEqual, Core__Result.isOk(result), false, undefined);
                        await FS$AgdaModeVscode.deleteRecursive(globalStorageUri);
                      }));
              }));
        var makeFakeDescriptor = function (name) {
          var asset = {
            url: "https://invalid.example.com/fake.zip",
            id: 0,
            node_id: "",
            name: name,
            label: undefined,
            content_type: "application/zip",
            state: "uploaded",
            size: 0,
            created_at: "",
            updated_at: "",
            browser_download_url: "https://invalid.example.com/fake.zip"
          };
          var release_assets = [asset];
          var release = {
            url: "",
            assets_url: "",
            upload_url: "",
            html_url: "",
            id: 0,
            node_id: "",
            tag_name: "v1",
            target_commitish: "",
            name: "v1",
            draft: false,
            prerelease: false,
            created_at: "",
            published_at: "",
            assets: release_assets,
            tarball_url: "",
            zipball_url: "",
            body: undefined
          };
          var descriptor = {
            release: release,
            asset: asset,
            saveAsFileName: "hardcoded-als"
          };
          return [
                  asset,
                  release,
                  descriptor
                ];
        };
        describe("download — in-flight sentinel ordering (Fix 3)", (function () {
                it("should abort with CannotWriteInFlightFile when claiming in-flight sentinel fails", (async function () {
                        var tempDir = Nodepath.join(Nodeos.tmpdir(), "agda-sentinel-write-fail-" + String(Date.now() | 0));
                        await Nodefs.promises.mkdir(tempDir, {
                              recursive: true,
                              mode: 511
                            });
                        var match = makeFakeDescriptor("als-Agda-2.8.0-macos-arm64.zip");
                        var globalStorageUri = Vscode.Uri.file(tempDir);
                        var mockWriteSentinel = function (_uri) {
                          return Promise.resolve({
                                      TAG: "Error",
                                      _0: "simulated sentinel write failure"
                                    });
                        };
                        var result = await Connection__Download__GitHub$AgdaModeVscode.download(match[2], globalStorageUri, (function (param) {
                                
                              }), mockWriteSentinel, undefined, undefined, undefined, undefined);
                        await FS$AgdaModeVscode.deleteRecursive(globalStorageUri);
                        return Curry._3(Assert.deepStrictEqual, result, {
                                    TAG: "Error",
                                    _0: {
                                      TAG: "CannotWriteInFlightFile",
                                      _0: "simulated sentinel write failure"
                                    }
                                  }, undefined);
                      }));
                it("should write then delete sentinel when binary is already cached", (async function () {
                        var tempDir = Nodepath.join(Nodeos.tmpdir(), "agda-sentinel-cache-hit-" + String(Date.now() | 0));
                        var binaryDir = Nodepath.join(tempDir, "hardcoded-als");
                        await Nodefs.promises.mkdir(binaryDir, {
                              recursive: true,
                              mode: 511
                            });
                        var match = makeFakeDescriptor("als-Agda-2.8.0-macos-arm64.zip");
                        var globalStorageUri = Vscode.Uri.file(tempDir);
                        var ops = {
                          contents: []
                        };
                        var mockWriteSentinel = function (_uri) {
                          ops.contents = ops.contents.concat(["write"]);
                          Nodefs.writeFileSync(Nodepath.join(binaryDir, "als"), Buffer.from("mock binary"));
                          return Promise.resolve({
                                      TAG: "Ok",
                                      _0: undefined
                                    });
                        };
                        var mockDeleteSentinel = function (_uri) {
                          ops.contents = ops.contents.concat(["delete"]);
                          return Promise.resolve({
                                      TAG: "Ok",
                                      _0: undefined
                                    });
                        };
                        var result = await Connection__Download__GitHub$AgdaModeVscode.download(match[2], globalStorageUri, (function (param) {
                                
                              }), mockWriteSentinel, mockDeleteSentinel, undefined, undefined, undefined);
                        await FS$AgdaModeVscode.deleteRecursive(globalStorageUri);
                        Curry._3(Assert.deepStrictEqual, result, {
                              TAG: "Ok",
                              _0: true
                            }, undefined);
                        return Curry._3(Assert.deepStrictEqual, ops.contents, [
                                    "write",
                                    "delete"
                                  ], undefined);
                      }));
                it("should return CannotDeleteFile when cached-binary path cannot release in-flight sentinel", (async function () {
                        var tempDir = Nodepath.join(Nodeos.tmpdir(), "agda-sentinel-delete-fail-" + String(Date.now() | 0));
                        var binaryDir = Nodepath.join(tempDir, "hardcoded-als");
                        await Nodefs.promises.mkdir(binaryDir, {
                              recursive: true,
                              mode: 511
                            });
                        Nodefs.writeFileSync(Nodepath.join(binaryDir, "als"), Buffer.from("mock binary"));
                        var match = makeFakeDescriptor("als-Agda-2.8.0-macos-arm64.zip");
                        var globalStorageUri = Vscode.Uri.file(tempDir);
                        var mockWriteSentinel = function (_uri) {
                          return Promise.resolve({
                                      TAG: "Ok",
                                      _0: undefined
                                    });
                        };
                        var mockDeleteSentinel = function (_uri) {
                          return Promise.resolve({
                                      TAG: "Error",
                                      _0: "simulated sentinel delete failure"
                                    });
                        };
                        var result = await Connection__Download__GitHub$AgdaModeVscode.download(match[2], globalStorageUri, (function (param) {
                                
                              }), mockWriteSentinel, mockDeleteSentinel, undefined, undefined, undefined);
                        await FS$AgdaModeVscode.deleteRecursive(globalStorageUri);
                        return Curry._3(Assert.deepStrictEqual, result, {
                                    TAG: "Error",
                                    _0: {
                                      TAG: "CannotDeleteFile",
                                      _0: "simulated sentinel delete failure"
                                    }
                                  }, undefined);
                      }));
              }));
        describe("download — post-unzip binary verification (Fix 4)", (function () {
                it("should return BinaryMissingAfterExtraction when unzip succeeds but binary is absent", (async function () {
                        var tempDir = Nodepath.join(Nodeos.tmpdir(), "agda-fix4-missing-" + String(Date.now() | 0));
                        await Nodefs.promises.mkdir(tempDir, {
                              recursive: true,
                              mode: 511
                            });
                        var match = makeFakeDescriptor("als-Agda-2.8.0-macos-arm64.zip");
                        var globalStorageUri = Vscode.Uri.file(tempDir);
                        var mockFetchFile = function (uri) {
                          Nodefs.writeFileSync(uri.fsPath, Buffer.from("mock download"));
                          return Promise.resolve({
                                      TAG: "Ok",
                                      _0: undefined
                                    });
                        };
                        var mockUnzip = function (_zipUri, _destUri) {
                          return Promise.resolve();
                        };
                        var result = await Connection__Download__GitHub$AgdaModeVscode.download(match[2], globalStorageUri, (function (param) {
                                
                              }), undefined, undefined, Caml_option.some(mockFetchFile), Caml_option.some(mockUnzip), undefined);
                        await FS$AgdaModeVscode.deleteRecursive(globalStorageUri);
                        var expectedPath = Nodepath.join(tempDir, "hardcoded-als", "als");
                        return Curry._3(Assert.deepStrictEqual, result, {
                                    TAG: "Error",
                                    _0: {
                                      TAG: "BinaryMissingAfterExtraction",
                                      _0: expectedPath
                                    }
                                  }, undefined);
                      }));
                it("should return Ok(false) and delete zip when unzip succeeds and binary exists", (async function () {
                        var tempDir = Nodepath.join(Nodeos.tmpdir(), "agda-fix4-exists-" + String(Date.now() | 0));
                        await Nodefs.promises.mkdir(tempDir, {
                              recursive: true,
                              mode: 511
                            });
                        var match = makeFakeDescriptor("als-Agda-2.8.0-macos-arm64.zip");
                        var globalStorageUri = Vscode.Uri.file(tempDir);
                        var mockFetchFile = function (uri) {
                          Nodefs.writeFileSync(uri.fsPath, Buffer.from("mock download"));
                          return Promise.resolve({
                                      TAG: "Ok",
                                      _0: undefined
                                    });
                        };
                        var mockUnzip = function (_zipUri, destUri) {
                          var binaryDir = destUri.fsPath;
                          Nodefs.mkdirSync(binaryDir, {
                                recursive: true
                              });
                          Nodefs.writeFileSync(Nodepath.join(binaryDir, "als"), Buffer.from("mock binary"));
                          return Promise.resolve();
                        };
                        var result = await Connection__Download__GitHub$AgdaModeVscode.download(match[2], globalStorageUri, (function (param) {
                                
                              }), undefined, undefined, Caml_option.some(mockFetchFile), Caml_option.some(mockUnzip), undefined);
                        var zipPath = Nodepath.join(tempDir, "in-flight.download.zip");
                        var zipExistsAfter = Nodefs.existsSync(zipPath);
                        await FS$AgdaModeVscode.deleteRecursive(globalStorageUri);
                        Curry._3(Assert.deepStrictEqual, result, {
                              TAG: "Ok",
                              _0: false
                            }, undefined);
                        return Curry._3(Assert.deepStrictEqual, zipExistsAfter, false, undefined);
                      }));
                it("should return CannotDeleteFile when zip cleanup fails after successful unzip and binary stat", (async function () {
                        var tempDir = Nodepath.join(Nodeos.tmpdir(), "agda-fix4-deletefail-" + String(Date.now() | 0));
                        await Nodefs.promises.mkdir(tempDir, {
                              recursive: true,
                              mode: 511
                            });
                        var match = makeFakeDescriptor("als-Agda-2.8.0-macos-arm64.zip");
                        var globalStorageUri = Vscode.Uri.file(tempDir);
                        var mockFetchFile = function (uri) {
                          Nodefs.writeFileSync(uri.fsPath, Buffer.from("mock download"));
                          return Promise.resolve({
                                      TAG: "Ok",
                                      _0: undefined
                                    });
                        };
                        var mockUnzip = function (_zipUri, destUri) {
                          var binaryDir = destUri.fsPath;
                          Nodefs.mkdirSync(binaryDir, {
                                recursive: true
                              });
                          Nodefs.writeFileSync(Nodepath.join(binaryDir, "als"), Buffer.from("mock binary"));
                          return Promise.resolve();
                        };
                        var mockDeleteZip = function (_uri) {
                          return Promise.resolve({
                                      TAG: "Error",
                                      _0: "simulated zip delete failure"
                                    });
                        };
                        var result = await Connection__Download__GitHub$AgdaModeVscode.download(match[2], globalStorageUri, (function (param) {
                                
                              }), undefined, undefined, Caml_option.some(mockFetchFile), Caml_option.some(mockUnzip), Caml_option.some(mockDeleteZip));
                        await FS$AgdaModeVscode.deleteRecursive(globalStorageUri);
                        return Curry._3(Assert.deepStrictEqual, result, {
                                    TAG: "Error",
                                    _0: {
                                      TAG: "CannotDeleteFile",
                                      _0: "simulated zip delete failure"
                                    }
                                  }, undefined);
                      }));
                it("should return Ok(false) for WASM asset without requiring destPath/als", (async function () {
                        var tempDir = Nodepath.join(Nodeos.tmpdir(), "agda-fix4-wasm-" + String(Date.now() | 0));
                        await Nodefs.promises.mkdir(tempDir, {
                              recursive: true,
                              mode: 511
                            });
                        var match = makeFakeDescriptor("als-Agda-2.8.0-macos-wasm.zip");
                        var globalStorageUri = Vscode.Uri.file(tempDir);
                        var mockFetchFile = function (uri) {
                          Nodefs.writeFileSync(uri.fsPath, Buffer.from("mock download"));
                          return Promise.resolve({
                                      TAG: "Ok",
                                      _0: undefined
                                    });
                        };
                        var shouldNotCallUnzip = function (_zipUri, _destUri) {
                          Assert.fail("unzip must not be called for WASM downloads");
                          return Promise.resolve();
                        };
                        var result = await Connection__Download__GitHub$AgdaModeVscode.download(match[2], globalStorageUri, (function (param) {
                                
                              }), undefined, undefined, Caml_option.some(mockFetchFile), Caml_option.some(shouldNotCallUnzip), undefined);
                        await FS$AgdaModeVscode.deleteRecursive(globalStorageUri);
                        return Curry._3(Assert.deepStrictEqual, result, {
                                    TAG: "Ok",
                                    _0: false
                                  }, undefined);
                      }));
              }));
        describe("Unzip.run", (function () {
                var withTimeout = function (ms, p) {
                  var timerId = {
                    contents: undefined
                  };
                  var clearTimer = function () {
                    Core__Option.forEach(timerId.contents, (function (id) {
                            clearTimeout(id);
                          }));
                  };
                  var wrappedP = p.then(function (v) {
                        clearTimer();
                        return Promise.resolve(v);
                      });
                  Core__Promise.$$catch(wrappedP, (function (param) {
                          clearTimer();
                          return Promise.resolve();
                        }));
                  return Promise.race([
                              wrappedP,
                              new Promise((function (param, reject) {
                                      timerId.contents = Caml_option.some(setTimeout((function () {
                                                  var err = (new Error("timed out after " + ms + "ms"));
                                                  reject(err);
                                                }), ms));
                                    }))
                            ]);
                };
                var expectToReject = async function (p) {
                  var didReject = false;
                  try {
                    await withTimeout(1000, p);
                  }
                  catch (raw_e){
                    var e = Caml_js_exceptions.internalToOCamlException(raw_e);
                    if (e.RE_EXN_ID === Js_exn.$$Error && Core__Option.getOr(e._1.message, "").startsWith("timed out")) {
                      Assert.fail("expected rejection but timed out — error event listener not wired");
                    } else {
                      didReject = true;
                    }
                  }
                  return Curry._3(Assert.deepStrictEqual, didReject, true, undefined);
                };
                it("attaches finish handler before piping", (async function () {
                        var fakeInput = new Nodestream.PassThrough();
                        var fakeOutput = new Nodestream.PassThrough();
                        var p = Connection__Download__Unzip$AgdaModeVscode.run(Vscode.Uri.file("/fake/src"), Vscode.Uri.file("/fake/dest"), Caml_option.some((function (param, param$1) {
                                    return [
                                            fakeInput,
                                            fakeOutput
                                          ];
                                  })), Caml_option.some((function (param, e) {
                                    e.emit("finish");
                                  })));
                        return await withTimeout(1000, p);
                      }));
                it("should not resolve on input stream close, but should resolve on extractor finish", (async function () {
                        var fakeInput = new Nodestream.PassThrough();
                        var fakeOutput = new Nodestream.PassThrough();
                        var resolved = {
                          contents: false
                        };
                        var p = Connection__Download__Unzip$AgdaModeVscode.run(Vscode.Uri.file("/fake/src"), Vscode.Uri.file("/fake/dest"), Caml_option.some((function (param, param$1) {
                                    return [
                                            fakeInput,
                                            fakeOutput
                                          ];
                                  })), undefined);
                        p.then(function () {
                              resolved.contents = true;
                              return Promise.resolve();
                            });
                        fakeInput.emit("close");
                        await Promise.resolve();
                        Curry._3(Assert.deepStrictEqual, resolved.contents, false, undefined);
                        fakeOutput.emit("finish");
                        await withTimeout(1000, p);
                        return Curry._3(Assert.deepStrictEqual, resolved.contents, true, undefined);
                      }));
                it("should reject when input stream emits error", (async function () {
                        var fakeInput = new Nodestream.PassThrough();
                        var fakeOutput = new Nodestream.PassThrough();
                        var p = Connection__Download__Unzip$AgdaModeVscode.run(Vscode.Uri.file("/fake/src"), Vscode.Uri.file("/fake/dest"), Caml_option.some((function (param, param$1) {
                                    return [
                                            fakeInput,
                                            fakeOutput
                                          ];
                                  })), undefined);
                        var fakeErr = (new Error("input stream error"));
                        fakeInput.emit("error", fakeErr);
                        return await expectToReject(p);
                      }));
                it("should reject when extraction stream emits error", (async function () {
                        var fakeInput = new Nodestream.PassThrough();
                        var fakeOutput = new Nodestream.PassThrough();
                        var p = Connection__Download__Unzip$AgdaModeVscode.run(Vscode.Uri.file("/fake/src"), Vscode.Uri.file("/fake/dest"), Caml_option.some((function (param, param$1) {
                                    return [
                                            fakeInput,
                                            fakeOutput
                                          ];
                                  })), undefined);
                        var fakeErr = (new Error("extraction stream error"));
                        fakeOutput.emit("error", fakeErr);
                        return await expectToReject(p);
                      }));
              }));
        describe("cleanupInFlightFiles", (function () {
                it("should delete both in-flight files when they exist", (async function () {
                        var tempDir = Nodepath.join(Nodeos.tmpdir(), "cleanup-inflight-test-" + String(Date.now() | 0));
                        await Nodefs.promises.mkdir(tempDir, {
                              recursive: true,
                              mode: 511
                            });
                        var inFlightFile = Nodepath.join(tempDir, "in-flight.download");
                        var inFlightZipFile = Nodepath.join(tempDir, "in-flight.download.zip");
                        Nodefs.writeFileSync(inFlightFile, Buffer.from("downloading..."));
                        Nodefs.writeFileSync(inFlightZipFile, Buffer.from("downloading..."));
                        Curry._3(Assert.deepStrictEqual, Nodefs.existsSync(inFlightFile), true, undefined);
                        Curry._3(Assert.deepStrictEqual, Nodefs.existsSync(inFlightZipFile), true, undefined);
                        var globalStorageUri = Vscode.Uri.file(tempDir);
                        await Connection__Download__GitHub$AgdaModeVscode.cleanupInFlightFiles(globalStorageUri);
                        var inFlightGone = !Nodefs.existsSync(inFlightFile);
                        var inFlightZipGone = !Nodefs.existsSync(inFlightZipFile);
                        await FS$AgdaModeVscode.deleteRecursive(globalStorageUri);
                        Curry._3(Assert.deepStrictEqual, inFlightGone, true, undefined);
                        return Curry._3(Assert.deepStrictEqual, inFlightZipGone, true, undefined);
                      }));
                it("should succeed when neither in-flight file exists", (async function () {
                        var tempDir = Nodepath.join(Nodeos.tmpdir(), "cleanup-inflight-empty-test-" + String(Date.now() | 0));
                        await Nodefs.promises.mkdir(tempDir, {
                              recursive: true,
                              mode: 511
                            });
                        var inFlightFile = Nodepath.join(tempDir, "in-flight.download");
                        var inFlightZipFile = Nodepath.join(tempDir, "in-flight.download.zip");
                        Curry._3(Assert.deepStrictEqual, Nodefs.existsSync(inFlightFile), false, undefined);
                        Curry._3(Assert.deepStrictEqual, Nodefs.existsSync(inFlightZipFile), false, undefined);
                        var globalStorageUri = Vscode.Uri.file(tempDir);
                        await Connection__Download__GitHub$AgdaModeVscode.cleanupInFlightFiles(globalStorageUri);
                        Curry._3(Assert.deepStrictEqual, Nodefs.existsSync(inFlightFile), false, undefined);
                        Curry._3(Assert.deepStrictEqual, Nodefs.existsSync(inFlightZipFile), false, undefined);
                        await FS$AgdaModeVscode.deleteRecursive(globalStorageUri);
                      }));
              }));
        describe("Integration Tests", (function () {
                describe.skip("`getReleaseManifestFromGitHub` without cache", (function () {
                        it("should fetch release from GitHub API", (async function () {
                                var globalStorageUri = Vscode.Uri.file("/tmp/test-dev-als");
                                var repo = Connection__LatestALS$AgdaModeVscode.makeRepo(globalStorageUri);
                                var memento = Memento$AgdaModeVscode.make(undefined);
                                var releaseResult = await Connection__Download$AgdaModeVscode.getReleaseManifestFromGitHub(memento, repo, false);
                                if (releaseResult.TAG === "Ok") {
                                  Assert.ok(releaseResult._0.length > 0);
                                  return ;
                                }
                                Assert.fail("Failed to fetch releases: " + Connection__Download$AgdaModeVscode.$$Error.toString(releaseResult._0));
                              }));
                      }));
              }));
      }));

var GitHub;

var Unzip;

exports.GitHub = GitHub;
exports.Unzip = Unzip;
/*  Not a pure module */
