// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Curry = require("rescript/lib/js/curry.js");
var Assert = require("assert");
var Util$AgdaModeVscode = require("../../src/Util/Util.bs.js");
var Test__Util$AgdaModeVscode = require("./Test__Util.bs.js");
var Registry__Connection$AgdaModeVscode = require("../../src/Registry__Connection.bs.js");

async function setup() {
  await Registry__Connection$AgdaModeVscode.shutdown();
  return await Util$AgdaModeVscode.Promise_.$$setTimeout(500);
}

function makeDummyConnection() {
  return ({
    TAG: "Agda",
    _0: {
      chan: { removeAllListeners: () => {} },
      process: { status: "Destroyed" },
      encountedFirstPrompt: false,
      version: "2.6.4",
      path: "mock-path"
    },
    _1: "mock-path",
    _2: "2.6.4"
  });
}

function makeDestroyThrowingConnection() {
  return {
    TAG: "Agda",
    _0: {
      // chan: null causes Chan.destroy to throw (calls null.removeAllListeners())
      // which exercises the terminate path where Connection.destroy throws
      chan: null,
      process: { status: "Destroyed" },
      encountedFirstPrompt: false,
      version: "2.6.4",
      path: "mock-path-broken-destroy"
    },
    _1: "mock-path-broken-destroy",
    _2: "2.6.4"
  };
}

describe("Registry__Connection", (function () {
        it("Singleton: acquire returns the same connection for different owners", (async function () {
                await setup();
                var makeCalled = {
                  contents: 0
                };
                var dummyConnection = makeDummyConnection();
                var make = async function () {
                  makeCalled.contents = makeCalled.contents + 1 | 0;
                  return {
                          TAG: "Ok",
                          _0: dummyConnection
                        };
                };
                var res1 = await Registry__Connection$AgdaModeVscode.acquire("owner1", make);
                var res2 = await Registry__Connection$AgdaModeVscode.acquire("owner2", make);
                Curry._3(Assert.deepStrictEqual, res1, {
                      TAG: "Ok",
                      _0: dummyConnection
                    }, undefined);
                Curry._3(Assert.deepStrictEqual, res2, {
                      TAG: "Ok",
                      _0: dummyConnection
                    }, undefined);
                Curry._3(Assert.deepStrictEqual, makeCalled.contents, 1, undefined);
                var view = Registry__Connection$AgdaModeVscode.inspect();
                Curry._3(Assert.deepStrictEqual, view.userCount, 2, undefined);
                return Curry._3(Assert.deepStrictEqual, view.status, "Active", undefined);
              }));
        it("Serialization: concurrent execution is queued", (async function () {
                await setup();
                var dummyConnection = makeDummyConnection();
                var make = async function () {
                  return {
                          TAG: "Ok",
                          _0: dummyConnection
                        };
                };
                await Registry__Connection$AgdaModeVscode.acquire("owner1", make);
                var executionOrder = [];
                var task1 = async function (param) {
                  executionOrder.push("task1-start");
                  await Util$AgdaModeVscode.Promise_.$$setTimeout(50);
                  executionOrder.push("task1-end");
                  return {
                          TAG: "Ok",
                          _0: undefined
                        };
                };
                var task2 = async function (param) {
                  executionOrder.push("task2-start");
                  return {
                          TAG: "Ok",
                          _0: undefined
                        };
                };
                var p1 = Registry__Connection$AgdaModeVscode.execute("owner1", task1);
                var p2 = Registry__Connection$AgdaModeVscode.execute("owner2", task2);
                await p1;
                await p2;
                return Curry._3(Assert.deepStrictEqual, executionOrder, [
                            "task1-start",
                            "task1-end",
                            "task2-start"
                          ], undefined);
              }));
        it("Reentrancy: nested execution from same owner is allowed", (async function () {
                await setup();
                var dummyConnection = makeDummyConnection();
                var make = async function () {
                  return {
                          TAG: "Ok",
                          _0: dummyConnection
                        };
                };
                await Registry__Connection$AgdaModeVscode.acquire("owner1", make);
                var executionOrder = [];
                var task = async function (param) {
                  executionOrder.push("outer-start");
                  await Registry__Connection$AgdaModeVscode.execute("owner1", (async function (param) {
                          executionOrder.push("inner");
                          return {
                                  TAG: "Ok",
                                  _0: undefined
                                };
                        }));
                  executionOrder.push("outer-end");
                  return {
                          TAG: "Ok",
                          _0: undefined
                        };
                };
                await Registry__Connection$AgdaModeVscode.execute("owner1", task);
                return Curry._3(Assert.deepStrictEqual, executionOrder, [
                            "outer-start",
                            "inner",
                            "outer-end"
                          ], undefined);
              }));
        it("execute should recover queue after a task throws", (async function () {
                await setup();
                var dummyConnection = makeDummyConnection();
                var make = async function () {
                  return {
                          TAG: "Ok",
                          _0: dummyConnection
                        };
                };
                await Registry__Connection$AgdaModeVscode.acquire("owner1", make);
                try {
                  await Registry__Connection$AgdaModeVscode.execute("owner1", (async function (param) {
                          throw {
                                RE_EXN_ID: "Failure",
                                _1: "boom",
                                Error: new Error()
                              };
                        }));
                }
                catch (exn){
                  
                }
                var secondResult = {
                  contents: undefined
                };
                var completion = Registry__Connection$AgdaModeVscode.execute("owner2", (async function (param) {
                          return {
                                  TAG: "Ok",
                                  _0: "recovered"
                                };
                        })).then(function (result) {
                      secondResult.contents = result;
                      return "done";
                    });
                var timeout = Util$AgdaModeVscode.Promise_.$$setTimeout(250).then(function () {
                      return "timeout";
                    });
                var winner = await Promise.race([
                      completion,
                      timeout
                    ]);
                switch (winner) {
                  case "done" :
                      return Curry._3(Assert.deepStrictEqual, secondResult.contents, {
                                  TAG: "Ok",
                                  _0: "recovered"
                                }, undefined);
                  case "timeout" :
                      Assert.fail("Queue remains blocked after a thrown task");
                      return ;
                  default:
                    Assert.fail("Unexpected race result");
                    return ;
                }
              }));
        it("acquire should recover after make throws", (async function () {
                await setup();
                var dummyConnection = makeDummyConnection();
                try {
                  await Registry__Connection$AgdaModeVscode.acquire("owner1", (async function () {
                          throw {
                                RE_EXN_ID: "Failure",
                                _1: "connect-boom",
                                Error: new Error()
                              };
                        }));
                }
                catch (exn){
                  
                }
                var secondResult = {
                  contents: undefined
                };
                var completion = Registry__Connection$AgdaModeVscode.acquire("owner2", (async function () {
                          return {
                                  TAG: "Ok",
                                  _0: dummyConnection
                                };
                        })).then(function (result) {
                      secondResult.contents = result;
                      return "done";
                    });
                var timeout = Util$AgdaModeVscode.Promise_.$$setTimeout(250).then(function () {
                      return "timeout";
                    });
                var winner = await Promise.race([
                      completion,
                      timeout
                    ]);
                switch (winner) {
                  case "done" :
                      Registry__Connection$AgdaModeVscode.status.contents = "Empty";
                      return Curry._3(Assert.deepStrictEqual, secondResult.contents, {
                                  TAG: "Ok",
                                  _0: dummyConnection
                                }, undefined);
                  case "timeout" :
                      Registry__Connection$AgdaModeVscode.status.contents = "Empty";
                      Assert.fail("Acquire remains blocked after make throws");
                      return ;
                  default:
                    Registry__Connection$AgdaModeVscode.status.contents = "Empty";
                    Assert.fail("Unexpected race result");
                    return ;
                }
              }));
        it("shutdown should not hang after make throws", (async function () {
                await setup();
                try {
                  await Registry__Connection$AgdaModeVscode.acquire("owner1", (async function () {
                          throw {
                                RE_EXN_ID: "Failure",
                                _1: "connect-boom",
                                Error: new Error()
                              };
                        }));
                }
                catch (exn){
                  
                }
                var completion = Registry__Connection$AgdaModeVscode.shutdown().then(function () {
                      return "done";
                    });
                var timeout = Util$AgdaModeVscode.Promise_.$$setTimeout(250).then(function () {
                      return "timeout";
                    });
                var winner = await Promise.race([
                      completion,
                      timeout
                    ]);
                switch (winner) {
                  case "done" :
                      Registry__Connection$AgdaModeVscode.status.contents = "Empty";
                      Assert.ok(true);
                      return ;
                  case "timeout" :
                      Registry__Connection$AgdaModeVscode.status.contents = "Empty";
                      Assert.fail("Shutdown hangs when Connecting is wedged");
                      return ;
                  default:
                    Registry__Connection$AgdaModeVscode.status.contents = "Empty";
                    Assert.fail("Unexpected race result");
                    return ;
                }
              }));
        it("acquire should not block after destroy throws during terminate", (async function () {
                await setup();
                var brokenConnection = makeDestroyThrowingConnection();
                var dummyConnection = makeDummyConnection();
                await Registry__Connection$AgdaModeVscode.acquire("owner1", (async function () {
                        return {
                                TAG: "Ok",
                                _0: brokenConnection
                              };
                      }));
                try {
                  await Registry__Connection$AgdaModeVscode.release("owner1");
                }
                catch (exn){
                  
                }
                var secondResult = {
                  contents: undefined
                };
                var completion = Registry__Connection$AgdaModeVscode.acquire("owner2", (async function () {
                          return {
                                  TAG: "Ok",
                                  _0: dummyConnection
                                };
                        })).then(function (result) {
                      secondResult.contents = result;
                      return "done";
                    });
                var timeout = Util$AgdaModeVscode.Promise_.$$setTimeout(250).then(function () {
                      return "timeout";
                    });
                var winner = await Promise.race([
                      completion,
                      timeout
                    ]);
                switch (winner) {
                  case "done" :
                      Registry__Connection$AgdaModeVscode.status.contents = "Empty";
                      return Curry._3(Assert.deepStrictEqual, secondResult.contents, {
                                  TAG: "Ok",
                                  _0: dummyConnection
                                }, undefined);
                  case "timeout" :
                      Registry__Connection$AgdaModeVscode.status.contents = "Empty";
                      Assert.fail("Acquire blocks after destroy throws during terminate");
                      return ;
                  default:
                    Registry__Connection$AgdaModeVscode.status.contents = "Empty";
                    Assert.fail("Unexpected race result");
                    return ;
                }
              }));
        it("shutdown should not block after destroy throws during terminate", (async function () {
                await setup();
                var brokenConnection = makeDestroyThrowingConnection();
                await Registry__Connection$AgdaModeVscode.acquire("owner1", (async function () {
                        return {
                                TAG: "Ok",
                                _0: brokenConnection
                              };
                      }));
                try {
                  await Registry__Connection$AgdaModeVscode.release("owner1");
                }
                catch (exn){
                  
                }
                var completion = Registry__Connection$AgdaModeVscode.shutdown().then(function () {
                      return "done";
                    });
                var timeout = Util$AgdaModeVscode.Promise_.$$setTimeout(250).then(function () {
                      return "timeout";
                    });
                var winner = await Promise.race([
                      completion,
                      timeout
                    ]);
                switch (winner) {
                  case "done" :
                      Registry__Connection$AgdaModeVscode.status.contents = "Empty";
                      Assert.ok(true);
                      return ;
                  case "timeout" :
                      Registry__Connection$AgdaModeVscode.status.contents = "Empty";
                      Assert.fail("Shutdown blocks after destroy throws during terminate");
                      return ;
                  default:
                    Registry__Connection$AgdaModeVscode.status.contents = "Empty";
                    Assert.fail("Unexpected race result");
                    return ;
                }
              }));
        it("release during connecting should not leave a phantom user", (async function () {
                await setup();
                var dummyConnection = makeDummyConnection();
                var match = Util$AgdaModeVscode.Promise_.pending();
                var connectionResult = match[0];
                var acquireOwner1 = Registry__Connection$AgdaModeVscode.acquire("owner1", (function () {
                        return connectionResult;
                      }));
                await Util$AgdaModeVscode.Promise_.$$setTimeout(0);
                await Registry__Connection$AgdaModeVscode.release("owner1");
                match[1]({
                      TAG: "Ok",
                      _0: dummyConnection
                    });
                await acquireOwner1;
                await Registry__Connection$AgdaModeVscode.acquire("owner2", (async function () {
                        return {
                                TAG: "Ok",
                                _0: dummyConnection
                              };
                      }));
                await Registry__Connection$AgdaModeVscode.release("owner2");
                var view = Registry__Connection$AgdaModeVscode.inspect();
                Curry._3(Assert.deepStrictEqual, view.status, "Empty", undefined);
                return Curry._3(Assert.deepStrictEqual, view.userCount, 0, undefined);
              }));
        it("Reference Counting: connection is destroyed only when all users release it", (async function () {
                await setup();
                var dummyConnection = makeDummyConnection();
                var make = async function () {
                  return {
                          TAG: "Ok",
                          _0: dummyConnection
                        };
                };
                await Registry__Connection$AgdaModeVscode.acquire("owner1", make);
                await Registry__Connection$AgdaModeVscode.acquire("owner2", make);
                Curry._3(Assert.deepStrictEqual, Registry__Connection$AgdaModeVscode.inspect().userCount, 2, undefined);
                await Registry__Connection$AgdaModeVscode.release("owner1");
                Curry._3(Assert.deepStrictEqual, Registry__Connection$AgdaModeVscode.inspect().userCount, 1, undefined);
                Curry._3(Assert.deepStrictEqual, Registry__Connection$AgdaModeVscode.inspect().status, "Active", undefined);
                await Registry__Connection$AgdaModeVscode.release("owner2");
                return Curry._3(Assert.deepStrictEqual, Registry__Connection$AgdaModeVscode.inspect().status, "Empty", undefined);
              }));
        describe("Real-world Scenario", (function () {
                this.timeout(30000);
                it("should share a single connection mechanism between two different Agda files", (async function () {
                        await setup();
                        var agdaA = await Test__Util$AgdaModeVscode.AgdaMode.makeAndLoad("GotoDefinition.agda");
                        var view1 = Registry__Connection$AgdaModeVscode.inspect();
                        Curry._3(Assert.deepStrictEqual, view1.makeCount, 1, undefined);
                        Curry._3(Assert.deepStrictEqual, view1.userCount, 1, undefined);
                        var agdaB = await Test__Util$AgdaModeVscode.AgdaMode.makeAndLoad("Lib.agda");
                        var view2 = Registry__Connection$AgdaModeVscode.inspect();
                        Curry._3(Assert.deepStrictEqual, view2.makeCount, 1, undefined);
                        Curry._3(Assert.deepStrictEqual, view2.userCount, 2, undefined);
                        Curry._3(Assert.deepStrictEqual, view2.status, "Active", undefined);
                        await Test__Util$AgdaModeVscode.AgdaMode.quit(agdaA);
                        Curry._3(Assert.deepStrictEqual, Registry__Connection$AgdaModeVscode.inspect().userCount, 1, undefined);
                        await Test__Util$AgdaModeVscode.AgdaMode.quit(agdaB);
                        return Curry._3(Assert.deepStrictEqual, Registry__Connection$AgdaModeVscode.inspect().status, "Empty", undefined);
                      }));
              }));
      }));

exports.setup = setup;
exports.makeDummyConnection = makeDummyConnection;
exports.makeDestroyThrowingConnection = makeDestroyThrowingConnection;
/*  Not a pure module */
