// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Curry = require("rescript/lib/js/curry.js");
var Assert = require("assert");
var Vscode = require("vscode");
var Nodefs = require("node:fs");
var Nodeos = require("node:os");
var Nodepath = require("node:path");
var FS$AgdaModeVscode = require("../../src/FS.bs.js");
var Mock$AgdaModeVscode = require("../../src/Main/Mock.bs.js");
var Desktop$AgdaModeVscode = require("../../src/Main/Desktop.bs.js");
var Connection__Download__Platform$AgdaModeVscode = require("../../src/Connection/Download/Connection__Download__Platform.bs.js");

async function withTempStorage(prefix, f) {
  var tempDir = Nodepath.join(Nodeos.tmpdir(), prefix + String(Date.now() | 0));
  var globalStorageUri = Vscode.Uri.file(tempDir);
  try {
    await Nodefs.promises.mkdir(tempDir, {
          recursive: true,
          mode: 511
        });
    await f(tempDir, globalStorageUri);
  }
  catch (exn){
    await FS$AgdaModeVscode.deleteRecursive(globalStorageUri);
    throw exn;
  }
  await FS$AgdaModeVscode.deleteRecursive(globalStorageUri);
}

describe("Platform dependent utilities", (function () {
        describe("Platform Abstraction", (function () {
                it("should create Desktop platform and have working operations", (async function () {
                        var platformDeps = Desktop$AgdaModeVscode.make();
                        await platformDeps.determinePlatform();
                        Assert.ok(true);
                      }));
                it("desktop alreadyDownloaded should find release-managed WASM artifact", (async function () {
                        return await withTempStorage("desktop-release-wasm-test-", (async function (tempDir, globalStorageUri) {
                                      var artifactDir = Nodepath.join(tempDir, "releases", "dev", "als-dev-Agda-2.8.0-wasm");
                                      var wasmFile = Nodepath.join(artifactDir, "als.wasm");
                                      await Nodefs.promises.mkdir(artifactDir, {
                                            recursive: true,
                                            mode: 511
                                          });
                                      Nodefs.writeFileSync(wasmFile, Buffer.from("mock wasm"));
                                      var platformDeps = Desktop$AgdaModeVscode.make();
                                      var result = await platformDeps.alreadyDownloaded(globalStorageUri);
                                      return Curry._3(Assert.deepStrictEqual, result, Vscode.Uri.file(wasmFile).toString(), undefined);
                                    }));
                      }));
                it("desktop alreadyDownloaded should find release-managed native artifact", (async function () {
                        var platformDeps = Desktop$AgdaModeVscode.make();
                        var platform = await platformDeps.determinePlatform();
                        if (platform.TAG === "Ok") {
                          var platform$1 = platform._0;
                          return await withTempStorage("desktop-release-latest-test-", (async function (tempDir, globalStorageUri) {
                                        var platformTag = Connection__Download__Platform$AgdaModeVscode.toAssetName(platform$1);
                                        var artifactDir = Nodepath.join(tempDir, "releases", "v6", "als-v6-Agda-2.8.0-" + platformTag);
                                        var alsExecutable = Nodepath.join(artifactDir, "als");
                                        await Nodefs.promises.mkdir(artifactDir, {
                                              recursive: true,
                                              mode: 511
                                            });
                                        Nodefs.writeFileSync(alsExecutable, Buffer.from("mock executable"));
                                        var result = await platformDeps.alreadyDownloaded(globalStorageUri);
                                        return Curry._3(Assert.deepStrictEqual, result, Vscode.Uri.file(alsExecutable).fsPath, undefined);
                                      }));
                        }
                        Assert.ok(true);
                      }));
              }));
        describe("Mock Platform for Testing", (function () {
                it("should allow custom mock platforms to be created for testing", (async function () {
                        var mockPlatformDeps = Mock$AgdaModeVscode.Platform.makeBasic();
                        var platformResult = await mockPlatformDeps.determinePlatform();
                        if (platformResult.TAG === "Ok") {
                          if (platformResult._0 === "MacOS_Arm") {
                            Assert.ok(true);
                            return ;
                          }
                          Assert.fail("Mock platform should return MacOS_Arm");
                          return ;
                        }
                        Assert.fail("Mock platform should return MacOS_Arm");
                      }));
              }));
      }));

exports.withTempStorage = withTempStorage;
/*  Not a pure module */
