// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Caml = require("rescript/lib/js/caml.js");
var Curry = require("rescript/lib/js/curry.js");
var Assert = require("assert");
var Vscode = require("vscode");
var Js_dict = require("rescript/lib/js/js_dict.js");
var Nodefs = require("node:fs");
var Nodeos = require("node:os");
var Nodepath = require("node:path");
var PervasivesU = require("rescript/lib/js/pervasivesU.js");
var Core__Option = require("@rescript/core/lib/js/src/Core__Option.bs.js");
var Promises = require("node:fs/promises");
var FS$AgdaModeVscode = require("../../src/FS.bs.js");
var OS$AgdaModeVscode = require("../../src/Util/OS.bs.js");
var Caml_js_exceptions = require("rescript/lib/js/caml_js_exceptions.js");
var Log$AgdaModeVscode = require("../../src/State/Log.bs.js");
var Chan$AgdaModeVscode = require("../../src/Util/Chan.bs.js");
var Mock$AgdaModeVscode = require("../../src/Main/Mock.bs.js");
var Config$AgdaModeVscode = require("../../src/Config.bs.js");
var Desktop$AgdaModeVscode = require("../../src/Main/Desktop.bs.js");
var Memento$AgdaModeVscode = require("../../src/Memento.bs.js");
var Connection$AgdaModeVscode = require("../../src/Connection/Connection.bs.js");
var Test__Util$AgdaModeVscode = require("./Test__Util.bs.js");
var Connection__URI$AgdaModeVscode = require("../../src/Connection/Shared/Connection__URI.bs.js");
var Connection__Command$AgdaModeVscode = require("../../src/Connection/Resolver/Connection__Command.bs.js");
var Connection__Download$AgdaModeVscode = require("../../src/Connection/Download/Connection__Download.bs.js");
var Connection__Download__Channel$AgdaModeVscode = require("../../src/Connection/Download/Connection__Download__Channel.bs.js");

async function withTempStorage(prefix, f) {
  var tempDir = await Promises.mkdtemp(Nodepath.join(Nodeos.tmpdir(), prefix));
  var globalStorageUri = Vscode.Uri.file(tempDir);
  var cleanup = async function () {
    await FS$AgdaModeVscode.deleteRecursive(globalStorageUri);
  };
  try {
    await f(tempDir, globalStorageUri);
    return await cleanup();
  }
  catch (exn){
    await cleanup();
    throw exn;
  }
}

async function getAgdaTarget() {
  var platformDeps = Desktop$AgdaModeVscode.make();
  var connection = await Connection$AgdaModeVscode.fromPathsOrCommands(platformDeps, [[
          "agda",
          "FromConfig"
        ]]);
  if (connection.TAG === "Ok") {
    return connection._0;
  } else {
    return PervasivesU.failwith("expected to find `agda`");
  }
}

describe("Connection", (function () {
        this.timeout(10000);
        describe("Target", (function () {
                var agdaMockPath = {
                  contents: ""
                };
                var agdaMockEndpoint = {
                  contents: undefined
                };
                before(async function () {
                      agdaMockPath.contents = await Test__Util$AgdaModeVscode.Candidate.Agda.mock("2.7.0.1", "agda-mock");
                      agdaMockEndpoint.contents = agdaMockPath.contents;
                    });
                after(async function () {
                      var target = agdaMockEndpoint.contents;
                      if (target !== undefined) {
                        await Test__Util$AgdaModeVscode.Candidate.Agda.destroy(target);
                        agdaMockEndpoint.contents = undefined;
                        return ;
                      }
                      
                    });
              }));
        describe("Command searching", (function () {
                it("should return an error when the command is not found", (async function () {
                        var _output = await Connection__Command$AgdaModeVscode.search("non-existent-command", undefined);
                        if (_output.TAG === "Ok") {
                          return PervasivesU.failwith("expected to not find `non-existent-command`");
                        }
                        
                      }));
              }));
        describe("checkForPrebuiltDataDirectory", (function () {
                it("should return asset path when data directory exists", (async function () {
                        var tempDir = Nodepath.join(Nodeos.tmpdir(), "agda-test-" + String(Date.now() | 0));
                        var execPath = Nodepath.join(tempDir, "bin", "agda-language-server");
                        var dataDir = Nodepath.join(tempDir, "bin", "data");
                        await Nodefs.promises.mkdir(Nodepath.join(tempDir, "bin"), {
                              recursive: true,
                              mode: 511
                            });
                        await Nodefs.promises.mkdir(dataDir, {
                              recursive: true,
                              mode: 511
                            });
                        var result = await Connection$AgdaModeVscode.checkForPrebuiltDataDirectory(execPath);
                        var expectedAssetPath = Nodepath.join(execPath, "..", "data");
                        Curry._3(Assert.deepStrictEqual, result, expectedAssetPath, undefined);
                        Nodefs.rmdirSync(dataDir);
                        Nodefs.rmdirSync(Nodepath.join(tempDir, "bin"));
                        Nodefs.rmdirSync(tempDir);
                      }));
                it("should return None when data directory does not exist", (async function () {
                        var tempDir = Nodepath.join(Nodeos.tmpdir(), "agda-test-" + String(Date.now() | 0));
                        var execPath = Nodepath.join(tempDir, "bin", "agda-language-server");
                        await Nodefs.promises.mkdir(Nodepath.join(tempDir, "bin"), {
                              recursive: true,
                              mode: 511
                            });
                        var result = await Connection$AgdaModeVscode.checkForPrebuiltDataDirectory(execPath);
                        Curry._3(Assert.deepStrictEqual, result, undefined, undefined);
                        Nodefs.rmdirSync(Nodepath.join(tempDir, "bin"));
                        Nodefs.rmdirSync(tempDir);
                      }));
              }));
        describe("`probeFilepath`", (function () {
                var agdaMockPath = {
                  contents: ""
                };
                var alsMockPath = {
                  contents: ""
                };
                before(async function () {
                      agdaMockPath.contents = await Test__Util$AgdaModeVscode.Candidate.Agda.mock("2.6.4.1", "agda-probe-mock");
                      alsMockPath.contents = await Test__Util$AgdaModeVscode.Candidate.ALS.mock("1.2.3", "2.6.4", "als-probe-mock");
                    });
                after(async function () {
                      if (agdaMockPath.contents !== "") {
                        await Test__Util$AgdaModeVscode.Candidate.Agda.destroy(agdaMockPath.contents);
                      }
                      if (alsMockPath.contents !== "") {
                        return await Test__Util$AgdaModeVscode.Candidate.ALS.destroy(alsMockPath.contents);
                      }
                      
                    });
                it("should correctly identify Agda executable", (async function () {
                        var result = await Connection$AgdaModeVscode.probeFilepath(agdaMockPath.contents);
                        if (result.TAG === "Ok") {
                          var match = result._0;
                          var version = match[1];
                          switch (version.TAG) {
                            case "IsAgda" :
                                Curry._3(Assert.deepStrictEqual, match[0], agdaMockPath.contents, undefined);
                                return Curry._3(Assert.deepStrictEqual, version._0, "2.6.4.1", undefined);
                            case "IsALS" :
                            case "IsALSWASM" :
                                Assert.fail("Expected Agda result, got ALS");
                                return ;
                            
                          }
                        } else {
                          Assert.fail("Expected successful probe of Agda mock");
                          return ;
                        }
                      }));
                it("should correctly identify ALS executable", (async function () {
                        var result = await Connection$AgdaModeVscode.probeFilepath(alsMockPath.contents);
                        if (result.TAG === "Ok") {
                          var match = result._0;
                          var match$1 = match[1];
                          switch (match$1.TAG) {
                            case "IsALS" :
                                Curry._3(Assert.deepStrictEqual, match[0], alsMockPath.contents, undefined);
                                Curry._3(Assert.deepStrictEqual, match$1._0, "1.2.3", undefined);
                                Curry._3(Assert.deepStrictEqual, match$1._1, "2.6.4", undefined);
                                return Curry._3(Assert.deepStrictEqual, match$1._2, undefined, undefined);
                            case "IsAgda" :
                            case "IsALSWASM" :
                                Assert.fail("Expected ALS result, got Agda");
                                return ;
                            
                          }
                        } else {
                          Assert.fail("Expected successful probe of ALS mock");
                          return ;
                        }
                      }));
                it("should return NotAgdaOrALS error for unrecognized executable", (async function () {
                        var mockOutput = "Some other program version 1.0";
                        var mockPath;
                        if (OS$AgdaModeVscode.onUnix) {
                          var content = "#!/bin/sh\necho '" + mockOutput + "'\nexit 0";
                          var tempFile = Nodepath.join(Nodeos.tmpdir(), "unrecognized-mock");
                          Nodefs.writeFileSync(tempFile, Buffer.from(content));
                          await Nodefs.promises.chmod(tempFile, 493);
                          mockPath = tempFile;
                        } else {
                          var content$1 = "@echo " + mockOutput;
                          var tempFile$1 = Nodepath.join(Nodeos.tmpdir(), "unrecognized-mock.bat");
                          Nodefs.writeFileSync(tempFile$1, Buffer.from(content$1));
                          mockPath = tempFile$1;
                        }
                        var result = await Connection$AgdaModeVscode.probeFilepath(mockPath);
                        if (result.TAG === "Ok") {
                          Assert.fail("Expected NotAgdaOrALS error");
                        } else {
                          var output = result._0;
                          if (output.TAG === "NotAgdaOrALS") {
                            Curry._3(Assert.deepStrictEqual, output._0.trim(), mockOutput, undefined);
                          } else {
                            Assert.fail("Expected NotAgdaOrALS error, got different error");
                          }
                        }
                        try {
                          Nodefs.unlinkSync(mockPath);
                          return ;
                        }
                        catch (exn){
                          return ;
                        }
                      }));
                it("should return CannotDetermineAgdaOrALS error for non-executable file", (async function () {
                        var nonExecutablePath = Nodepath.join(Nodeos.tmpdir(), "non-executable.txt");
                        Nodefs.writeFileSync(nonExecutablePath, Buffer.from("not executable"));
                        var result = await Connection$AgdaModeVscode.probeFilepath(nonExecutablePath);
                        if (result.TAG === "Ok") {
                          Assert.fail("Expected CannotDetermineAgdaOrALS error");
                        } else if (result._0.TAG !== "CannotDetermineAgdaOrALS") {
                          Assert.fail("Expected CannotDetermineAgdaOrALS error, got different error");
                        }
                        try {
                          Nodefs.unlinkSync(nonExecutablePath);
                          return ;
                        }
                        catch (exn){
                          return ;
                        }
                      }));
                it("should return CannotDetermineAgdaOrALS error for non-existent file", (async function () {
                        var result = await Connection$AgdaModeVscode.probeFilepath("/path/that/does/not/exist/executable");
                        if (result.TAG === "Ok") {
                          Assert.fail("Expected CannotDetermineAgdaOrALS error");
                          return ;
                        }
                        if (result._0.TAG === "CannotDetermineAgdaOrALS") {
                          return ;
                        }
                        Assert.fail("Expected CannotDetermineAgdaOrALS error, got different error");
                      }));
                it("should detect prebuilt data directory for ALS with LSP options", (async function () {
                        var tempDir = Nodepath.join(Nodeos.tmpdir(), "als-data-test-" + String(Date.now() | 0));
                        var binDir = Nodepath.join(tempDir, "bin");
                        var dataDir = Nodepath.join(binDir, "data");
                        var alsPath = Nodepath.join(binDir, "als-with-data");
                        await Nodefs.promises.mkdir(binDir, {
                              recursive: true,
                              mode: 511
                            });
                        await Nodefs.promises.mkdir(dataDir, {
                              recursive: true,
                              mode: 511
                            });
                        var alsOutput = "Agda v2.6.4 Language Server v1.2.3";
                        var match = OS$AgdaModeVscode.onUnix ? [
                            alsPath,
                            "#!/bin/sh\necho '" + alsOutput + "'\nexit 0"
                          ] : [
                            alsPath + ".bat",
                            "@echo " + alsOutput
                          ];
                        var fileName = match[0];
                        Nodefs.writeFileSync(fileName, Buffer.from(match[1]));
                        if (OS$AgdaModeVscode.onUnix) {
                          await Nodefs.promises.chmod(fileName, 493);
                        }
                        var result = await Connection$AgdaModeVscode.probeFilepath(fileName);
                        var match$1 = Connection__URI$AgdaModeVscode.parse(fileName);
                        var normalizedFileName = match$1._1.fsPath;
                        if (result.TAG === "Ok") {
                          var match$2 = result._0;
                          var match$3 = match$2[1];
                          switch (match$3.TAG) {
                            case "IsAgda" :
                                Assert.fail("Expected ALS result, got Agda");
                                break;
                            case "IsALS" :
                                var lspOptions = match$3._2;
                                if (lspOptions !== undefined) {
                                  Curry._3(Assert.deepStrictEqual, match$2[0], normalizedFileName, undefined);
                                  Curry._3(Assert.deepStrictEqual, match$3._0, "1.2.3", undefined);
                                  Curry._3(Assert.deepStrictEqual, match$3._1, "2.6.4", undefined);
                                  var dataDirPath = Core__Option.flatMap(lspOptions.env, (function (__x) {
                                          return Js_dict.get(__x, "Agda_datadir");
                                        }));
                                  if (dataDirPath !== undefined) {
                                    Assert.ok(dataDirPath.includes("data"));
                                  } else {
                                    Assert.fail("Expected Agda_datadir in LSP options");
                                  }
                                } else {
                                  Assert.fail("Expected LSP options with data directory");
                                }
                                break;
                            case "IsALSWASM" :
                                Assert.fail("Expected ALS result, got ALSWASM");
                                break;
                            
                          }
                        } else {
                          Assert.fail("Expected successful probe of ALS with data directory");
                        }
                        try {
                          Nodefs.unlinkSync(fileName);
                          Nodefs.rmdirSync(dataDir);
                          Nodefs.rmdirSync(binDir);
                          Nodefs.rmdirSync(tempDir);
                          return ;
                        }
                        catch (exn){
                          return ;
                        }
                      }));
              }));
        describe("`make`", (function () {
                var agdaMockPath = {
                  contents: ""
                };
                var alsMockPath = {
                  contents: ""
                };
                before(async function () {
                      agdaMockPath.contents = await Test__Util$AgdaModeVscode.Candidate.Agda.mock("2.6.3", "agda-make-mock");
                      alsMockPath.contents = await Test__Util$AgdaModeVscode.Candidate.ALS.mock("1.3.1", "2.6.3", "als-make-mock");
                    });
                after(async function () {
                      if (agdaMockPath.contents !== "") {
                        await Test__Util$AgdaModeVscode.Candidate.Agda.destroy(agdaMockPath.contents);
                      }
                      if (alsMockPath.contents !== "") {
                        return await Test__Util$AgdaModeVscode.Candidate.ALS.destroy(alsMockPath.contents);
                      }
                      
                    });
                it("should successfully create Agda connection from valid Agda path", (async function () {
                        var result = await Connection$AgdaModeVscode.make(agdaMockPath.contents, "FromConfig");
                        if (result.TAG === "Ok") {
                          var connection = result._0;
                          switch (connection.TAG) {
                            case "Agda" :
                                Curry._3(Assert.deepStrictEqual, connection._1, agdaMockPath.contents, undefined);
                                return Curry._3(Assert.deepStrictEqual, connection._2, "2.6.3", undefined);
                            case "ALS" :
                                Assert.fail("Expected Agda connection, got ALS");
                                return ;
                            case "ALSWASM" :
                                Assert.fail("Expected Agda connection, got ALSWASM");
                                return ;
                            
                          }
                        } else {
                          Assert.fail("Expected successful connection creation");
                          return ;
                        }
                      }));
                it("should handle ALS executable probing (without full connection)", (async function () {
                        this.retries(2);
                        var result = await Connection$AgdaModeVscode.probeFilepath(alsMockPath.contents);
                        if (result.TAG === "Ok") {
                          var match = result._0;
                          var match$1 = match[1];
                          switch (match$1.TAG) {
                            case "IsAgda" :
                                Assert.fail("Expected ALS result, got Agda");
                                return ;
                            case "IsALS" :
                                Curry._3(Assert.deepStrictEqual, match[0], alsMockPath.contents, undefined);
                                Curry._3(Assert.deepStrictEqual, match$1._0, "1.3.1", undefined);
                                Curry._3(Assert.deepStrictEqual, match$1._1, "2.6.3", undefined);
                                return Curry._3(Assert.deepStrictEqual, match$1._2, undefined, undefined);
                            case "IsALSWASM" :
                                Assert.fail("Expected ALS result, got ALSWASM");
                                return ;
                            
                          }
                        } else {
                          Assert.fail("Expected successful probe");
                          return ;
                        }
                      }));
                it("should return Establish error for non-existent path", (async function () {
                        var nonExistentPath = "/path/that/does/not/exist/agda";
                        var result = await Connection$AgdaModeVscode.make(nonExistentPath, "FromConfig");
                        if (result.TAG === "Ok") {
                          Assert.fail("Expected error for non-existent path");
                          return ;
                        }
                        var match = result._0.probes[nonExistentPath];
                        if (match !== undefined) {
                          if (match[0].TAG === "CannotDetermineAgdaOrALS") {
                            return ;
                          }
                          Assert.fail("Expected CannotDetermineAgdaOrALS error");
                          return ;
                        }
                        Assert.fail("Expected probe error for non-existent path");
                      }));
                it("should return Establish error for unrecognized executable", (async function () {
                        var mockOutput = "Unknown Program v1.0.0";
                        var mockPath;
                        if (OS$AgdaModeVscode.onUnix) {
                          var content = "#!/bin/sh\necho '" + mockOutput + "'\nexit 0";
                          var tempFile = Nodepath.join(Nodeos.tmpdir(), "unrecognized-make-mock");
                          Nodefs.writeFileSync(tempFile, Buffer.from(content));
                          await Nodefs.promises.chmod(tempFile, 493);
                          mockPath = tempFile;
                        } else {
                          var content$1 = "@echo " + mockOutput;
                          var tempFile$1 = Nodepath.join(Nodeos.tmpdir(), "unrecognized-make-mock.bat");
                          Nodefs.writeFileSync(tempFile$1, Buffer.from(content$1));
                          mockPath = tempFile$1;
                        }
                        var result = await Connection$AgdaModeVscode.make(mockPath, "FromConfig");
                        if (result.TAG === "Ok") {
                          Assert.fail("Expected error for unrecognized executable");
                        } else {
                          var match = result._0.probes[mockPath];
                          if (match !== undefined) {
                            var error = match[0];
                            if (error.TAG === "NotAgdaOrALS") {
                              Curry._3(Assert.deepStrictEqual, error._0.trim(), mockOutput, undefined);
                            } else {
                              Assert.fail("Expected NotAgdaOrALS error");
                            }
                          } else {
                            Assert.fail("Expected probe error for unrecognized executable");
                          }
                        }
                        try {
                          Nodefs.unlinkSync(mockPath);
                          return ;
                        }
                        catch (exn){
                          return ;
                        }
                      }));
                it("should detect prebuilt data directory correctly in probing phase", (async function () {
                        var tempDir = Nodepath.join(Nodeos.tmpdir(), "als-probe-data-test-" + String(Date.now() | 0));
                        var binDir = Nodepath.join(tempDir, "bin");
                        var dataDir = Nodepath.join(binDir, "data");
                        var alsPath = Nodepath.join(binDir, "als-with-data-probe-test");
                        await Nodefs.promises.mkdir(binDir, {
                              recursive: true,
                              mode: 511
                            });
                        await Nodefs.promises.mkdir(dataDir, {
                              recursive: true,
                              mode: 511
                            });
                        var alsOutput = "Agda v2.6.3 Language Server v1.3.1";
                        var match = OS$AgdaModeVscode.onUnix ? [
                            alsPath,
                            "#!/bin/sh\necho '" + alsOutput + "'\nexit 0"
                          ] : [
                            alsPath + ".bat",
                            "@echo " + alsOutput
                          ];
                        var fileName = match[0];
                        Nodefs.writeFileSync(fileName, Buffer.from(match[1]));
                        if (OS$AgdaModeVscode.onUnix) {
                          await Nodefs.promises.chmod(fileName, 493);
                        }
                        var result = await Connection$AgdaModeVscode.probeFilepath(fileName);
                        var match$1 = Connection__URI$AgdaModeVscode.parse(fileName);
                        var normalizedFileName = match$1._1.fsPath;
                        if (result.TAG === "Ok") {
                          var match$2 = result._0;
                          var match$3 = match$2[1];
                          switch (match$3.TAG) {
                            case "IsAgda" :
                                Assert.fail("Expected ALS result, got Agda");
                                break;
                            case "IsALS" :
                                var lspOptions = match$3._2;
                                if (lspOptions !== undefined) {
                                  Curry._3(Assert.deepStrictEqual, match$2[0], normalizedFileName, undefined);
                                  Curry._3(Assert.deepStrictEqual, match$3._0, "1.3.1", undefined);
                                  Curry._3(Assert.deepStrictEqual, match$3._1, "2.6.3", undefined);
                                  var dataDirPath = Core__Option.flatMap(lspOptions.env, (function (__x) {
                                          return Js_dict.get(__x, "Agda_datadir");
                                        }));
                                  if (dataDirPath !== undefined) {
                                    Assert.ok(dataDirPath.includes("data"));
                                  } else {
                                    Assert.fail("Expected Agda_datadir in LSP options");
                                  }
                                } else {
                                  Assert.fail("Expected LSP options with data directory");
                                }
                                break;
                            case "IsALSWASM" :
                                Assert.fail("Expected ALS result, got ALSWASM");
                                break;
                            
                          }
                        } else {
                          Assert.fail("Expected successful probe with data directory");
                        }
                        try {
                          Nodefs.unlinkSync(fileName);
                          Nodefs.rmdirSync(dataDir);
                          Nodefs.rmdirSync(binDir);
                          Nodefs.rmdirSync(tempDir);
                          return ;
                        }
                        catch (exn){
                          return ;
                        }
                      }));
                it("should handle error construction correctly for probe errors", (async function () {
                        var invalidPath = "/definitely/does/not/exist/agda";
                        var result = await Connection$AgdaModeVscode.make(invalidPath, "FromConfig");
                        if (result.TAG === "Ok") {
                          Assert.fail("Expected construction error");
                          return ;
                        }
                        var errors = result._0;
                        Curry._3(Assert.deepStrictEqual, Object.entries(errors.probes).length, 1, undefined);
                        Curry._3(Assert.deepStrictEqual, Object.entries(errors.commands).length, 0, undefined);
                        Curry._3(Assert.deepStrictEqual, errors.download, "NotAttempted", undefined);
                        var match = errors.probes[invalidPath];
                        if (match !== undefined) {
                          if (match[0].TAG === "CannotDetermineAgdaOrALS") {
                            return ;
                          }
                          Assert.fail("Expected CannotDetermineAgdaOrALS error");
                          return ;
                        }
                        Assert.fail("Expected probe error to be present");
                      }));
              }));
        describe("`fromPathsOrCommands`", (function () {
                describe("paths", (function () {
                        var agdaMockEndpoint = {
                          contents: undefined
                        };
                        before(async function () {
                              try {
                                var path = await Test__Util$AgdaModeVscode.Candidate.Agda.mock("2.7.0.1", "agda-mock-paths-or-commands");
                                agdaMockEndpoint.contents = path;
                                return ;
                              }
                              catch (raw_error){
                                var error = Caml_js_exceptions.internalToOCamlException(raw_error);
                                return PervasivesU.failwith("Failed to create Agda mock: " + String(error));
                              }
                            });
                        after(async function () {
                              var path = agdaMockEndpoint.contents;
                              if (path === undefined) {
                                return ;
                              }
                              try {
                                Nodefs.unlinkSync(path);
                                return ;
                              }
                              catch (exn){
                                return ;
                              }
                            });
                        it("should connect successfully with valid path", (async function () {
                                var path = agdaMockEndpoint.contents;
                                var mockPath = path !== undefined ? path : PervasivesU.failwith("Mock candidate not available");
                                var platformDeps = Mock$AgdaModeVscode.Platform.makeBasic();
                                var paths = [[
                                    mockPath,
                                    "FromConfig"
                                  ]];
                                var result = await Connection$AgdaModeVscode.fromPathsOrCommands(platformDeps, paths);
                                if (result.TAG === "Ok") {
                                  var connection = result._0;
                                  switch (connection.TAG) {
                                    case "Agda" :
                                        Curry._3(Assert.deepStrictEqual, connection._1, mockPath, undefined);
                                        return Curry._3(Assert.deepStrictEqual, connection._2, "2.7.0.1", undefined);
                                    case "ALS" :
                                    case "ALSWASM" :
                                        Assert.fail("Expected Agda connection");
                                        return ;
                                    
                                  }
                                } else {
                                  Assert.fail("Expected successful connection");
                                  return ;
                                }
                              }));
                        it("should try multiple paths and use first valid one", (async function () {
                                var path = agdaMockEndpoint.contents;
                                var mockPath = path !== undefined ? path : PervasivesU.failwith("Mock candidate not available");
                                var platformDeps = Mock$AgdaModeVscode.Platform.makeBasic();
                                var paths = [
                                  [
                                    "invalid/path/1",
                                    "FromConfig"
                                  ],
                                  [
                                    "invalid/path/2",
                                    "FromConfig"
                                  ],
                                  [
                                    mockPath,
                                    "FromConfig"
                                  ],
                                  [
                                    "invalid/path/3",
                                    "FromConfig"
                                  ]
                                ];
                                var result = await Connection$AgdaModeVscode.fromPathsOrCommands(platformDeps, paths);
                                if (result.TAG === "Ok") {
                                  var connection = result._0;
                                  switch (connection.TAG) {
                                    case "Agda" :
                                        Curry._3(Assert.deepStrictEqual, connection._1, mockPath, undefined);
                                        return Curry._3(Assert.deepStrictEqual, connection._2, "2.7.0.1", undefined);
                                    case "ALS" :
                                    case "ALSWASM" :
                                        Assert.fail("Expected Agda connection");
                                        return ;
                                    
                                  }
                                } else {
                                  Assert.fail("Expected successful connection to first valid path");
                                  return ;
                                }
                              }));
                        it("should return Construction error when all paths are invalid", (async function () {
                                var platformDeps = Mock$AgdaModeVscode.Platform.makeBasic();
                                var paths = [
                                  [
                                    "invalid/path/1",
                                    "FromConfig"
                                  ],
                                  [
                                    "invalid/path/2",
                                    "FromConfig"
                                  ],
                                  [
                                    "invalid/path/3",
                                    "FromConfig"
                                  ]
                                ];
                                var result = await Connection$AgdaModeVscode.fromPathsOrCommands(platformDeps, paths);
                                if (result.TAG === "Ok") {
                                  Assert.fail("Expected error with invalid paths");
                                  return ;
                                }
                                var errors = result._0;
                                var probeErrors = Object.entries(errors.probes);
                                Curry._3(Assert.deepStrictEqual, probeErrors.length, 3, undefined);
                                var commandErrors = Object.entries(errors.commands);
                                Curry._3(Assert.deepStrictEqual, commandErrors.length, 0, undefined);
                                return Curry._3(Assert.deepStrictEqual, errors.download, "NotAttempted", undefined);
                              }));
                        it("should preserve FromConfig as probe source for resource candidates", (async function () {
                                var platformDeps = Mock$AgdaModeVscode.Platform.makeBasic();
                                var path = "/definitely/not/a/real/agda";
                                var result = await Connection$AgdaModeVscode.fromPathsOrCommands(platformDeps, [[
                                        path,
                                        "FromConfig"
                                      ]]);
                                if (result.TAG === "Ok") {
                                  Assert.fail("Expected error with invalid resource candidate");
                                  return ;
                                }
                                var match = result._0.probes[path];
                                if (match !== undefined) {
                                  return Curry._3(Assert.deepStrictEqual, match[1], "FromConfig", undefined);
                                } else {
                                  Assert.fail("Expected probe error entry for invalid resource candidate");
                                  return ;
                                }
                              }));
                        it("should return empty error when no paths provided", (async function () {
                                var platformDeps = Mock$AgdaModeVscode.Platform.makeBasic();
                                var paths = [];
                                var result = await Connection$AgdaModeVscode.fromPathsOrCommands(platformDeps, paths);
                                if (result.TAG === "Ok") {
                                  Assert.fail("Expected error with empty paths");
                                  return ;
                                }
                                var errors = result._0;
                                var probeErrors = Object.entries(errors.probes);
                                Curry._3(Assert.deepStrictEqual, probeErrors.length, 0, undefined);
                                var commandErrors = Object.entries(errors.commands);
                                Curry._3(Assert.deepStrictEqual, commandErrors.length, 0, undefined);
                                return Curry._3(Assert.deepStrictEqual, errors.download, "NotAttempted", undefined);
                              }));
                      }));
                describe("commands", (function () {
                        it("should connect successfully with valid command", (async function () {
                                var platformDeps = Desktop$AgdaModeVscode.make();
                                var commands = [
                                  [
                                    "agda",
                                    "FromConfig"
                                  ],
                                  [
                                    "als",
                                    "FromConfig"
                                  ]
                                ];
                                var result = await Connection$AgdaModeVscode.fromPathsOrCommands(platformDeps, commands);
                                if (result.TAG === "Ok") {
                                  return ;
                                }
                                Assert.fail("Expected successful connection via command");
                              }));
                        it("should try multiple commands and use first valid one", (async function () {
                                var platformDeps = Desktop$AgdaModeVscode.make();
                                var commands = [
                                  [
                                    "non-existent-cmd",
                                    "FromConfig"
                                  ],
                                  [
                                    "agda",
                                    "FromConfig"
                                  ],
                                  [
                                    "als",
                                    "FromConfig"
                                  ]
                                ];
                                var result = await Connection$AgdaModeVscode.fromPathsOrCommands(platformDeps, commands);
                                if (result.TAG === "Ok") {
                                  return ;
                                }
                                Assert.fail("Expected successful connection to valid command");
                              }));
                        it("should return Construction error when all commands are invalid", (async function () {
                                var platformDeps = Desktop$AgdaModeVscode.make();
                                var commands = [
                                  [
                                    "non-existent-cmd-1",
                                    "FromConfig"
                                  ],
                                  [
                                    "non-existent-cmd-2",
                                    "FromConfig"
                                  ],
                                  [
                                    "non-existent-cmd-3",
                                    "FromConfig"
                                  ]
                                ];
                                var result = await Connection$AgdaModeVscode.fromPathsOrCommands(platformDeps, commands);
                                if (result.TAG === "Ok") {
                                  Assert.fail("Expected error with invalid commands");
                                  return ;
                                }
                                var errors = result._0;
                                var probeErrors = Object.entries(errors.probes);
                                Curry._3(Assert.deepStrictEqual, probeErrors.length, 0, undefined);
                                var commandErrors = Object.entries(errors.commands);
                                Curry._3(Assert.deepStrictEqual, commandErrors.length, 3, undefined);
                                var commandNames = commandErrors.map(function (param) {
                                        return param[0];
                                      }).toSorted(Caml.string_compare);
                                Curry._3(Assert.deepStrictEqual, commandNames, [
                                      "non-existent-cmd-1",
                                      "non-existent-cmd-2",
                                      "non-existent-cmd-3"
                                    ], undefined);
                                return Curry._3(Assert.deepStrictEqual, errors.download, "NotAttempted", undefined);
                              }));
                        it("should rewrite command candidate source to FromCommandLookup when resolved command fails to connect", (async function () {
                                var determinePlatform = async function () {
                                  return {
                                          TAG: "Ok",
                                          _0: "MacOS_Arm"
                                        };
                                };
                                var askUserAboutDownloadPolicy = async function () {
                                  return "No";
                                };
                                var alreadyDownloaded = function (_globalStorageUri) {
                                  return Promise.resolve(undefined);
                                };
                                var resolveDownloadChannel = function (_target, param) {
                                  return async function (param, param$1, param$2) {
                                    return {
                                            TAG: "Error",
                                            _0: "CannotFindCompatibleALSRelease"
                                          };
                                  };
                                };
                                var download = function (_globalStorageUri, _downloadDescriptor, $staropt$star) {
                                  $staropt$star !== undefined;
                                  return Promise.resolve({
                                              TAG: "Error",
                                              _0: "CannotFindCompatibleALSRelease"
                                            });
                                };
                                var findCommand = function (command, _timeoutOpt) {
                                  if (command === "agda") {
                                    return Promise.resolve({
                                                TAG: "Ok",
                                                _0: "/definitely/not/a/real/agda"
                                              });
                                  } else {
                                    return Promise.resolve({
                                                TAG: "Error",
                                                _0: "NotFound"
                                              });
                                  }
                                };
                                var platformDeps = {
                                  determinePlatform: determinePlatform,
                                  findCommand: findCommand,
                                  alreadyDownloaded: alreadyDownloaded,
                                  resolveDownloadChannel: resolveDownloadChannel,
                                  download: download,
                                  askUserAboutDownloadPolicy: askUserAboutDownloadPolicy
                                };
                                var result = await Connection$AgdaModeVscode.fromPathsOrCommands(platformDeps, [[
                                        "agda",
                                        "FromConfig"
                                      ]]);
                                if (result.TAG === "Ok") {
                                  Assert.fail("Expected error when resolved agda path cannot be probed");
                                  return ;
                                }
                                var match = result._0.probes["/definitely/not/a/real/agda"];
                                if (match !== undefined) {
                                  return Curry._3(Assert.deepStrictEqual, match[1], {
                                              TAG: "FromCommandLookup",
                                              _0: "agda"
                                            }, undefined);
                                } else {
                                  Assert.fail("Expected probe error for resolved agda path");
                                  return ;
                                }
                              }));
                        it("should return empty error when no commands provided", (async function () {
                                var platformDeps = Desktop$AgdaModeVscode.make();
                                var commands = [];
                                var result = await Connection$AgdaModeVscode.fromPathsOrCommands(platformDeps, commands);
                                if (result.TAG === "Ok") {
                                  Assert.fail("Expected error with empty commands");
                                  return ;
                                }
                                var errors = result._0;
                                var probeErrors = Object.entries(errors.probes);
                                Curry._3(Assert.deepStrictEqual, probeErrors.length, 0, undefined);
                                var commandErrors = Object.entries(errors.commands);
                                Curry._3(Assert.deepStrictEqual, commandErrors.length, 0, undefined);
                                return Curry._3(Assert.deepStrictEqual, errors.download, "NotAttempted", undefined);
                              }));
                      }));
              }));
        describe("make with logging", (function () {
                it("should log ConnectedToAgda when Agda connection succeeds", (async function () {
                        var loggedEvents = [];
                        var logChannel = Chan$AgdaModeVscode.make();
                        Chan$AgdaModeVscode.on(logChannel, (function (logEvent) {
                                loggedEvents.push(logEvent);
                              }));
                        var agdaMockPath = await Test__Util$AgdaModeVscode.Candidate.Agda.mock("2.6.4", "agda-mock-for-make");
                        var memento = Memento$AgdaModeVscode.make(undefined);
                        var platformDeps = Desktop$AgdaModeVscode.make();
                        var globalStorageUri = Vscode.Uri.file("/tmp/test-storage");
                        var connection = await Connection$AgdaModeVscode.makeWithFallback(platformDeps, memento, globalStorageUri, [agdaMockPath], [], logChannel);
                        if (connection.TAG === "Ok") {
                          var connection$1 = connection._0;
                          Curry._3(Assert.deepStrictEqual, loggedEvents, [{
                                  TAG: "Connection",
                                  _0: {
                                    TAG: "ConnectedToAgda",
                                    _0: agdaMockPath,
                                    _1: "2.6.4"
                                  }
                                }], undefined);
                          switch (connection$1.TAG) {
                            case "Agda" :
                                Curry._3(Assert.deepStrictEqual, connection$1._1, agdaMockPath, undefined);
                                Curry._3(Assert.deepStrictEqual, connection$1._2, "2.6.4", undefined);
                                break;
                            case "ALS" :
                            case "ALSWASM" :
                                Assert.fail("Expected Agda connection");
                                break;
                            
                          }
                        } else {
                          Assert.fail("Expected connection to succeed");
                        }
                        try {
                          Nodefs.unlinkSync(agdaMockPath);
                          return ;
                        }
                        catch (exn){
                          return ;
                        }
                      }));
                it("should log connection events when using real agda command", (async function () {
                        var logChannel = Chan$AgdaModeVscode.make();
                        var listener = Log$AgdaModeVscode.collect(logChannel);
                        var memento = Memento$AgdaModeVscode.make(undefined);
                        var platformDeps = Desktop$AgdaModeVscode.make();
                        var globalStorageUri = Vscode.Uri.file("/tmp/test-storage");
                        var result = await Connection$AgdaModeVscode.makeWithFallback(platformDeps, memento, globalStorageUri, [], ["agda"], logChannel);
                        var loggedEvents = listener(Log$AgdaModeVscode.isConnection);
                        if (result.TAG !== "Ok") {
                          return Curry._3(Assert.deepStrictEqual, loggedEvents, [], undefined);
                        }
                        var connection = result._0;
                        switch (connection.TAG) {
                          case "Agda" :
                              return Curry._3(Assert.deepStrictEqual, loggedEvents, [{
                                            TAG: "Connection",
                                            _0: {
                                              TAG: "ConnectedToAgda",
                                              _0: connection._1,
                                              _1: connection._2
                                            }
                                          }], undefined);
                          case "ALS" :
                              var match = connection._2;
                              var v = match.alsVersion;
                              var path = connection._1;
                              if (v !== undefined) {
                                return Curry._3(Assert.deepStrictEqual, loggedEvents, [{
                                              TAG: "Connection",
                                              _0: {
                                                TAG: "ConnectedToALS",
                                                _0: path,
                                                _1: [
                                                  v,
                                                  match.agdaVersion
                                                ]
                                              }
                                            }], undefined);
                              } else {
                                return Curry._3(Assert.deepStrictEqual, loggedEvents, [{
                                              TAG: "Connection",
                                              _0: {
                                                TAG: "ConnectedToALS",
                                                _0: path,
                                                _1: undefined
                                              }
                                            }], undefined);
                              }
                          case "ALSWASM" :
                              var match$1 = connection._3;
                              var v$1 = match$1.alsVersion;
                              var path$1 = connection._2;
                              if (v$1 !== undefined) {
                                return Curry._3(Assert.deepStrictEqual, loggedEvents, [{
                                              TAG: "Connection",
                                              _0: {
                                                TAG: "ConnectedToALS",
                                                _0: path$1,
                                                _1: [
                                                  v$1,
                                                  match$1.agdaVersion
                                                ]
                                              }
                                            }], undefined);
                              } else {
                                return Curry._3(Assert.deepStrictEqual, loggedEvents, [{
                                              TAG: "Connection",
                                              _0: {
                                                TAG: "ConnectedToALS",
                                                _0: path$1,
                                                _1: undefined
                                              }
                                            }], undefined);
                              }
                          
                        }
                      }));
                it("should not log connection events when connection fails", (async function () {
                        var loggedEvents = [];
                        var logChannel = Chan$AgdaModeVscode.make();
                        Chan$AgdaModeVscode.on(logChannel, (function (logEvent) {
                                loggedEvents.push(logEvent);
                              }));
                        var memento = Memento$AgdaModeVscode.make(undefined);
                        var platformDeps = Mock$AgdaModeVscode.Platform.makeBasic();
                        var globalStorageUri = Vscode.Uri.file("/tmp/test-storage");
                        var match = await Connection$AgdaModeVscode.makeWithFallback(platformDeps, memento, globalStorageUri, ["/nonexistent/path"], ["nonexistent-command"], logChannel);
                        if (match.TAG !== "Ok") {
                          return Curry._3(Assert.deepStrictEqual, loggedEvents, [], undefined);
                        }
                        Assert.fail("Expected connection to fail");
                      }));
                it.skip("should log connection events even when falling back to downloads", (async function () {
                        var loggedEvents = [];
                        var logChannel = Chan$AgdaModeVscode.make();
                        Chan$AgdaModeVscode.on(logChannel, (function (logEvent) {
                                loggedEvents.push(logEvent);
                              }));
                        var memento = Memento$AgdaModeVscode.make(undefined);
                        var platformDeps = Desktop$AgdaModeVscode.make();
                        var globalStorageUri = Vscode.Uri.file("/tmp/test-storage");
                        var match = await Connection$AgdaModeVscode.makeWithFallback(platformDeps, memento, globalStorageUri, ["/invalid/path"], ["invalid-command"], logChannel);
                        if (match.TAG !== "Ok") {
                          return Curry._3(Assert.deepStrictEqual, loggedEvents, [], undefined);
                        }
                        var len = loggedEvents.length;
                        if (len !== 1) {
                          if (len !== 0) {
                            Assert.fail("Expected exactly one connection event");
                          } else {
                            Assert.fail("Expected connection event to be logged");
                          }
                          return ;
                        }
                        var match$1 = loggedEvents[0];
                        if (match$1.TAG === "Connection") {
                          switch (match$1._0.TAG) {
                            case "ConnectedToAgda" :
                            case "ConnectedToALS" :
                                return ;
                            case "Disconnected" :
                                Assert.fail("Expected exactly one connection event");
                                return ;
                            
                          }
                        } else {
                          Assert.fail("Expected exactly one connection event");
                          return ;
                        }
                      }));
              }));
        describe("make fromPaths scenarios", (function () {
                it("should prioritize valid paths over download fallback", (async function () {
                        var logChannel = Chan$AgdaModeVscode.make();
                        var listener = Log$AgdaModeVscode.collect(logChannel);
                        var agdaMockPath = await Test__Util$AgdaModeVscode.Candidate.Agda.mock("2.6.4", "agda-mock-for-path-priority");
                        var memento = Memento$AgdaModeVscode.make(undefined);
                        var platformDeps = Desktop$AgdaModeVscode.make();
                        var globalStorageUri = Vscode.Uri.file("/tmp/test-storage");
                        var result = await Connection$AgdaModeVscode.makeWithFallback(platformDeps, memento, globalStorageUri, [agdaMockPath], ["invalid-command"], logChannel);
                        var loggedEvents = listener(Log$AgdaModeVscode.isConnection);
                        if (result.TAG === "Ok") {
                          var connection = result._0;
                          Curry._3(Assert.deepStrictEqual, loggedEvents, [{
                                  TAG: "Connection",
                                  _0: {
                                    TAG: "ConnectedToAgda",
                                    _0: agdaMockPath,
                                    _1: "2.6.4"
                                  }
                                }], undefined);
                          switch (connection.TAG) {
                            case "Agda" :
                                Curry._3(Assert.deepStrictEqual, connection._1, agdaMockPath, undefined);
                                Curry._3(Assert.deepStrictEqual, connection._2, "2.6.4", undefined);
                                break;
                            case "ALS" :
                            case "ALSWASM" :
                                Assert.fail("Expected Agda connection");
                                break;
                            
                          }
                        } else {
                          Assert.fail("Expected connection to mock Agda path");
                        }
                        try {
                          Nodefs.unlinkSync(agdaMockPath);
                          return ;
                        }
                        catch (exn){
                          return ;
                        }
                      }));
              }));
        describe("Connection contract coverage", (function () {
                var configAgda = {
                  contents: ""
                };
                var pickedAgda = {
                  contents: ""
                };
                var downloadedAgda = {
                  contents: ""
                };
                before(async function () {
                      configAgda.contents = await Test__Util$AgdaModeVscode.Candidate.Agda.mock("2.7.0.1", "agda-spec-config");
                      pickedAgda.contents = await Test__Util$AgdaModeVscode.Candidate.Agda.mock("2.7.0.2", "agda-spec-picked");
                      downloadedAgda.contents = await Test__Util$AgdaModeVscode.Candidate.Agda.mock("2.7.0.3", "agda-spec-downloaded");
                    });
                after(async function () {
                      try {
                        await Test__Util$AgdaModeVscode.Candidate.Agda.destroy(configAgda.contents);
                      }
                      catch (exn){
                        
                      }
                      try {
                        await Test__Util$AgdaModeVscode.Candidate.Agda.destroy(pickedAgda.contents);
                      }
                      catch (exn$1){
                        
                      }
                      try {
                        return await Test__Util$AgdaModeVscode.Candidate.Agda.destroy(downloadedAgda.contents);
                      }
                      catch (exn$2){
                        return ;
                      }
                    });
                var makePickedFailureExecutable = async function (name) {
                  var stamp = String(Date.now() | 0);
                  var base = name + "-" + stamp;
                  var tmp = Nodeos.tmpdir();
                  var markerPath = Nodepath.join(tmp, base + ".marker");
                  var executablePath = Nodepath.join(tmp, base + (
                        OS$AgdaModeVscode.onUnix ? "" : ".bat"
                      ));
                  var content = OS$AgdaModeVscode.onUnix ? "#!/bin/sh\necho picked >> \"" + markerPath + "\"\necho not-agda\nexit 0\n" : "@echo off\r\necho picked>>\"" + markerPath + "\"\r\necho not-agda\r\n";
                  Nodefs.writeFileSync(executablePath, Buffer.from(content));
                  if (OS$AgdaModeVscode.onUnix) {
                    await Nodefs.promises.chmod(executablePath, 493);
                  }
                  return [
                          executablePath,
                          markerPath
                        ];
                };
                var makeFirstFailThenSuccessExecutable = async function (name) {
                  var stamp = String(Date.now() | 0);
                  var base = name + "-" + stamp;
                  var tmp = Nodeos.tmpdir();
                  var flagPath = Nodepath.join(tmp, base + ".flag");
                  var executablePath = Nodepath.join(tmp, base + (
                        OS$AgdaModeVscode.onUnix ? "" : ".bat"
                      ));
                  var content = OS$AgdaModeVscode.onUnix ? "#!/bin/sh\nif [ -f \"" + flagPath + "\" ]; then\n  echo \"Agda version 9.9.9\"\nelse\n  echo seen > \"" + flagPath + "\"\n  echo not-agda\nfi\nexit 0\n" : "@echo off\r\nif exist \"" + flagPath + "\" (\r\n  @echo Agda version 9.9.9\r\n) else (\r\n  @echo seen>\"" + flagPath + "\"\r\n  @echo not-agda\r\n)\r\n";
                  Nodefs.writeFileSync(executablePath, Buffer.from(content));
                  if (OS$AgdaModeVscode.onUnix) {
                    await Nodefs.promises.chmod(executablePath, 493);
                  }
                  return [
                          executablePath,
                          flagPath
                        ];
                };
                var cleanupIfExists = function (path) {
                  if (!Nodefs.existsSync(path)) {
                    return ;
                  }
                  try {
                    Nodefs.unlinkSync(path);
                    return ;
                  }
                  catch (exn){
                    return ;
                  }
                };
                it("should try PreferredCandidate first even when it is not in connection.paths", (async function () {
                        var memento = Memento$AgdaModeVscode.make(undefined);
                        await Memento$AgdaModeVscode.Module.PreferredCandidate.set(memento, pickedAgda.contents);
                        var result = await Connection$AgdaModeVscode.makeWithFallback(Mock$AgdaModeVscode.Platform.makeBasic(), memento, Vscode.Uri.file("/tmp/test-storage"), [configAgda.contents], [], Chan$AgdaModeVscode.make());
                        if (result.TAG === "Ok") {
                          return Curry._3(Assert.deepStrictEqual, Connection$AgdaModeVscode.getPath(result._0), pickedAgda.contents, undefined);
                        }
                        Assert.fail("Expected connection to succeed via preferred candidate");
                      }));
                it("should continue to later steps when PreferredCandidate fails", (async function () {
                        var match = await makePickedFailureExecutable("agda-picked-fail");
                        var markerPath = match[1];
                        var pickedPath = match[0];
                        var memento = Memento$AgdaModeVscode.make(undefined);
                        await Memento$AgdaModeVscode.Module.PreferredCandidate.set(memento, pickedPath);
                        var result = await Connection$AgdaModeVscode.makeWithFallback(Mock$AgdaModeVscode.Platform.makeBasic(), memento, Vscode.Uri.file("/tmp/test-storage"), [configAgda.contents], [], Chan$AgdaModeVscode.make());
                        if (result.TAG === "Ok") {
                          Curry._3(Assert.deepStrictEqual, Connection$AgdaModeVscode.getPath(result._0), configAgda.contents, undefined);
                          Curry._3(Assert.deepStrictEqual, Nodefs.existsSync(markerPath), true, undefined);
                        } else {
                          Assert.fail("Expected fallback to later chain steps after picked failure");
                        }
                        cleanupIfExists(pickedPath);
                        return cleanupIfExists(markerPath);
                      }));
                it("should not re-probe PreferredCandidate in the paths step", (async function () {
                        var match = await makeFirstFailThenSuccessExecutable("agda-picked-duplicate");
                        var flagPath = match[1];
                        var pickedPath = match[0];
                        var memento = Memento$AgdaModeVscode.make(undefined);
                        await Memento$AgdaModeVscode.Module.PreferredCandidate.set(memento, pickedPath);
                        var result = await Connection$AgdaModeVscode.makeWithFallback(Mock$AgdaModeVscode.Platform.makeBasic(), memento, Vscode.Uri.file("/tmp/test-storage"), [
                              pickedPath,
                              configAgda.contents,
                              pickedPath
                            ], [], Chan$AgdaModeVscode.make());
                        if (result.TAG === "Ok") {
                          Curry._3(Assert.deepStrictEqual, Connection$AgdaModeVscode.getPath(result._0), configAgda.contents, undefined);
                          Curry._3(Assert.deepStrictEqual, Nodefs.existsSync(flagPath), true, undefined);
                        } else {
                          Assert.fail("Expected connection to succeed via non-duplicate path probing");
                        }
                        cleanupIfExists(pickedPath);
                        return cleanupIfExists(flagPath);
                      }));
                it("should resolve bare command names in connection.paths via findCommand", (async function () {
                        await Config$AgdaModeVscode.Connection.DownloadPolicy.set("No");
                        var agdaCount = {
                          contents: 0
                        };
                        var alsCount = {
                          contents: 0
                        };
                        var findCommand = function (command, _timeoutOpt) {
                          switch (command) {
                            case "agda" :
                                agdaCount.contents = agdaCount.contents + 1 | 0;
                                break;
                            case "als" :
                                alsCount.contents = alsCount.contents + 1 | 0;
                                break;
                            default:
                              
                          }
                          return Promise.resolve({
                                      TAG: "Error",
                                      _0: "NotFound"
                                    });
                        };
                        var platform_determinePlatform = Desktop$AgdaModeVscode.Desktop.determinePlatform;
                        var platform_alreadyDownloaded = Desktop$AgdaModeVscode.Desktop.alreadyDownloaded;
                        var platform_resolveDownloadChannel = Desktop$AgdaModeVscode.Desktop.resolveDownloadChannel;
                        var platform_download = Desktop$AgdaModeVscode.Desktop.download;
                        var platform_askUserAboutDownloadPolicy = Desktop$AgdaModeVscode.Desktop.askUserAboutDownloadPolicy;
                        var platform = {
                          determinePlatform: platform_determinePlatform,
                          findCommand: findCommand,
                          alreadyDownloaded: platform_alreadyDownloaded,
                          resolveDownloadChannel: platform_resolveDownloadChannel,
                          download: platform_download,
                          askUserAboutDownloadPolicy: platform_askUserAboutDownloadPolicy
                        };
                        var result = await Connection$AgdaModeVscode.makeWithFallback(platform, Memento$AgdaModeVscode.make(undefined), Vscode.Uri.file("/tmp/test-storage"), [
                              "agda",
                              "als"
                            ], [
                              "agda",
                              "als"
                            ], Chan$AgdaModeVscode.make());
                        if (result.TAG === "Ok") {
                          Assert.fail("Expected failure with all commands unresolved");
                          return ;
                        }
                        Curry._3(Assert.deepStrictEqual, agdaCount.contents, 1, undefined);
                        return Curry._3(Assert.deepStrictEqual, alsCount.contents, 1, undefined);
                      }));
                it("should not persist resolved absolute command paths back into connection.paths", (async function () {
                        var logChannel = Chan$AgdaModeVscode.make();
                        await Config$AgdaModeVscode.Connection.setAgdaPaths(logChannel, ["agda"]);
                        var findCommand = function (command, _timeoutOpt) {
                          if (command === "agda") {
                            return Promise.resolve({
                                        TAG: "Ok",
                                        _0: configAgda.contents
                                      });
                          } else {
                            return Promise.resolve({
                                        TAG: "Error",
                                        _0: "NotFound"
                                      });
                          }
                        };
                        var platform_determinePlatform = Desktop$AgdaModeVscode.Desktop.determinePlatform;
                        var platform_alreadyDownloaded = Desktop$AgdaModeVscode.Desktop.alreadyDownloaded;
                        var platform_resolveDownloadChannel = Desktop$AgdaModeVscode.Desktop.resolveDownloadChannel;
                        var platform_download = Desktop$AgdaModeVscode.Desktop.download;
                        var platform_askUserAboutDownloadPolicy = Desktop$AgdaModeVscode.Desktop.askUserAboutDownloadPolicy;
                        var platform = {
                          determinePlatform: platform_determinePlatform,
                          findCommand: findCommand,
                          alreadyDownloaded: platform_alreadyDownloaded,
                          resolveDownloadChannel: platform_resolveDownloadChannel,
                          download: platform_download,
                          askUserAboutDownloadPolicy: platform_askUserAboutDownloadPolicy
                        };
                        var result = await Connection$AgdaModeVscode.makeWithFallback(platform, Memento$AgdaModeVscode.make(undefined), Vscode.Uri.file("/tmp/test-storage"), ["agda"], [], logChannel);
                        if (result.TAG === "Ok") {
                          Curry._3(Assert.deepStrictEqual, Connection$AgdaModeVscode.getPath(result._0), configAgda.contents, undefined);
                          return Curry._3(Assert.deepStrictEqual, Config$AgdaModeVscode.Connection.getAgdaPaths(), ["agda"], undefined);
                        }
                        Assert.fail("Expected command-resolved connection");
                      }));
                it("should update connection.paths but not PreferredCandidate after automatic fallback download", (async function () {
                        var logChannel = Chan$AgdaModeVscode.make();
                        await Config$AgdaModeVscode.Connection.setAgdaPaths(logChannel, []);
                        await Config$AgdaModeVscode.Connection.DownloadPolicy.set("Undecided");
                        var memento = Memento$AgdaModeVscode.make(undefined);
                        var platform = Mock$AgdaModeVscode.Platform.makeWithSuccessfulDownload(downloadedAgda.contents);
                        var result = await Connection$AgdaModeVscode.makeWithFallback(platform, memento, Vscode.Uri.file("/tmp/test-storage"), ["/invalid/path"], ["invalid-command"], logChannel);
                        if (result.TAG === "Ok") {
                          Curry._3(Assert.deepStrictEqual, Connection$AgdaModeVscode.getPath(result._0), downloadedAgda.contents, undefined);
                          Curry._3(Assert.deepStrictEqual, Config$AgdaModeVscode.Connection.getAgdaPaths(), [
                                downloadedAgda.contents,
                                "/invalid/path"
                              ], undefined);
                          return Curry._3(Assert.deepStrictEqual, Memento$AgdaModeVscode.Module.PreferredCandidate.get(memento), undefined, undefined);
                        }
                        Assert.fail("Expected fallback download to succeed");
                      }));
                it("should not overwrite existing PreferredCandidate after automatic fallback download", (async function () {
                        var logChannel = Chan$AgdaModeVscode.make();
                        var existingPicked = "/usr/local/bin/agda";
                        await Config$AgdaModeVscode.Connection.setAgdaPaths(logChannel, []);
                        await Config$AgdaModeVscode.Connection.DownloadPolicy.set("Undecided");
                        var memento = Memento$AgdaModeVscode.make(undefined);
                        await Memento$AgdaModeVscode.Module.PreferredCandidate.set(memento, existingPicked);
                        var platform = Mock$AgdaModeVscode.Platform.makeWithSuccessfulDownload(downloadedAgda.contents);
                        var result = await Connection$AgdaModeVscode.makeWithFallback(platform, memento, Vscode.Uri.file("/tmp/test-storage"), ["/invalid/path"], ["invalid-command"], logChannel);
                        if (result.TAG === "Ok") {
                          Curry._3(Assert.deepStrictEqual, Connection$AgdaModeVscode.getPath(result._0), downloadedAgda.contents, undefined);
                          Curry._3(Assert.deepStrictEqual, Config$AgdaModeVscode.Connection.getAgdaPaths(), [
                                downloadedAgda.contents,
                                "/invalid/path"
                              ], undefined);
                          return Curry._3(Assert.deepStrictEqual, Memento$AgdaModeVscode.Module.PreferredCandidate.get(memento), existingPicked, undefined);
                        }
                        Assert.fail("Expected fallback download to succeed");
                      }));
                it("should prepend downloaded path to connection.paths on automatic fallback", (async function () {
                        var logChannel = Chan$AgdaModeVscode.make();
                        var existingPaths = [
                          "/broken/path1",
                          "/broken/path2"
                        ];
                        await Config$AgdaModeVscode.Connection.setAgdaPaths(logChannel, existingPaths);
                        await Config$AgdaModeVscode.Connection.DownloadPolicy.set("Undecided");
                        var memento = Memento$AgdaModeVscode.make(undefined);
                        var platform = Mock$AgdaModeVscode.Platform.makeWithSuccessfulDownload(downloadedAgda.contents);
                        var result = await Connection$AgdaModeVscode.makeWithFallback(platform, memento, Vscode.Uri.file("/tmp/test-storage"), existingPaths, ["invalid-command"], logChannel);
                        if (result.TAG === "Ok") {
                          Curry._3(Assert.deepStrictEqual, Connection$AgdaModeVscode.getPath(result._0), downloadedAgda.contents, undefined);
                          return Curry._3(Assert.deepStrictEqual, Config$AgdaModeVscode.Connection.getAgdaPaths(), [
                                      downloadedAgda.contents,
                                      "/broken/path1",
                                      "/broken/path2"
                                    ], undefined);
                        }
                        Assert.fail("Expected fallback download to succeed");
                      }));
                it("should use selected channel for automatic fallback downloads", (async function () {
                        var logChannel = Chan$AgdaModeVscode.make();
                        await Config$AgdaModeVscode.Connection.setAgdaPaths(logChannel, ["/nonexistent/path"]);
                        await Config$AgdaModeVscode.Connection.DownloadPolicy.set("Undecided");
                        var memento = Memento$AgdaModeVscode.make(undefined);
                        await Memento$AgdaModeVscode.Module.SelectedChannel.set(memento, Connection__Download__Channel$AgdaModeVscode.toString("DevALS"));
                        var downloadedChannel = {
                          contents: undefined
                        };
                        var determinePlatform = async function () {
                          return {
                                  TAG: "Ok",
                                  _0: "MacOS_Arm"
                                };
                        };
                        var askUserAboutDownloadPolicy = async function () {
                          return "Yes";
                        };
                        var alreadyDownloaded = function (_globalStorageUri) {
                          return Promise.resolve(undefined);
                        };
                        var resolveDownloadChannel = Mock$AgdaModeVscode.DownloadDescriptor.mockWith(function (channel) {
                              if (channel === "LatestALS") {
                                return {
                                        TAG: "Error",
                                        _0: "CannotFindCompatibleALSRelease"
                                      };
                              } else {
                                return {
                                        TAG: "Ok",
                                        _0: {
                                          TAG: "FromURL",
                                          _0: "DevALS",
                                          _1: "https://example.invalid/dev-als",
                                          _2: "dev-als"
                                        }
                                      };
                              }
                            });
                        var download = function (_globalStorageUri, source, $staropt$star) {
                          $staropt$star !== undefined;
                          var channel = source._0;
                          downloadedChannel.contents = channel;
                          return Promise.resolve({
                                      TAG: "Ok",
                                      _0: downloadedAgda.contents
                                    });
                        };
                        var findCommand = function (_command, _timeoutOpt) {
                          return Promise.resolve({
                                      TAG: "Error",
                                      _0: "NotFound"
                                    });
                        };
                        var platform = {
                          determinePlatform: determinePlatform,
                          findCommand: findCommand,
                          alreadyDownloaded: alreadyDownloaded,
                          resolveDownloadChannel: resolveDownloadChannel,
                          download: download,
                          askUserAboutDownloadPolicy: askUserAboutDownloadPolicy
                        };
                        var result = await Connection$AgdaModeVscode.makeWithFallback(platform, memento, Vscode.Uri.file("/tmp/test-storage"), ["/nonexistent/path"], [], logChannel);
                        if (result.TAG === "Ok") {
                          return Curry._3(Assert.deepStrictEqual, downloadedChannel.contents, "DevALS", undefined);
                        }
                        Assert.fail("Expected download fallback to succeed");
                      }));
                it("should NOT try command probes — resolution is preferred then paths then download", (async function () {
                        var logChannel = Chan$AgdaModeVscode.make();
                        await Config$AgdaModeVscode.Connection.setAgdaPaths(logChannel, ["/nonexistent/path"]);
                        await Config$AgdaModeVscode.Connection.DownloadPolicy.set("Undecided");
                        var memento = Memento$AgdaModeVscode.make(undefined);
                        var commandProbed = {
                          contents: false
                        };
                        var determinePlatform = async function () {
                          return {
                                  TAG: "Ok",
                                  _0: "MacOS_Arm"
                                };
                        };
                        var askUserAboutDownloadPolicy = async function () {
                          return "Yes";
                        };
                        var alreadyDownloaded = function (_globalStorageUri) {
                          return Promise.resolve(undefined);
                        };
                        var resolveDownloadChannel = Mock$AgdaModeVscode.DownloadDescriptor.mockWith(function (channel) {
                              if (channel === "LatestALS") {
                                return {
                                        TAG: "Error",
                                        _0: "CannotFindCompatibleALSRelease"
                                      };
                              } else {
                                return {
                                        TAG: "Ok",
                                        _0: {
                                          TAG: "FromGitHub",
                                          _0: channel,
                                          _1: Mock$AgdaModeVscode.DownloadDescriptor.mockDevALSDescriptor
                                        }
                                      };
                              }
                            });
                        var download = function (_globalStorageUri, _downloadDescriptor, $staropt$star) {
                          $staropt$star !== undefined;
                          return Promise.resolve({
                                      TAG: "Ok",
                                      _0: downloadedAgda.contents
                                    });
                        };
                        var findCommand = function (_command, _timeoutOpt) {
                          commandProbed.contents = true;
                          return Promise.resolve({
                                      TAG: "Error",
                                      _0: "NotFound"
                                    });
                        };
                        var platform = {
                          determinePlatform: determinePlatform,
                          findCommand: findCommand,
                          alreadyDownloaded: alreadyDownloaded,
                          resolveDownloadChannel: resolveDownloadChannel,
                          download: download,
                          askUserAboutDownloadPolicy: askUserAboutDownloadPolicy
                        };
                        var result = await Connection$AgdaModeVscode.makeWithFallback(platform, memento, Vscode.Uri.file("/tmp/test-storage"), ["/nonexistent/path"], ["agda"], logChannel);
                        if (result.TAG === "Ok") {
                          return Curry._3(Assert.deepStrictEqual, commandProbed.contents, false, undefined);
                        }
                        Assert.fail("Expected download fallback to succeed");
                      }));
                it("should normalize desktop WASM candidate to fsPath, not preserve raw URI", (async function () {
                        var wasmPath = Nodepath.join(Nodeos.tmpdir(), "test-normalize-" + String(Date.now() | 0) + ".wasm");
                        Nodefs.writeFileSync(wasmPath, Buffer.from("mock wasm content"));
                        var fileUri = Vscode.Uri.file(wasmPath);
                        var uriString = fileUri.toString();
                        var result = await Connection$AgdaModeVscode.probeFilepath(uriString);
                        if (result.TAG === "Ok") {
                          Curry._3(Assert.deepStrictEqual, result._0[0], wasmPath, undefined);
                        } else {
                          Assert.fail("Expected WASM probe to succeed");
                        }
                        Nodefs.unlinkSync(wasmPath);
                      }));
                it("should return managed-download error when cached native has no same-release WASM fallback", (async function () {
                        var logChannel = Chan$AgdaModeVscode.make();
                        await Config$AgdaModeVscode.Connection.setAgdaPaths(logChannel, []);
                        await Config$AgdaModeVscode.Connection.DownloadPolicy.set("Undecided");
                        var memento = Memento$AgdaModeVscode.make(undefined);
                        var storagePath = Nodepath.join(Nodeos.tmpdir(), "agda-managed-no-wasm-" + String(Date.now() | 0));
                        var globalStorageUri = Vscode.Uri.file(storagePath);
                        var cachedNativePath = Nodepath.join(storagePath, "releases", "dev", "als-dev-Agda-2.8.0-macos-arm64", "als");
                        var resolveCalled = {
                          contents: false
                        };
                        var downloadCalled = {
                          contents: false
                        };
                        var determinePlatform = async function () {
                          return {
                                  TAG: "Ok",
                                  _0: "MacOS_Arm"
                                };
                        };
                        var askUserAboutDownloadPolicy = async function () {
                          return "Yes";
                        };
                        var alreadyDownloaded = function (_globalStorageUri) {
                          return Promise.resolve(cachedNativePath);
                        };
                        var resolveDownloadChannel = Mock$AgdaModeVscode.DownloadDescriptor.mockWith(function (_channel) {
                              resolveCalled.contents = true;
                              return {
                                      TAG: "Error",
                                      _0: "CannotFindCompatibleALSRelease"
                                    };
                            });
                        var download = function (_globalStorageUri, _source, $staropt$star) {
                          $staropt$star !== undefined;
                          downloadCalled.contents = true;
                          return Promise.resolve({
                                      TAG: "Error",
                                      _0: "CannotFindCompatibleALSRelease"
                                    });
                        };
                        var findCommand = function (_command, _timeoutOpt) {
                          return Promise.resolve({
                                      TAG: "Error",
                                      _0: "NotFound"
                                    });
                        };
                        var platform = {
                          determinePlatform: determinePlatform,
                          findCommand: findCommand,
                          alreadyDownloaded: alreadyDownloaded,
                          resolveDownloadChannel: resolveDownloadChannel,
                          download: download,
                          askUserAboutDownloadPolicy: askUserAboutDownloadPolicy
                        };
                        var result = await Connection$AgdaModeVscode.makeWithFallback(platform, memento, globalStorageUri, [], [], logChannel);
                        Curry._3(Assert.deepStrictEqual, resolveCalled.contents, false, undefined);
                        Curry._3(Assert.deepStrictEqual, downloadCalled.contents, false, undefined);
                        if (result.TAG === "Ok") {
                          Assert.fail("Expected error when cached native has no same-release WASM fallback");
                          return ;
                        }
                        var errors = result._0;
                        switch (errors.TAG) {
                          case "Establish" :
                              var match = errors._0.probes[cachedNativePath];
                              if (match !== undefined) {
                                return Curry._3(Assert.deepStrictEqual, match[1], "FromManagedDownload", undefined);
                              } else {
                                Assert.fail("Expected probe error for cached managed download");
                                return ;
                              }
                          case "CommWithAgda" :
                          case "CommWithALS" :
                              Assert.fail("Expected establish error");
                              return ;
                          
                        }
                      }));
                it("should not resolve selected channel when cached managed native has no same-release WASM fallback", (async function () {
                        var logChannel = Chan$AgdaModeVscode.make();
                        await Config$AgdaModeVscode.Connection.setAgdaPaths(logChannel, []);
                        await Config$AgdaModeVscode.Connection.DownloadPolicy.set("Undecided");
                        var memento = Memento$AgdaModeVscode.make(undefined);
                        await Memento$AgdaModeVscode.Module.SelectedChannel.set(memento, Connection__Download__Channel$AgdaModeVscode.toString("DevALS"));
                        var storagePath = Nodepath.join(Nodeos.tmpdir(), "agda-managed-no-dev-fallback-" + String(Date.now() | 0));
                        var globalStorageUri = Vscode.Uri.file(storagePath);
                        var cachedNativePath = Nodepath.join(storagePath, "releases", "v6", "als-v6-Agda-2.8.0-macos-arm64", "als");
                        var resolvedDevChannel = {
                          contents: false
                        };
                        var devWasmDownloaded = {
                          contents: false
                        };
                        var determinePlatform = async function () {
                          return {
                                  TAG: "Ok",
                                  _0: "MacOS_Arm"
                                };
                        };
                        var askUserAboutDownloadPolicy = async function () {
                          return "Yes";
                        };
                        var alreadyDownloaded = function (_globalStorageUri) {
                          return Promise.resolve(cachedNativePath);
                        };
                        var resolveDownloadChannel = Mock$AgdaModeVscode.DownloadDescriptor.mockWith(function (channel) {
                              if (channel === "LatestALS") {
                                return {
                                        TAG: "Error",
                                        _0: "CannotFindCompatibleALSRelease"
                                      };
                              }
                              resolvedDevChannel.contents = true;
                              return {
                                      TAG: "Ok",
                                      _0: {
                                        TAG: "FromGitHub",
                                        _0: channel,
                                        _1: Mock$AgdaModeVscode.DownloadDescriptor.mockDevALSDescriptor
                                      }
                                    };
                            });
                        var download = function (_globalStorageUri, source, $staropt$star) {
                          $staropt$star !== undefined;
                          if (source.TAG === "FromGitHub" && source._1.asset.name.includes("wasm")) {
                            devWasmDownloaded.contents = true;
                            return Promise.resolve({
                                        TAG: "Ok",
                                        _0: "/tmp/cached-wasm-als.wasm"
                                      });
                          } else {
                            return Promise.resolve({
                                        TAG: "Error",
                                        _0: "CannotFindCompatibleALSRelease"
                                      });
                          }
                        };
                        var findCommand = function (_command, _timeoutOpt) {
                          return Promise.resolve({
                                      TAG: "Error",
                                      _0: "NotFound"
                                    });
                        };
                        var platform = {
                          determinePlatform: determinePlatform,
                          findCommand: findCommand,
                          alreadyDownloaded: alreadyDownloaded,
                          resolveDownloadChannel: resolveDownloadChannel,
                          download: download,
                          askUserAboutDownloadPolicy: askUserAboutDownloadPolicy
                        };
                        var result = await Connection$AgdaModeVscode.makeWithFallback(platform, memento, globalStorageUri, [], [], logChannel);
                        Curry._3(Assert.deepStrictEqual, resolvedDevChannel.contents, false, undefined);
                        Curry._3(Assert.deepStrictEqual, devWasmDownloaded.contents, false, undefined);
                        if (result.TAG === "Ok") {
                          Assert.fail("Expected error when cached native managed fallback has no same-release WASM");
                          return ;
                        }
                        var errors = result._0;
                        switch (errors.TAG) {
                          case "Establish" :
                              var errors$1 = errors._0;
                              var match = errors$1.probes[cachedNativePath];
                              if (match !== undefined) {
                                Curry._3(Assert.deepStrictEqual, match[1], "FromManagedDownload", undefined);
                              } else {
                                Assert.fail("Expected probe error for cached managed native download");
                              }
                              return Curry._3(Assert.deepStrictEqual, errors$1.probes["/tmp/cached-wasm-als.wasm"], undefined, undefined);
                          case "CommWithAgda" :
                          case "CommWithALS" :
                              Assert.fail("Expected establish error");
                              return ;
                          
                        }
                      }));
                it("should use same-release managed WASM fallback without resolving selected channel", (async function () {
                        var logChannel = Chan$AgdaModeVscode.make();
                        await Config$AgdaModeVscode.Connection.setAgdaPaths(logChannel, []);
                        await Config$AgdaModeVscode.Connection.DownloadPolicy.set("Undecided");
                        var memento = Memento$AgdaModeVscode.make(undefined);
                        await Memento$AgdaModeVscode.Module.SelectedChannel.set(memento, Connection__Download__Channel$AgdaModeVscode.toString("DevALS"));
                        return await withTempStorage("agda-managed-local-wasm-fallback-", (async function (storagePath, globalStorageUri) {
                                      var nativeDir = Nodepath.join(storagePath, "releases", "v6", "als-v6-Agda-2.8.0-macos-arm64");
                                      var wasmDir = Nodepath.join(storagePath, "releases", "v6", "als-v6-Agda-2.8.0-wasm");
                                      var cachedNativePath = Nodepath.join(nativeDir, "als");
                                      var cachedWasmPath = Nodepath.join(wasmDir, "als.wasm");
                                      await Nodefs.promises.mkdir(nativeDir, {
                                            recursive: true,
                                            mode: 511
                                          });
                                      await Nodefs.promises.mkdir(wasmDir, {
                                            recursive: true,
                                            mode: 511
                                          });
                                      Nodefs.writeFileSync(cachedNativePath, Buffer.from("mock native"));
                                      Nodefs.writeFileSync(cachedWasmPath, Buffer.from("mock wasm"));
                                      var resolveCalled = {
                                        contents: false
                                      };
                                      var downloadCalled = {
                                        contents: false
                                      };
                                      var determinePlatform = async function () {
                                        return {
                                                TAG: "Ok",
                                                _0: "MacOS_Arm"
                                              };
                                      };
                                      var askUserAboutDownloadPolicy = async function () {
                                        return "Yes";
                                      };
                                      var alreadyDownloaded = function (_globalStorageUri) {
                                        return Promise.resolve(cachedNativePath);
                                      };
                                      var resolveDownloadChannel = Mock$AgdaModeVscode.DownloadDescriptor.mockWith(function (_channel) {
                                            resolveCalled.contents = true;
                                            return {
                                                    TAG: "Error",
                                                    _0: "CannotFindCompatibleALSRelease"
                                                  };
                                          });
                                      var download = function (_globalStorageUri, _source, $staropt$star) {
                                        $staropt$star !== undefined;
                                        downloadCalled.contents = true;
                                        return Promise.resolve({
                                                    TAG: "Error",
                                                    _0: "CannotFindCompatibleALSRelease"
                                                  });
                                      };
                                      var findCommand = function (_command, _timeoutOpt) {
                                        return Promise.resolve({
                                                    TAG: "Error",
                                                    _0: "NotFound"
                                                  });
                                      };
                                      var platform = {
                                        determinePlatform: determinePlatform,
                                        findCommand: findCommand,
                                        alreadyDownloaded: alreadyDownloaded,
                                        resolveDownloadChannel: resolveDownloadChannel,
                                        download: download,
                                        askUserAboutDownloadPolicy: askUserAboutDownloadPolicy
                                      };
                                      var result = await Connection$AgdaModeVscode.makeWithFallback(platform, memento, globalStorageUri, [], [], logChannel);
                                      Curry._3(Assert.deepStrictEqual, resolveCalled.contents, false, undefined);
                                      Curry._3(Assert.deepStrictEqual, downloadCalled.contents, false, undefined);
                                      if (result.TAG === "Ok") {
                                        Assert.fail("Expected both managed native and managed WASM probes to fail");
                                        return ;
                                      }
                                      var errors = result._0;
                                      switch (errors.TAG) {
                                        case "Establish" :
                                            var errors$1 = errors._0;
                                            var match = errors$1.probes[cachedNativePath];
                                            if (match !== undefined) {
                                              Curry._3(Assert.deepStrictEqual, match[1], "FromManagedDownload", undefined);
                                            } else {
                                              Assert.fail("Expected probe error for cached managed native download");
                                            }
                                            var match$1 = errors$1.probes[cachedWasmPath];
                                            if (match$1 !== undefined) {
                                              return Curry._3(Assert.deepStrictEqual, match$1[1], "FromManagedDownload", undefined);
                                            } else {
                                              Assert.fail("Expected probe error for cached managed WASM fallback");
                                              return ;
                                            }
                                        case "CommWithAgda" :
                                        case "CommWithALS" :
                                            Assert.fail("Expected establish error");
                                            return ;
                                        
                                      }
                                    }));
                      }));
                it("should reject lsp:// URI and treat as filepath", (function () {
                        Connection__URI$AgdaModeVscode.parse("lsp://localhost:8080");
                        Assert.ok(true);
                      }));
                it("should preserve vscode-userdata: URI scheme for web storage paths", (function () {
                        var result = Connection__URI$AgdaModeVscode.parse("vscode-userdata:/some/path");
                        Curry._3(Assert.deepStrictEqual, result._1.scheme, "vscode-userdata", undefined);
                      }));
                it("should preserve vscode-vfs: URI scheme for remote FS providers", (function () {
                        var result = Connection__URI$AgdaModeVscode.parse("vscode-vfs://github/user/repo/path");
                        Curry._3(Assert.deepStrictEqual, result._1.scheme, "vscode-vfs", undefined);
                      }));
                it("download(FromGitHub) and downloadFromURL should return consistent WASM paths", (async function () {
                        var storagePath = Nodepath.join(Nodeos.tmpdir(), "agda-i21-wasm-" + String(Date.now() | 0));
                        var globalStorageUri = Vscode.Uri.file(storagePath);
                        await FS$AgdaModeVscode.createDirectory(globalStorageUri);
                        var wasmDirUri = Vscode.Uri.joinPath(globalStorageUri, "test-wasm-als");
                        await FS$AgdaModeVscode.createDirectory(wasmDirUri);
                        var wasmFilePath = Nodepath.join(wasmDirUri.fsPath, "als.wasm");
                        Nodefs.writeFileSync(wasmFilePath, Buffer.from("mock-wasm"));
                        var init = Mock$AgdaModeVscode.DownloadDescriptor.mockAsset;
                        var wasmDescriptor_release = Mock$AgdaModeVscode.DownloadDescriptor.mockRelease;
                        var wasmDescriptor_asset = {
                          url: init.url,
                          id: init.id,
                          node_id: init.node_id,
                          name: "als-wasm-Agda-2.6.3.wasm",
                          label: init.label,
                          content_type: init.content_type,
                          state: init.state,
                          size: init.size,
                          created_at: init.created_at,
                          updated_at: init.updated_at,
                          browser_download_url: init.browser_download_url
                        };
                        var wasmDescriptor = {
                          release: wasmDescriptor_release,
                          asset: wasmDescriptor_asset,
                          saveAsFileName: "test-wasm-als"
                        };
                        var fromGitHubResult = await Connection__Download$AgdaModeVscode.download(globalStorageUri, {
                              TAG: "FromGitHub",
                              _0: "DevALS",
                              _1: wasmDescriptor
                            }, undefined, undefined, undefined);
                        var fromURLResult = await Connection__Download$AgdaModeVscode.downloadFromURL(globalStorageUri, "https://example.com/als.wasm", "test-wasm-als", "test", undefined, undefined);
                        var fromGitHubPath;
                        if (fromGitHubResult.TAG === "Ok") {
                          fromGitHubPath = fromGitHubResult._0;
                        } else {
                          Assert.fail("download(FromGitHub) should succeed for cached WASM");
                          fromGitHubPath = "";
                        }
                        var fromURLPath;
                        if (fromURLResult.TAG === "Ok") {
                          fromURLPath = fromURLResult._0;
                        } else {
                          Assert.fail("downloadFromURL should succeed for cached WASM");
                          fromURLPath = "";
                        }
                        Curry._3(Assert.deepStrictEqual, fromURLPath, fromGitHubPath, undefined);
                        await FS$AgdaModeVscode.deleteRecursive(globalStorageUri);
                      }));
              }));
      }));

exports.withTempStorage = withTempStorage;
exports.getAgdaTarget = getAgdaTarget;
/*  Not a pure module */
