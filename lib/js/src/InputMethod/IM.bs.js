// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Caml = require("rescript/lib/js/caml.js");
var Curry = require("rescript/lib/js/curry.js");
var Vscode = require("vscode");
var $$Promise = require("reason-promise/lib/js/src/js/promise.bs.js");
var Js_array = require("rescript/lib/js/js_array.js");
var Belt_List = require("rescript/lib/js/belt_List.js");
var Belt_Array = require("rescript/lib/js/belt_Array.js");
var Belt_Option = require("rescript/lib/js/belt_Option.js");
var Caml_option = require("rescript/lib/js/caml_option.js");
var Chan$AgdaModeVscode = require("../Util/Chan.bs.js");
var Buffer$AgdaModeVscode = require("./Buffer.bs.js");
var Common$AgdaModeVscode = require("../View/Common.bs.js");
var Editor$AgdaModeVscode = require("../Editor.bs.js");
var Parser$AgdaModeVscode = require("../Parser/Parser.bs.js");

function fromTextDocumentChangeEvent(editor, $$event) {
  var fileName = Parser$AgdaModeVscode.filepath(editor.document.fileName);
  var eventFileName = Parser$AgdaModeVscode.filepath($$event.document.fileName);
  if (fileName === eventFileName) {
    return Belt_Array.map($$event.contentChanges, (function (change) {
                  return {
                          offset: change.rangeOffset,
                          insertedText: change.text,
                          replacedTextLength: change.rangeLength
                        };
                }));
  } else {
    return [];
  }
}

var Input = {
  fromTextDocumentChangeEvent: fromTextDocumentChangeEvent
};

var Output = {};

function fromOutput(xs) {
  return Belt_Array.map(xs, (function (x) {
                if (typeof x === "number") {
                  if (x === /* Activate */0) {
                    return /* Activate */2;
                  } else {
                    return /* Deactivate */3;
                  }
                } else if (x.TAG === /* UpdateView */0) {
                  return /* UpdateView */0;
                } else {
                  return {
                          _0: x._0,
                          [Symbol.for("name")]: "RewriteIssued"
                        };
                }
              }));
}

var Log = {
  fromOutput: fromOutput
};

function withIn(instance, offset) {
  return Common$AgdaModeVscode.Interval.contains(instance.interval, offset);
}

function destroy(instance) {
  Belt_Option.forEach(instance.decoration, Editor$AgdaModeVscode.Decoration.destroy);
}

function logOutput(self, output) {
  if (output.length !== 0) {
    return Chan$AgdaModeVscode.emit(self.chanLog, fromOutput(output));
  }
  
}

function make(chanLog) {
  return {
          instances: [],
          activated: false,
          semaphore: false,
          chanLog: chanLog
        };
}

function validateCursorPositions(instances, intervals) {
  return Belt_Array.keep(instances, (function (instance) {
                var survived = Belt_Array.some(intervals, (function (param) {
                        if (withIn(instance, param[0])) {
                          return withIn(instance, param[1]);
                        } else {
                          return false;
                        }
                      }));
                if (!survived) {
                  destroy(instance);
                }
                return survived;
              }));
}

function toRewrites(instances, modify) {
  var accum = {
    contents: 0
  };
  return Belt_Array.keepMap(instances, (function (instance) {
                var match = instance.interval;
                var end_ = match[1];
                var start = match[0];
                instance.interval = [
                  start + accum.contents | 0,
                  end_ + accum.contents | 0
                ];
                return Belt_Option.map(Curry._1(modify, instance), (function (replacement) {
                              var delta = replacement.length - (end_ - start | 0) | 0;
                              accum.contents = accum.contents + delta | 0;
                              instance.interval = [
                                instance.interval[0],
                                instance.interval[1] + delta | 0
                              ];
                              return {
                                      interval: instance.interval,
                                      text: replacement,
                                      instance: instance
                                    };
                            }));
              }));
}

function groupChangeWithInstances(instances, changes) {
  var changes$1 = Js_array.sortInPlaceWith((function (x, y) {
          return Caml.int_compare(x.offset, y.offset);
        }), changes);
  var go = function (_accum, _x) {
    while(true) {
      var x = _x;
      var accum = _accum;
      var match = x[0];
      if (match) {
        var match$1 = x[1];
        if (!match$1) {
          return /* [] */0;
        }
        var is = match$1.tl;
        var instance = match$1.hd;
        var cs = match.tl;
        var change = match.hd;
        var match$2 = instance.interval;
        var end_ = match$2[1];
        var start = match$2[0];
        var delta = change.insertedText.length - change.replacedTextLength | 0;
        if (withIn(instance, change.offset)) {
          instance.interval = [
            accum + start | 0,
            (accum + end_ | 0) + delta | 0
          ];
          return {
                  hd: [
                    instance,
                    {
                      offset: change.offset + accum | 0,
                      insertedText: change.insertedText,
                      replacedTextLength: change.replacedTextLength
                    }
                  ],
                  tl: go(accum + delta | 0, [
                        cs,
                        is
                      ])
                };
        }
        if (change.offset < instance.interval[0]) {
          _x = [
            cs,
            {
              hd: instance,
              tl: is
            }
          ];
          _accum = accum + delta | 0;
          continue ;
        }
        instance.interval = [
          accum + start | 0,
          accum + end_ | 0
        ];
        return {
                hd: [
                  instance,
                  undefined
                ],
                tl: go(accum, [
                      {
                        hd: change,
                        tl: cs
                      },
                      is
                    ])
              };
      }
      var match$3 = x[1];
      if (match$3) {
        return Belt_List.map({
                    hd: match$3.hd,
                    tl: match$3.tl
                  }, (function (i) {
                      return [
                              i,
                              undefined
                            ];
                    }));
      } else {
        return /* [] */0;
      }
    };
  };
  return Belt_List.toArray(go(0, [
                  Belt_List.fromArray(changes$1),
                  Belt_List.fromArray(instances)
                ]));
}

function updateInstances(instances, changes) {
  var instancesWithChanges = groupChangeWithInstances(instances, changes);
  var rewrites = [];
  var accum = {
    contents: 0
  };
  var instances$1 = Belt_Array.keepMap(instancesWithChanges, (function (param) {
          var change = param[1];
          var instance = param[0];
          if (change === undefined) {
            return instance;
          }
          var match = Buffer$AgdaModeVscode.update(instance.buffer, instance.interval[0], change);
          var buffer = match[0];
          Belt_Option.forEach(match[1], (function (text) {
                  var match = instance.interval;
                  var end_ = match[1];
                  var start = match[0];
                  var delta = text.length - (end_ - start | 0) | 0;
                  instance.interval = [
                    start + accum.contents | 0,
                    (end_ + accum.contents | 0) + delta | 0
                  ];
                  Js_array.push({
                        interval: [
                          start,
                          end_
                        ],
                        text: text,
                        instance: buffer.translation.further ? instance : undefined
                      }, rewrites);
                  accum.contents = accum.contents + delta | 0;
                }));
          var intervalLengthIs0 = instance.interval[0] === instance.interval[1];
          var numberOfCandidateSymbols = buffer.translation.candidateSymbols.length;
          var shouldDestroy = intervalLengthIs0 || !buffer.translation.further && numberOfCandidateSymbols <= 1;
          if (shouldDestroy) {
            destroy(instance);
            return ;
          } else {
            instance.buffer = buffer;
            return instance;
          }
        }));
  return [
          instances$1,
          rewrites
        ];
}

function bufferIsEmpty(im) {
  return Belt_Array.every(im.instances, (function (instance) {
                return Buffer$AgdaModeVscode.isEmpty(instance.buffer);
              }));
}

function isActivated(self) {
  return self.activated;
}

function deactivate(self) {
  Belt_Array.forEach(self.instances, destroy);
  self.instances = [];
  self.activated = false;
}

function applyRewrites(self, editor, rewrites) {
  self.semaphore = true;
  var replacements = Belt_Array.map(rewrites, (function (param) {
          return [
                  param.interval,
                  param.text
                ];
        }));
  var match = $$Promise.pending(undefined);
  var resolve = match[1];
  $$Promise.get(match[0], (function (param) {
          Belt_Array.forEach(rewrites, (function (rewrite) {
                  Belt_Option.forEach(rewrite.instance, (function (instance) {
                          Belt_Option.forEach(editor, (function (param) {
                                  Belt_Option.forEach(instance.decoration, Editor$AgdaModeVscode.Decoration.destroy);
                                  var $$document = param.document;
                                  var match = instance.interval;
                                  var start = $$document.positionAt(match[0]);
                                  var end_ = $$document.positionAt(match[1]);
                                  var range = new Vscode.Range(start, end_);
                                  instance.decoration = Caml_option.some(Editor$AgdaModeVscode.Decoration.underlineText(param, range));
                                }));
                        }));
                }));
          self.semaphore = false;
          Chan$AgdaModeVscode.emit(self.chanLog, [/* RewriteApplied */1]);
        }));
  var instance = Belt_Array.get(self.instances, 0);
  if (instance !== undefined) {
    return [
            {
              TAG: 1,
              _0: replacements,
              _1: resolve,
              [Symbol.for("name")]: "Rewrite"
            },
            {
              TAG: 0,
              _0: Buffer$AgdaModeVscode.toSequence(instance.buffer),
              _1: instance.buffer.translation,
              _2: instance.buffer.candidateIndex,
              [Symbol.for("name")]: "UpdateView"
            }
          ];
  } else {
    deactivate(self);
    return [
            {
              TAG: 1,
              _0: replacements,
              _1: resolve,
              [Symbol.for("name")]: "Rewrite"
            },
            /* Deactivate */1
          ];
  }
}

function run(self, editor, input) {
  var output;
  if (typeof input === "number") {
    deactivate(self);
    output = [/* Deactivate */1];
  } else {
    switch (input.TAG | 0) {
      case /* Activate */0 :
          self.activated = true;
          self.instances = Belt_Array.map(Js_array.sortInPlaceWith((function (x, y) {
                      return Caml.int_compare(x[0], y[0]);
                    }), input._0), (function (param) {
                  if (editor === undefined) {
                    return {
                            interval: param,
                            decoration: undefined,
                            buffer: Buffer$AgdaModeVscode.make(undefined)
                          };
                  }
                  var editor$1 = Caml_option.valFromOption(editor);
                  var $$document = editor$1.document;
                  var range = Editor$AgdaModeVscode.$$Range.fromInterval($$document, param);
                  return {
                          interval: param,
                          decoration: Caml_option.some(Editor$AgdaModeVscode.Decoration.underlineText(editor$1, range)),
                          buffer: Buffer$AgdaModeVscode.make(undefined)
                        };
                }));
          output = [/* Activate */0];
          break;
      case /* KeyUpdate */1 :
          var changes = input._0;
          if (changes.length !== 0 && self.activated && !self.semaphore) {
            var match = updateInstances(self.instances, changes);
            self.instances = match[0];
            output = applyRewrites(self, editor, match[1]);
          } else {
            output = [];
          }
          break;
      case /* MouseSelect */2 :
          if (self.activated && !self.semaphore) {
            self.instances = validateCursorPositions(self.instances, input._0);
            output = self.instances.length === 0 ? run(self, editor, /* Deactivate */0) : [];
          } else {
            output = [];
          }
          break;
      case /* Candidate */3 :
          var action = input._0;
          var callback;
          if (typeof action === "number") {
            switch (action) {
              case /* BrowseUp */0 :
                  callback = (function (instance) {
                      instance.buffer = Buffer$AgdaModeVscode.moveUp(instance.buffer);
                      return Belt_Array.get(instance.buffer.translation.candidateSymbols, instance.buffer.candidateIndex);
                    });
                  break;
              case /* BrowseDown */1 :
                  callback = (function (instance) {
                      instance.buffer = Buffer$AgdaModeVscode.moveDown(instance.buffer);
                      return Belt_Array.get(instance.buffer.translation.candidateSymbols, instance.buffer.candidateIndex);
                    });
                  break;
              case /* BrowseLeft */2 :
                  callback = (function (instance) {
                      instance.buffer = Buffer$AgdaModeVscode.moveLeft(instance.buffer);
                      return Belt_Array.get(instance.buffer.translation.candidateSymbols, instance.buffer.candidateIndex);
                    });
                  break;
              case /* BrowseRight */3 :
                  callback = (function (instance) {
                      instance.buffer = Buffer$AgdaModeVscode.moveRight(instance.buffer);
                      return Belt_Array.get(instance.buffer.translation.candidateSymbols, instance.buffer.candidateIndex);
                    });
                  break;
              
            }
          } else {
            var symbol = action._0;
            callback = (function (param) {
                return symbol;
              });
          }
          var rewrites = toRewrites(self.instances, callback);
          output = applyRewrites(self, editor, rewrites);
          break;
      
    }
  }
  logOutput(self, output);
  return output;
}

var Module = {
  make: make,
  isActivated: isActivated,
  bufferIsEmpty: bufferIsEmpty,
  run: run
};

exports.Input = Input;
exports.Output = Output;
exports.Log = Log;
exports.Module = Module;
exports.make = make;
exports.isActivated = isActivated;
exports.bufferIsEmpty = bufferIsEmpty;
exports.run = run;
/* vscode Not a pure module */
