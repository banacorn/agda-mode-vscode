// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Vscode = require("vscode");
var Caml_obj = require("rescript/lib/js/caml_obj.js");
var Nodepath = require("node:path");
var Caml_option = require("rescript/lib/js/caml_option.js");
var Core__Array = require("@rescript/core/lib/js/src/Core__Array.bs.js");
var PervasivesU = require("rescript/lib/js/pervasivesU.js");
var FS$AgdaModeVscode = require("../FS.bs.js");
var Chan$AgdaModeVscode = require("../Util/Chan.bs.js");
var Item$AgdaModeVscode = require("../View/Component/Item.bs.js");
var Util$AgdaModeVscode = require("../Util/Util.bs.js");
var Config$AgdaModeVscode = require("../Config.bs.js");
var Memento$AgdaModeVscode = require("../Memento.bs.js");
var Connection$AgdaModeVscode = require("../Connection/Connection.bs.js");
var State__View$AgdaModeVscode = require("./State__View.bs.js");
var Connection__Error$AgdaModeVscode = require("../Connection/Shared/Connection__Error.bs.js");
var Connection__Download$AgdaModeVscode = require("../Connection/Download/Connection__Download.bs.js");
var Registry__Connection$AgdaModeVscode = require("../Registry__Connection.bs.js");
var Connection__Hardcoded$AgdaModeVscode = require("../Connection/Download/Connection__Hardcoded.bs.js");

var agdaVersionPrefix = "Agda v";

var alsWithSquirrel = "$(squirrel)  ALS v";

var downloadNativeALS = "$(cloud-download)  Download Agda Language Server (native)";

var downloadWasmALS = "$(cloud-download)  Download Agda Language Server (WASM)";

var selectOtherChannels = "$(tag)  Select other channels";

var downloadUnavailable = "Not available for this platform";

var checkingAvailability = "Checking availability...";

var deleteDownloads = "$(trash)  Delete downloads";

var clearPreferred = "$(close)  Clear preferred version";

var downloadedAndInstalled = "Downloaded and installed";

var Constants = {
  agdaVersionPrefix: agdaVersionPrefix,
  alsWithSquirrel: alsWithSquirrel,
  downloadNativeALS: downloadNativeALS,
  downloadWasmALS: downloadWasmALS,
  selectOtherChannels: selectOtherChannels,
  downloadUnavailable: downloadUnavailable,
  checkingAvailability: checkingAvailability,
  deleteDownloads: deleteDownloads,
  clearPreferred: clearPreferred,
  downloadedAndInstalled: downloadedAndInstalled
};

function toString(item) {
  if (typeof item !== "object") {
    switch (item) {
      case "SelectOtherChannels" :
          return "SelectOtherChannels";
      case "DeleteDownloads" :
          return "DeleteDownloads";
      case "ClearPreferred" :
          return "ClearPreferred";
      case "NoInstallations" :
          return "NoInstallations";
      
    }
  } else {
    switch (item.TAG) {
      case "Endpoint" :
          return "Endpoint: " + item._0 + ", " + Memento$AgdaModeVscode.Module.Endpoints.endpointToString(item._1.endpoint) + (
                  item._2 ? ", selected" : ""
                );
      case "DownloadAction" :
          return "DownloadAction: downloaded=" + PervasivesU.string_of_bool(item._0) + ", versionString=" + item._1 + ", type=" + item._2;
      case "Separator" :
          return "Separator: " + item._0;
      
    }
  }
}

function shouldEndpointHaveIcon(endpoint) {
  if (typeof endpoint !== "object" || endpoint.TAG !== "Agda") {
    return false;
  } else {
    return true;
  }
}

function getEndpointDisplayInfo(filename, entry) {
  var match = entry.endpoint;
  var match$1 = entry.error;
  if (typeof match !== "object") {
    if (match$1 !== undefined) {
      return [
              "$(error) " + filename,
              "Error: " + match$1
            ];
    } else {
      return [
              "$(question) " + filename,
              "Unknown executable"
            ];
    }
  }
  if (match.TAG === "Agda") {
    var version = match._0;
    if (version !== undefined) {
      return [
              agdaVersionPrefix + version,
              undefined
            ];
    } else {
      return [
              "Agda (version unknown)",
              undefined
            ];
    }
  }
  var match$2 = match._0;
  if (match$2 !== undefined) {
    return [
            alsWithSquirrel + match$2[0] + ", " + agdaVersionPrefix + match$2[1],
            undefined
          ];
  } else {
    return [
            alsWithSquirrel + "(version unknown)",
            undefined
          ];
  }
}

function entriesToItemData(entries, pickedPath, downloadItems, downloadHeaderOpt, showChannelSelectorOpt) {
  var downloadHeader = downloadHeaderOpt !== undefined ? downloadHeaderOpt : "Download (channel: Hardcoded)";
  var showChannelSelector = showChannelSelectorOpt !== undefined ? showChannelSelectorOpt : false;
  var entriesArray = Object.entries(entries);
  var hasEndpoints = entriesArray.length > 0;
  var toFsPath = function (s) {
    if (s.startsWith("file://")) {
      return Vscode.Uri.parse(s).fsPath;
    } else {
      return s;
    }
  };
  var endpointItems;
  if (pickedPath !== undefined) {
    var normalizedPicked = toFsPath(pickedPath);
    var matched = {
      contents: false
    };
    endpointItems = entriesArray.map(function (param) {
          var path = param[0];
          var isSelected = !matched.contents && normalizedPicked === toFsPath(path);
          if (isSelected) {
            matched.contents = true;
          }
          return {
                  TAG: "Endpoint",
                  _0: path,
                  _1: param[1],
                  _2: isSelected
                };
        });
  } else {
    endpointItems = entriesArray.map(function (param) {
          return {
                  TAG: "Endpoint",
                  _0: param[0],
                  _1: param[1],
                  _2: false
                };
        });
  }
  var sectionsWithInstalled = hasEndpoints ? [{
          TAG: "Separator",
          _0: "Installed"
        }].concat(endpointItems) : [];
  var sectionsWithDownload;
  if (downloadItems.length > 0) {
    var downloadSectionItems = downloadItems.map(function (param) {
          return {
                  TAG: "DownloadAction",
                  _0: param[0],
                  _1: param[1],
                  _2: param[2]
                };
        });
    sectionsWithDownload = sectionsWithInstalled.concat([{
              TAG: "Separator",
              _0: downloadHeader
            }].concat(downloadSectionItems));
  } else {
    sectionsWithDownload = sectionsWithInstalled;
  }
  var sectionsWithChannels = showChannelSelector ? sectionsWithDownload.concat(["SelectOtherChannels"]) : sectionsWithDownload;
  var miscItems = pickedPath !== undefined ? [
      {
        TAG: "Separator",
        _0: "Misc"
      },
      "ClearPreferred",
      "DeleteDownloads"
    ] : [
      {
        TAG: "Separator",
        _0: "Misc"
      },
      "DeleteDownloads"
    ];
  return sectionsWithChannels.concat(miscItems);
}

var ItemData = {
  toString: toString,
  shouldEndpointHaveIcon: shouldEndpointHaveIcon,
  getEndpointDisplayInfo: getEndpointDisplayInfo,
  entriesToItemData: entriesToItemData
};

function createQuickPickItem(label, description, detail) {
  if (description !== undefined) {
    if (detail !== undefined) {
      return {
              description: description,
              detail: detail,
              label: label
            };
    } else {
      return {
              description: description,
              label: label
            };
    }
  } else if (detail !== undefined) {
    return {
            detail: detail,
            label: label
          };
  } else {
    return {
            label: label
          };
  }
}

function fromItemData(itemData, extensionUri) {
  var match;
  if (typeof itemData !== "object") {
    switch (itemData) {
      case "SelectOtherChannels" :
          match = [
            selectOtherChannels,
            undefined,
            undefined
          ];
          break;
      case "DeleteDownloads" :
          match = [
            deleteDownloads,
            "Delete all downloaded files and clear cached release metadata",
            undefined
          ];
          break;
      case "ClearPreferred" :
          match = [
            clearPreferred,
            "Reset to automatic version resolution",
            undefined
          ];
          break;
      case "NoInstallations" :
          match = [
            "$(info) No installations found",
            "Try installing Agda or ALS first",
            "No executable paths detected"
          ];
          break;
      
    }
  } else {
    switch (itemData.TAG) {
      case "Endpoint" :
          var path = itemData._0;
          var filename = Nodepath.basename(path);
          var match$1 = getEndpointDisplayInfo(filename, itemData._1);
          var errorDescription = match$1[1];
          var description = itemData._2 ? (
              errorDescription !== undefined ? errorDescription : "Selected"
            ) : (
              errorDescription !== undefined ? errorDescription : ""
            );
          match = [
            match$1[0],
            description,
            path
          ];
          break;
      case "DownloadAction" :
          var label;
          switch (itemData._2) {
            case "native" :
                label = downloadNativeALS;
                break;
            case "wasm" :
                label = downloadWasmALS;
                break;
            default:
              label = "$(cloud-download)  Download Agda Language Server";
          }
          var description$1 = itemData._0 ? downloadedAndInstalled : "";
          match = [
            label,
            description$1,
            itemData._1
          ];
          break;
      case "Separator" :
          match = [
            itemData._0,
            undefined,
            undefined
          ];
          break;
      
    }
  }
  var detail = match[2];
  var description$2 = match[1];
  var label$1 = match[0];
  if (typeof itemData !== "object") {
    return createQuickPickItem(label$1, description$2, detail);
  }
  switch (itemData.TAG) {
    case "Endpoint" :
        var baseItem = createQuickPickItem(label$1, description$2, detail);
        if (!shouldEndpointHaveIcon(itemData._1.endpoint)) {
          return baseItem;
        }
        var newrecord = Caml_obj.obj_dup(baseItem);
        newrecord.iconPath = {
          dark: Vscode.Uri.joinPath(extensionUri, "asset/dark.png"),
          light: Vscode.Uri.joinPath(extensionUri, "asset/light.png")
        };
        return newrecord;
    case "Separator" :
        return {
                kind: -1,
                label: label$1
              };
    default:
      return createQuickPickItem(label$1, description$2, detail);
  }
}

function fromItemDataArray(itemDataArray, extensionUri) {
  return itemDataArray.map(function (itemData) {
              return fromItemData(itemData, extensionUri);
            });
}

var Item = {
  createQuickPickItem: createQuickPickItem,
  fromItemData: fromItemData,
  fromItemDataArray: fromItemDataArray
};

function make(log) {
  return {
          log: log,
          quickPick: Vscode.window.createQuickPick(),
          subscriptions: [],
          items: []
        };
}

function setPlaceholder(self, placeholder) {
  self.quickPick.placeholder = placeholder;
}

function updateItems(self, items) {
  self.items = items;
  self.quickPick.items = items;
}

function show(self) {
  self.quickPick.show();
}

function onSelection(self, handler) {
  Util$AgdaModeVscode.Disposable.add(self.quickPick.onDidChangeSelection(handler), self.subscriptions);
}

function onHide(self, handler) {
  Util$AgdaModeVscode.Disposable.add(self.quickPick.onDidHide(handler), self.subscriptions);
}

function destroy(self) {
  self.quickPick.dispose();
  self.subscriptions.forEach(function (sub) {
        sub.dispose();
      });
  Chan$AgdaModeVscode.emit(self.log, {
        TAG: "SwitchVersionUI",
        _0: "Destroyed"
      });
}

var View = {
  make: make,
  setPlaceholder: setPlaceholder,
  updateItems: updateItems,
  show: show,
  onSelection: onSelection,
  onHide: onHide,
  destroy: destroy
};

function make$1(state) {
  return {
          entries: Memento$AgdaModeVscode.Module.Endpoints.entries(state.memento),
          memento: state.memento,
          globalStorageUri: state.globalStorageUri
        };
}

async function getItemData(self, downloadItems, downloadHeaderOpt, showChannelSelectorOpt) {
  var downloadHeader = downloadHeaderOpt !== undefined ? downloadHeaderOpt : "Download (channel: Hardcoded)";
  var showChannelSelector = showChannelSelectorOpt !== undefined ? showChannelSelectorOpt : false;
  var storedPath = Memento$AgdaModeVscode.Module.PickedConnection.get(self.memento);
  var pickedPath;
  if (storedPath !== undefined) {
    pickedPath = storedPath;
  } else {
    var resource = Registry__Connection$AgdaModeVscode.status.contents;
    pickedPath = typeof resource !== "object" || resource.TAG !== "Active" ? undefined : Connection$AgdaModeVscode.getPath(resource._0.connection);
  }
  return entriesToItemData(self.entries, pickedPath, downloadItems, downloadHeader, showChannelSelector);
}

function refreshFromMemento(self) {
  var newEntries = Memento$AgdaModeVscode.Module.Endpoints.entries(self.memento);
  var changed = newEntries !== self.entries;
  if (changed) {
    self.entries = newEntries;
  }
  return changed;
}

function inferEndpointType(filename) {
  var baseName = Nodepath.basename(filename.toLowerCase());
  var cleanName = baseName.replace(".exe", "").replace(".cmd", "").replace(".bat", "").replace(".wasm", "");
  if (cleanName === "agda" || cleanName.startsWith("agda-")) {
    return {
            TAG: "Agda",
            _0: undefined
          };
  } else if (cleanName === "als" || cleanName.startsWith("als-")) {
    return {
            TAG: "ALS",
            _0: undefined
          };
  } else {
    return "Unknown";
  }
}

async function syncWithFilesystem(self, platformDeps) {
  var endpoints = {};
  Config$AgdaModeVscode.Connection.getAgdaPaths().forEach(function (path) {
        var filename = Nodepath.basename(path);
        var endpoint = inferEndpointType(filename);
        endpoints[path] = endpoint;
      });
  var path = await platformDeps.findCommand("agda", undefined);
  if (path.TAG === "Ok") {
    endpoints[path._0] = {
      TAG: "Agda",
      _0: undefined
    };
  }
  var path$1 = await platformDeps.findCommand("als", undefined);
  if (path$1.TAG === "Ok") {
    endpoints[path$1._0] = {
      TAG: "ALS",
      _0: undefined
    };
  }
  await Memento$AgdaModeVscode.Module.Endpoints.syncWithPaths(self.memento, endpoints);
  return refreshFromMemento(self);
}

async function probeVersions(self) {
  var pathsToProbe = Core__Array.filterMap(Object.entries(self.entries), (function (param) {
          var match = param[1].endpoint;
          if (typeof match !== "object") {
            return ;
          }
          match.TAG === "Agda";
          if (match._0 !== undefined) {
            return ;
          } else {
            return param[0];
          }
        }));
  if (pathsToProbe.length === 0) {
    return false;
  }
  var probePromises = pathsToProbe.map(async function (path) {
        var error = await Connection$AgdaModeVscode.probeFilepath(path);
        if (error.TAG === "Ok") {
          var agdaVersion = error._0[1];
          switch (agdaVersion.TAG) {
            case "IsAgda" :
                await Memento$AgdaModeVscode.Module.Endpoints.setVersion(self.memento, path, {
                      TAG: "Agda",
                      _0: agdaVersion._0
                    });
                return path;
            case "IsALS" :
                await Memento$AgdaModeVscode.Module.Endpoints.setVersion(self.memento, path, {
                      TAG: "ALS",
                      _0: [
                        agdaVersion._0,
                        agdaVersion._1,
                        agdaVersion._2
                      ]
                    });
                return path;
            case "IsALSOfUnknownVersion" :
            case "IsALSWASM" :
                break;
            
          }
          await Memento$AgdaModeVscode.Module.Endpoints.setVersion(self.memento, path, {
                TAG: "ALS",
                _0: undefined
              });
          return path;
        }
        await Memento$AgdaModeVscode.Module.Endpoints.setError(self.memento, path, Connection__Error$AgdaModeVscode.Probe.toString(error._0));
        return path;
      });
  var updateResults = await Promise.all(probePromises);
  var updatedPaths = Core__Array.filterMap(updateResults, (function (x) {
          return x;
        }));
  if (updatedPaths.length > 0) {
    refreshFromMemento(self);
    return true;
  } else {
    return false;
  }
}

var SwitchVersionManager = {
  make: make$1,
  getItemData: getItemData,
  refreshFromMemento: refreshFromMemento,
  inferEndpointType: inferEndpointType,
  syncWithFilesystem: syncWithFilesystem,
  probeVersions: probeVersions
};

async function resolveDownloadChannelWithPlatform(platformOps, target, memento, globalStorageUri) {
  var _error = await platformOps.determinePlatform();
  if (_error.TAG === "Ok") {
    return await platformOps.resolveDownloadChannel(target, true)(memento, globalStorageUri, _error._0);
  } else {
    return {
            TAG: "Error",
            _0: "CannotFindCompatibleALSRelease"
          };
  }
}

async function switchAgdaVersion(state, selectedPath) {
  await State__View$AgdaModeVscode.Panel.displayConnectionStatus(state, undefined);
  await State__View$AgdaModeVscode.Panel.display(state, {
        TAG: "Plain",
        _0: "Switching connection..."
      }, []);
  var conn = await Connection$AgdaModeVscode.fromPathsOrCommands(state.platformDeps, [[
          selectedPath,
          "FromConfig"
        ]]);
  if (conn.TAG === "Ok") {
    var conn$1 = conn._0;
    Util$AgdaModeVscode.log("[ debug ] switchAgdaVersion: connection succeeded", Connection$AgdaModeVscode.toString(conn$1));
    await Registry__Connection$AgdaModeVscode.shutdown();
    await Memento$AgdaModeVscode.Module.PickedConnection.set(state.memento, selectedPath);
    await State__View$AgdaModeVscode.Panel.displayConnectionStatus(state, conn$1);
    await State__View$AgdaModeVscode.Panel.display(state, {
          TAG: "Success",
          _0: "Switched to " + Connection$AgdaModeVscode.toString(conn$1)
        }, []);
    switch (conn$1.TAG) {
      case "Agda" :
          await Memento$AgdaModeVscode.Module.Endpoints.setVersion(state.memento, selectedPath, {
                TAG: "Agda",
                _0: conn$1._2
              });
          break;
      case "ALS" :
          var match = conn$1._2;
          if (match !== undefined) {
            await Memento$AgdaModeVscode.Module.Endpoints.setVersion(state.memento, selectedPath, {
                  TAG: "ALS",
                  _0: [
                    match[0],
                    match[1],
                    match[2]
                  ]
                });
          }
          break;
      case "ALSWASM" :
          var match$1 = conn$1._3;
          if (match$1 !== undefined) {
            await Memento$AgdaModeVscode.Module.Endpoints.setVersion(state.memento, selectedPath, {
                  TAG: "ALS",
                  _0: [
                    match$1[0],
                    match$1[1],
                    match$1[2]
                  ]
                });
          }
          break;
      
    }
    await Connection$AgdaModeVscode.destroy(conn$1, state.channels.log);
    return ;
  }
  var match$2 = Connection__Error$AgdaModeVscode.toString({
        TAG: "Establish",
        _0: conn._0
      });
  var errorBody = match$2[1];
  var errorHeader = match$2[0];
  Util$AgdaModeVscode.log("[ debug ] switchAgdaVersion: connection failed", errorHeader + " | " + errorBody);
  var header = {
    TAG: "Error",
    _0: "Failed to switch to a different installation: " + errorHeader
  };
  var body = [Item$AgdaModeVscode.plainText(errorBody)];
  return await State__View$AgdaModeVscode.Panel.display(state, header, body);
}

function variantToTag(variant) {
  if (variant === "Native") {
    return "native";
  } else {
    return "wasm";
  }
}

function variantFromTag(tag) {
  switch (tag) {
    case "native" :
        return "Native";
    case "wasm" :
        return "WASM";
    default:
      return ;
  }
}

function variantDisplayName(variant) {
  if (variant === "Native") {
    return "Agda Language Server (native)";
  } else {
    return "Agda Language Server (WASM)";
  }
}

function variantFileName(variant) {
  if (variant === "Native") {
    return "als";
  } else {
    return "als.wasm";
  }
}

function expectedPathForVariant(globalStorageUri, variant) {
  var uri = Vscode.Uri.joinPath(globalStorageUri, "hardcoded-als", variantFileName(variant));
  if (variant === "Native") {
    return uri.fsPath;
  } else {
    return uri.toString();
  }
}

function pathAliases(path) {
  if (!path.startsWith("file://")) {
    if (Nodepath.isAbsolute(path)) {
      return [
              path,
              Vscode.Uri.file(path).toString()
            ];
    } else {
      return [path];
    }
  }
  var uri = Vscode.Uri.parse(path);
  return [
          path,
          uri.fsPath
        ];
}

function configContainsExpectedPath(configPaths, expectedPath) {
  var expectedAliases = pathAliases(expectedPath);
  return configPaths.some(function (configPath) {
              var configAliases = pathAliases(configPath);
              return configAliases.some(function (configAlias) {
                          return expectedAliases.some(function (expectedAlias) {
                                      return expectedAlias === configAlias;
                                    });
                        });
            });
}

function suppressManagedVariants(globalStorageUri, configPaths, downloadItems) {
  return downloadItems.filter(function (param) {
              var variant = variantFromTag(param[2]);
              if (variant === undefined) {
                return true;
              }
              var expectedPath = expectedPathForVariant(globalStorageUri, variant);
              return !configContainsExpectedPath(configPaths, expectedPath);
            });
}

function sourceForVariant(platform, variant) {
  if (variant !== "Native") {
    return {
            TAG: "FromURL",
            _0: "Hardcoded",
            _1: Connection__Hardcoded$AgdaModeVscode.wasmUrl,
            _2: "hardcoded-als"
          };
  }
  var url = Connection__Hardcoded$AgdaModeVscode.nativeUrlForPlatform(platform);
  if (url !== undefined) {
    return {
            TAG: "FromURL",
            _0: "Hardcoded",
            _1: url,
            _2: "hardcoded-als"
          };
  }
  
}

async function isDownloaded(globalStorageUri, variant) {
  var uri = Vscode.Uri.joinPath(globalStorageUri, "hardcoded-als", variantFileName(variant));
  var match = await FS$AgdaModeVscode.stat(uri);
  if (match.TAG === "Ok") {
    return true;
  } else {
    return false;
  }
}

async function makeDownloadItem(state, variant, source) {
  var downloaded = await isDownloaded(state.globalStorageUri, variant);
  return [
          downloaded,
          Connection__Download$AgdaModeVscode.Source.toVersionString(source),
          variantToTag(variant)
        ];
}

function unavailableItem(variant) {
  return [
          false,
          downloadUnavailable,
          variantToTag(variant)
        ];
}

function channelToLabel(channel) {
  switch (channel) {
    case "LatestALS" :
        return "LatestALS";
    case "DevALS" :
        return "DevALS";
    case "Hardcoded" :
        return "Hardcoded";
    
  }
}

function channelFromLabel(label) {
  switch (label) {
    case "DevALS" :
        return "DevALS";
    case "Hardcoded" :
        return "Hardcoded";
    case "LatestALS" :
        return "LatestALS";
    default:
      return ;
  }
}

async function getAvailableChannels(_platformDeps) {
  return ["Hardcoded"];
}

async function getPlaceholderDownloadItems(platformDeps) {
  var match = await platformDeps.determinePlatform();
  if (match.TAG === "Ok" && match._0 === "Web") {
    return [[
              false,
              checkingAvailability,
              "wasm"
            ]];
  }
  return [
          [
            false,
            checkingAvailability,
            "native"
          ],
          [
            false,
            checkingAvailability,
            "wasm"
          ]
        ];
}

async function getAllAvailableDownloads(state, platformDeps) {
  var platform = await platformDeps.determinePlatform();
  var allItems;
  if (platform.TAG === "Ok") {
    var platform$1 = platform._0;
    if (platform$1 === "Web") {
      var wasmSource = sourceForVariant("Web", "WASM");
      allItems = wasmSource !== undefined ? [await makeDownloadItem(state, "WASM", wasmSource)] : [unavailableItem("WASM")];
    } else {
      var source = sourceForVariant(platform$1, "Native");
      var nativeItem = source !== undefined ? await makeDownloadItem(state, "Native", source) : unavailableItem("Native");
      var source$1 = sourceForVariant(platform$1, "WASM");
      var wasmItem = source$1 !== undefined ? await makeDownloadItem(state, "WASM", source$1) : unavailableItem("WASM");
      allItems = [
        nativeItem,
        wasmItem
      ];
    }
  } else {
    allItems = [
      unavailableItem("Native"),
      unavailableItem("WASM")
    ];
  }
  return suppressManagedVariants(state.globalStorageUri, Config$AgdaModeVscode.Connection.getAgdaPaths(), allItems);
}

var Download = {
  variantToTag: variantToTag,
  variantFromTag: variantFromTag,
  variantDisplayName: variantDisplayName,
  variantFileName: variantFileName,
  expectedPathForVariant: expectedPathForVariant,
  pathAliases: pathAliases,
  configContainsExpectedPath: configContainsExpectedPath,
  suppressManagedVariants: suppressManagedVariants,
  sourceForVariant: sourceForVariant,
  isDownloaded: isDownloaded,
  makeDownloadItem: makeDownloadItem,
  unavailableItem: unavailableItem,
  channelToLabel: channelToLabel,
  channelFromLabel: channelFromLabel,
  getAvailableChannels: getAvailableChannels,
  getPlaceholderDownloadItems: getPlaceholderDownloadItems,
  getAllAvailableDownloads: getAllAvailableDownloads
};

var downloadDirectoryNames = [
  "hardcoded-als",
  "latest-als",
  "dev-als",
  "dev-wasm-als"
];

function isUnderPrefix(path, prefix, separator) {
  if (path === prefix) {
    return true;
  } else {
    return path.startsWith(prefix + separator);
  }
}

function isPathUnderDownloadDirectory(globalStorageUri, path) {
  return downloadDirectoryNames.some(function (dirName) {
              var dirUri = Vscode.Uri.joinPath(globalStorageUri, dirName);
              var dirFsPath = dirUri.fsPath;
              var dirUriPath = dirUri.toString();
              var dirUnescapedUriPath = "file://" + dirFsPath;
              var pathCandidates;
              if (path.startsWith("file://")) {
                try {
                  var parsedFsPath = Vscode.Uri.parse(path).fsPath;
                  pathCandidates = parsedFsPath === path ? [path] : [
                      path,
                      parsedFsPath
                    ];
                }
                catch (exn){
                  pathCandidates = [path];
                }
              } else {
                pathCandidates = [path];
              }
              return pathCandidates.some(function (candidate) {
                          if (isUnderPrefix(candidate, dirFsPath, Nodepath.sep) || isUnderPrefix(candidate, dirFsPath, "/") || isUnderPrefix(candidate, dirUriPath, "/")) {
                            return true;
                          } else {
                            return isUnderPrefix(candidate, dirUnescapedUriPath, "/");
                          }
                        });
            });
}

function isKnownEndpointSelection(manager, selectedItem) {
  var selectedPath = selectedItem.detail;
  if (selectedPath === undefined) {
    return ;
  }
  var entry = Memento$AgdaModeVscode.Module.Endpoints.get(manager.memento, selectedPath);
  if (entry === undefined) {
    return ;
  }
  var filename = Nodepath.basename(selectedPath);
  var match = getEndpointDisplayInfo(filename, entry);
  if (selectedItem.label === match[0]) {
    return selectedPath;
  }
  
}

async function ensureEndpointRegistered(state, path) {
  var match = Memento$AgdaModeVscode.Module.Endpoints.get(state.memento, path);
  if (match !== undefined) {
    var endpoint = match.endpoint;
    if (typeof endpoint !== "object") {
      var endpointType = inferEndpointType(path);
      return await Memento$AgdaModeVscode.Module.Endpoints.setVersion(state.memento, path, endpointType);
    }
    endpoint.TAG === "Agda";
    if (match.error !== undefined) {
      return await Memento$AgdaModeVscode.Module.Endpoints.setVersion(state.memento, path, endpoint);
    } else {
      return ;
    }
  }
  var endpointType$1 = inferEndpointType(path);
  return await Memento$AgdaModeVscode.Module.Endpoints.setVersion(state.memento, path, endpointType$1);
}

async function handleDownload(state, platformDeps, variant, downloaded, versionString, refreshUIOpt) {
  var refreshUI = refreshUIOpt !== undefined ? Caml_option.valFromOption(refreshUIOpt) : undefined;
  Chan$AgdaModeVscode.emit(state.channels.log, {
        TAG: "SwitchVersionUI",
        _0: {
          TAG: "SelectedDownloadAction",
          _0: downloaded,
          _1: versionString
        }
      });
  if (downloaded) {
    var downloadedPath = expectedPathForVariant(state.globalStorageUri, variant);
    await Config$AgdaModeVscode.Connection.addAgdaPath(state.channels.log, downloadedPath);
    await Memento$AgdaModeVscode.Module.PickedConnection.set(state.memento, downloadedPath);
    await ensureEndpointRegistered(state, downloadedPath);
    Vscode.window.showInformationMessage(versionString + " is already downloaded");
  } else {
    var platform = await platformDeps.determinePlatform();
    var downloadResult;
    if (platform.TAG === "Ok") {
      var source = sourceForVariant(platform._0, variant);
      downloadResult = source !== undefined ? await platformDeps.download(state.globalStorageUri, source) : ({
            TAG: "Error",
            _0: "CannotFindCompatibleALSRelease"
          });
    } else {
      downloadResult = {
        TAG: "Error",
        _0: "CannotFindCompatibleALSRelease"
      };
    }
    if (downloadResult.TAG === "Ok") {
      var downloadedPath$1 = downloadResult._0;
      await Config$AgdaModeVscode.Connection.addAgdaPath(state.channels.log, downloadedPath$1);
      await Memento$AgdaModeVscode.Module.PickedConnection.set(state.memento, downloadedPath$1);
      await ensureEndpointRegistered(state, downloadedPath$1);
      if (refreshUI !== undefined) {
        await refreshUI();
      }
      Vscode.window.showInformationMessage(versionString + " successfully downloaded");
    } else {
      Vscode.window.showErrorMessage(Connection__Download$AgdaModeVscode.$$Error.toString(downloadResult._0));
    }
  }
  return Chan$AgdaModeVscode.emit(state.channels.log, {
              TAG: "SwitchVersionUI",
              _0: "SelectionCompleted"
            });
}

async function handleChannelSwitch(state, platformDeps, manager, selectedChannel, channel, updateUI) {
  selectedChannel.contents = channel;
  await Memento$AgdaModeVscode.Module.SelectedChannel.set(state.memento, channelToLabel(channel));
  var newDownloadItems = await getAllAvailableDownloads(state, platformDeps);
  refreshFromMemento(manager);
  return await updateUI(newDownloadItems);
}

function onSelection$1(state, platformDeps, manager, availableChannels, selectedChannel, updateUI, view, selectedItems) {
  ((async function () {
          var selectedItem = selectedItems[0];
          if (selectedItem !== undefined) {
            Util$AgdaModeVscode.log("[ debug ] user selected item: " + selectedItem.label, "");
            if (selectedItem.label === checkingAvailability) {
              return ;
            }
            if (selectedItem.label === selectOtherChannels) {
              var channelLabels = availableChannels.contents.map(channelToLabel);
              var selection = await Vscode.window.showQuickPick(Promise.resolve(channelLabels), {
                    canPickMany: false,
                    placeHolder: "Select download channel"
                  }, undefined);
              if (selection === undefined) {
                return ;
              }
              var label = selection[0];
              if (label === undefined) {
                return ;
              }
              var channel = channelFromLabel(label);
              if (channel !== undefined) {
                return await handleChannelSwitch(state, platformDeps, manager, selectedChannel, channel, updateUI);
              } else {
                return ;
              }
            }
            if (selectedItem.label === deleteDownloads) {
              Util$AgdaModeVscode.log("[ debug ] user clicked: Delete Downloads", "");
              destroy(view);
              var deleteDir = async function (dirName) {
                var uri = Vscode.Uri.joinPath(state.globalStorageUri, dirName);
                await FS$AgdaModeVscode.deleteRecursive(uri);
              };
              await deleteDir("dev-wasm-als");
              await deleteDir("dev-als");
              await deleteDir("latest-als");
              await deleteDir("hardcoded-als");
              await Memento$AgdaModeVscode.Module.ALSReleaseCache.clear(state.memento, "agda", "agda-language-server");
              await Memento$AgdaModeVscode.Module.ALSReleaseCache.clear(state.memento, "banacorn", "agda-language-server");
              await Memento$AgdaModeVscode.Module.Endpoints.clear(state.memento);
              Chan$AgdaModeVscode.emit(state.channels.log, {
                    TAG: "SwitchVersionUI",
                    _0: "SelectionCompleted"
                  });
              Vscode.window.showInformationMessage("All downloads and cache deleted");
              return ;
            }
            if (selectedItem.label === clearPreferred) {
              Util$AgdaModeVscode.log("[ debug ] user clicked: Clear preferred version", "");
              destroy(view);
              await Memento$AgdaModeVscode.Module.PickedConnection.clear(state.memento);
              return Chan$AgdaModeVscode.emit(state.channels.log, {
                          TAG: "SwitchVersionUI",
                          _0: "SelectionCompleted"
                        });
            }
            if (selectedItem.label === downloadNativeALS || selectedItem.label === downloadWasmALS) {
              Util$AgdaModeVscode.log("[ debug ] user clicked: download button = " + selectedItem.label, "");
              destroy(view);
              var selectedVariant = selectedItem.label === downloadNativeALS ? "Native" : "WASM";
              var downloadItems = await getAllAvailableDownloads(state, platformDeps);
              var selectedDownload = downloadItems.find(function (param) {
                    return param[2] === variantToTag(selectedVariant);
                  });
              if (selectedDownload !== undefined) {
                return await handleDownload(state, platformDeps, selectedVariant, selectedDownload[0], selectedDownload[1], Caml_option.some((async function () {
                                  var newDownloadItems = await getAllAvailableDownloads(state, platformDeps);
                                  refreshFromMemento(manager);
                                  return await updateUI(newDownloadItems);
                                })));
              } else {
                await Vscode.window.showErrorMessage("Download not available for this platform");
                return Chan$AgdaModeVscode.emit(state.channels.log, {
                            TAG: "SwitchVersionUI",
                            _0: "SelectionCompleted"
                          });
              }
            }
            var selectedPath = isKnownEndpointSelection(manager, selectedItem);
            if (selectedPath === undefined) {
              return Chan$AgdaModeVscode.emit(state.channels.log, {
                          TAG: "SwitchVersionUI",
                          _0: "SelectionCompleted"
                        });
            }
            Util$AgdaModeVscode.log("[ debug ] user selected endpoint: " + selectedPath, "");
            destroy(view);
            var path = Memento$AgdaModeVscode.Module.PickedConnection.get(manager.memento);
            var changed = path !== undefined ? selectedPath !== path : true;
            if (!changed) {
              return Chan$AgdaModeVscode.emit(state.channels.log, {
                          TAG: "SwitchVersionUI",
                          _0: "SelectionCompleted"
                        });
            }
            var entry = manager.entries[selectedPath];
            if (entry !== undefined) {
              Chan$AgdaModeVscode.emit(state.channels.log, {
                    TAG: "SwitchVersionUI",
                    _0: {
                      TAG: "SelectedEndpoint",
                      _0: selectedPath,
                      _1: entry,
                      _2: true
                    }
                  });
            }
            await switchAgdaVersion(state, selectedPath);
            return Chan$AgdaModeVscode.emit(state.channels.log, {
                        TAG: "SwitchVersionUI",
                        _0: "SelectionCompleted"
                      });
          }
          destroy(view);
          return Chan$AgdaModeVscode.emit(state.channels.log, {
                      TAG: "SwitchVersionUI",
                      _0: "SelectionCompleted"
                    });
        })());
}

function onHide$1(view) {
  Util$AgdaModeVscode.log("[ debug ] QuickPick hidden/cancelled by user", "");
  destroy(view);
}

async function onActivate(state, platformDeps) {
  var manager = make$1(state);
  var view = make(state.channels.log);
  var availableChannels = {
    contents: ["Hardcoded"]
  };
  var label = Memento$AgdaModeVscode.Module.SelectedChannel.get(state.memento);
  var restoredChannel;
  if (label !== undefined) {
    var channel = channelFromLabel(label);
    restoredChannel = channel !== undefined ? channel : "Hardcoded";
  } else {
    restoredChannel = "Hardcoded";
  }
  var selectedChannel = {
    contents: restoredChannel
  };
  var updateUI = async function (downloadItems) {
    var downloadHeader = "Download (channel: " + channelToLabel(selectedChannel.contents) + ")";
    var itemData = await getItemData(manager, downloadItems, downloadHeader, availableChannels.contents.length >= 2);
    var endpointItemDatas = Core__Array.filterMap(itemData, (function (item) {
            if (typeof item !== "object") {
              return ;
            }
            if (item.TAG !== "Endpoint") {
              return ;
            }
            var entry = item._1;
            return [
                    item._0,
                    entry.endpoint,
                    entry.error,
                    item._2
                  ];
          }));
    Chan$AgdaModeVscode.emit(state.channels.log, {
          TAG: "SwitchVersionUI",
          _0: {
            TAG: "UpdatedEndpoints",
            _0: endpointItemDatas
          }
        });
    Chan$AgdaModeVscode.emit(state.channels.log, {
          TAG: "SwitchVersionUI",
          _0: {
            TAG: "Others",
            _0: downloadHeader
          }
        });
    var items = fromItemDataArray(itemData, state.extensionUri);
    return updateItems(view, items);
  };
  setPlaceholder(view, "Switch Agda Version");
  availableChannels.contents = await getAvailableChannels(platformDeps);
  await updateUI(await getPlaceholderDownloadItems(platformDeps));
  view.quickPick.show();
  Chan$AgdaModeVscode.emit(state.channels.log, {
        TAG: "SwitchVersionUI",
        _0: {
          TAG: "Others",
          _0: "QuickPick shown"
        }
      });
  var downloadItemsPromise = getAllAvailableDownloads(state, platformDeps);
  onSelection(view, (function (selectedItems) {
          onSelection$1(state, platformDeps, manager, availableChannels, selectedChannel, updateUI, view, selectedItems);
        }));
  onHide(view, (function () {
          onHide$1(view);
        }));
  var backgroundUpdate = async function () {
    try {
      var downloadItems = await downloadItemsPromise;
      var phase2Changed = await syncWithFilesystem(manager, platformDeps);
      if (phase2Changed) {
        await updateUI(downloadItems);
      }
      var phase3Changed = await probeVersions(manager);
      if (phase3Changed) {
        await updateUI(downloadItems);
      }
      if (!phase2Changed && !phase3Changed) {
        return await updateUI(downloadItems);
      } else {
        return ;
      }
    }
    catch (_exn){
      return ;
    }
  };
  backgroundUpdate();
}

var Handler = {
  downloadDirectoryNames: downloadDirectoryNames,
  isUnderPrefix: isUnderPrefix,
  isPathUnderDownloadDirectory: isPathUnderDownloadDirectory,
  isKnownEndpointSelection: isKnownEndpointSelection,
  ensureEndpointRegistered: ensureEndpointRegistered,
  handleDownload: handleDownload,
  handleChannelSwitch: handleChannelSwitch,
  onSelection: onSelection$1,
  onHide: onHide$1,
  onActivate: onActivate
};

var activate = onActivate;

exports.Constants = Constants;
exports.ItemData = ItemData;
exports.Item = Item;
exports.View = View;
exports.SwitchVersionManager = SwitchVersionManager;
exports.resolveDownloadChannelWithPlatform = resolveDownloadChannelWithPlatform;
exports.switchAgdaVersion = switchAgdaVersion;
exports.Download = Download;
exports.Handler = Handler;
exports.activate = activate;
/* vscode Not a pure module */
