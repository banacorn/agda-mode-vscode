// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Js_exn = require("rescript/lib/js/js_exn.js");
var VSCode = require("rescript-vscode/lib/js/src/VSCode.bs.js");
var Vscode = require("vscode");
var Caml_obj = require("rescript/lib/js/caml_obj.js");
var Nodepath = require("node:path");
var Belt_Array = require("rescript/lib/js/belt_Array.js");
var Caml_option = require("rescript/lib/js/caml_option.js");
var Core__Array = require("@rescript/core/lib/js/src/Core__Array.bs.js");
var PervasivesU = require("rescript/lib/js/pervasivesU.js");
var Core__Option = require("@rescript/core/lib/js/src/Core__Option.bs.js");
var Core__Result = require("@rescript/core/lib/js/src/Core__Result.bs.js");
var FS$AgdaModeVscode = require("../FS.bs.js");
var Caml_js_exceptions = require("rescript/lib/js/caml_js_exceptions.js");
var Chan$AgdaModeVscode = require("../Util/Chan.bs.js");
var Item$AgdaModeVscode = require("../View/Component/Item.bs.js");
var Util$AgdaModeVscode = require("../Util/Util.bs.js");
var Config$AgdaModeVscode = require("../Config.bs.js");
var Memento$AgdaModeVscode = require("../Memento.bs.js");
var Connection$AgdaModeVscode = require("../Connection/Connection.bs.js");
var State__View$AgdaModeVscode = require("./State__View.bs.js");
var Connection__URI$AgdaModeVscode = require("../Connection/Shared/Connection__URI.bs.js");
var Connection__Error$AgdaModeVscode = require("../Connection/Shared/Connection__Error.bs.js");
var Connection__DevALS$AgdaModeVscode = require("../Connection/Download/Connection__DevALS.bs.js");
var Connection__Download$AgdaModeVscode = require("../Connection/Download/Connection__Download.bs.js");
var Registry__Connection$AgdaModeVscode = require("../Registry__Connection.bs.js");
var Connection__Candidate$AgdaModeVscode = require("../Connection/Connection__Candidate.bs.js");

var agdaVersionPrefix = "Agda ";

var alsWithSquirrel = "$(squirrel)  Agda ";

var downloadNativeALS = "$(cloud-download)  Download Agda Language Server (native)";

var downloadWasmALS = "$(cloud-download)  Download Agda Language Server (WASM)";

var selectOtherChannels = "$(tag)  Select other channels";

var downloadUnavailable = "Not available for this platform";

var checkingAvailability = "Checking availability...";

var deleteDownloads = "$(trash)  Delete downloads";

var downloadedAndInstalled = "Downloaded and installed";

var Constants = {
  agdaVersionPrefix: agdaVersionPrefix,
  alsWithSquirrel: alsWithSquirrel,
  downloadNativeALS: downloadNativeALS,
  downloadWasmALS: downloadWasmALS,
  selectOtherChannels: selectOtherChannels,
  downloadUnavailable: downloadUnavailable,
  checkingAvailability: checkingAvailability,
  deleteDownloads: deleteDownloads,
  downloadedAndInstalled: downloadedAndInstalled
};

function toString(item) {
  if (typeof item !== "object") {
    switch (item) {
      case "SelectOtherChannels" :
          return "SelectOtherChannels";
      case "DeleteDownloads" :
          return "DeleteDownloads";
      case "NoInstallations" :
          return "NoInstallations";
      
    }
  } else {
    switch (item.TAG) {
      case "Candidate" :
          return "Candidate: " + item._0 + " -> " + item._1 + ", " + Memento$AgdaModeVscode.Module.ResolvedMetadata.kindToString(item._2.kind) + (
                  item._3 ? ", selected" : ""
                );
      case "DownloadAction" :
          return "DownloadAction: downloaded=" + PervasivesU.string_of_bool(item._0) + ", versionString=" + item._1 + ", type=" + item._2;
      case "Separator" :
          return "Separator: " + item._0;
      
    }
  }
}

function shouldCandidateHaveIcon(kind) {
  if (typeof kind !== "object" || kind.TAG !== "Agda") {
    return false;
  } else {
    return true;
  }
}

function getCandidateDisplayInfo(raw, entry) {
  var command = Connection__Candidate$AgdaModeVscode.make(raw);
  var filename;
  filename = command.TAG === "Command" ? command._0 : Nodepath.basename(command._0.path);
  var match = entry.kind;
  var match$1 = entry.error;
  if (typeof match !== "object") {
    if (match$1 !== undefined) {
      return [
              "$(error) " + filename,
              "Error: " + match$1
            ];
    } else {
      return [
              "$(question) " + filename,
              "Unknown executable"
            ];
    }
  }
  if (match.TAG === "Agda") {
    var version = match._0;
    if (version !== undefined) {
      return [
              agdaVersionPrefix + version,
              undefined
            ];
    } else {
      return [
              "Agda (version unknown)",
              undefined
            ];
    }
  }
  if (match._0 === "Native") {
    var match$2 = match._1;
    if (match$2 === undefined) {
      return [
              "$(squirrel)  Agda Language Server (version unknown)",
              undefined
            ];
    }
    var agdaVersion = match$2[1];
    var alsVersion = match$2[0];
    return [
            alsVersion === "dev" ? alsWithSquirrel + agdaVersion + " Language Server (dev build)" : alsWithSquirrel + agdaVersion + " Language Server " + Connection__Download$AgdaModeVscode.DownloadArtifact.versionLabel(alsVersion),
            undefined
          ];
  }
  var match$3 = match._1;
  if (match$3 === undefined) {
    return [
            "$(squirrel)  Agda Language Server (version unknown) WASM",
            undefined
          ];
  }
  var agdaVersion$1 = match$3[1];
  var alsVersion$1 = match$3[0];
  return [
          alsVersion$1 === "dev" ? alsWithSquirrel + agdaVersion$1 + " Language Server (dev build) WASM" : alsWithSquirrel + agdaVersion$1 + " Language Server " + Connection__Download$AgdaModeVscode.DownloadArtifact.versionLabel(alsVersion$1) + " WASM",
          undefined
        ];
}

function entriesToItemData(entries, pickedPath, downloadItems, downloadHeaderOpt) {
  var downloadHeader = downloadHeaderOpt !== undefined ? downloadHeaderOpt : "Download (channel: DevALS)";
  var hasCandidates = entries.length > 0;
  var candidateItems;
  if (pickedPath !== undefined) {
    var pickedCandidate = Connection__Candidate$AgdaModeVscode.make(pickedPath);
    var matched = {
      contents: false
    };
    candidateItems = entries.map(function (param) {
          var path = param[0];
          var isSelected = !matched.contents && Connection__Candidate$AgdaModeVscode.equal(pickedCandidate, Connection__Candidate$AgdaModeVscode.make(path));
          if (isSelected) {
            matched.contents = true;
          }
          return {
                  TAG: "Candidate",
                  _0: path,
                  _1: param[1],
                  _2: param[2],
                  _3: isSelected
                };
        });
  } else {
    candidateItems = entries.map(function (param) {
          return {
                  TAG: "Candidate",
                  _0: param[0],
                  _1: param[1],
                  _2: param[2],
                  _3: false
                };
        });
  }
  var candidateSection = hasCandidates ? [{
          TAG: "Separator",
          _0: "Candidates"
        }].concat(candidateItems) : [];
  var downloadSectionItems = downloadItems.map(function (param) {
        return {
                TAG: "DownloadAction",
                _0: param[0],
                _1: param[1],
                _2: param[2]
              };
      });
  var channelItems = ["SelectOtherChannels"];
  var downloadSection = [{
        TAG: "Separator",
        _0: downloadHeader
      }].concat(downloadSectionItems.concat(channelItems.concat(["DeleteDownloads"])));
  return candidateSection.concat(downloadSection);
}

var ItemData = {
  toString: toString,
  shouldCandidateHaveIcon: shouldCandidateHaveIcon,
  getCandidateDisplayInfo: getCandidateDisplayInfo,
  entriesToItemData: entriesToItemData
};

function createQuickPickItem(label, description, detail, data) {
  if (description !== undefined) {
    if (detail !== undefined) {
      return {
              label: label,
              description: description,
              detail: detail,
              data: data
            };
    } else {
      return {
              label: label,
              description: description,
              data: data
            };
    }
  } else if (detail !== undefined) {
    return {
            label: label,
            detail: detail,
            data: data
          };
  } else {
    return {
            label: label,
            data: data
          };
  }
}

function fromItemData(itemData, extensionUri) {
  var match;
  if (typeof itemData !== "object") {
    switch (itemData) {
      case "SelectOtherChannels" :
          match = [
            selectOtherChannels,
            undefined,
            undefined
          ];
          break;
      case "DeleteDownloads" :
          match = [
            deleteDownloads,
            "Delete all downloaded files and clear cached release metadata",
            undefined
          ];
          break;
      case "NoInstallations" :
          match = [
            "$(info) No installations found",
            "Try installing Agda or ALS first",
            "No executable paths detected"
          ];
          break;
      
    }
  } else {
    switch (itemData.TAG) {
      case "Candidate" :
          var match$1 = getCandidateDisplayInfo(itemData._0, itemData._2);
          var errorDescription = match$1[1];
          var description = itemData._3 ? (
              errorDescription !== undefined ? errorDescription : "selected"
            ) : (
              errorDescription !== undefined ? errorDescription : ""
            );
          match = [
            match$1[0],
            description,
            itemData._1
          ];
          break;
      case "DownloadAction" :
          var label;
          switch (itemData._2) {
            case "native" :
                label = downloadNativeALS;
                break;
            case "wasm" :
                label = downloadWasmALS;
                break;
            default:
              label = "$(cloud-download)  Download Agda Language Server";
          }
          var description$1 = itemData._0 ? downloadedAndInstalled : "";
          match = [
            label,
            description$1,
            itemData._1
          ];
          break;
      case "Separator" :
          match = [
            itemData._0,
            undefined,
            undefined
          ];
          break;
      
    }
  }
  var detail = match[2];
  var description$2 = match[1];
  var label$1 = match[0];
  if (typeof itemData !== "object") {
    return createQuickPickItem(label$1, description$2, detail, itemData);
  }
  switch (itemData.TAG) {
    case "Candidate" :
        var baseItem = createQuickPickItem(label$1, description$2, detail, itemData);
        if (!shouldCandidateHaveIcon(itemData._2.kind)) {
          return baseItem;
        }
        var newrecord = Caml_obj.obj_dup(baseItem);
        newrecord.iconPath = {
          dark: Vscode.Uri.joinPath(extensionUri, "asset/dark.png"),
          light: Vscode.Uri.joinPath(extensionUri, "asset/light.png")
        };
        return newrecord;
    case "Separator" :
        return {
                label: label$1,
                kind: -1,
                data: itemData
              };
    default:
      return createQuickPickItem(label$1, description$2, detail, itemData);
  }
}

function fromItemDataArray(itemDataArray, extensionUri) {
  return itemDataArray.map(function (itemData) {
              return fromItemData(itemData, extensionUri);
            });
}

var Item = {
  createQuickPickItem: createQuickPickItem,
  fromItemData: fromItemData,
  fromItemDataArray: fromItemDataArray
};

function make(log) {
  var quickPick = Vscode.window.createQuickPick();
  return {
          log: log,
          quickPick: quickPick,
          subscriptions: [],
          items: [],
          pendingHides: 0
        };
}

function setPlaceholder(self, placeholder) {
  self.quickPick.placeholder = placeholder;
}

function updateItems(self, items) {
  self.items = items;
  self.quickPick.items = items;
}

function show(self) {
  self.quickPick.show();
}

function onSelection(self, handler) {
  Util$AgdaModeVscode.Disposable.add(self.quickPick.onDidChangeSelection(handler), self.subscriptions);
}

function onHide(self, handler) {
  Util$AgdaModeVscode.Disposable.add(self.quickPick.onDidHide(handler), self.subscriptions);
}

function destroy(self) {
  self.quickPick.dispose();
  self.subscriptions.forEach(function (sub) {
        sub.dispose();
      });
  Chan$AgdaModeVscode.emit(self.log, {
        TAG: "SwitchVersionUI",
        _0: "Destroyed"
      });
}

var View = {
  make: make,
  setPlaceholder: setPlaceholder,
  updateItems: updateItems,
  show: show,
  onSelection: onSelection,
  onHide: onHide,
  destroy: destroy
};

function inferCandidateKind(raw) {
  var command = Connection__Candidate$AgdaModeVscode.make(raw);
  var match;
  if (command.TAG === "Command") {
    match = [
      command._0.toLowerCase(),
      undefined,
      undefined,
      false
    ];
  } else {
    var path = command._0.path;
    var parentPath = Nodepath.dirname(path);
    var releasePath = Nodepath.dirname(parentPath);
    var releasesPath = Nodepath.dirname(releasePath);
    match = [
      Nodepath.basename(path).toLowerCase(),
      Nodepath.basename(parentPath),
      Nodepath.basename(releasePath),
      Nodepath.basename(releasesPath) === "releases"
    ];
  }
  var isUnderManagedReleases = match[3];
  var releaseDirName = match[2];
  var parentDirName = match[1];
  var baseName = match[0];
  var isWasm = baseName.endsWith(".wasm");
  var cleanName = baseName.replace(".exe", "").replace(".cmd", "").replace(".bat", "").replace(".wasm", "");
  if (cleanName === "agda" || cleanName.startsWith("agda-")) {
    return {
            TAG: "Agda",
            _0: undefined
          };
  }
  if (!(cleanName === "als" || cleanName.startsWith("als-"))) {
    return "Unknown";
  }
  if (parentDirName === undefined) {
    return {
            TAG: "ALS",
            _0: isWasm ? "WASM" : "Native",
            _1: undefined
          };
  }
  var artifact = Connection__Download$AgdaModeVscode.DownloadArtifact.parseName(parentDirName);
  if (artifact === undefined) {
    if (isUnderManagedReleases) {
      return "Unknown";
    } else {
      return {
              TAG: "ALS",
              _0: isWasm ? "WASM" : "Native",
              _1: undefined
            };
    }
  }
  if (releaseDirName !== undefined && isUnderManagedReleases && releaseDirName !== artifact.releaseTag) {
    return "Unknown";
  }
  return {
          TAG: "ALS",
          _0: isWasm ? "WASM" : "Native",
          _1: [
            artifact.releaseTag,
            artifact.agdaVersion,
            undefined
          ]
        };
}

function make$1(state) {
  return {
          memento: state.memento,
          globalStorageUri: state.globalStorageUri
        };
}

async function getItemData(self, downloadItems, downloadHeaderOpt, platformDepsOpt) {
  var downloadHeader = downloadHeaderOpt !== undefined ? downloadHeaderOpt : "Download (channel: DevALS)";
  var platformDeps = platformDepsOpt !== undefined ? Caml_option.valFromOption(platformDepsOpt) : undefined;
  var storedPath = Memento$AgdaModeVscode.Module.PreferredCandidate.get(self.memento);
  var pickedPath;
  if (storedPath !== undefined) {
    pickedPath = storedPath;
  } else {
    var resource = Registry__Connection$AgdaModeVscode.status.contents;
    pickedPath = typeof resource !== "object" || resource.TAG !== "Active" ? undefined : Connection$AgdaModeVscode.getPath(resource._0.connection);
  }
  var detailForCandidate = async function (path, candidate) {
    if (candidate.TAG !== "Command") {
      return path;
    }
    var command = candidate._0;
    if (platformDeps === undefined) {
      return command;
    }
    var resolved = await Connection__Candidate$AgdaModeVscode.resolve(platformDeps.findCommand, candidate);
    if (resolved.TAG !== "Ok") {
      return command;
    }
    var resolved$1 = resolved._0;
    var resolvedPath = resolved$1.resource.scheme === "file" ? resolved$1.resource.fsPath : resolved$1.resource.toString();
    return command + " (" + resolvedPath + ")";
  };
  var candidateEntries = await Promise.all(Config$AgdaModeVscode.Connection.getAgdaPaths().toReversed().map(async function (path) {
            var candidate = Connection__Candidate$AgdaModeVscode.make(path);
            var detail = await detailForCandidate(path, candidate);
            var resolvedMetadata;
            if (candidate.TAG === "Command") {
              if (platformDeps !== undefined) {
                var resolved = await Connection__Candidate$AgdaModeVscode.resolve(platformDeps.findCommand, candidate);
                resolvedMetadata = resolved.TAG === "Ok" ? Memento$AgdaModeVscode.Module.ResolvedMetadata.get(self.memento, resolved._0) : undefined;
              } else {
                resolvedMetadata = undefined;
              }
            } else {
              var resolved_resource = candidate._0;
              var resolved$1 = {
                original: candidate,
                resource: resolved_resource
              };
              resolvedMetadata = Memento$AgdaModeVscode.Module.ResolvedMetadata.get(self.memento, resolved$1);
            }
            var entry = resolvedMetadata !== undefined ? resolvedMetadata : ({
                  kind: inferCandidateKind(path),
                  timestamp: new Date(),
                  error: undefined
                });
            return [
                    path,
                    detail,
                    entry
                  ];
          }));
  return entriesToItemData(candidateEntries, pickedPath, downloadItems, downloadHeader);
}

async function probeVersions(self, platformDeps) {
  var pathsToProbe = Config$AgdaModeVscode.Connection.getAgdaPaths();
  if (pathsToProbe.length === 0) {
    return false;
  }
  var probePromises = pathsToProbe.map(async function (path) {
        var candidate = Connection__Candidate$AgdaModeVscode.make(path);
        var error = await Connection$AgdaModeVscode.probeCandidate(platformDeps, candidate);
        if (error.TAG === "Ok") {
          var match = error._0;
          var agdaVersion = match[1];
          var resolved = match[0];
          switch (agdaVersion.TAG) {
            case "IsAgda" :
                await Memento$AgdaModeVscode.Module.ResolvedMetadata.setKind(self.memento, resolved, {
                      TAG: "Agda",
                      _0: agdaVersion._0
                    });
                return path;
            case "IsALS" :
                await Memento$AgdaModeVscode.Module.ResolvedMetadata.setKind(self.memento, resolved, {
                      TAG: "ALS",
                      _0: "Native",
                      _1: [
                        agdaVersion._0,
                        agdaVersion._1,
                        agdaVersion._2
                      ]
                    });
                return path;
            case "IsALSWASM" :
                var match$1 = Memento$AgdaModeVscode.Module.ResolvedMetadata.get(self.memento, resolved);
                var kind;
                var exit = 0;
                if (match$1 !== undefined) {
                  var match$2 = match$1.kind;
                  if (typeof match$2 !== "object" || match$2.TAG === "Agda" || match$2._0 === "Native") {
                    exit = 1;
                  } else {
                    var versionInfo = match$2._1;
                    if (versionInfo !== undefined) {
                      kind = {
                        TAG: "ALS",
                        _0: "WASM",
                        _1: versionInfo
                      };
                    } else {
                      exit = 1;
                    }
                  }
                } else {
                  exit = 1;
                }
                if (exit === 1) {
                  var match$3 = inferCandidateKind(path);
                  if (typeof match$3 !== "object" || match$3.TAG === "Agda" || match$3._0 === "Native") {
                    kind = {
                      TAG: "ALS",
                      _0: "WASM",
                      _1: undefined
                    };
                  } else {
                    var versionInfo$1 = match$3._1;
                    kind = versionInfo$1 !== undefined ? ({
                          TAG: "ALS",
                          _0: "WASM",
                          _1: versionInfo$1
                        }) : ({
                          TAG: "ALS",
                          _0: "WASM",
                          _1: undefined
                        });
                  }
                }
                await Memento$AgdaModeVscode.Module.ResolvedMetadata.setKind(self.memento, resolved, kind);
                return path;
            
          }
        } else {
          var match$4 = Connection__Error$AgdaModeVscode.toString({
                TAG: "Establish",
                _0: error._0
              });
          var resolved$1 = await Connection__Candidate$AgdaModeVscode.resolve(platformDeps.findCommand, candidate);
          if (resolved$1.TAG === "Ok") {
            await Memento$AgdaModeVscode.Module.ResolvedMetadata.setError(self.memento, resolved$1._0, match$4[1]);
          }
          return path;
        }
      });
  var updateResults = await Promise.all(probePromises);
  var updatedPaths = Core__Array.filterMap(updateResults, (function (x) {
          return x;
        }));
  return updatedPaths.length > 0;
}

var SwitchVersionManager = {
  inferCandidateKind: inferCandidateKind,
  make: make$1,
  getItemData: getItemData,
  probeVersions: probeVersions
};

async function switchAgdaVersion(state, selectedPath) {
  var resolvedFromConnectionPath = function (path) {
    return {
            original: Connection__Candidate$AgdaModeVscode.make(selectedPath),
            resource: Connection__URI$AgdaModeVscode.parse(path)._1
          };
  };
  await State__View$AgdaModeVscode.Panel.displayConnectionStatus(state, undefined);
  await State__View$AgdaModeVscode.Panel.display(state, {
        TAG: "Plain",
        _0: "Switching connection..."
      }, []);
  var conn = await Connection$AgdaModeVscode.fromPathsOrCommands(state.platformDeps, [[
          selectedPath,
          "FromConfig"
        ]]);
  if (conn.TAG === "Ok") {
    var conn$1 = conn._0;
    Util$AgdaModeVscode.log("[ debug ] switchAgdaVersion: connection succeeded", Connection$AgdaModeVscode.toString(conn$1));
    await Registry__Connection$AgdaModeVscode.shutdown();
    await Memento$AgdaModeVscode.Module.PreferredCandidate.set(state.memento, selectedPath);
    await State__View$AgdaModeVscode.Panel.displayConnectionStatus(state, conn$1);
    await State__View$AgdaModeVscode.Panel.display(state, {
          TAG: "Success",
          _0: "Switched to " + Connection$AgdaModeVscode.toString(conn$1)
        }, []);
    switch (conn$1.TAG) {
      case "Agda" :
          var resolved = resolvedFromConnectionPath(conn$1._1);
          await Memento$AgdaModeVscode.Module.ResolvedMetadata.setKind(state.memento, resolved, {
                TAG: "Agda",
                _0: conn$1._2
              });
          break;
      case "ALS" :
          var match = conn$1._2;
          var v = match.alsVersion;
          if (v !== undefined) {
            var resolved$1 = resolvedFromConnectionPath(conn$1._1);
            await Memento$AgdaModeVscode.Module.ResolvedMetadata.setKind(state.memento, resolved$1, {
                  TAG: "ALS",
                  _0: "Native",
                  _1: [
                    v,
                    match.agdaVersion,
                    match.lspOptions
                  ]
                });
          }
          break;
      case "ALSWASM" :
          var match$1 = conn$1._3;
          var v$1 = match$1.alsVersion;
          if (v$1 !== undefined) {
            var resolved$2 = resolvedFromConnectionPath(conn$1._2);
            await Memento$AgdaModeVscode.Module.ResolvedMetadata.setKind(state.memento, resolved$2, {
                  TAG: "ALS",
                  _0: "WASM",
                  _1: [
                    v$1,
                    match$1.agdaVersion,
                    match$1.lspOptions
                  ]
                });
          }
          break;
      
    }
    await Connection$AgdaModeVscode.destroy(conn$1, state.channels.log);
    return ;
  }
  var match$2 = Connection__Error$AgdaModeVscode.toString({
        TAG: "Establish",
        _0: conn._0
      });
  var errorBody = match$2[1];
  var errorHeader = match$2[0];
  Util$AgdaModeVscode.log("[ debug ] switchAgdaVersion: connection failed", errorHeader + " | " + errorBody);
  var header = {
    TAG: "Error",
    _0: "Failed to switch to a different installation: " + errorHeader
  };
  var body = [Item$AgdaModeVscode.plainText(errorBody)];
  return await State__View$AgdaModeVscode.Panel.display(state, header, body);
}

function variantToTag(variant) {
  if (variant === "Native") {
    return "native";
  } else {
    return "wasm";
  }
}

function variantFromTag(tag) {
  switch (tag) {
    case "native" :
        return "Native";
    case "wasm" :
        return "WASM";
    default:
      return ;
  }
}

function variantDisplayName(variant) {
  if (variant === "Native") {
    return "Agda Language Server (native)";
  } else {
    return "Agda Language Server (WASM)";
  }
}

function variantFileName(variant) {
  if (variant === "Native") {
    return "als";
  } else {
    return "als.wasm";
  }
}

function expectedPathForSource(globalStorageUri, source) {
  return Connection__Download$AgdaModeVscode.expectedPathForSource(globalStorageUri, source);
}

async function isDownloadedSource(globalStorageUri, source) {
  var match = await FS$AgdaModeVscode.stat(Connection__Download$AgdaModeVscode.expectedUriForSource(globalStorageUri, source));
  if (match.TAG === "Ok") {
    return true;
  } else {
    return false;
  }
}

async function suppressManagedVariants(globalStorageUri, configPaths, downloadItems) {
  var shouldKeep = async function (item) {
    var source = item.source;
    if (source === undefined) {
      return true;
    }
    var expectedPath = Connection__Download$AgdaModeVscode.expectedPathForSource(globalStorageUri, source);
    var expectedCandidate = Connection__Candidate$AgdaModeVscode.make(expectedPath);
    var inConfig = configPaths.some(function (configPath) {
          return Connection__Candidate$AgdaModeVscode.equal(Connection__Candidate$AgdaModeVscode.make(configPath), expectedCandidate);
        });
    if (inConfig) {
      return !Core__Result.isOk(await FS$AgdaModeVscode.stat(Connection__Download$AgdaModeVscode.expectedUriForSource(globalStorageUri, source)));
    } else {
      return true;
    }
  };
  var keeps = await Promise.all(downloadItems.map(shouldKeep));
  return downloadItems.filter(function (param, i) {
              return Belt_Array.getExn(keeps, i);
            });
}

async function makeDownloadItem(state, variant, source) {
  var downloaded = await isDownloadedSource(state.globalStorageUri, source);
  return {
          downloaded: downloaded,
          versionString: Connection__Download$AgdaModeVscode.Source.toVersionString(source),
          variantTag: variantToTag(variant),
          source: source
        };
}

function unavailableSourceItem(variant) {
  return {
          downloaded: false,
          versionString: downloadUnavailable,
          variantTag: variantToTag(variant),
          source: undefined
        };
}

function unavailableItem(variant) {
  return [
          false,
          downloadUnavailable,
          variantToTag(variant)
        ];
}

function channelToLabel(channel) {
  if (channel === "LatestALS") {
    return "LatestALS";
  } else {
    return "DevALS";
  }
}

function channelFromLabel(label) {
  switch (label) {
    case "Development" :
        return "DevALS";
    case "Latest" :
        return "LatestALS";
    default:
      return ;
  }
}

function channelPickerItem(channel, selectedChannel) {
  var description = channel === selectedChannel ? "selected" : "";
  if (channel === "LatestALS") {
    return {
            label: "Latest",
            description: description,
            detail: "Tracks the latest stable release"
          };
  } else {
    return {
            label: "Development",
            description: description,
            detail: "Tracks the latest commit of the master branch"
          };
  }
}

function channelPickerItems(selectedChannel) {
  return [
            "LatestALS",
            "DevALS"
          ].map(function (ch) {
              return channelPickerItem(ch, selectedChannel);
            });
}

async function getAvailableChannels(_platformDeps) {
  return [
          "DevALS",
          "LatestALS"
        ];
}

async function getPlaceholderDownloadItems(platformDeps) {
  var match = await platformDeps.determinePlatform();
  if (match.TAG === "Ok" && match._0 === "Web") {
    return [[
              false,
              checkingAvailability,
              "wasm"
            ]];
  }
  return [
          [
            false,
            checkingAvailability,
            "native"
          ],
          [
            false,
            checkingAvailability,
            "wasm"
          ]
        ];
}

async function getAllAvailableDownloads(state, platformDeps, channelOpt) {
  var channel = channelOpt !== undefined ? channelOpt : "DevALS";
  var platform = await platformDeps.determinePlatform();
  var allItems;
  if (platform.TAG === "Ok") {
    var platform$1 = platform._0;
    if (platform$1 === "Web") {
      var resolver = platformDeps.resolveDownloadChannel(channel, true);
      var match = await resolver(state.memento, state.globalStorageUri, "Web");
      if (match.TAG === "Ok") {
        var match$1 = match._0;
        if (match$1.TAG === "FromGitHub") {
          var descriptor = match$1._1;
          var release = descriptor.release;
          var makeSource = function (asset) {
            return {
                    TAG: "FromGitHub",
                    _0: channel,
                    _1: {
                      release: release,
                      asset: asset,
                      saveAsFileName: descriptor.saveAsFileName
                    }
                  };
          };
          var wasmAssets;
          wasmAssets = channel === "LatestALS" ? release.assets.filter(function (asset) {
                  if (!asset.name.endsWith(".wasm")) {
                    return false;
                  }
                  var artifact = Connection__Download$AgdaModeVscode.DownloadArtifact.parseName(asset.name);
                  if (artifact !== undefined) {
                    return artifact.platform === "Wasm";
                  } else {
                    return false;
                  }
                }) : Connection__DevALS$AgdaModeVscode.allWasmAssets(release);
          allItems = wasmAssets.length === 0 ? [unavailableSourceItem("WASM")] : await Promise.all(wasmAssets.map(async function (asset) {
                      return await makeDownloadItem(state, "WASM", makeSource(asset));
                    }));
        } else {
          allItems = [unavailableSourceItem("WASM")];
        }
      } else {
        allItems = [unavailableSourceItem("WASM")];
      }
    } else {
      var resolver$1 = platformDeps.resolveDownloadChannel(channel, true);
      var match$2 = await resolver$1(state.memento, state.globalStorageUri, platform$1);
      if (match$2.TAG === "Ok") {
        var match$3 = match$2._0;
        if (match$3.TAG === "FromGitHub") {
          var descriptor$1 = match$3._1;
          var release$1 = descriptor$1.release;
          var makeSource$1 = function (asset) {
            return {
                    TAG: "FromGitHub",
                    _0: channel,
                    _1: {
                      release: release$1,
                      asset: asset,
                      saveAsFileName: descriptor$1.saveAsFileName
                    }
                  };
          };
          if (channel === "LatestALS") {
            var artifact = Connection__Download$AgdaModeVscode.DownloadArtifact.parseName(descriptor$1.asset.name);
            var nativeItems = artifact !== undefined ? (
                Connection__Download$AgdaModeVscode.DownloadArtifact.Platform.matchesDownloadPlatform(artifact.platform, platform$1) ? [await makeDownloadItem(state, "Native", makeSource$1(descriptor$1.asset))] : [unavailableSourceItem("Native")]
              ) : [unavailableSourceItem("Native")];
            var wasmAssets$1 = release$1.assets.filter(function (asset) {
                  if (!asset.name.endsWith(".wasm")) {
                    return false;
                  }
                  var artifact = Connection__Download$AgdaModeVscode.DownloadArtifact.parseName(asset.name);
                  if (artifact !== undefined) {
                    return artifact.platform === "Wasm";
                  } else {
                    return false;
                  }
                });
            var wasmItems = wasmAssets$1.length === 0 ? [unavailableSourceItem("WASM")] : await Promise.all(wasmAssets$1.map(async function (asset) {
                        return await makeDownloadItem(state, "WASM", makeSource$1(asset));
                      }));
            allItems = nativeItems.concat(wasmItems);
          } else {
            var nativeAssets = Connection__DevALS$AgdaModeVscode.allNativeAssetsForPlatform(release$1, platform$1);
            var wasmAssets$2 = Connection__DevALS$AgdaModeVscode.allWasmAssets(release$1);
            var nativeItems$1 = await Promise.all(nativeAssets.map(async function (asset) {
                      return await makeDownloadItem(state, "Native", makeSource$1(asset));
                    }));
            var wasmItems$1 = await Promise.all(wasmAssets$2.map(async function (asset) {
                      return await makeDownloadItem(state, "WASM", makeSource$1(asset));
                    }));
            allItems = nativeItems$1.concat(wasmItems$1);
          }
        } else {
          allItems = [
            unavailableSourceItem("Native"),
            unavailableSourceItem("WASM")
          ];
        }
      } else {
        allItems = [
          unavailableSourceItem("Native"),
          unavailableSourceItem("WASM")
        ];
      }
    }
  } else {
    allItems = [
      unavailableSourceItem("Native"),
      unavailableSourceItem("WASM")
    ];
  }
  var filteredItems = await suppressManagedVariants(state.globalStorageUri, Config$AgdaModeVscode.Connection.getAgdaPaths(), allItems);
  return filteredItems.map(function (item) {
              return [
                      item.downloaded,
                      item.versionString,
                      item.variantTag
                    ];
            });
}

var Download = {
  variantToTag: variantToTag,
  variantFromTag: variantFromTag,
  variantDisplayName: variantDisplayName,
  variantFileName: variantFileName,
  expectedPathForSource: expectedPathForSource,
  isDownloadedSource: isDownloadedSource,
  suppressManagedVariants: suppressManagedVariants,
  makeDownloadItem: makeDownloadItem,
  unavailableSourceItem: unavailableSourceItem,
  unavailableItem: unavailableItem,
  channelToLabel: channelToLabel,
  channelFromLabel: channelFromLabel,
  channelPickerItem: channelPickerItem,
  channelPickerItems: channelPickerItems,
  getAvailableChannels: getAvailableChannels,
  getPlaceholderDownloadItems: getPlaceholderDownloadItems,
  getAllAvailableDownloads: getAllAvailableDownloads
};

async function handleDownload(state, platformDeps, variant, downloaded, versionString, channelOpt, refreshUIOpt) {
  var channel = channelOpt !== undefined ? channelOpt : "DevALS";
  var refreshUI = refreshUIOpt !== undefined ? Caml_option.valFromOption(refreshUIOpt) : undefined;
  Chan$AgdaModeVscode.emit(state.channels.log, {
        TAG: "SwitchVersionUI",
        _0: {
          TAG: "SelectedDownloadAction",
          _0: downloaded,
          _1: versionString
        }
      });
  if (downloaded) {
    var releasesUri = Vscode.Uri.joinPath(state.globalStorageUri, "releases");
    var found = {
      contents: undefined
    };
    var tagEntries = await FS$AgdaModeVscode.readDirectory(releasesUri);
    if (tagEntries.TAG === "Ok") {
      await Promise.all(tagEntries._0.map(async function (param) {
                var tagDirName = param[0];
                var tagUri = Vscode.Uri.joinPath(releasesUri, tagDirName);
                var artifactEntries = await FS$AgdaModeVscode.readDirectory(tagUri);
                if (artifactEntries.TAG !== "Ok") {
                  return ;
                }
                artifactEntries._0.forEach(function (param) {
                      var artifactDirName = param[0];
                      if (!Core__Option.isNone(found.contents)) {
                        return ;
                      }
                      var artifact = Connection__Download$AgdaModeVscode.DownloadArtifact.parseName(artifactDirName);
                      if (artifact === undefined) {
                        return ;
                      }
                      var isWasm = Connection__Download$AgdaModeVscode.DownloadArtifact.Platform.isWasm(artifact.platform);
                      var variantMatches;
                      variantMatches = variant === "Native" ? !isWasm : isWasm;
                      var channelTagMatches;
                      channelTagMatches = channel === "LatestALS" ? artifact.releaseTag !== "dev" : artifact.releaseTag === "dev";
                      if (!(variantMatches && channelTagMatches)) {
                        return ;
                      }
                      var vs;
                      vs = channel === "LatestALS" ? "Agda v" + artifact.agdaVersion + " Language Server " + Connection__Download$AgdaModeVscode.DownloadArtifact.versionLabel(artifact.releaseTag) : "Agda v" + artifact.agdaVersion + " Language Server (dev build)";
                      if (vs !== versionString) {
                        return ;
                      }
                      var execName = Connection__Download$AgdaModeVscode.DownloadArtifact.executableName(artifact);
                      var execUri = Vscode.Uri.joinPath(releasesUri, tagDirName, artifactDirName, execName);
                      found.contents = Caml_option.some(execUri);
                    });
              }));
    }
    var execUri = found.contents;
    if (execUri !== undefined) {
      var execUri$1 = Caml_option.valFromOption(execUri);
      var match = await FS$AgdaModeVscode.stat(execUri$1);
      if (match.TAG === "Ok") {
        var downloadedPath = Connection__Download$AgdaModeVscode.uriToPath(execUri$1);
        await Config$AgdaModeVscode.Connection.addAgdaPath(state.channels.log, downloadedPath);
        Vscode.window.showInformationMessage(versionString + " is already downloaded");
      }
      
    }
    
  } else {
    var onTrace = function ($$event) {
      Chan$AgdaModeVscode.emit(state.channels.log, {
            TAG: "DownloadTrace",
            _0: $$event
          });
    };
    var platform = await platformDeps.determinePlatform();
    var downloadResult;
    if (platform.TAG === "Ok") {
      var platform$1 = platform._0;
      var resolver = platformDeps.resolveDownloadChannel(channel, true);
      var e = await resolver(state.memento, state.globalStorageUri, platform$1);
      if (e.TAG === "Ok") {
        var source = e._0;
        if (source.TAG === "FromGitHub") {
          var descriptor = source._1;
          var release = descriptor.release;
          var assets;
          assets = variant === "Native" ? Connection__DevALS$AgdaModeVscode.allNativeAssetsForPlatform(release, platform$1) : Connection__DevALS$AgdaModeVscode.allWasmAssets(release);
          var matchingSource = Core__Array.reduce(assets, undefined, (function (found, asset) {
                  if (found !== undefined) {
                    return found;
                  }
                  var src_1 = {
                    release: release,
                    asset: asset,
                    saveAsFileName: descriptor.saveAsFileName
                  };
                  var src = {
                    TAG: "FromGitHub",
                    _0: channel,
                    _1: src_1
                  };
                  if (Connection__Download$AgdaModeVscode.Source.toVersionString(src) === versionString) {
                    return src;
                  }
                  
                }));
          downloadResult = matchingSource !== undefined ? await platformDeps.download(state.globalStorageUri, matchingSource, onTrace) : ({
                TAG: "Error",
                _0: "CannotFindCompatibleALSRelease"
              });
        } else {
          downloadResult = await platformDeps.download(state.globalStorageUri, source, onTrace);
        }
      } else {
        downloadResult = {
          TAG: "Error",
          _0: e._0
        };
      }
    } else {
      downloadResult = {
        TAG: "Error",
        _0: "CannotFindCompatibleALSRelease"
      };
    }
    if (downloadResult.TAG === "Ok") {
      await Config$AgdaModeVscode.Connection.addAgdaPath(state.channels.log, downloadResult._0);
      if (refreshUI !== undefined) {
        await refreshUI();
      }
      Vscode.window.showInformationMessage(versionString + " successfully downloaded");
    } else {
      Vscode.window.showErrorMessage(Connection__Download$AgdaModeVscode.$$Error.toString(downloadResult._0));
    }
  }
  return Chan$AgdaModeVscode.emit(state.channels.log, {
              TAG: "SwitchVersionUI",
              _0: "SelectionCompleted"
            });
}

async function handleChannelSwitch(state, platformDeps, _manager, selectedChannel, channel, updateUI) {
  selectedChannel.contents = channel;
  await Memento$AgdaModeVscode.Module.SelectedChannel.set(state.memento, Connection__Download$AgdaModeVscode.Channel.toString(channel));
  var newDownloadItems = await getAllAvailableDownloads(state, platformDeps, channel);
  return await updateUI(newDownloadItems);
}

function onSelection$1(state, platformDeps, manager, availableChannels, selectedChannel, updateUI, view, selectedItems, showChannelPickerOpt) {
  var showChannelPicker = showChannelPickerOpt !== undefined ? showChannelPickerOpt : (async function (items, placeHolder) {
        var result = await Vscode.window.showQuickPick(VSCode.PromiseOr.make({
                  TAG: "Others",
                  _0: items
                }), {
              canPickMany: false,
              placeHolder: placeHolder
            }, undefined);
        return Core__Option.map(result, (function (item) {
                      return item.label;
                    }));
      });
  ((async function () {
          var selectedItem = selectedItems[0];
          if (selectedItem !== undefined) {
            Util$AgdaModeVscode.log("[ debug ] user selected item: " + selectedItem.label, "");
            var match = selectedItem.data;
            if (typeof match !== "object") {
              switch (match) {
                case "SelectOtherChannels" :
                    var pickerItems = channelPickerItems(selectedChannel.contents);
                    view.pendingHides = view.pendingHides + 1 | 0;
                    try {
                      var channelResult = await showChannelPicker(pickerItems, "Select download channel");
                      if (channelResult !== undefined) {
                        var channel = channelFromLabel(channelResult);
                        if (channel !== undefined) {
                          await handleChannelSwitch(state, platformDeps, manager, selectedChannel, channel, updateUI);
                          view.quickPick.show();
                        } else {
                          view.quickPick.show();
                        }
                        return ;
                      }
                      view.quickPick.show();
                      return ;
                    }
                    catch (raw_exn){
                      var exn = Caml_js_exceptions.internalToOCamlException(raw_exn);
                      view.pendingHides = 0;
                      var msg = exn.RE_EXN_ID === Js_exn.$$Error ? Core__Option.getOr(exn._1.message, "unknown error") : "unknown error";
                      Util$AgdaModeVscode.log("[ debug ] channel picker failed", msg);
                      view.quickPick.show();
                      return ;
                    }
                case "DeleteDownloads" :
                    Util$AgdaModeVscode.log("[ debug ] user clicked: Delete Downloads", "");
                    destroy(view);
                    var cleanedDirectories = {
                      contents: []
                    };
                    var failedUris = {
                      contents: []
                    };
                    var deleteRoot = async function (uri) {
                      Util$AgdaModeVscode.log("[ debug ] delete downloads: deleting managed directory", uri.toString());
                      var error = await FS$AgdaModeVscode.deleteRecursive(uri);
                      if (error.TAG === "Ok") {
                        Util$AgdaModeVscode.log("[ debug ] delete downloads: deleted managed directory", uri.toString());
                        cleanedDirectories.contents = cleanedDirectories.contents.concat([uri]);
                        return ;
                      }
                      Util$AgdaModeVscode.log("[ debug ] delete downloads: deleteRecursive failed", uri.toString() + ": " + error._0);
                      var match = await FS$AgdaModeVscode.stat(uri);
                      if (match.TAG === "Ok") {
                        Util$AgdaModeVscode.log("[ debug ] delete downloads: directory still exists after failed delete", uri.toString());
                        failedUris.contents = failedUris.contents.concat([uri]);
                        return ;
                      }
                      Util$AgdaModeVscode.log("[ debug ] delete downloads: directory already missing, treating as cleaned", uri.toString());
                      cleanedDirectories.contents = cleanedDirectories.contents.concat([uri]);
                    };
                    var managedRoots = Connection__Download$AgdaModeVscode.managedDeleteRoots(state.globalStorageUri);
                    Util$AgdaModeVscode.log("[ debug ] delete downloads: globalStorageUri", state.globalStorageUri.toString());
                    Util$AgdaModeVscode.log("[ debug ] delete downloads: managed roots", managedRoots.map(function (root) {
                                return root.toString();
                              }).join(" | "));
                    var deleteAllRoots = async function (index) {
                      if (index < managedRoots.length) {
                        await deleteRoot(Belt_Array.getExn(managedRoots, index));
                        return await deleteAllRoots(index + 1 | 0);
                      }
                      
                    };
                    await deleteAllRoots(0);
                    var inFlightUri = Vscode.Uri.joinPath(state.globalStorageUri, "in-flight.download");
                    var inFlightZipUri = Vscode.Uri.joinPath(state.globalStorageUri, "in-flight.download.zip");
                    await FS$AgdaModeVscode.$$delete(inFlightUri);
                    await FS$AgdaModeVscode.$$delete(inFlightZipUri);
                    await Memento$AgdaModeVscode.Module.ALSReleaseCache.clear(state.memento, "agda", "agda-language-server");
                    await Memento$AgdaModeVscode.Module.ALSReleaseCache.clear(state.memento, "banacorn", "agda-language-server");
                    Util$AgdaModeVscode.log("[ debug ] delete downloads: cleaned directories", cleanedDirectories.contents.map(function (dir) {
                                return dir.toString();
                              }).join(" | "));
                    Util$AgdaModeVscode.log("[ debug ] delete downloads: failed uris", failedUris.contents.map(function (uri) {
                                return uri.toString();
                              }).join(", "));
                    await Memento$AgdaModeVscode.Module.ResolvedMetadata.clearUnderDirectories(state.memento, cleanedDirectories.contents);
                    var currentPaths = Config$AgdaModeVscode.Connection.getAgdaPaths();
                    Util$AgdaModeVscode.log("[ debug ] delete downloads: current connection.paths", currentPaths.join(" | "));
                    var filteredPaths = currentPaths.filter(function (candidate) {
                          var matchedDirectory = Core__Array.reduce(cleanedDirectories.contents, undefined, (function (found, dirUri) {
                                  if (found !== undefined) {
                                    return found;
                                  } else if (Connection__Candidate$AgdaModeVscode.isUnderDirectory(Connection__Candidate$AgdaModeVscode.make(candidate), dirUri)) {
                                    return Caml_option.some(dirUri);
                                  } else {
                                    return ;
                                  }
                                }));
                          if (matchedDirectory !== undefined) {
                            Util$AgdaModeVscode.log("[ debug ] delete downloads: removing connection.path candidate", candidate + " under " + Caml_option.valFromOption(matchedDirectory).toString());
                            return false;
                          } else {
                            Util$AgdaModeVscode.log("[ debug ] delete downloads: preserving connection.path candidate", candidate);
                            return true;
                          }
                        });
                    Util$AgdaModeVscode.log("[ debug ] delete downloads: filtered connection.paths", filteredPaths.join(" | "));
                    await Config$AgdaModeVscode.Connection.setAgdaPaths(state.channels.log, filteredPaths);
                    Chan$AgdaModeVscode.emit(state.channels.log, {
                          TAG: "SwitchVersionUI",
                          _0: "SelectionCompleted"
                        });
                    if (failedUris.contents.length === 0) {
                      Vscode.window.showInformationMessage("All downloads and cache deleted");
                    } else {
                      Vscode.window.showWarningMessage("Some downloads could not be deleted: " + failedUris.contents.map(function (uri) {
                                  return uri.toString();
                                }).join(", "));
                    }
                    return ;
                case "NoInstallations" :
                    return Chan$AgdaModeVscode.emit(state.channels.log, {
                                TAG: "SwitchVersionUI",
                                _0: "SelectionCompleted"
                              });
                
              }
            } else {
              switch (match.TAG) {
                case "Candidate" :
                    var selectedPath = match._0;
                    Util$AgdaModeVscode.log("[ debug ] user selected kind: " + selectedPath, "");
                    destroy(view);
                    var path = Memento$AgdaModeVscode.Module.PreferredCandidate.get(manager.memento);
                    var changed = path !== undefined ? !Connection__Candidate$AgdaModeVscode.equal(Connection__Candidate$AgdaModeVscode.make(selectedPath), Connection__Candidate$AgdaModeVscode.make(path)) : true;
                    if (changed) {
                      Chan$AgdaModeVscode.emit(state.channels.log, {
                            TAG: "SwitchVersionUI",
                            _0: {
                              TAG: "SelectedCandidate",
                              _0: selectedPath,
                              _1: match._2,
                              _2: true
                            }
                          });
                      await switchAgdaVersion(state, selectedPath);
                      return Chan$AgdaModeVscode.emit(state.channels.log, {
                                  TAG: "SwitchVersionUI",
                                  _0: "SelectionCompleted"
                                });
                    } else {
                      return Chan$AgdaModeVscode.emit(state.channels.log, {
                                  TAG: "SwitchVersionUI",
                                  _0: "SelectionCompleted"
                                });
                    }
                case "DownloadAction" :
                    var versionString = match._1;
                    if (versionString === checkingAvailability) {
                      return ;
                    }
                    Util$AgdaModeVscode.log("[ debug ] user clicked: download button = " + selectedItem.label, "");
                    destroy(view);
                    var selectedVariant = Core__Option.getOr(variantFromTag(match._2), "Native");
                    var currentChannel = selectedChannel.contents;
                    return await handleDownload(state, platformDeps, selectedVariant, match._0, versionString, currentChannel, undefined);
                case "Separator" :
                    return Chan$AgdaModeVscode.emit(state.channels.log, {
                                TAG: "SwitchVersionUI",
                                _0: "SelectionCompleted"
                              });
                
              }
            }
          } else {
            destroy(view);
            return Chan$AgdaModeVscode.emit(state.channels.log, {
                        TAG: "SwitchVersionUI",
                        _0: "SelectionCompleted"
                      });
          }
        })());
}

function onHide$1(view) {
  if (view.pendingHides > 0) {
    view.pendingHides = view.pendingHides - 1 | 0;
    return ;
  } else {
    Util$AgdaModeVscode.log("[ debug ] QuickPick hidden/cancelled by user", "");
    return destroy(view);
  }
}

async function backgroundUpdateFailureFallback(platformDeps, updateUI) {
  var match = await platformDeps.determinePlatform();
  var fallback;
  var exit = 0;
  if (match.TAG === "Ok" && match._0 === "Web") {
    fallback = [unavailableItem("WASM")];
  } else {
    exit = 1;
  }
  if (exit === 1) {
    fallback = [
      unavailableItem("Native"),
      unavailableItem("WASM")
    ];
  }
  try {
    return await updateUI(fallback);
  }
  catch (exn){
    return ;
  }
}

async function runBackgroundUpdate(downloadItemsPromise, platformDeps, manager, updateUI) {
  try {
    var downloadItems = await downloadItemsPromise;
    var phase3Changed = await probeVersions(manager, platformDeps);
    if (phase3Changed) {
      await updateUI(downloadItems);
    }
    if (!phase3Changed) {
      return await updateUI(downloadItems);
    } else {
      return ;
    }
  }
  catch (_exn){
    return await backgroundUpdateFailureFallback(platformDeps, updateUI);
  }
}

async function onActivate(state, platformDeps, downloadItemsPromiseOverrideOpt) {
  var downloadItemsPromiseOverride = downloadItemsPromiseOverrideOpt !== undefined ? Caml_option.valFromOption(downloadItemsPromiseOverrideOpt) : undefined;
  var manager = make$1(state);
  var view = make(state.channels.log);
  var availableChannels = {
    contents: ["DevALS"]
  };
  var label = Memento$AgdaModeVscode.Module.SelectedChannel.get(state.memento);
  var restoredChannel = label !== undefined ? Core__Option.getOr(Connection__Download$AgdaModeVscode.Channel.fromString(label), "DevALS") : "DevALS";
  var selectedChannel = {
    contents: restoredChannel
  };
  var updateUI = async function (downloadItems) {
    var downloadHeader = "Download (channel: " + channelToLabel(selectedChannel.contents) + ")";
    var itemData = await getItemData(manager, downloadItems, downloadHeader, Caml_option.some(platformDeps));
    var candidateItemDatas = Core__Array.filterMap(itemData, (function (item) {
            if (typeof item !== "object") {
              return ;
            }
            if (item.TAG !== "Candidate") {
              return ;
            }
            var entry = item._2;
            return [
                    item._0,
                    entry.kind,
                    entry.error,
                    item._3
                  ];
          }));
    Chan$AgdaModeVscode.emit(state.channels.log, {
          TAG: "SwitchVersionUI",
          _0: {
            TAG: "UpdatedCandidates",
            _0: candidateItemDatas
          }
        });
    Chan$AgdaModeVscode.emit(state.channels.log, {
          TAG: "SwitchVersionUI",
          _0: {
            TAG: "Others",
            _0: downloadHeader
          }
        });
    Chan$AgdaModeVscode.emit(state.channels.log, {
          TAG: "SwitchVersionUI",
          _0: {
            TAG: "UpdatedDownloadItems",
            _0: downloadItems
          }
        });
    var items = fromItemDataArray(itemData, state.extensionUri);
    return updateItems(view, items);
  };
  setPlaceholder(view, "Switch Agda Version");
  availableChannels.contents = await getAvailableChannels(platformDeps);
  if (!availableChannels.contents.includes(selectedChannel.contents)) {
    selectedChannel.contents = Core__Option.getOr(availableChannels.contents[0], "DevALS");
  }
  await updateUI(await getPlaceholderDownloadItems(platformDeps));
  view.quickPick.show();
  Chan$AgdaModeVscode.emit(state.channels.log, {
        TAG: "SwitchVersionUI",
        _0: {
          TAG: "Others",
          _0: "QuickPick shown"
        }
      });
  var downloadItemsPromise = downloadItemsPromiseOverride !== undefined ? Caml_option.valFromOption(downloadItemsPromiseOverride) : getAllAvailableDownloads(state, platformDeps, selectedChannel.contents);
  onSelection(view, (function (selectedItems) {
          onSelection$1(state, platformDeps, manager, availableChannels, selectedChannel, updateUI, view, selectedItems, undefined);
        }));
  onHide(view, (function () {
          onHide$1(view);
        }));
  runBackgroundUpdate(downloadItemsPromise, platformDeps, manager, updateUI);
}

var Handler = {
  handleDownload: handleDownload,
  handleChannelSwitch: handleChannelSwitch,
  onSelection: onSelection$1,
  onHide: onHide$1,
  backgroundUpdateFailureFallback: backgroundUpdateFailureFallback,
  runBackgroundUpdate: runBackgroundUpdate,
  onActivate: onActivate
};

var Candidate;

var ResolvedMetadata;

var activate = onActivate;

exports.Candidate = Candidate;
exports.ResolvedMetadata = ResolvedMetadata;
exports.Constants = Constants;
exports.ItemData = ItemData;
exports.Item = Item;
exports.View = View;
exports.SwitchVersionManager = SwitchVersionManager;
exports.switchAgdaVersion = switchAgdaVersion;
exports.Download = Download;
exports.Handler = Handler;
exports.activate = activate;
/* vscode Not a pure module */
