// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Caml_obj = require("rescript/lib/js/caml_obj.js");
var Caml_option = require("rescript/lib/js/caml_option.js");
var Core__Option = require("@rescript/core/lib/js/src/Core__Option.bs.js");
var Belt_SetString = require("rescript/lib/js/belt_SetString.js");
var Caml_js_exceptions = require("rescript/lib/js/caml_js_exceptions.js");
var Chan$AgdaModeVscode = require("./Util/Chan.bs.js");
var Util$AgdaModeVscode = require("./Util/Util.bs.js");
var Connection__Core$AgdaModeVscode = require("./Connection/Connection__Core.bs.js");
var Connection__Error$AgdaModeVscode = require("./Connection/Shared/Connection__Error.bs.js");

var Resource = {};

var Connecting = {};

var storage = ((() => {
    if (typeof require !== "function") {
      return null;
    }
    try {
      const { AsyncLocalStorage } = require("node:async_hooks");
      return new AsyncLocalStorage();
    } catch (_) {
      return null;
    }
  })());

function withOwner(id, task) {
  if (storage == null) {
    return task();
  } else {
    return storage.run(id, task);
  }
}

function isOwnerActive(id) {
  if (storage == null) {
    return false;
  } else {
    return Caml_obj.equal(Caml_option.nullable_to_opt(storage.getStore()), id);
  }
}

function hasAsyncOwnerContext() {
  return !(storage == null);
}

var ExecutionContext = {
  storage: storage,
  withOwner: withOwner,
  isOwnerActive: isOwnerActive,
  hasAsyncOwnerContext: hasAsyncOwnerContext
};

var status = {
  contents: "Empty"
};

var makeCount = {
  contents: 0
};

function inspect() {
  var resource = status.contents;
  if (typeof resource !== "object") {
    return {
            status: "Empty",
            userCount: 0,
            currentOwnerId: undefined,
            makeCount: makeCount.contents
          };
  }
  switch (resource.TAG) {
    case "Connecting" :
        return {
                status: "Connecting",
                userCount: 0,
                currentOwnerId: undefined,
                makeCount: makeCount.contents
              };
    case "Active" :
        var resource$1 = resource._0;
        return {
                status: "Active",
                userCount: Belt_SetString.size(resource$1.users),
                currentOwnerId: resource$1.currentOwnerId,
                makeCount: makeCount.contents
              };
    case "Closing" :
        return {
                status: "Closing",
                userCount: 0,
                currentOwnerId: undefined,
                makeCount: makeCount.contents
              };
    
  }
}

async function terminate(resource) {
  var connectionDestroyed = status.contents;
  if (typeof connectionDestroyed === "object" && connectionDestroyed.TAG === "Closing") {
    return await connectionDestroyed._0;
  }
  var match = Util$AgdaModeVscode.Promise_.pending();
  status.contents = {
    TAG: "Closing",
    _0: match[0]
  };
  var destroyError;
  try {
    await Connection__Core$AgdaModeVscode.destroy(resource.connection, Chan$AgdaModeVscode.make());
  }
  catch (raw_exn){
    var exn = Caml_js_exceptions.internalToOCamlException(raw_exn);
    destroyError = exn;
  }
  status.contents = "Empty";
  makeCount.contents = 0;
  match[1]();
  return Core__Option.forEach(destroyError, (function (exn) {
                throw exn;
              }));
}

async function acquire(id, make) {
  var connect = async function () {
    var match = Util$AgdaModeVscode.Promise_.pending();
    var connecting = {
      connectionEstablished: match[0],
      releasedUsers: undefined
    };
    status.contents = {
      TAG: "Connecting",
      _0: connecting
    };
    var result;
    try {
      result = await make();
    }
    catch (exn){
      result = {
        TAG: "Error",
        _0: {
          TAG: "Establish",
          _0: Connection__Error$AgdaModeVscode.Establish.mergeMany([])
        }
      };
    }
    if (result.TAG === "Ok") {
      makeCount.contents = makeCount.contents + 1 | 0;
      var users = Belt_SetString.has(connecting.releasedUsers, id) ? undefined : Belt_SetString.fromArray([id]);
      var resource = {
        connection: result._0,
        users: users,
        currentOwnerId: undefined,
        queue: Promise.resolve()
      };
      status.contents = {
        TAG: "Active",
        _0: resource
      };
    } else {
      status.contents = "Empty";
    }
    match[1](result);
    return result;
  };
  var wait = async function (connecting) {
    var result = await connecting.connectionEstablished;
    if (result.TAG !== "Ok") {
      return result;
    }
    var resource = status.contents;
    if (typeof resource !== "object") {
      return await acquire(id, make);
    }
    if (resource.TAG !== "Active") {
      return await acquire(id, make);
    }
    var resource$1 = resource._0;
    if (!Belt_SetString.has(connecting.releasedUsers, id)) {
      resource$1.users = Belt_SetString.add(resource$1.users, id);
    }
    return result;
  };
  var join = function (resource) {
    resource.users = Belt_SetString.add(resource.users, id);
    return Promise.resolve({
                TAG: "Ok",
                _0: resource.connection
              });
  };
  var connecting = status.contents;
  if (typeof connecting !== "object") {
    return await connect();
  }
  switch (connecting.TAG) {
    case "Connecting" :
        var connecting$1 = connecting._0;
        connecting$1.releasedUsers = Belt_SetString.remove(connecting$1.releasedUsers, id);
        return await wait(connecting$1);
    case "Active" :
        return await join(connecting._0);
    case "Closing" :
        await connecting._0;
        return await acquire(id, make);
    
  }
}

async function release(id) {
  var connecting = status.contents;
  if (typeof connecting !== "object") {
    return ;
  }
  switch (connecting.TAG) {
    case "Connecting" :
        var connecting$1 = connecting._0;
        connecting$1.releasedUsers = Belt_SetString.add(connecting$1.releasedUsers, id);
        return ;
    case "Active" :
        var resource = connecting._0;
        resource.users = Belt_SetString.remove(resource.users, id);
        if (Belt_SetString.isEmpty(resource.users)) {
          return await terminate(resource);
        } else {
          return ;
        }
    case "Closing" :
        return ;
    
  }
}

async function shutdown() {
  var resource = status.contents;
  if (typeof resource !== "object") {
    return ;
  }
  switch (resource.TAG) {
    case "Connecting" :
        await resource._0.connectionEstablished;
        return await shutdown();
    case "Active" :
        return await terminate(resource._0);
    case "Closing" :
        return await resource._0;
    
  }
}

async function execute(id, task) {
  var runSerialized = async function (resource) {
    var previousTaskDone = resource.queue;
    var match = Util$AgdaModeVscode.Promise_.pending();
    var resolve = match[1];
    resource.queue = match[0];
    var cleanup = function () {
      resource.currentOwnerId = undefined;
      resolve();
    };
    var staleConnectionError = function () {
      var err = {
        TAG: "Establish",
        _0: Connection__Error$AgdaModeVscode.Establish.mergeMany([])
      };
      return {
              TAG: "Error",
              _0: err
            };
    };
    var exit = 0;
    var val;
    try {
      val = await previousTaskDone;
      exit = 1;
    }
    catch (exn){
      cleanup();
      throw exn;
    }
    if (exit === 1) {
      var activeResource = status.contents;
      var exit$1 = 0;
      if (typeof activeResource !== "object" || !(activeResource.TAG === "Active" && activeResource._0 === resource)) {
        exit$1 = 2;
      } else {
        resource.currentOwnerId = id;
        var exit$2 = 0;
        var result;
        try {
          result = await withOwner(id, (function () {
                  return task(resource.connection);
                }));
          exit$2 = 3;
        }
        catch (exn$1){
          cleanup();
          throw exn$1;
        }
        if (exit$2 === 3) {
          cleanup();
          return result;
        }
        
      }
      if (exit$1 === 2) {
        cleanup();
        return staleConnectionError();
      }
      
    }
    
  };
  var resource = status.contents;
  if (typeof resource === "object" && resource.TAG === "Active") {
    var resource$1 = resource._0;
    var canBypassQueue = hasAsyncOwnerContext() ? Caml_obj.equal(resource$1.currentOwnerId, id) && isOwnerActive(id) : Caml_obj.equal(resource$1.currentOwnerId, id);
    if (canBypassQueue) {
      return await task(resource$1.connection);
    } else {
      return await runSerialized(resource$1);
    }
  }
  var err = {
    TAG: "Establish",
    _0: Connection__Error$AgdaModeVscode.Establish.mergeMany([])
  };
  return {
          TAG: "Error",
          _0: err
        };
}

function withOwnerContext(id, task) {
  return withOwner(id, task);
}

var Core;

exports.Core = Core;
exports.Resource = Resource;
exports.Connecting = Connecting;
exports.ExecutionContext = ExecutionContext;
exports.status = status;
exports.makeCount = makeCount;
exports.inspect = inspect;
exports.terminate = terminate;
exports.acquire = acquire;
exports.release = release;
exports.shutdown = shutdown;
exports.execute = execute;
exports.withOwnerContext = withOwnerContext;
/* storage Not a pure module */
