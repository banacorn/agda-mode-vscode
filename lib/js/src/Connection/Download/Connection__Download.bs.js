// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Js_exn = require("rescript/lib/js/js_exn.js");
var Vscode = require("vscode");
var Caml_obj = require("rescript/lib/js/caml_obj.js");
var Caml_option = require("rescript/lib/js/caml_option.js");
var Core__Array = require("@rescript/core/lib/js/src/Core__Array.bs.js");
var Core__Option = require("@rescript/core/lib/js/src/Core__Option.bs.js");
var Caml_splice_call = require("rescript/lib/js/caml_splice_call.js");
var FS$AgdaModeVscode = require("../../FS.bs.js");
var OS$AgdaModeVscode = require("../../Util/OS.bs.js");
var Caml_js_exceptions = require("rescript/lib/js/caml_js_exceptions.js");
var Util$AgdaModeVscode = require("../../Util/Util.bs.js");
var Connection__Download__Util$AgdaModeVscode = require("./Connection__Download__Util.bs.js");
var Connection__Download__Trace$AgdaModeVscode = require("./Connection__Download__Trace.bs.js");
var Connection__Download__Unzip$AgdaModeVscode = require("./Connection__Download__Unzip.bs.js");
var Connection__Download__GitHub$AgdaModeVscode = require("./Connection__Download__GitHub.bs.js");

function toDisplayString(channel) {
  if (channel === "LatestALS") {
    return "Latest Agda Language Server";
  } else {
    return "Development Agda Language Server";
  }
}

function toString(channel) {
  if (channel === "LatestALS") {
    return "latest";
  } else {
    return "dev";
  }
}

function fromString(s) {
  switch (s) {
    case "dev" :
        return "DevALS";
    case "latest" :
        return "LatestALS";
    default:
      return ;
  }
}

var all = [
  "DevALS",
  "LatestALS"
];

var Channel = {
  toDisplayString: toDisplayString,
  toString: toString,
  fromString: fromString,
  all: all
};

var SelectionVariant = {};

function parse(tag) {
  switch (tag) {
    case "macos-arm64" :
        return "MacOSArm64";
    case "macos-x64" :
        return "MacOSX64";
    case "ubuntu" :
        return "Ubuntu";
    case "wasm" :
        return "Wasm";
    case "windows" :
        return "Windows";
    default:
      return ;
  }
}

function toAssetTag(platform) {
  switch (platform) {
    case "Wasm" :
        return "wasm";
    case "Ubuntu" :
        return "ubuntu";
    case "MacOSArm64" :
        return "macos-arm64";
    case "MacOSX64" :
        return "macos-x64";
    case "Windows" :
        return "windows";
    
  }
}

function isWasm(platform) {
  if (platform === "Wasm") {
    return true;
  } else {
    return false;
  }
}

function matchesDownloadPlatform(artifactPlatform, downloadPlatform) {
  switch (artifactPlatform) {
    case "Wasm" :
        if (downloadPlatform === "Web") {
          return true;
        } else {
          return false;
        }
    case "Ubuntu" :
        if (downloadPlatform === "Ubuntu") {
          return true;
        } else {
          return false;
        }
    case "MacOSArm64" :
        if (downloadPlatform === "MacOS_Arm") {
          return true;
        } else {
          return false;
        }
    case "MacOSX64" :
        if (downloadPlatform === "MacOS_Intel") {
          return true;
        } else {
          return false;
        }
    case "Windows" :
        if (downloadPlatform === "Windows") {
          return true;
        } else {
          return false;
        }
    
  }
}

var Platform = {
  parse: parse,
  toAssetTag: toAssetTag,
  isWasm: isWasm,
  matchesDownloadPlatform: matchesDownloadPlatform
};

function extensionMatchesPlatform(platform, extension) {
  if (extension !== undefined) {
    if (platform === "Wasm") {
      if (extension === "wasm") {
        return true;
      } else {
        return false;
      }
    } else if (extension === "zip") {
      return true;
    } else {
      return false;
    }
  } else {
    return true;
  }
}

function parseName(raw) {
  var match = raw.match(/^als-([A-Za-z0-9.]+)-Agda-([0-9]+(?:\.[0-9]+)*)-(wasm|ubuntu|macos-arm64|macos-x64|windows)(?:\.(wasm|zip))?$/);
  if (match == null) {
    return ;
  }
  if (match.length !== 5) {
    return ;
  }
  var releaseTag = match[1];
  if (releaseTag === undefined) {
    return ;
  }
  var agdaVersion = match[2];
  if (agdaVersion === undefined) {
    return ;
  }
  var platformTag = match[3];
  if (platformTag === undefined) {
    return ;
  }
  var extension = match[4];
  var platform = parse(platformTag);
  if (platform !== undefined && extensionMatchesPlatform(platform, extension)) {
    return {
            releaseTag: releaseTag,
            agdaVersion: agdaVersion,
            platform: platform
          };
  }
  
}

function cacheName(artifact) {
  return "als-" + artifact.releaseTag + "-Agda-" + artifact.agdaVersion + "-" + toAssetTag(artifact.platform);
}

function directoryParts(artifact) {
  return [
          "releases",
          artifact.releaseTag,
          cacheName(artifact)
        ];
}

function executableName(artifact) {
  if (isWasm(artifact.platform)) {
    return "als.wasm";
  } else {
    return "als";
  }
}

function versionLabel(releaseTag) {
  if (releaseTag.startsWith("v")) {
    return releaseTag;
  } else {
    return "v" + releaseTag;
  }
}

function matchesChannel(channel, artifact) {
  if (channel === "LatestALS") {
    return artifact.releaseTag !== "dev";
  } else {
    return artifact.releaseTag === "dev";
  }
}

function toVersionString(channel, artifact) {
  if (channel === "LatestALS") {
    return "Agda v" + artifact.agdaVersion + " Language Server " + versionLabel(artifact.releaseTag);
  } else {
    return "Agda v" + artifact.agdaVersion + " Language Server (dev build)";
  }
}

function managedExecutableUri(globalStorageUri, artifact) {
  return Caml_splice_call.spliceApply(Vscode.Uri.joinPath, [
              globalStorageUri,
              directoryParts(artifact).concat([executableName(artifact)])
            ]);
}

var DownloadArtifact = {
  Platform: Platform,
  extensionMatchesPlatform: extensionMatchesPlatform,
  parseName: parseName,
  cacheName: cacheName,
  directoryParts: directoryParts,
  executableName: executableName,
  versionLabel: versionLabel,
  matchesChannel: matchesChannel,
  toVersionString: toVersionString,
  managedExecutableUri: managedExecutableUri
};

function toString$1(channel) {
  if (channel.TAG === "FromGitHub") {
    return toDisplayString(channel._0) + Connection__Download__GitHub$AgdaModeVscode.DownloadDescriptor.toString(channel._1);
  } else {
    return toDisplayString(channel._0) + " from " + channel._1;
  }
}

function toVersionString$1(channel) {
  if (channel.TAG !== "FromGitHub") {
    return toDisplayString(channel._0);
  }
  var descriptor = channel._1;
  var abstractChannel = channel._0;
  var artifact = parseName(descriptor.asset.name);
  if (artifact !== undefined) {
    return toVersionString(abstractChannel, artifact);
  }
  var agdaVersion;
  agdaVersion = abstractChannel === "LatestALS" ? descriptor.asset.name.replace(/als-Agda-/, "").replace(/-.*/, "") : descriptor.asset.name.replace(/als-dev-Agda-/, "").replace(/-.*/, "");
  if (abstractChannel !== "LatestALS") {
    return "Agda v" + agdaVersion + " Language Server (dev build)";
  }
  var alsVersion = Core__Option.getOr(Core__Array.last(descriptor.release.name.split(".")), descriptor.release.name);
  return "Agda v" + agdaVersion + " Language Server " + versionLabel(alsVersion);
}

var Source = {
  toString: toString$1,
  toVersionString: toVersionString$1
};

function toString$2(x) {
  if (typeof x !== "object") {
    if (x === "OptedNotToDownload") {
      return "Opted not to download the Agda Language Server";
    } else {
      return "Cannot find compatible Agda Language Server release for download. Prebuilts are only available for download on Ubuntu, Windows, and macOS (arm64, x64).\nPlease build from source if you are on a different platform. \nSee https://github.com/agda/agda-language-server for more information.";
    }
  }
  switch (x.TAG) {
    case "PlatformNotSupported" :
        var platform = x._0;
        return "The platform `" + platform.os + "/" + platform.dist + "` is not supported for downloading the Agda Language Server.\n";
    case "CannotFetchALSReleases" :
        return "Cannot fetch releases of Agda Language Server: " + Connection__Download__GitHub$AgdaModeVscode.$$Error.toString(x._0);
    case "CannotDownloadALS" :
        return "Failed to download the Agda Language Server: " + Connection__Download__GitHub$AgdaModeVscode.$$Error.toString(x._0);
    case "CannotDownloadFromURL" :
        return "Failed to download from URL: " + Connection__Download__GitHub$AgdaModeVscode.$$Error.toString(x._0);
    
  }
}

var $$Error = {
  toString: toString$2
};

function uriToPath(uri) {
  if (uri.scheme === "file") {
    return uri.fsPath;
  } else {
    return uri.toString();
  }
}

function sortDirectoryEntries(entries) {
  return entries.toSorted(function (param, param$1) {
              var b = param$1[0];
              var a = param[0];
              if (Caml_obj.greaterthan(a, b)) {
                return -1;
              } else if (Caml_obj.lessthan(a, b)) {
                return 1;
              } else {
                return 0;
              }
            });
}

function managedDeleteRoots(globalStorageUri) {
  return [Vscode.Uri.joinPath(globalStorageUri, "releases")];
}

function downloadDestinationForDescriptor(descriptor) {
  var artifact = parseName(descriptor.asset.name);
  if (artifact !== undefined) {
    return {
            directoryParts: directoryParts(artifact),
            executableName: executableName(artifact),
            isWasm: isWasm(artifact.platform)
          };
  } else {
    return Connection__Download__GitHub$AgdaModeVscode.downloadDestination(descriptor);
  }
}

function expectedUriForSource(globalStorageUri, source) {
  var destinationParts;
  if (source.TAG === "FromGitHub") {
    var destination = downloadDestinationForDescriptor(source._1);
    destinationParts = destination.directoryParts.concat([destination.executableName]);
  } else {
    var executableName = source._1.endsWith(".wasm") ? "als.wasm" : "als";
    destinationParts = [
      source._2,
      executableName
    ];
  }
  return Caml_splice_call.spliceApply(Vscode.Uri.joinPath, [
              globalStorageUri,
              destinationParts
            ]);
}

function expectedPathForSource(globalStorageUri, source) {
  return uriToPath(expectedUriForSource(globalStorageUri, source));
}

async function getReleaseManifestFromGitHub(memento, repo, useCacheOpt) {
  var useCache = useCacheOpt !== undefined ? useCacheOpt : true;
  var match = await Connection__Download__GitHub$AgdaModeVscode.ReleaseManifest.$$fetch(memento, repo, useCache);
  var error = match[0];
  if (error.TAG === "Ok") {
    return {
            TAG: "Ok",
            _0: error._0
          };
  } else {
    return {
            TAG: "Error",
            _0: {
              TAG: "CannotFetchALSReleases",
              _0: error._0
            }
          };
  }
}

async function downloadFromURL(globalStorageUri, url, saveAsFileName, displayName, traceOpt, $$fetch) {
  var trace = traceOpt !== undefined ? traceOpt : Connection__Download__Trace$AgdaModeVscode.noop;
  var reportProgress = await Connection__Download__Util$AgdaModeVscode.Progress.report(displayName, undefined);
  var destDirUri = Vscode.Uri.joinPath(globalStorageUri, saveAsFileName);
  var match = await FS$AgdaModeVscode.stat(destDirUri);
  if (match.TAG !== "Ok") {
    await FS$AgdaModeVscode.createDirectory(destDirUri);
  }
  var isWasm = url.endsWith(".wasm");
  var execFileName = isWasm ? "als.wasm" : "als";
  var execPathUri = Vscode.Uri.joinPath(destDirUri, execFileName);
  var match$1 = await FS$AgdaModeVscode.stat(execPathUri);
  if (match$1.TAG === "Ok") {
    Util$AgdaModeVscode.log("[ debug ] downloadFromURL: already cached, returning without chmod", uriToPath(execPathUri));
    return {
            TAG: "Ok",
            _0: uriToPath(execPathUri)
          };
  }
  Util$AgdaModeVscode.log("[ debug ] downloadFromURL: not cached, downloading from", url);
  try {
    var urlObj = new URL(url);
    var host = urlObj.host;
    var path = urlObj.pathname + urlObj.search;
    var tempFileUri = Vscode.Uri.joinPath(destDirUri, "download.tmp");
    var httpOptions = {
      host: host,
      path: path,
      headers: {
        "User-Agent": "agda/agda-mode-vscode",
        Accept: "*/*"
      }
    };
    var error = await Connection__Download__Util$AgdaModeVscode.asFile(httpOptions, tempFileUri, reportProgress, trace, $$fetch, undefined, undefined);
    if (error.TAG === "Ok") {
      if (isWasm) {
        await FS$AgdaModeVscode.rename(tempFileUri, execPathUri);
        return {
                TAG: "Ok",
                _0: uriToPath(execPathUri)
              };
      }
      var readResult = await FS$AgdaModeVscode.readFile(tempFileUri);
      var isPKHeader;
      if (readResult.TAG === "Ok") {
        var uint8Array = readResult._0;
        isPKHeader = uint8Array.length >= 4 ? Core__Option.getOr(uint8Array[0], 0) === 80 && Core__Option.getOr(uint8Array[1], 0) === 75 && Core__Option.getOr(uint8Array[2], 0) === 3 && Core__Option.getOr(uint8Array[3], 0) === 4 : false;
      } else {
        isPKHeader = false;
      }
      if (isPKHeader) {
        var zipFileUri = Vscode.Uri.joinPath(destDirUri, "download.zip");
        await FS$AgdaModeVscode.rename(tempFileUri, zipFileUri);
        await Connection__Download__Unzip$AgdaModeVscode.run(zipFileUri, destDirUri, undefined, undefined);
        await FS$AgdaModeVscode.$$delete(zipFileUri);
        if (OS$AgdaModeVscode.onUnix) {
          await Connection__Download__GitHub$AgdaModeVscode.chmodExecutable(execPathUri.fsPath);
        }
        return {
                TAG: "Ok",
                _0: execPathUri.fsPath
              };
      }
      await FS$AgdaModeVscode.$$delete(tempFileUri);
      var genericError = {
        message: "Downloaded file is not a ZIP file. The URL may require authentication or may not be a direct download link."
      };
      return {
              TAG: "Error",
              _0: {
                TAG: "CannotDownloadFromURL",
                _0: {
                  TAG: "CannotReadFile",
                  _0: genericError
                }
              }
            };
    }
    var error$1 = error._0;
    var convertedError;
    if (typeof error$1 !== "object") {
      convertedError = {
        TAG: "CannotReadFile",
        _0: {
          message: "No redirect location"
        }
      };
    } else {
      switch (error$1.TAG) {
        case "Timeout" :
            convertedError = {
              TAG: "CannotReadFile",
              _0: {
                message: "Timeout after " + String(error$1._0) + "ms"
              }
            };
            break;
        case "JsonParseError" :
            convertedError = {
              TAG: "CannotReadFile",
              _0: {
                message: "JSON parse error: " + error$1._0
              }
            };
            break;
        case "ServerResponseError" :
        case "CannotWriteFile" :
            convertedError = {
              TAG: "CannotReadFile",
              _0: error$1._0
            };
            break;
        
      }
    }
    return {
            TAG: "Error",
            _0: {
              TAG: "CannotDownloadFromURL",
              _0: convertedError
            }
          };
  }
  catch (raw_obj){
    var obj = Caml_js_exceptions.internalToOCamlException(raw_obj);
    if (obj.RE_EXN_ID === Js_exn.$$Error) {
      return {
              TAG: "Error",
              _0: {
                TAG: "CannotDownloadFromURL",
                _0: {
                  TAG: "CannotReadFile",
                  _0: obj._1
                }
              }
            };
    }
    var genericError$1 = {
      message: "Invalid URL"
    };
    return {
            TAG: "Error",
            _0: {
              TAG: "CannotDownloadFromURL",
              _0: {
                TAG: "CannotReadFile",
                _0: genericError$1
              }
            }
          };
  }
}

async function download(globalStorageUri, channel, traceOpt, fetchFileOpt, $$fetch) {
  var trace = traceOpt !== undefined ? traceOpt : Connection__Download__Trace$AgdaModeVscode.noop;
  var fetchFile = fetchFileOpt !== undefined ? Caml_option.valFromOption(fetchFileOpt) : undefined;
  if (channel.TAG !== "FromGitHub") {
    return await downloadFromURL(globalStorageUri, channel._1, channel._2, "Agda Language Server", trace, $$fetch);
  }
  var reportProgress = await Connection__Download__Util$AgdaModeVscode.Progress.report("Agda Language Server", undefined);
  var error = await Connection__Download__GitHub$AgdaModeVscode.download(channel._1, globalStorageUri, reportProgress, trace, undefined, undefined, Caml_option.some(fetchFile), undefined, undefined, downloadDestinationForDescriptor);
  if (error.TAG === "Ok") {
    return {
            TAG: "Ok",
            _0: expectedPathForSource(globalStorageUri, channel)
          };
  } else {
    return {
            TAG: "Error",
            _0: {
              TAG: "CannotDownloadALS",
              _0: error._0
            }
          };
  }
}

exports.Channel = Channel;
exports.SelectionVariant = SelectionVariant;
exports.DownloadArtifact = DownloadArtifact;
exports.Source = Source;
exports.$$Error = $$Error;
exports.uriToPath = uriToPath;
exports.sortDirectoryEntries = sortDirectoryEntries;
exports.managedDeleteRoots = managedDeleteRoots;
exports.downloadDestinationForDescriptor = downloadDestinationForDescriptor;
exports.expectedUriForSource = expectedUriForSource;
exports.expectedPathForSource = expectedPathForSource;
exports.getReleaseManifestFromGitHub = getReleaseManifestFromGitHub;
exports.downloadFromURL = downloadFromURL;
exports.download = download;
/* vscode Not a pure module */
