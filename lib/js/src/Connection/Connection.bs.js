// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Js_exn = require("rescript/lib/js/js_exn.js");
var Vscode = require("vscode");
var Nodepath = require("node:path");
var Caml_option = require("rescript/lib/js/caml_option.js");
var Core__Option = require("@rescript/core/lib/js/src/Core__Option.bs.js");
var FS$AgdaModeVscode = require("../FS.bs.js");
var Caml_js_exceptions = require("rescript/lib/js/caml_js_exceptions.js");
var Chan$AgdaModeVscode = require("../Util/Chan.bs.js");
var Util$AgdaModeVscode = require("../Util/Util.bs.js");
var Config$AgdaModeVscode = require("../Config.bs.js");
var Parser$AgdaModeVscode = require("../Parser/Parser.bs.js");
var Memento$AgdaModeVscode = require("../Memento.bs.js");
var Request$AgdaModeVscode = require("../Request.bs.js");
var WASMLoader$AgdaModeVscode = require("./Binding/WASMLoader.bs.js");
var Json_Encode$JsonCombinators = require("@glennsl/rescript-json-combinators/lib/js/src/Json_Encode.bs.js");
var Connection__URI$AgdaModeVscode = require("./Shared/Connection__URI.bs.js");
var Connection__Error$AgdaModeVscode = require("./Shared/Connection__Error.bs.js");
var Connection__Candidate$AgdaModeVscode = require("./Connection__Candidate.bs.js");
var Connection__Hardcoded$AgdaModeVscode = require("./Download/Connection__Hardcoded.bs.js");
var Connection__Endpoint__ALS$AgdaModeVscode = require("./Endpoint/Connection__Endpoint__ALS.bs.js");
var Connection__Process__Exec$AgdaModeVscode = require("./Shared/Connection__Process__Exec.bs.js");
var Connection__Endpoint__Agda$AgdaModeVscode = require("./Endpoint/Connection__Endpoint__Agda.bs.js");

function getFromConfig() {
  var param = {
    commandLineOptions: Config$AgdaModeVscode.Connection.getCommandLineOptions()
  };
  return {
          commandLineOptions: Json_Encode$JsonCombinators.array(function (prim) {
                  return prim;
                })(param.commandLineOptions)
        };
}

function toString(connection) {
  switch (connection.TAG) {
    case "Agda" :
        return "Agda v" + connection._2;
    case "ALS" :
        var match = connection._2;
        if (match !== undefined) {
          return "Agda v" + match[1] + " Language Server v" + match[0];
        } else {
          return "Agda Language Server of unknown version";
        }
    case "ALSWASM" :
        var match$1 = connection._3;
        if (match$1 !== undefined) {
          return "Agda v" + match$1[1] + " Language Server v" + match$1[0] + " (WASM)";
        } else {
          return "Agda Language Server of unknown version (WASM)";
        }
    
  }
}

function getPath(connection) {
  switch (connection.TAG) {
    case "Agda" :
    case "ALS" :
        return connection._1;
    case "ALSWASM" :
        return connection._2;
    
  }
}

async function destroy(connection, logChannel) {
  if (connection === undefined) {
    return {
            TAG: "Ok",
            _0: undefined
          };
  }
  Chan$AgdaModeVscode.emit(logChannel, {
        TAG: "Connection",
        _0: {
          TAG: "Disconnected",
          _0: getPath(connection)
        }
      });
  switch (connection.TAG) {
    case "Agda" :
        await Connection__Endpoint__Agda$AgdaModeVscode.destroy(connection._0);
        return {
                TAG: "Ok",
                _0: undefined
              };
    case "ALS" :
        var error = await Connection__Endpoint__ALS$AgdaModeVscode.destroy(connection._0);
        if (error.TAG === "Ok") {
          return {
                  TAG: "Ok",
                  _0: undefined
                };
        } else {
          return {
                  TAG: "Error",
                  _0: {
                    TAG: "CommWithALS",
                    _0: error._0
                  }
                };
        }
    case "ALSWASM" :
        return {
                TAG: "Ok",
                _0: undefined
              };
    
  }
}

async function checkForPrebuiltDataDirectory(executablePath) {
  var prebuildDataDirPath = Nodepath.join(executablePath, "..", "data");
  var prebuildDataDirURI = Vscode.Uri.file(prebuildDataDirPath);
  var match = await FS$AgdaModeVscode.stat(prebuildDataDirURI);
  if (match.TAG === "Ok") {
    return Nodepath.join(executablePath, "..", "data");
  }
  
}

function resolvedFromRawResource(raw) {
  var match = Connection__URI$AgdaModeVscode.parse(raw);
  var resource = match._1;
  return {
          original: {
            TAG: "Resource",
            _0: resource
          },
          resource: resource
        };
}

async function probeResolved(resolved) {
  var resource = resolved.resource;
  var resourceString = resource.toString();
  if (resourceString.endsWith(".wasm")) {
    var pathKey = resource.scheme === "file" ? resource.fsPath : resourceString;
    return {
            TAG: "Ok",
            _0: [
              pathKey,
              {
                TAG: "IsALSWASM",
                _0: resource
              }
            ]
          };
  }
  var fsPath = resource.fsPath;
  var result = await Connection__Process__Exec$AgdaModeVscode.run(fsPath, ["--version"], 3000, undefined);
  if (result.TAG !== "Ok") {
    return {
            TAG: "Error",
            _0: {
              TAG: "CannotDetermineAgdaOrALS",
              _0: result._0
            }
          };
  }
  var output = result._0;
  var match = output.match(/Agda version (.*)/);
  if (!(match == null) && match.length === 2) {
    var version = match[1];
    if (version !== undefined) {
      return {
              TAG: "Ok",
              _0: [
                fsPath,
                {
                  TAG: "IsAgda",
                  _0: version
                }
              ]
            };
    }
    
  }
  var match$1 = output.match(/Agda v(.*) Language Server v(.*)/);
  if (match$1 == null) {
    return {
            TAG: "Error",
            _0: {
              TAG: "NotAgdaOrALS",
              _0: output
            }
          };
  }
  if (match$1.length !== 3) {
    return {
            TAG: "Error",
            _0: {
              TAG: "NotAgdaOrALS",
              _0: output
            }
          };
  }
  var agdaVersion = match$1[1];
  if (agdaVersion === undefined) {
    return {
            TAG: "Error",
            _0: {
              TAG: "NotAgdaOrALS",
              _0: output
            }
          };
  }
  var alsVersion = match$1[2];
  if (alsVersion === undefined) {
    return {
            TAG: "Error",
            _0: {
              TAG: "NotAgdaOrALS",
              _0: output
            }
          };
  }
  var assetPath = await checkForPrebuiltDataDirectory(fsPath);
  var lspOptions;
  if (assetPath !== undefined) {
    var env = Object.fromEntries([[
            "Agda_datadir",
            assetPath
          ]]);
    lspOptions = {
      env: env
    };
  } else {
    lspOptions = undefined;
  }
  return {
          TAG: "Ok",
          _0: [
            fsPath,
            {
              TAG: "IsALS",
              _0: alsVersion,
              _1: agdaVersion,
              _2: lspOptions
            }
          ]
        };
}

async function probeFilepath(path) {
  return await probeResolved(resolvedFromRawResource(path));
}

async function probeCandidate(platformDeps, candidate) {
  var resolved = await Connection__Candidate$AgdaModeVscode.resolve(platformDeps.findCommand, candidate);
  if (resolved.TAG === "Ok") {
    var resolved$1 = resolved._0;
    var command = resolved$1.original;
    var source;
    source = command.TAG === "Command" ? ({
          TAG: "FromCommandLookup",
          _0: command._0
        }) : "FromConfig";
    var error = await probeResolved(resolved$1);
    if (error.TAG === "Ok") {
      return {
              TAG: "Ok",
              _0: [
                resolved$1,
                error._0[1]
              ]
            };
    } else {
      return {
              TAG: "Error",
              _0: Connection__Error$AgdaModeVscode.Establish.fromProbeError(Connection__Candidate$AgdaModeVscode.Resolved.toString(resolved$1), error._0, source)
            };
    }
  }
  if (candidate.TAG === "Command") {
    return {
            TAG: "Error",
            _0: Connection__Error$AgdaModeVscode.Establish.fromCommandError(candidate._0, resolved._0)
          };
  }
  throw {
        RE_EXN_ID: "Failure",
        _1: "Connection__Candidate.resolve returned Error for Resource candidate",
        Error: new Error()
      };
}

async function makeResolved(resolved, source, pathForErrorsOpt) {
  var pathForErrors = pathForErrorsOpt !== undefined ? pathForErrorsOpt : resolved.resource.toString();
  var error = await probeResolved(resolved);
  if (error.TAG !== "Ok") {
    return {
            TAG: "Error",
            _0: Connection__Error$AgdaModeVscode.Establish.fromProbeError(pathForErrors, error._0, source)
          };
  }
  var match = error._0;
  var agdaVersion = match[1];
  var path = match[0];
  switch (agdaVersion.TAG) {
    case "IsAgda" :
        var agdaVersion$1 = agdaVersion._0;
        var connection = await Connection__Endpoint__Agda$AgdaModeVscode.make(path, agdaVersion$1);
        return {
                TAG: "Ok",
                _0: {
                  TAG: "Agda",
                  _0: connection,
                  _1: path,
                  _2: agdaVersion$1
                }
              };
    case "IsALS" :
        var lspOptions = agdaVersion._2;
        var error$1 = await Connection__Endpoint__ALS$AgdaModeVscode.make({
              TAG: "ViaPipe",
              _0: path,
              _1: []
            }, lspOptions, getFromConfig());
        if (error$1.TAG === "Ok") {
          return {
                  TAG: "Ok",
                  _0: {
                    TAG: "ALS",
                    _0: error$1._0,
                    _1: path,
                    _2: [
                      agdaVersion._0,
                      agdaVersion._1,
                      lspOptions
                    ]
                  }
                };
        } else {
          return {
                  TAG: "Error",
                  _0: Connection__Error$AgdaModeVscode.Establish.fromProbeError(path, {
                        TAG: "CannotMakeConnectionWithALS",
                        _0: error$1._0
                      }, source)
                };
        }
    case "IsALSWASM" :
        var extension = Vscode.extensions.getExtension("qbane.als-wasm-loader");
        if (extension === undefined) {
          return {
                  TAG: "Error",
                  _0: Connection__Error$AgdaModeVscode.Establish.fromProbeError(path, {
                        TAG: "CannotMakeConnectionWithALSWASMYet",
                        _0: "Extension 'qbane.als-wasm-loader' not found. Either it is not installed or is disabled."
                      }, source)
                };
        }
        var extension$1 = Caml_option.valFromOption(extension);
        try {
          if (!extension$1.isActive) {
            await extension$1.activate();
          }
          var error$2 = await FS$AgdaModeVscode.readFile(agdaVersion._0);
          if (error$2.TAG !== "Ok") {
            return {
                    TAG: "Error",
                    _0: Connection__Error$AgdaModeVscode.Establish.fromProbeError(path, {
                          TAG: "CannotMakeConnectionWithALSWASMYet",
                          _0: "Failed to read WASM file: " + error$2._0
                        }, source)
                  };
          }
          var wasmLoader = await WASMLoader$AgdaModeVscode.make(extension$1, error$2._0);
          var isDesktop = (Vscode.env.uiKind === Vscode.UIKind.Desktop);
          var sabAvailable = (typeof SharedArrayBuffer !== 'undefined');
          var coi = (typeof crossOriginIsolated !== 'undefined' ? crossOriginIsolated : false);
          if (!isDesktop && (!sabAvailable || !coi)) {
            var reason = sabAvailable ? "The page is not crossOriginIsolated, which is required to enable SharedArrayBuffer." : "SharedArrayBuffer is not available in this browser environment.";
            return {
                    TAG: "Error",
                    _0: Connection__Error$AgdaModeVscode.Establish.fromProbeError(path, {
                          TAG: "CannotMakeConnectionWithALSWASMYet",
                          _0: reason + " Please serve the web extension with Cross-Origin Opener Policy (COOP) and Cross-Origin Embedder Policy (COEP) headers."
                        }, source)
                  };
          }
          var error$3 = await Connection__Endpoint__ALS$AgdaModeVscode.make({
                TAG: "ViaWASM",
                _0: wasmLoader
              }, undefined, getFromConfig());
          if (error$3.TAG !== "Ok") {
            return {
                    TAG: "Error",
                    _0: Connection__Error$AgdaModeVscode.Establish.fromProbeError(path, {
                          TAG: "CannotMakeConnectionWithALS",
                          _0: error$3._0
                        }, source)
                  };
          }
          var conn = error$3._0;
          var version = conn.alsVersion;
          var version$1 = version !== undefined ? [
              conn.agdaVersion,
              version,
              undefined
            ] : undefined;
          return {
                  TAG: "Ok",
                  _0: {
                    TAG: "ALSWASM",
                    _0: conn,
                    _1: wasmLoader,
                    _2: path,
                    _3: version$1
                  }
                };
        }
        catch (raw_error){
          var error$4 = Caml_js_exceptions.internalToOCamlException(raw_error);
          if (error$4.RE_EXN_ID === Js_exn.$$Error) {
            var errorMessage = Core__Option.getOr(error$4._1.message, "Unknown error");
            return {
                    TAG: "Error",
                    _0: Connection__Error$AgdaModeVscode.Establish.fromProbeError(path, {
                          TAG: "CannotMakeConnectionWithALSWASMYet",
                          _0: "Unexpected error: " + errorMessage
                        }, source)
                  };
          }
          throw error$4;
        }
    
  }
}

async function make(rawpath, source) {
  return await makeResolved(resolvedFromRawResource(rawpath), source, rawpath);
}

async function tryUntilSuccess(xs) {
  var loop = async function (remaining, errors) {
    var task = remaining[0];
    if (task === undefined) {
      return {
              TAG: "Error",
              _0: errors
            };
    }
    var success = await task();
    if (success.TAG === "Ok") {
      return {
              TAG: "Ok",
              _0: success._0
            };
    }
    var newErrors = errors.concat([success._0]);
    var nextTasks = remaining.slice(1);
    return await loop(nextTasks, newErrors);
  };
  return await loop(xs, []);
}

function isCommand(raw) {
  var match = Connection__Candidate$AgdaModeVscode.make(raw);
  if (match.TAG === "Command") {
    return true;
  } else {
    return false;
  }
}

async function tryResolved(resolved, source) {
  return await makeResolved(resolved, source, undefined);
}

async function tryCandidate(platformDeps, candidate, source) {
  var resolved = await Connection__Candidate$AgdaModeVscode.resolve(platformDeps.findCommand, candidate);
  if (resolved.TAG === "Ok") {
    var resolved$1 = resolved._0;
    var command = resolved$1.original;
    var resolvedSource;
    resolvedSource = command.TAG === "Command" ? ({
          TAG: "FromCommandLookup",
          _0: command._0
        }) : source;
    return await tryResolved(resolved$1, resolvedSource);
  }
  if (candidate.TAG === "Command") {
    return {
            TAG: "Error",
            _0: Connection__Error$AgdaModeVscode.Establish.fromCommandError(candidate._0, resolved._0)
          };
  }
  throw {
        RE_EXN_ID: "Failure",
        _1: "Connection__Candidate.resolve returned Error for Resource candidate",
        Error: new Error()
      };
}

async function fromPathsOrCommands(platformDeps, entries) {
  var tasks = entries.map(function (param) {
        var source = param[1];
        var candidate = Connection__Candidate$AgdaModeVscode.make(param[0]);
        return function () {
          return tryCandidate(platformDeps, candidate, source);
        };
      });
  var connection = await tryUntilSuccess(tasks);
  if (connection.TAG === "Ok") {
    return {
            TAG: "Ok",
            _0: connection._0
          };
  } else {
    return {
            TAG: "Error",
            _0: Connection__Error$AgdaModeVscode.Establish.mergeMany(connection._0)
          };
  }
}

async function fromDownloads(platformDeps, memento, globalStorageUri) {
  var platform = await platformDeps.determinePlatform();
  if (platform.TAG !== "Ok") {
    return {
            TAG: "Error",
            _0: Connection__Error$AgdaModeVscode.Establish.fromDownloadError({
                  TAG: "PlatformNotSupported",
                  _0: platform._0
                })
          };
  }
  var platform$1 = platform._0;
  var policy;
  if (platform$1 === "Web") {
    policy = "Yes";
  } else {
    var policy$1 = Config$AgdaModeVscode.Connection.DownloadPolicy.get();
    switch (policy$1) {
      case "Yes" :
      case "No" :
          policy = policy$1;
          break;
      case "Undecided" :
          policy = await platformDeps.askUserAboutDownloadPolicy();
          break;
      
    }
  }
  Util$AgdaModeVscode.log("[ debug ] fromDownloads: policy = " + Config$AgdaModeVscode.Connection.DownloadPolicy.toString(policy), "");
  switch (policy) {
    case "Yes" :
        await Config$AgdaModeVscode.Connection.DownloadPolicy.set("Yes");
        var channel;
        if (platform$1 === "Web") {
          channel = "Hardcoded";
        } else {
          var match = Memento$AgdaModeVscode.Module.SelectedChannel.get(memento);
          channel = match === "DevALS" ? "DevALS" : "Hardcoded";
        }
        var channelDirName;
        switch (channel) {
          case "LatestALS" :
              channelDirName = "latest-als";
              break;
          case "DevALS" :
              channelDirName = "dev-als";
              break;
          case "Hardcoded" :
              channelDirName = "hardcoded-als";
              break;
          
        }
        var tryWasmFallback = async function () {
          var path = await platformDeps.download(globalStorageUri, {
                TAG: "FromURL",
                _0: channel,
                _1: Connection__Hardcoded$AgdaModeVscode.wasmUrl,
                _2: channelDirName
              });
          if (path.TAG === "Ok") {
            return {
                    TAG: "Ok",
                    _0: path._0
                  };
          } else {
            return {
                    TAG: "Error",
                    _0: path._0
                  };
          }
        };
        var tryDownloadSource = async function (source) {
          var path = await platformDeps.download(globalStorageUri, source);
          if (path.TAG === "Ok") {
            return {
                    TAG: "Ok",
                    _0: path._0
                  };
          }
          var error = path._0;
          if (source.TAG === "FromGitHub") {
            if (source._1.asset.name.includes("wasm")) {
              return {
                      TAG: "Error",
                      _0: error
                    };
            } else {
              return await tryWasmFallback();
            }
          } else if (source._1.endsWith(".wasm")) {
            return {
                    TAG: "Error",
                    _0: error
                  };
          } else {
            return await tryWasmFallback();
          }
        };
        var path = await platformDeps.alreadyDownloaded(globalStorageUri, channel);
        var downloadResult;
        if (path !== undefined) {
          Util$AgdaModeVscode.log("[ debug ] fromDownloads: alreadyDownloaded = Some", path);
          downloadResult = {
            TAG: "Ok",
            _0: path
          };
        } else {
          Util$AgdaModeVscode.log("[ debug ] fromDownloads: alreadyDownloaded = None", "");
          var resolveError = await platformDeps.resolveDownloadChannel(channel, true)(memento, globalStorageUri, platform$1);
          if (resolveError.TAG === "Ok") {
            var error = await tryDownloadSource(resolveError._0);
            downloadResult = error.TAG === "Ok" ? ({
                  TAG: "Ok",
                  _0: error._0
                }) : ({
                  TAG: "Error",
                  _0: Connection__Error$AgdaModeVscode.Establish.fromDownloadError(error._0)
                });
          } else {
            var path$1 = await tryWasmFallback();
            downloadResult = path$1.TAG === "Ok" ? ({
                  TAG: "Ok",
                  _0: path$1._0
                }) : ({
                  TAG: "Error",
                  _0: Connection__Error$AgdaModeVscode.Establish.fromDownloadError(resolveError._0)
                });
          }
        }
        if (downloadResult.TAG !== "Ok") {
          return {
                  TAG: "Error",
                  _0: downloadResult._0
                };
        }
        var path$2 = downloadResult._0;
        var connection = await make(path$2, {
              TAG: "FromDownload",
              _0: channel
            });
        if (connection.TAG === "Ok") {
          return {
                  TAG: "Ok",
                  _0: connection._0
                };
        }
        var nativeError = connection._0;
        if (path$2.endsWith(".wasm")) {
          return {
                  TAG: "Error",
                  _0: {
                    probes: nativeError.probes,
                    commands: nativeError.commands,
                    download: "Succeeded"
                  }
                };
        }
        var wasmPath = await tryWasmFallback();
        if (wasmPath.TAG !== "Ok") {
          return {
                  TAG: "Error",
                  _0: {
                    probes: nativeError.probes,
                    commands: nativeError.commands,
                    download: "Succeeded"
                  }
                };
        }
        var connection$1 = await make(wasmPath._0, {
              TAG: "FromDownload",
              _0: channel
            });
        if (connection$1.TAG === "Ok") {
          return {
                  TAG: "Ok",
                  _0: connection$1._0
                };
        }
        var wasmError = connection$1._0;
        return {
                TAG: "Error",
                _0: Connection__Error$AgdaModeVscode.Establish.merge({
                      probes: nativeError.probes,
                      commands: nativeError.commands,
                      download: "Succeeded"
                    }, {
                      probes: wasmError.probes,
                      commands: wasmError.commands,
                      download: "Succeeded"
                    })
              };
    case "No" :
    case "Undecided" :
        break;
    
  }
  await Config$AgdaModeVscode.Connection.DownloadPolicy.set("No");
  return {
          TAG: "Error",
          _0: Connection__Error$AgdaModeVscode.Establish.fromDownloadError("OptedNotToDownload")
        };
}

async function makeWithFallback(platformDeps, memento, globalStorageUri, paths, _commands, logChannel) {
  var logConnection = function (connection) {
    switch (connection.TAG) {
      case "Agda" :
          return Chan$AgdaModeVscode.emit(logChannel, {
                      TAG: "Connection",
                      _0: {
                        TAG: "ConnectedToAgda",
                        _0: connection._1,
                        _1: connection._2
                      }
                    });
      case "ALS" :
          var match = connection._2;
          var path = connection._1;
          if (match !== undefined) {
            return Chan$AgdaModeVscode.emit(logChannel, {
                        TAG: "Connection",
                        _0: {
                          TAG: "ConnectedToALS",
                          _0: path,
                          _1: [
                            match[0],
                            match[1]
                          ]
                        }
                      });
          } else {
            return Chan$AgdaModeVscode.emit(logChannel, {
                        TAG: "Connection",
                        _0: {
                          TAG: "ConnectedToALS",
                          _0: path,
                          _1: undefined
                        }
                      });
          }
      case "ALSWASM" :
          var match$1 = connection._3;
          var path$1 = connection._2;
          if (match$1 !== undefined) {
            return Chan$AgdaModeVscode.emit(logChannel, {
                        TAG: "Connection",
                        _0: {
                          TAG: "ConnectedToALS",
                          _0: path$1,
                          _1: [
                            match$1[0],
                            match$1[1]
                          ]
                        }
                      });
          } else {
            return Chan$AgdaModeVscode.emit(logChannel, {
                        TAG: "Connection",
                        _0: {
                          TAG: "ConnectedToALS",
                          _0: path$1,
                          _1: undefined
                        }
                      });
          }
      
    }
  };
  var pickedConnection = Memento$AgdaModeVscode.Module.PickedConnection.get(memento);
  var pickedEntries = pickedConnection !== undefined ? [[
        pickedConnection,
        "FromConfig"
      ]] : [];
  var remainingPaths = pickedConnection !== undefined ? paths.filter(function (path) {
          return path !== pickedConnection;
        }) : paths;
  var pathsWithSource = remainingPaths.toReversed().map(function (path) {
        return [
                path,
                "FromConfig"
              ];
      });
  var connection = await fromPathsOrCommands(platformDeps, pickedEntries);
  if (connection.TAG === "Ok") {
    var connection$1 = connection._0;
    logConnection(connection$1);
    return {
            TAG: "Ok",
            _0: connection$1
          };
  }
  var connection$2 = await fromPathsOrCommands(platformDeps, pathsWithSource);
  if (connection$2.TAG === "Ok") {
    var connection$3 = connection$2._0;
    logConnection(connection$3);
    return {
            TAG: "Ok",
            _0: connection$3
          };
  }
  var pathErrors = Connection__Error$AgdaModeVscode.Establish.mergeMany([
        connection._0,
        connection$2._0
      ]);
  var connection$4 = await fromDownloads(platformDeps, memento, globalStorageUri);
  if (connection$4.TAG !== "Ok") {
    return {
            TAG: "Error",
            _0: {
              TAG: "Establish",
              _0: Connection__Error$AgdaModeVscode.Establish.mergeMany([
                    pathErrors,
                    connection$4._0
                  ])
            }
          };
  }
  var connection$5 = connection$4._0;
  logConnection(connection$5);
  var downloadedPath = getPath(connection$5);
  if (!paths.includes(downloadedPath)) {
    await Config$AgdaModeVscode.Connection.setAgdaPaths(logChannel, [downloadedPath].concat(paths));
  }
  return {
          TAG: "Ok",
          _0: connection$5
        };
}

async function sendRequest(connection, $$document, request, handler) {
  var encodeRequest = function ($$document, version) {
    var filepath = Parser$AgdaModeVscode.filepath($$document.fileName);
    var libraryPath = Config$AgdaModeVscode.getLibraryPath();
    var highlightingMethod = Config$AgdaModeVscode.Highlighting.getHighlightingMethod();
    var backend = Config$AgdaModeVscode.getBackend();
    return Request$AgdaModeVscode.encode($$document, version, filepath, backend, libraryPath, highlightingMethod, request);
  };
  var encodeRequestForWASM = function (wasmLoader, $$document, version) {
    var libraryPath = Config$AgdaModeVscode.getLibraryPath();
    var highlightingMethod = Config$AgdaModeVscode.Highlighting.getHighlightingMethod();
    var backend = Config$AgdaModeVscode.getBackend();
    var codeUri = $$document.uri;
    var converters = wasmLoader.createUriConverters();
    var protoUri = (function(conv, uri){ return conv.code2Protocol(uri); })(converters, codeUri);
    var filepath;
    try {
      filepath = Vscode.Uri.parse(protoUri).path;
    }
    catch (raw_exn){
      var exn = Caml_js_exceptions.internalToOCamlException(raw_exn);
      if (exn.RE_EXN_ID === Js_exn.$$Error) {
        filepath = protoUri;
      } else {
        throw exn;
      }
    }
    var workspaceFolders = Core__Option.getOr(Vscode.workspace.workspaceFolders, []);
    var prefix = workspaceFolders.length === 1 ? "/workspace" : "/workspaces";
    var filepath$1;
    if (filepath.startsWith(prefix)) {
      var rest = filepath.slice(prefix.length);
      filepath$1 = rest === "" || filepath.startsWith(prefix + "/") ? filepath : prefix + "/" + rest;
    } else {
      filepath$1 = filepath;
    }
    return Request$AgdaModeVscode.encode($$document, version, filepath$1, backend, libraryPath, highlightingMethod, request);
  };
  switch (connection.TAG) {
    case "Agda" :
        var error = await Connection__Endpoint__Agda$AgdaModeVscode.sendRequest(connection._0, encodeRequest($$document, connection._2), handler);
        if (error.TAG === "Ok") {
          return {
                  TAG: "Ok",
                  _0: undefined
                };
        }
        await destroy(connection, Chan$AgdaModeVscode.make());
        return {
                TAG: "Error",
                _0: {
                  TAG: "CommWithAgda",
                  _0: error._0
                }
              };
    case "ALS" :
        var conn = connection._0;
        var error$1 = await Connection__Endpoint__ALS$AgdaModeVscode.sendRequest(conn, encodeRequest($$document, conn.agdaVersion), handler);
        if (error$1.TAG === "Ok") {
          return {
                  TAG: "Ok",
                  _0: undefined
                };
        }
        await destroy(connection, Chan$AgdaModeVscode.make());
        return {
                TAG: "Error",
                _0: {
                  TAG: "CommWithALS",
                  _0: error$1._0
                }
              };
    case "ALSWASM" :
        var conn$1 = connection._0;
        var payload = encodeRequestForWASM(connection._1, $$document, conn$1.agdaVersion);
        var error$2 = await Connection__Endpoint__ALS$AgdaModeVscode.sendRequest(conn$1, payload, handler);
        if (error$2.TAG === "Ok") {
          return {
                  TAG: "Ok",
                  _0: undefined
                };
        }
        await destroy(connection, Chan$AgdaModeVscode.make());
        return {
                TAG: "Error",
                _0: {
                  TAG: "CommWithALS",
                  _0: error$2._0
                }
              };
    
  }
}

var Module = {
  make: make,
  makeWithFallback: makeWithFallback,
  toString: toString,
  getPath: getPath,
  destroy: destroy,
  isCommand: isCommand,
  fromPathsOrCommands: fromPathsOrCommands,
  fromDownloads: fromDownloads,
  sendRequest: sendRequest,
  checkForPrebuiltDataDirectory: checkForPrebuiltDataDirectory,
  probeFilepath: probeFilepath,
  probeCandidate: probeCandidate
};

var $$Error;

var Agda;

var ALS;

var URI;

var Candidate;

exports.$$Error = $$Error;
exports.Agda = Agda;
exports.ALS = ALS;
exports.URI = URI;
exports.Candidate = Candidate;
exports.Module = Module;
exports.make = make;
exports.makeWithFallback = makeWithFallback;
exports.toString = toString;
exports.getPath = getPath;
exports.destroy = destroy;
exports.isCommand = isCommand;
exports.fromPathsOrCommands = fromPathsOrCommands;
exports.fromDownloads = fromDownloads;
exports.sendRequest = sendRequest;
exports.checkForPrebuiltDataDirectory = checkForPrebuiltDataDirectory;
exports.probeFilepath = probeFilepath;
exports.probeCandidate = probeCandidate;
/* vscode Not a pure module */
