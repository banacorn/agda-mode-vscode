// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Vscode = require("vscode");
var Nodeurl = require("node:url");
var Nodepath = require("node:path");
var Untildify = require("untildify");
var Caml_option = require("rescript/lib/js/caml_option.js");
var OS$AgdaModeVscode = require("../../Util/OS.bs.js");

function parse(path) {
  var result;
  try {
    result = new Nodeurl.URL(path);
  }
  catch (exn){
    result = undefined;
  }
  var result$1 = result !== undefined && result.protocol === "lsp:" ? result : undefined;
  if (result$1 !== undefined) {
    return {
            TAG: "LspURI",
            _0: result$1
          };
  }
  var path$1 = Untildify(path);
  var path$2 = Nodepath.normalize(path$1);
  var path$3 = OS$AgdaModeVscode.onUnix ? path$2 : path$2.replace(/^\\([a-zA-Z])\\/, "$1\:\\");
  var absolutePath = Nodepath.resolve(path$3);
  return {
          TAG: "FileURI",
          _0: Vscode.Uri.file(absolutePath)
        };
}

function toVSCodeURI(uri) {
  if (uri.TAG === "FileURI") {
    return Caml_option.some(uri._0);
  }
  
}

function toString(uri) {
  if (uri.TAG === "FileURI") {
    return uri._0.toString();
  } else {
    return uri._0.toString();
  }
}

function equal(x, y) {
  if (x.TAG === "FileURI") {
    if (y.TAG === "FileURI") {
      return x._0.toString() === y._0.toString();
    } else {
      return false;
    }
  } else if (y.TAG === "FileURI") {
    return false;
  } else {
    return x._0.toString() === y._0.toString();
  }
}

exports.parse = parse;
exports.toVSCodeURI = toVSCodeURI;
exports.toString = toString;
exports.equal = equal;
/* vscode Not a pure module */
