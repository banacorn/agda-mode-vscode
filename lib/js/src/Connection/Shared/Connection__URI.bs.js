// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Vscode = require("vscode");
var Nodepath = require("node:path");
var Untildify = require("untildify");
var OS$AgdaModeVscode = require("../../Util/OS.bs.js");

function startsWithWindowsDriveAfterSlash(path) {
  if (path.length < 3) {
    return false;
  }
  var match = path.charAt(0);
  var match$1 = path.charAt(1);
  var match$2 = path.charAt(2);
  if (match !== "/") {
    return false;
  }
  if (match$2 !== "/") {
    return false;
  }
  var code = match$1.charCodeAt(0);
  if (code >= 65 && code <= 90) {
    return true;
  } else if (code >= 97) {
    return code <= 122;
  } else {
    return false;
  }
}

function normalizeToBackslashes(path) {
  var _index = 0;
  var _out = "";
  while(true) {
    var out = _out;
    var index = _index;
    if (index >= path.length) {
      return out;
    }
    var ch = path.charAt(index);
    var out$1 = ch === "/" ? out + "\\" : out + ch;
    _out = out$1;
    _index = index + 1 | 0;
    continue ;
  };
}

function windowsSlashAbsoluteToDrivePath(path) {
  if (!startsWithWindowsDriveAfterSlash(path)) {
    return ;
  }
  var driveLetter = path.charAt(1);
  var rest = path.slice(2);
  var windowsRest = normalizeToBackslashes(rest);
  return Nodepath.normalize(driveLetter + ":" + windowsRest);
}

function parse(raw) {
  if (raw.startsWith("vscode-")) {
    return {
            TAG: "FileURI",
            _0: raw,
            _1: Vscode.Uri.parse(raw)
          };
  }
  var filePath = raw.startsWith("file://") ? Vscode.Uri.parse(raw).fsPath : raw;
  var path = Untildify(filePath);
  var absolutePath;
  if (OS$AgdaModeVscode.onUnix) {
    var path$1 = Nodepath.normalize(path);
    absolutePath = Nodepath.resolve(path$1);
  } else {
    var windowsDrivePath = windowsSlashAbsoluteToDrivePath(path);
    if (windowsDrivePath !== undefined) {
      absolutePath = Nodepath.resolve(windowsDrivePath);
    } else {
      var path$2 = Nodepath.normalize(path);
      absolutePath = Nodepath.resolve(path$2);
    }
  }
  return {
          TAG: "FileURI",
          _0: raw,
          _1: Vscode.Uri.file(absolutePath)
        };
}

function toString(uri) {
  return uri._1.toString();
}

function equal(x, y) {
  return x._1.toString() === y._1.toString();
}

exports.startsWithWindowsDriveAfterSlash = startsWithWindowsDriveAfterSlash;
exports.normalizeToBackslashes = normalizeToBackslashes;
exports.windowsSlashAbsoluteToDrivePath = windowsSlashAbsoluteToDrivePath;
exports.parse = parse;
exports.toString = toString;
exports.equal = equal;
/* vscode Not a pure module */
