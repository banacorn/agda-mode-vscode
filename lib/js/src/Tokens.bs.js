// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Fs = require("fs");
var Js_exn = require("rescript/lib/js/js_exn.js");
var Vscode = require("vscode");
var Caml_obj = require("rescript/lib/js/caml_obj.js");
var Belt_Array = require("rescript/lib/js/belt_Array.js");
var Core__List = require("@rescript/core/lib/js/src/Core__List.bs.js");
var FastCheck = require("fast-check");
var Core__Array = require("@rescript/core/lib/js/src/Core__Array.bs.js");
var PervasivesU = require("rescript/lib/js/pervasivesU.js");
var Core__Option = require("@rescript/core/lib/js/src/Core__Option.bs.js");
var Agda$AgdaModeVscode = require("./Agda.bs.js");
var Util$AgdaModeVscode = require("./Util/Util.bs.js");
var Json$JsonCombinators = require("@glennsl/rescript-json-combinators/lib/js/src/Json.bs.js");
var Editor$AgdaModeVscode = require("./Editor.bs.js");
var Parser$AgdaModeVscode = require("./Parser/Parser.bs.js");
var AVLTree$AgdaModeVscode = require("./Util/AVLTree.bs.js");
var Node__Fs$AgdaModeVscode = require("./Node/Node__Fs.bs.js");
var Json_Decode$JsonCombinators = require("@glennsl/rescript-json-combinators/lib/js/src/Json_Decode.bs.js");
var BinarySearchTree = require("@datastructures-js/binary-search-tree");
var Highlighting__AgdaAspect$AgdaModeVscode = require("./Highlighting/Highlighting__AgdaAspect.bs.js");
var Highlighting__Decoration$AgdaModeVscode = require("./Highlighting/Highlighting__Decoration.bs.js");
var Highlighting__SemanticToken$AgdaModeVscode = require("./Highlighting/Highlighting__SemanticToken.bs.js");

function toString(self) {
  var match = self.source;
  return "(" + String(self.start) + ", " + String(self.end_) + ") " + Util$AgdaModeVscode.Pretty.list(Core__List.fromArray(self.aspects.map(Highlighting__AgdaAspect$AgdaModeVscode.toString))) + (
          match !== undefined ? " [src: " + String(match[1]) + "]" : ""
        );
}

function parse(x) {
  if (x.TAG === "A") {
    return ;
  }
  var xs = x._0;
  var len = xs.length;
  if (len >= 7) {
    return ;
  }
  switch (len) {
    case 3 :
        var start$p = xs[0];
        if (start$p.TAG !== "A") {
          return ;
        }
        var end_$p = xs[1];
        if (end_$p.TAG !== "A") {
          return ;
        }
        var end_$p$1 = end_$p._0;
        var aspects = xs[2];
        return Core__Option.flatMap(PervasivesU.int_of_string_opt(start$p._0), (function (start) {
                      return Core__Option.map(PervasivesU.int_of_string_opt(end_$p$1), (function (end_) {
                                    return {
                                            start: start - 1 | 0,
                                            end_: end_ - 1 | 0,
                                            aspects: Parser$AgdaModeVscode.SExpression.flatten(aspects).map(Highlighting__AgdaAspect$AgdaModeVscode.parse),
                                            isTokenBased: false,
                                            note: undefined,
                                            source: undefined
                                          };
                                  }));
                    }));
    case 4 :
        var start$p$1 = xs[0];
        if (start$p$1.TAG !== "A") {
          return ;
        }
        var end_$p$2 = xs[1];
        if (end_$p$2.TAG !== "A") {
          return ;
        }
        var end_$p$3 = end_$p$2._0;
        var aspects$1 = xs[2];
        return Core__Option.flatMap(PervasivesU.int_of_string_opt(start$p$1._0), (function (start) {
                      return Core__Option.map(PervasivesU.int_of_string_opt(end_$p$3), (function (end_) {
                                    return {
                                            start: start - 1 | 0,
                                            end_: end_ - 1 | 0,
                                            aspects: Parser$AgdaModeVscode.SExpression.flatten(aspects$1).map(Highlighting__AgdaAspect$AgdaModeVscode.parse),
                                            isTokenBased: false,
                                            note: undefined,
                                            source: undefined
                                          };
                                  }));
                    }));
    case 0 :
    case 1 :
    case 2 :
    case 5 :
        return ;
    case 6 :
        var start$p$2 = xs[0];
        if (start$p$2.TAG !== "A") {
          return ;
        }
        var end_$p$4 = xs[1];
        if (end_$p$4.TAG !== "A") {
          return ;
        }
        var end_$p$5 = end_$p$4._0;
        var aspects$2 = xs[2];
        var match = xs[5];
        if (match.TAG === "A") {
          return ;
        }
        var match$1 = match._0;
        if (match$1.length !== 3) {
          return ;
        }
        var filepath = match$1[0];
        if (filepath.TAG !== "A") {
          return ;
        }
        var filepath$1 = filepath._0;
        var index$p = match$1[2];
        if (index$p.TAG !== "A") {
          return ;
        }
        var index$p$1 = index$p._0;
        return Core__Option.flatMap(PervasivesU.int_of_string_opt(start$p$2._0), (function (start) {
                      return Core__Option.flatMap(PervasivesU.int_of_string_opt(end_$p$5), (function (end_) {
                                    return Core__Option.map(PervasivesU.int_of_string_opt(index$p$1), (function (index) {
                                                  return {
                                                          start: start - 1 | 0,
                                                          end_: end_ - 1 | 0,
                                                          aspects: Parser$AgdaModeVscode.SExpression.flatten(aspects$2).map(Highlighting__AgdaAspect$AgdaModeVscode.parse),
                                                          isTokenBased: false,
                                                          note: undefined,
                                                          source: [
                                                            filepath$1,
                                                            index
                                                          ]
                                                        };
                                                }));
                                  }));
                    }));
    
  }
}

function parseDirectHighlightings(tokens) {
  return Core__Array.filterMap(tokens.slice(2).map(parse), (function (x) {
                return x;
              }));
}

var decodeToken = Json_Decode$JsonCombinators.map(Util$AgdaModeVscode.Decode.tuple6(Json_Decode$JsonCombinators.$$int, Json_Decode$JsonCombinators.$$int, Json_Decode$JsonCombinators.array(Json_Decode$JsonCombinators.string), Json_Decode$JsonCombinators.bool, Json_Decode$JsonCombinators.option(Json_Decode$JsonCombinators.string), Json_Decode$JsonCombinators.option(Json_Decode$JsonCombinators.pair(Json_Decode$JsonCombinators.string, Json_Decode$JsonCombinators.$$int))), (function (param) {
        return {
                start: param[0] - 1 | 0,
                end_: param[1] - 1 | 0,
                aspects: param[2].map(Highlighting__AgdaAspect$AgdaModeVscode.parse),
                isTokenBased: param[3],
                note: param[4],
                source: param[5]
              };
      }));

var decodeResponseHighlightingInfoDirect = Json_Decode$JsonCombinators.map(Json_Decode$JsonCombinators.pair(Json_Decode$JsonCombinators.bool, Json_Decode$JsonCombinators.array(decodeToken)), (function (param) {
        return [
                param[0],
                param[1]
              ];
      }));

var Token = {
  toString: toString,
  parse: parse,
  parseDirectHighlightings: parseDirectHighlightings,
  decodeToken: decodeToken,
  decodeResponseHighlightingInfoDirect: decodeResponseHighlightingInfoDirect
};

function toString$1(self) {
  return "-" + String(self.removed) + " +" + String(self.inserted) + " @ " + String(self.offset);
}

function fromTextDocumentContentChangeEvent($$event) {
  return {
          offset: $$event.rangeOffset,
          removed: $$event.rangeLength,
          inserted: $$event.text.length
        };
}

function delta(x) {
  return x.inserted - x.removed | 0;
}

function totalDelta(xs) {
  return Core__Array.reduce(xs, 0, (function (acc, x) {
                return acc + delta(x) | 0;
              }));
}

function removedInterval(x) {
  if (x.removed === 0) {
    return ;
  } else {
    return [
            x.offset,
            x.offset + x.removed | 0
          ];
  }
}

function areValid(xs) {
  return Core__Array.reduce(xs, [
                true,
                undefined
              ], (function (param, x) {
                  var prevEnd = param[1];
                  var end = x.offset + x.removed | 0;
                  if (prevEnd !== undefined) {
                    if (prevEnd > x.offset) {
                      return [
                              false,
                              end
                            ];
                    } else {
                      return [
                              param[0],
                              end
                            ];
                    }
                  } else {
                    return [
                            true,
                            end
                          ];
                  }
                }))[0];
}

function arbitrary(after, kindOpt) {
  var kind = kindOpt !== undefined ? kindOpt : "Mixed";
  return FastCheck.integer(after, after + 10 | 0).chain(function (offset) {
              return FastCheck.tuple(FastCheck.integer(0, offset), FastCheck.integer(0, 10)).map(function (param) {
                          var inserted = param[1];
                          var removed = param[0];
                          switch (kind) {
                            case "RemovalOnly" :
                                return {
                                        offset: offset,
                                        removed: removed,
                                        inserted: 0
                                      };
                            case "InsertionOnly" :
                                return {
                                        offset: offset,
                                        removed: 0,
                                        inserted: inserted
                                      };
                            case "Mixed" :
                                return {
                                        offset: offset,
                                        removed: removed,
                                        inserted: inserted
                                      };
                            
                          }
                        });
            });
}

function arbitraryBatch(batchSize, kindOpt) {
  var kind = kindOpt !== undefined ? kindOpt : "Mixed";
  var aux = function (after, size) {
    if (size === 0) {
      return FastCheck.constant([]);
    } else {
      return arbitrary(after, kind).chain(function (change) {
                  return aux((change.offset + change.removed | 0) + 1 | 0, size - 1 | 0).map(function (changes) {
                              return Belt_Array.concatMany([
                                          [change],
                                          changes
                                        ]);
                            });
                });
    }
  };
  if (batchSize !== undefined) {
    return aux(0, batchSize);
  } else {
    return FastCheck.integer(0, 10).chain(function (size) {
                return aux(0, size);
              });
  }
}

var Change = {
  toString: toString$1,
  fromTextDocumentContentChangeEvent: fromTextDocumentContentChangeEvent,
  delta: delta,
  totalDelta: totalDelta,
  removedInterval: removedInterval,
  areValid: areValid,
  arbitrary: arbitrary,
  arbitraryBatch: arbitraryBatch
};

function deltaToString(delta) {
  if (delta > 0) {
    return " +" + String(delta) + " ";
  } else if (delta < 0) {
    return " " + String(delta) + " ";
  } else {
    return " +0 ";
  }
}

function toString$2(xs) {
  if (typeof xs !== "object") {
    return "EOF";
  }
  var tail = xs._3;
  var delta = xs._2;
  var end = xs._1;
  var start = xs._0;
  if (start === end) {
    return String(start) + "┃" + String(end) + deltaToString(delta) + (
            tail === "EOF" ? "" : toString$2(tail)
          );
  } else {
    return String(start) + "┣━━┫" + String(end) + deltaToString(delta) + (
            tail === "EOF" ? "" : toString$2(tail)
          );
  }
}

function hasError(xs) {
  var _prevEnd = 0;
  var _before = 0;
  var _xs = xs;
  while(true) {
    var xs$1 = _xs;
    var before = _before;
    var prevEnd = _prevEnd;
    if (typeof xs$1 !== "object") {
      return ;
    }
    var after = xs$1._2;
    var end = xs$1._1;
    var start = xs$1._0;
    var removed = end - start | 0;
    var inserted = (after - before | 0) + removed | 0;
    if (start < prevEnd) {
      return "Overlapping";
    }
    if (start > end) {
      return "ReversedOrder";
    }
    if (inserted < 0) {
      return "NegativeInsertion";
    }
    _xs = xs$1._3;
    _before = after;
    _prevEnd = end;
    continue ;
  };
}

function debugIsValid(xs) {
  var match = hasError(xs);
  var aux;
  if (match !== undefined) {
    switch (match) {
      case "Overlapping" :
          aux = "Has overlapping intervals";
          break;
      case "ReversedOrder" :
          aux = "Has reversed order intervals";
          break;
      case "NegativeInsertion" :
          aux = "Has negative insertion";
          break;
      
    }
  } else {
    aux = undefined;
  }
  if (aux !== undefined) {
    console.log("Intervals: " + toString$2(xs));
    console.log("Error: " + aux);
    return ;
  }
  
}

function totalDelta$1(_xs) {
  while(true) {
    var xs = _xs;
    if (typeof xs !== "object") {
      return 0;
    }
    var tail = xs._3;
    if (typeof tail !== "object") {
      return xs._2;
    }
    _xs = tail;
    continue ;
  };
}

function removedIntervals(_xs) {
  while(true) {
    var xs = _xs;
    if (typeof xs !== "object") {
      return [];
    }
    var tail = xs._3;
    var end = xs._1;
    var start = xs._0;
    if (start !== end) {
      return Belt_Array.concatMany([
                  [[
                      start,
                      end
                    ]],
                  removedIntervals(tail)
                ]);
    }
    _xs = tail;
    continue ;
  };
}

function isValidWRTChanges(xs, changes) {
  var sameTotalDelta = totalDelta$1(xs) === totalDelta(changes);
  var sameRemovedIntervals = Caml_obj.equal(removedIntervals(xs), Core__Array.filterMap(changes.map(removedInterval), (function (x) {
              return x;
            })));
  if (hasError(xs) === undefined && sameTotalDelta) {
    return sameRemovedIntervals;
  } else {
    return false;
  }
}

function isValidWRTChangeBatches(xs, batches) {
  var sameTotalDelta = totalDelta$1(xs) === Core__Array.reduce(batches, 0, (function (acc, changes) {
          return acc + totalDelta(changes) | 0;
        }));
  if (hasError(xs) === undefined) {
    return sameTotalDelta;
  } else {
    return false;
  }
}

function calculateOriginalOffset(deltaBefore, start, end, deltaAfter, x) {
  if (x >= (end + deltaAfter | 0)) {
    return {
            TAG: "After",
            _0: x - deltaAfter | 0,
            [Symbol.for("name")]: "After"
          };
  } else if (x <= (deltaBefore + start | 0)) {
    return {
            TAG: "Before",
            _0: x - deltaBefore | 0,
            [Symbol.for("name")]: "Before"
          };
  } else {
    return {
            TAG: "Replaced",
            _0: x - deltaBefore | 0,
            _1: start,
            _2: end,
            [Symbol.for("name")]: "Replaced"
          };
  }
}

function adjustDelta(delta, xs) {
  if (typeof xs !== "object") {
    return "EOF";
  } else {
    return {
            TAG: "Replace",
            _0: xs._0,
            _1: xs._1,
            _2: delta + xs._2 | 0,
            _3: adjustDelta(delta, xs._3),
            [Symbol.for("name")]: "Replace"
          };
  }
}

function printInterval(deltaBefore, start, end, deltaAfter) {
  console.log("interval delta before: " + String(deltaBefore));
  console.log("         delta after: " + String(deltaAfter));
  console.log("         start: " + String(start));
  console.log("         end: " + String(end));
  console.log("         removed: " + String(end - start | 0));
  console.log("         inserted: " + String(((deltaAfter - deltaBefore | 0) + end | 0) - start | 0));
}

function applyChangeAux(_xs, _change, _deltaBefore) {
  while(true) {
    var deltaBefore = _deltaBefore;
    var change = _change;
    var xs = _xs;
    if (typeof xs !== "object") {
      console.log("== EOF =====================================");
      var deltaAfter = deltaBefore + delta(change) | 0;
      console.log("change: " + toString$1(change));
      console.log("delta : " + delta(change).toString());
      console.log("delta before: " + deltaBefore.toString());
      return {
              TAG: "Replace",
              _0: change.offset - deltaBefore | 0,
              _1: (change.offset + change.removed | 0) - deltaBefore | 0,
              _2: deltaAfter,
              _3: "EOF",
              [Symbol.for("name")]: "Replace"
            };
    }
    var tail = xs._3;
    var deltaAfter$1 = xs._2;
    var end = xs._1;
    var start = xs._0;
    var changeRemovalStart = calculateOriginalOffset(deltaBefore, start, end, deltaAfter$1, change.offset);
    var changeRemovalEnd = calculateOriginalOffset(deltaBefore, start, end, deltaAfter$1, change.offset + change.removed | 0);
    switch (changeRemovalStart.TAG) {
      case "Before" :
          var changeStart = changeRemovalStart._0;
          switch (changeRemovalEnd.TAG) {
            case "Before" :
                console.log("== Before/Before =====================================");
                printInterval(deltaBefore, start, end, deltaAfter$1);
                console.log("change: " + toString$1(change));
                console.log("delta : " + delta(change).toString());
                var delta$1 = delta(change);
                return {
                        TAG: "Replace",
                        _0: changeStart,
                        _1: changeRemovalEnd._0,
                        _2: deltaBefore + delta$1 | 0,
                        _3: adjustDelta(delta$1, xs),
                        [Symbol.for("name")]: "Replace"
                      };
            case "Replaced" :
                console.log("== Before/Replaced =====================================");
                console.log("change: " + toString$1(change));
                console.log("delta : " + delta(change).toString());
                var delta$2 = delta(change);
                return {
                        TAG: "Replace",
                        _0: changeStart,
                        _1: end,
                        _2: deltaAfter$1 + delta$2 | 0,
                        _3: adjustDelta(delta$2, tail),
                        [Symbol.for("name")]: "Replace"
                      };
            case "After" :
                console.log("== Before/After ==== " + deltaBefore.toString() + " =================================");
                printInterval(deltaBefore, start, end, deltaAfter$1);
                console.log("change: " + toString$1(change));
                console.log("delta : " + delta(change).toString());
                var change1_offset = change.offset;
                var change1_removed = end - changeStart | 0;
                var change1 = {
                  offset: change1_offset,
                  removed: change1_removed,
                  inserted: 0
                };
                var change2_removed = changeRemovalEnd._0 - end | 0;
                var change2_inserted = change.inserted;
                var change2 = {
                  offset: end,
                  removed: change2_removed,
                  inserted: change2_inserted
                };
                _deltaBefore = deltaAfter$1;
                _change = change2;
                _xs = applyChangeAux(tail, change1, deltaBefore);
                continue ;
            
          }
      case "Replaced" :
          switch (changeRemovalEnd.TAG) {
            case "Before" :
                return Js_exn.raiseError("Not a possible case");
            case "Replaced" :
                console.log("== Replaced/Replaced ==========================");
                console.log("change: " + toString$1(change));
                console.log("delta : " + delta(change).toString());
                var delta$3 = delta(change);
                return {
                        TAG: "Replace",
                        _0: start,
                        _1: end,
                        _2: deltaAfter$1 + delta$3 | 0,
                        _3: adjustDelta(delta$3, tail),
                        [Symbol.for("name")]: "Replace"
                      };
            case "After" :
                console.log("== Replaced/After ==========================");
                console.log("change: " + toString$1(change));
                console.log("delta : " + delta(change).toString());
                var delta$4 = delta(change);
                return {
                        TAG: "Replace",
                        _0: start,
                        _1: changeRemovalEnd._0,
                        _2: deltaAfter$1 + delta$4 | 0,
                        _3: adjustDelta(delta$4, tail),
                        [Symbol.for("name")]: "Replace"
                      };
            
          }
      case "After" :
          switch (changeRemovalEnd.TAG) {
            case "Before" :
            case "Replaced" :
                return Js_exn.raiseError("Not a possible case");
            case "After" :
                console.log("== After/After ==========================");
                printInterval(deltaBefore, start, end, deltaAfter$1);
                console.log("change: " + toString$1(change));
                console.log("delta : " + delta(change).toString());
                console.log("change*: " + changeRemovalStart._0.toString() + " ~ " + changeRemovalEnd._0.toString());
                return {
                        TAG: "Replace",
                        _0: start,
                        _1: end,
                        _2: deltaAfter$1,
                        _3: applyChangeAux(tail, change, deltaAfter$1),
                        [Symbol.for("name")]: "Replace"
                      };
            
          }
      
    }
  };
}

function applyChangesAux(xs, changes) {
  return Core__List.reduce(changes, xs, (function (xs, change) {
                return applyChangeAux(xs, change, 0);
              }));
}

function applyChanges(xs, changes) {
  return applyChangesAux(xs, Core__List.fromArray(changes));
}

var Intervals = {
  deltaToString: deltaToString,
  toString: toString$2,
  empty: "EOF",
  hasError: hasError,
  debugIsValid: debugIsValid,
  totalDelta: totalDelta$1,
  removedIntervals: removedIntervals,
  isValidWRTChanges: isValidWRTChanges,
  isValidWRTChangeBatches: isValidWRTChangeBatches,
  calculateOriginalOffset: calculateOriginalOffset,
  adjustDelta: adjustDelta,
  printInterval: printInterval,
  applyChangeAux: applyChangeAux,
  applyChangesAux: applyChangesAux,
  applyChanges: applyChanges
};

function toFilepath(format) {
  return format._0;
}

async function readAndParse(format) {
  try {
    var content = await Node__Fs$AgdaModeVscode.readFile(format._0);
    if (format.TAG === "Emacs") {
      var match = Parser$AgdaModeVscode.SExpression.parse(content)[0];
      var tokens;
      if (match !== undefined) {
        if (match.TAG === "Ok") {
          var xs = match._0;
          tokens = xs.TAG === "A" ? [] : xs._0;
        } else {
          tokens = [];
        }
      } else {
        tokens = [];
      }
      var match$1 = tokens[0];
      var removeTokenBasedHighlighting = match$1 !== undefined && match$1.TAG === "A" && match$1._0 === "remove" ? true : false;
      var tokens$1 = Core__Array.filterMap(tokens.slice(1), parse);
      return [
              removeTokenBasedHighlighting,
              tokens$1
            ];
    }
    var exit = 0;
    var json;
    try {
      json = JSON.parse(content);
      exit = 1;
    }
    catch (_e){
      return [
              false,
              []
            ];
    }
    if (exit === 1) {
      var _err = Json$JsonCombinators.decode(json, decodeResponseHighlightingInfoDirect);
      if (_err.TAG === "Ok") {
        var match$2 = _err._0;
        return [
                match$2[0],
                match$2[1]
              ];
      }
      console.log("Error in decoding JSON: " + _err._0);
      return [
              false,
              []
            ];
    }
    
  }
  catch (exn){
    return [
            false,
            []
          ];
  }
}

function toString$3(self) {
  var tempFiles = self.tempFiles.length === 0 ? "" : "\n    " + self.tempFiles.map(toFilepath).join("\n    ");
  var tokens = AVLTree$AgdaModeVscode.toArray(self.tokens).map(function (param) {
          return toString(param[0]) + " " + Editor$AgdaModeVscode.$$Range.toString(param[1]);
        }).join("\n    ");
  return "Tokens:\n  tempFiles (" + String(self.tempFiles.length) + ") " + tempFiles + "\n  tokens:\n    " + tokens;
}

function make() {
  return {
          tempFiles: [],
          tokens: new BinarySearchTree.BinarySearchTree()
        };
}

function insert(self, editor, tokens) {
  tokens.forEach(function (info) {
        var $$document = editor.document;
        var text = Editor$AgdaModeVscode.$$Text.getAll($$document);
        var offsetConverter = Agda$AgdaModeVscode.OffsetConverter.make(text);
        var startOffset = Agda$AgdaModeVscode.OffsetConverter.convert(offsetConverter, info.start);
        var existing = AVLTree$AgdaModeVscode.find(self.tokens, startOffset);
        if (existing !== undefined) {
          var old = existing[0];
          self.tokens.remove(startOffset);
          var newAspects = Caml_obj.equal(old.aspects, info.aspects) ? old.aspects : old.aspects.concat(info.aspects);
          var new_start = old.start;
          var new_end_ = old.end_;
          var new_isTokenBased = old.isTokenBased;
          var new_note = old.note;
          var new_source = old.source;
          var $$new = {
            start: new_start,
            end_: new_end_,
            aspects: newAspects,
            isTokenBased: new_isTokenBased,
            note: new_note,
            source: new_source
          };
          self.tokens.insert(startOffset, [
                $$new,
                existing[1]
              ]);
          return ;
        }
        var start = $$document.positionAt(startOffset);
        var end_ = $$document.positionAt(Agda$AgdaModeVscode.OffsetConverter.convert(offsetConverter, info.end_));
        var range = new Vscode.Range(start, end_);
        self.tokens.insert(startOffset, [
              info,
              range
            ]);
      });
}

function addEmacsFilePath(self, filepath) {
  self.tempFiles.push({
        TAG: "Emacs",
        _0: filepath,
        [Symbol.for("name")]: "Emacs"
      });
}

function addJSONFilePath(self, filepath) {
  self.tempFiles.push({
        TAG: "JSON",
        _0: filepath,
        [Symbol.for("name")]: "JSON"
      });
}

async function readTempFiles(self, editor) {
  var xs = await Promise.all(self.tempFiles.map(readAndParse));
  var tokens = xs.map(function (prim) {
          return prim[1];
        }).flat();
  insert(self, editor, tokens);
  self.tempFiles = [];
}

function clear(self) {
  self.tempFiles.forEach(function (format) {
        Fs.unlink(format._0, (function (param) {
                
              }));
      });
  self.tokens = new BinarySearchTree.BinarySearchTree();
}

function toArray(self) {
  return AVLTree$AgdaModeVscode.toArray(self.tokens);
}

function lookupSrcLoc(self, offset) {
  return Core__Option.map(Core__Option.flatMap(AVLTree$AgdaModeVscode.lowerBound(self.tokens, offset), (function (param) {
                    var range = param[1];
                    return Core__Option.map(param[0].source, (function (param) {
                                  return [
                                          range,
                                          param[0],
                                          param[1]
                                        ];
                                }));
                  })), (function (param) {
                var offset = param[2];
                var filepath = param[1];
                var range = param[0];
                return Vscode.workspace.openTextDocument(filepath).then(function ($$document) {
                            var text = Editor$AgdaModeVscode.$$Text.getAll($$document);
                            var offsetConverter = Agda$AgdaModeVscode.OffsetConverter.make(text);
                            var offset$1 = Agda$AgdaModeVscode.OffsetConverter.convert(offsetConverter, offset - 1 | 0);
                            var position = $$document.positionAt(offset$1);
                            return [[
                                      range,
                                      filepath,
                                      position
                                    ]];
                          });
              }));
}

function toDecorationsAndSemanticTokens(tokens, editor) {
  var match = Belt_Array.unzip(Core__Array.filterMap(AVLTree$AgdaModeVscode.toArray(tokens.tokens).map(function (param) {
                  var info = param[0];
                  var ranges = Highlighting__SemanticToken$AgdaModeVscode.Module.SingleLineRange.splitRange(editor.document, param[1]);
                  return ranges.map(function (range) {
                              return [
                                      info.aspects,
                                      range
                                    ];
                            });
                }).flat(), (function (param) {
              var range = param[1];
              var match = Belt_Array.unzip(param[0].map(Highlighting__AgdaAspect$AgdaModeVscode.toTokenTypeAndModifiersAndDecoration));
              var match$1 = Belt_Array.unzip(match[0]);
              var tokenTypes = Core__Array.filterMap(match$1[0], (function (x) {
                      return x;
                    }));
              var tokenModifiers = match$1[1].flat();
              var decorations = Core__Array.filterMap(match[1], (function (x) {
                      return Core__Option.map(x, (function (x) {
                                    return [
                                            x,
                                            Highlighting__SemanticToken$AgdaModeVscode.Module.SingleLineRange.toVsCodeRange(range)
                                          ];
                                  }));
                    }));
              var semanticToken = Core__Option.map(tokenTypes[0], (function (tokenType) {
                      return {
                              range: range,
                              type_: tokenType,
                              modifiers: tokenModifiers
                            };
                    }));
              return [
                      semanticToken,
                      decorations
                    ];
            })));
  var semanticTokens = Core__Array.filterMap(match[0], (function (x) {
          return x;
        }));
  var decorations = Highlighting__Decoration$AgdaModeVscode.toVSCodeDecorations(match[1].flat(), editor);
  return [
          decorations,
          semanticTokens
        ];
}

function toDecorations(self, editor) {
  var aspects = AVLTree$AgdaModeVscode.toArray(self.tokens).map(function (param) {
          var range = param[1];
          return param[0].aspects.map(function (aspect) {
                      return [
                              aspect,
                              range
                            ];
                    });
        }).flat();
  return Highlighting__Decoration$AgdaModeVscode.toVSCodeDecorations(Core__Array.filterMap(aspects, (function (param) {
                    var range = param[1];
                    return Core__Option.map(Highlighting__AgdaAspect$AgdaModeVscode.toDecoration(param[0]), (function (x) {
                                  return [
                                          x,
                                          range
                                        ];
                                }));
                  })), editor);
}

var Module = {
  toString: toString$3,
  make: make,
  addEmacsFilePath: addEmacsFilePath,
  addJSONFilePath: addJSONFilePath,
  readTempFiles: readTempFiles,
  insert: insert,
  clear: clear,
  toArray: toArray,
  lookupSrcLoc: lookupSrcLoc,
  toDecorations: toDecorations,
  toDecorationsAndSemanticTokens: toDecorationsAndSemanticTokens
};

var Aspect;

exports.Aspect = Aspect;
exports.Token = Token;
exports.Change = Change;
exports.Intervals = Intervals;
exports.Module = Module;
exports.toString = toString$3;
exports.make = make;
exports.addEmacsFilePath = addEmacsFilePath;
exports.addJSONFilePath = addJSONFilePath;
exports.readTempFiles = readTempFiles;
exports.insert = insert;
exports.clear = clear;
exports.toArray = toArray;
exports.lookupSrcLoc = lookupSrcLoc;
exports.toDecorations = toDecorations;
exports.toDecorationsAndSemanticTokens = toDecorationsAndSemanticTokens;
/* decodeToken Not a pure module */
