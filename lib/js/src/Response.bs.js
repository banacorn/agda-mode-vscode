// Generated by BUCKLESCRIPT, PLEASE EDIT WITH CARE
'use strict';

var Belt_Array = require("bs-platform/lib/js/belt_Array.js");
var Belt_Option = require("bs-platform/lib/js/belt_Option.js");
var Caml_format = require("bs-platform/lib/js/caml_format.js");
var Util$AgdaModeVscode = require("./Util/Util.bs.js");
var Caml_chrome_debugger = require("bs-platform/lib/js/caml_chrome_debugger.js");
var Parser$AgdaModeVscode = require("./Parser.bs.js");

function toString(string) {
  if (typeof string === "number") {
    if (string === /* CompilationOk */0) {
      return "CompilationOk";
    } else {
      return "AllGoalsWarnings ";
    }
  }
  switch (string.tag | 0) {
    case /* Constraints */0 :
        var string$1 = string[0];
        if (string$1 !== undefined) {
          return "Constraints " + string$1;
        } else {
          return "Constraints";
        }
    case /* Time */1 :
        return "Time " + string[0];
    case /* Error */2 :
        return "Error " + string[0];
    case /* Intro */3 :
        return "Intro " + string[0];
    case /* Auto */4 :
        return "Auto " + string[0];
    case /* ModuleContents */5 :
        return "ModuleContents " + string[0];
    case /* SearchAbout */6 :
        return "SearchAbout " + string[0];
    case /* WhyInScope */7 :
        return "WhyInScope " + string[0];
    case /* NormalForm */8 :
        return "NormalForm " + string[0];
    case /* GoalType */9 :
        return "GoalType " + string[0];
    case /* CurrentGoal */10 :
        return "CurrentGoal " + string[0];
    case /* InferredType */11 :
        return "InferredType " + string[0];
    case /* Context */12 :
        return "Context " + string[0];
    case /* HelperFunction */13 :
        return "HelperFunction " + string[0];
    case /* Version */14 :
        return "Version " + string[0];
    
  }
}

function parse(xs) {
  var match = Belt_Array.get(xs, 1);
  if (match === undefined) {
    return ;
  }
  if (match.tag) {
    return ;
  }
  var payload = match[0].replace(/\\r\\n|\\n/g, "\n");
  var match$1 = Belt_Array.get(xs, 0);
  if (match$1 === undefined) {
    return ;
  }
  if (match$1.tag) {
    return ;
  }
  switch (match$1[0]) {
    case "*Agda Version*" :
        return /* Version */Caml_chrome_debugger.variant("Version", 14, [payload]);
    case "*Auto*" :
        return /* Auto */Caml_chrome_debugger.variant("Auto", 4, [payload]);
    case "*Compilation result*" :
        return /* CompilationOk */0;
    case "*Constraints*" :
        if (payload === "nil") {
          return /* Constraints */Caml_chrome_debugger.variant("Constraints", 0, [undefined]);
        } else {
          return /* Constraints */Caml_chrome_debugger.variant("Constraints", 0, [payload]);
        }
    case "*Context*" :
        return /* Context */Caml_chrome_debugger.variant("Context", 12, [payload]);
    case "*Current Goal*" :
        return /* CurrentGoal */Caml_chrome_debugger.variant("CurrentGoal", 10, [payload]);
    case "*Error*" :
        return /* Error */Caml_chrome_debugger.variant("Error", 2, [payload]);
    case "*Goal type etc.*" :
        return /* GoalType */Caml_chrome_debugger.variant("GoalType", 9, [payload]);
    case "*Helper function*" :
        return /* HelperFunction */Caml_chrome_debugger.variant("HelperFunction", 13, [payload]);
    case "*Inferred Type*" :
        return /* InferredType */Caml_chrome_debugger.variant("InferredType", 11, [payload]);
    case "*Intro*" :
        return /* Intro */Caml_chrome_debugger.variant("Intro", 3, [payload]);
    case "*Module contents*" :
        return /* ModuleContents */Caml_chrome_debugger.variant("ModuleContents", 5, [payload]);
    case "*Normal Form*" :
        return /* NormalForm */Caml_chrome_debugger.variant("NormalForm", 8, [payload]);
    case "*Scope Info*" :
        return /* WhyInScope */Caml_chrome_debugger.variant("WhyInScope", 7, [payload]);
    case "*Search About*" :
        return /* SearchAbout */Caml_chrome_debugger.variant("SearchAbout", 6, [payload]);
    case "*Time*" :
        return /* Time */Caml_chrome_debugger.variant("Time", 1, [payload]);
    default:
      return /* AllGoalsWarnings */1;
  }
}

var Info = {
  toString: toString,
  parse: parse
};

function toString$1(filepath) {
  if (typeof filepath === "number") {
    switch (filepath) {
      case /* HighlightingInfoDirect */0 :
          return "HighlightingInfoDirect ";
      case /* NoStatus */1 :
          return "NoStatus";
      case /* ClearRunningInfo */2 :
          return "ClearRunningInfo";
      case /* ClearHighlighting */3 :
          return "ClearHighlighting";
      case /* DoneAborting */4 :
          return "DoneAborting";
      
    }
  } else {
    switch (filepath.tag | 0) {
      case /* HighlightingInfoIndirect */0 :
          return "HighlightingInfoIndirect " + filepath[0];
      case /* Status */1 :
          return "Status: implicit arguments " + ((
                    filepath[0] ? "displayed, " : "not displayed, "
                  ) + ("module " + (
                      filepath[1] ? "type checked" : "not type checked"
                    )));
      case /* JumpToError */2 :
          return "JumpToError " + (filepath[0] + (" " + String(filepath[1])));
      case /* InteractionPoints */3 :
          return "InteractionPoints " + Util$AgdaModeVscode.Pretty.array(Belt_Array.map(filepath[0], (function (prim) {
                            return String(prim);
                          })));
      case /* GiveAction */4 :
          var string = filepath[1];
          var index = filepath[0];
          if (typeof string === "number") {
            if (string !== 0) {
              return "GiveAction " + (String(index) + " NoParen");
            } else {
              return "GiveAction " + (String(index) + " Paren");
            }
          } else {
            return "GiveAction " + (String(index) + (" String " + string[0]));
          }
      case /* MakeCase */5 :
          if (filepath[0]) {
            return "MakeCase ExtendedLambda " + Util$AgdaModeVscode.Pretty.array(filepath[1]);
          } else {
            return "MakeCase Function " + Util$AgdaModeVscode.Pretty.array(filepath[1]);
          }
      case /* SolveAll */6 :
          return "SolveAll " + Util$AgdaModeVscode.Pretty.array(Belt_Array.map(filepath[0], (function (param) {
                            return String(param[0]) + (" " + param[1]);
                          })));
      case /* DisplayInfo */7 :
          return "DisplayInfo " + toString(filepath[0]);
      case /* RunningInfo */8 :
          return "RunningInfo " + (String(filepath[0]) + (" " + filepath[1]));
      
    }
  }
}

function parseWithPriority(_priority, tokens) {
  if (!tokens.tag) {
    return /* Error */Caml_chrome_debugger.variant("Error", 1, [/* Response */Caml_chrome_debugger.variant("Response", 1, [
                  0,
                  tokens
                ])]);
  }
  var xs = tokens[0];
  var match = Belt_Array.get(xs, 0);
  var exit = 0;
  if (match === undefined) {
    return /* Error */Caml_chrome_debugger.variant("Error", 1, [/* Response */Caml_chrome_debugger.variant("Response", 1, [
                  14,
                  tokens
                ])]);
  }
  if (match.tag) {
    return /* Error */Caml_chrome_debugger.variant("Error", 1, [/* Response */Caml_chrome_debugger.variant("Response", 1, [
                  14,
                  tokens
                ])]);
  }
  switch (match[0]) {
    case "agda2-abort-done" :
        return /* Ok */Caml_chrome_debugger.variant("Ok", 0, [/* DoneAborting */4]);
    case "agda2-give-action" :
        var match$1 = Belt_Array.get(xs, 1);
        if (match$1 !== undefined && !match$1.tag) {
          return Belt_Option.mapWithDefault(Belt_Option.flatMap(Parser$AgdaModeVscode.$$int(match$1[0]), (function (i) {
                            var match = Belt_Array.get(xs, 2);
                            if (match === undefined) {
                              return ;
                            }
                            if (match.tag) {
                              return ;
                            }
                            var result = match[0];
                            switch (result) {
                              case "no-paren" :
                                  return /* GiveAction */Caml_chrome_debugger.variant("GiveAction", 4, [
                                            i,
                                            /* NoParen */1
                                          ]);
                              case "paren" :
                                  return /* GiveAction */Caml_chrome_debugger.variant("GiveAction", 4, [
                                            i,
                                            /* Paren */0
                                          ]);
                              default:
                                return /* GiveAction */Caml_chrome_debugger.variant("GiveAction", 4, [
                                          i,
                                          /* String */Caml_chrome_debugger.simpleVariant("String", [result])
                                        ]);
                            }
                          })), /* Error */Caml_chrome_debugger.variant("Error", 1, [/* Response */Caml_chrome_debugger.variant("Response", 1, [
                            6,
                            tokens
                          ])]), (function (x) {
                        return /* Ok */Caml_chrome_debugger.variant("Ok", 0, [x]);
                      }));
        } else {
          return /* Error */Caml_chrome_debugger.variant("Error", 1, [/* Response */Caml_chrome_debugger.variant("Response", 1, [
                        7,
                        tokens
                      ])]);
        }
    case "agda2-goals-action" :
        var xs$1 = Belt_Array.get(xs, 1);
        if (xs$1 !== undefined) {
          return /* Ok */Caml_chrome_debugger.variant("Ok", 0, [/* InteractionPoints */Caml_chrome_debugger.variant("InteractionPoints", 3, [Belt_Array.keepMap(Parser$AgdaModeVscode.SExpression.flatten(xs$1), Parser$AgdaModeVscode.$$int)])]);
        } else {
          return /* Error */Caml_chrome_debugger.variant("Error", 1, [/* Response */Caml_chrome_debugger.variant("Response", 1, [
                        5,
                        tokens
                      ])]);
        }
    case "agda2-highlight-add-annotations" :
        var match$2 = Belt_Array.get(xs, 1);
        if (match$2 === undefined) {
          return /* Ok */Caml_chrome_debugger.variant("Ok", 0, [/* HighlightingInfoDirect */0]);
        }
        if (match$2.tag) {
          return /* Ok */Caml_chrome_debugger.variant("Ok", 0, [/* HighlightingInfoDirect */0]);
        }
        switch (match$2[0]) {
          case "nil" :
          case "remove" :
              return /* Ok */Caml_chrome_debugger.variant("Ok", 0, [/* HighlightingInfoDirect */0]);
          default:
            return /* Ok */Caml_chrome_debugger.variant("Ok", 0, [/* HighlightingInfoDirect */0]);
        }
    case "agda2-highlight-clear" :
        return /* Ok */Caml_chrome_debugger.variant("Ok", 0, [/* ClearHighlighting */3]);
    case "agda2-highlight-load-and-delete-action" :
        var match$3 = Belt_Array.get(xs, 1);
        if (match$3 !== undefined && !match$3.tag) {
          return /* Ok */Caml_chrome_debugger.variant("Ok", 0, [/* HighlightingInfoIndirect */Caml_chrome_debugger.variant("HighlightingInfoIndirect", 0, [match$3[0]])]);
        } else {
          return /* Error */Caml_chrome_debugger.variant("Error", 1, [/* Response */Caml_chrome_debugger.variant("Response", 1, [
                        2,
                        tokens
                      ])]);
        }
    case "agda2-info-action" :
    case "agda2-info-action-and-copy" :
        exit = 1;
        break;
    case "agda2-make-case-action" :
        var xs$2 = Belt_Array.get(xs, 1);
        if (xs$2 !== undefined) {
          return /* Ok */Caml_chrome_debugger.variant("Ok", 0, [/* MakeCase */Caml_chrome_debugger.variant("MakeCase", 5, [
                        /* Function */0,
                        Parser$AgdaModeVscode.SExpression.flatten(xs$2)
                      ])]);
        } else {
          return /* Error */Caml_chrome_debugger.variant("Error", 1, [/* Response */Caml_chrome_debugger.variant("Response", 1, [
                        8,
                        tokens
                      ])]);
        }
    case "agda2-make-case-action-extendlam" :
        var xs$3 = Belt_Array.get(xs, 1);
        if (xs$3 !== undefined) {
          return /* Ok */Caml_chrome_debugger.variant("Ok", 0, [/* MakeCase */Caml_chrome_debugger.variant("MakeCase", 5, [
                        /* ExtendedLambda */1,
                        Parser$AgdaModeVscode.SExpression.flatten(xs$3)
                      ])]);
        } else {
          return /* Error */Caml_chrome_debugger.variant("Error", 1, [/* Response */Caml_chrome_debugger.variant("Response", 1, [
                        9,
                        tokens
                      ])]);
        }
    case "agda2-maybe-goto" :
        var match$4 = Belt_Array.get(xs, 1);
        if (match$4 === undefined) {
          return /* Error */Caml_chrome_debugger.variant("Error", 1, [/* Response */Caml_chrome_debugger.variant("Response", 1, [
                        4,
                        tokens
                      ])]);
        }
        if (!match$4.tag) {
          return /* Error */Caml_chrome_debugger.variant("Error", 1, [/* Response */Caml_chrome_debugger.variant("Response", 1, [
                        4,
                        tokens
                      ])]);
        }
        var match$5 = match$4[0];
        if (match$5.length !== 3) {
          return /* Error */Caml_chrome_debugger.variant("Error", 1, [/* Response */Caml_chrome_debugger.variant("Response", 1, [
                        4,
                        tokens
                      ])]);
        }
        var filepath = match$5[0];
        if (filepath.tag) {
          return /* Error */Caml_chrome_debugger.variant("Error", 1, [/* Response */Caml_chrome_debugger.variant("Response", 1, [
                        4,
                        tokens
                      ])]);
        }
        var filepath$1 = filepath[0];
        var index$prime = match$5[2];
        if (index$prime.tag) {
          return /* Error */Caml_chrome_debugger.variant("Error", 1, [/* Response */Caml_chrome_debugger.variant("Response", 1, [
                        4,
                        tokens
                      ])]);
        } else {
          return Belt_Option.mapWithDefault(Belt_Option.flatMap(Parser$AgdaModeVscode.$$int(index$prime[0]), (function (index) {
                            return /* JumpToError */Caml_chrome_debugger.variant("JumpToError", 2, [
                                      filepath$1,
                                      index
                                    ]);
                          })), /* Error */Caml_chrome_debugger.variant("Error", 1, [/* Response */Caml_chrome_debugger.variant("Response", 1, [
                            3,
                            tokens
                          ])]), (function (x) {
                        return /* Ok */Caml_chrome_debugger.variant("Ok", 0, [x]);
                      }));
        }
    case "agda2-solveAll-action" :
        var xs$4 = Belt_Array.get(xs, 1);
        if (xs$4 === undefined) {
          return /* Error */Caml_chrome_debugger.variant("Error", 1, [/* Response */Caml_chrome_debugger.variant("Response", 1, [
                        10,
                        tokens
                      ])]);
        }
        var tokens$1 = Parser$AgdaModeVscode.SExpression.flatten(xs$4);
        var isEven = function (i) {
          return i % 2 === 0;
        };
        var i = {
          contents: 0
        };
        var solutions = Belt_Array.keepMap(tokens$1, (function (token) {
                var solution = isEven(i.contents) ? Belt_Option.flatMap(Parser$AgdaModeVscode.$$int(token), (function (index) {
                          return Belt_Option.map(Belt_Array.get(tokens$1, i.contents + 1 | 0), (function (s) {
                                        return /* tuple */[
                                                index,
                                                s
                                              ];
                                      }));
                        })) : undefined;
                i.contents = i.contents + 1 | 0;
                return solution;
              }));
        return /* Ok */Caml_chrome_debugger.variant("Ok", 0, [/* SolveAll */Caml_chrome_debugger.variant("SolveAll", 6, [solutions])]);
    case "agda2-status-action" :
        var match$6 = Belt_Array.get(xs, 1);
        if (match$6 === undefined) {
          return /* Ok */Caml_chrome_debugger.variant("Ok", 0, [/* NoStatus */1]);
        }
        if (match$6.tag) {
          return /* Ok */Caml_chrome_debugger.variant("Ok", 0, [/* NoStatus */1]);
        }
        var pulp = match$6[0].split(",");
        return /* Ok */Caml_chrome_debugger.variant("Ok", 0, [/* Status */Caml_chrome_debugger.variant("Status", 1, [
                      pulp.includes("ShowImplicit"),
                      pulp.includes("Checked")
                    ])]);
    case "agda2-verbose" :
        var match$7 = Belt_Array.get(xs, 1);
        if (match$7 !== undefined && !match$7.tag) {
          return /* Ok */Caml_chrome_debugger.variant("Ok", 0, [/* RunningInfo */Caml_chrome_debugger.variant("RunningInfo", 8, [
                        2,
                        match$7[0]
                      ])]);
        } else {
          return /* Error */Caml_chrome_debugger.variant("Error", 1, [/* Response */Caml_chrome_debugger.variant("Response", 1, [
                        13,
                        tokens
                      ])]);
        }
    default:
      return /* Error */Caml_chrome_debugger.variant("Error", 1, [/* Response */Caml_chrome_debugger.variant("Response", 1, [
                    14,
                    tokens
                  ])]);
  }
  if (exit === 1) {
    var match$8 = Belt_Array.get(xs, 1);
    var exit$1 = 0;
    if (match$8 !== undefined && !match$8.tag) {
      if (match$8[0] === "*Type-checking*") {
        var match$9 = Belt_Array.get(xs, 3);
        if (match$9 === undefined) {
          return /* Ok */Caml_chrome_debugger.variant("Ok", 0, [/* ClearRunningInfo */2]);
        }
        if (match$9.tag) {
          return /* Ok */Caml_chrome_debugger.variant("Ok", 0, [/* ClearRunningInfo */2]);
        }
        if (match$9[0] !== "t") {
          return /* Ok */Caml_chrome_debugger.variant("Ok", 0, [/* ClearRunningInfo */2]);
        }
        var match$10 = Belt_Array.get(xs, 2);
        if (match$10 !== undefined && !match$10.tag) {
          return /* Ok */Caml_chrome_debugger.variant("Ok", 0, [/* RunningInfo */Caml_chrome_debugger.variant("RunningInfo", 8, [
                        1,
                        match$10[0]
                      ])]);
        } else {
          return /* Error */Caml_chrome_debugger.variant("Error", 1, [/* Response */Caml_chrome_debugger.variant("Response", 1, [
                        11,
                        tokens
                      ])]);
        }
      }
      exit$1 = 2;
    } else {
      exit$1 = 2;
    }
    if (exit$1 === 2) {
      var info = parse(xs.slice(1));
      if (info !== undefined) {
        return /* Ok */Caml_chrome_debugger.variant("Ok", 0, [/* DisplayInfo */Caml_chrome_debugger.variant("DisplayInfo", 7, [info])]);
      } else {
        return /* Error */Caml_chrome_debugger.variant("Error", 1, [/* Response */Caml_chrome_debugger.variant("Response", 1, [
                      12,
                      tokens
                    ])]);
      }
    }
    
  }
  
}

function parse$1(tokens) {
  if (!tokens.tag) {
    return parseWithPriority(0, tokens);
  }
  var match = tokens[0];
  if (match.length !== 3) {
    return parseWithPriority(0, tokens);
  }
  var match$1 = match[0];
  if (!match$1.tag) {
    return parseWithPriority(0, tokens);
  }
  var match$2 = match$1[0];
  if (match$2.length !== 3) {
    return parseWithPriority(0, tokens);
  }
  var match$3 = match$2[0];
  if (match$3.tag) {
    return parseWithPriority(0, tokens);
  }
  if (match$3[0] !== "last") {
    return parseWithPriority(0, tokens);
  }
  var match$4 = match$2[1];
  if (match$4.tag) {
    return parseWithPriority(0, tokens);
  }
  if (match$4[0] !== ".") {
    return parseWithPriority(0, tokens);
  }
  var priority = match$2[2];
  if (priority.tag) {
    return parseWithPriority(0, tokens);
  }
  var match$5 = match[1];
  if (match$5.tag) {
    return parseWithPriority(0, tokens);
  }
  if (match$5[0] !== ".") {
    return parseWithPriority(0, tokens);
  }
  var xs = match[2];
  return parseWithPriority(Caml_format.caml_int_of_string(priority[0]), xs);
}

var Token;

exports.Token = Token;
exports.Info = Info;
exports.toString = toString$1;
exports.parseWithPriority = parseWithPriority;
exports.parse = parse$1;
/* Util-AgdaModeVscode Not a pure module */
