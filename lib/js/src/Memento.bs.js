// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Vscode = require("vscode");
var Caml_option = require("rescript/lib/js/caml_option.js");
var Core__Array = require("@rescript/core/lib/js/src/Core__Array.bs.js");
var Core__Option = require("@rescript/core/lib/js/src/Core__Option.bs.js");
var Connection__Candidate$AgdaModeVscode = require("./Connection/Connection__Candidate.bs.js");

var Any = {};

function make(memento) {
  if (memento !== undefined) {
    return {
            TAG: "Memento",
            _0: Caml_option.valFromOption(memento)
          };
  } else {
    return {
            TAG: "Mock",
            _0: {}
          };
  }
}

function get(context, key) {
  if (context.TAG === "Memento") {
    return context._0.get(key);
  } else {
    return context._0[key];
  }
}

function set(context, key, value) {
  if (context.TAG === "Memento") {
    return context._0.update(key, value);
  } else {
    return Promise.resolve((context._0[key] = value, undefined));
  }
}

function toString(context) {
  if (context.TAG === "Memento") {
    var context$1 = context._0;
    var entries = context$1.keys().map(function (key) {
          var value = context$1.get(key);
          if (value !== undefined) {
            return key + ": " + Caml_option.valFromOption(value);
          } else {
            return key + ": None";
          }
        });
    return "Memento: {\n" + entries.join("\n") + "}";
  }
  var entries$1 = Object.entries(context._0).map(function (param) {
        return param[0] + ": " + String(param[1]);
      });
  return "Mock: {\n" + entries$1.join("\n") + "}";
}

function kindToString(kind) {
  if (typeof kind !== "object") {
    return "Unknown";
  }
  if (kind.TAG === "Agda") {
    var version = kind._0;
    if (version !== undefined) {
      return "Agda v" + version;
    } else {
      return "Agda (version unknown)";
    }
  }
  if (kind._0 === "Native") {
    var match = kind._1;
    if (match === undefined) {
      return "Agda Language Server (version unknown)";
    }
    var agdaVersion = match[1];
    var alsVersion = match[0];
    if (alsVersion === "dev") {
      return "Agda v" + agdaVersion + " Language Server (dev build)";
    } else {
      return "Agda v" + agdaVersion + " Language Server v" + alsVersion;
    }
  }
  var match$1 = kind._1;
  if (match$1 === undefined) {
    return "Agda Language Server (version unknown) WASM";
  }
  var agdaVersion$1 = match$1[1];
  var alsVersion$1 = match$1[0];
  if (alsVersion$1 === "dev") {
    return "Agda v" + agdaVersion$1 + " Language Server (dev build) WASM";
  } else {
    return "Agda v" + agdaVersion$1 + " Language Server v" + alsVersion$1 + " WASM";
  }
}

var key = "resolvedCandidateMetadata";

function readEntries(memento, key) {
  if (memento.TAG === "Memento") {
    return memento._0.get(key, {});
  }
  var value = memento._0[key];
  if (value !== undefined) {
    return Caml_option.valFromOption(value);
  } else {
    return {};
  }
}

async function writeEntries(memento, cache) {
  return await set(memento, key, cache);
}

function entries(memento) {
  return readEntries(memento, key);
}

function get$1(memento, resolved) {
  var cache = readEntries(memento, key);
  return cache[Connection__Candidate$AgdaModeVscode.Resolved.toString(resolved)];
}

async function setKind(memento, resolved, kind) {
  var cache = readEntries(memento, key);
  var entry_timestamp = new Date();
  var entry = {
    kind: kind,
    timestamp: entry_timestamp,
    error: undefined
  };
  cache[Connection__Candidate$AgdaModeVscode.Resolved.toString(resolved)] = entry;
  return await writeEntries(memento, cache);
}

async function setError(memento, resolved, error) {
  var cache = readEntries(memento, key);
  var resolvedKey = Connection__Candidate$AgdaModeVscode.Resolved.toString(resolved);
  var existingEntry = cache[resolvedKey];
  var existingKind = existingEntry !== undefined ? existingEntry.kind : "Unknown";
  var entry_timestamp = new Date();
  var entry_error = error;
  var entry = {
    kind: existingKind,
    timestamp: entry_timestamp,
    error: entry_error
  };
  cache[resolvedKey] = entry;
  return await writeEntries(memento, cache);
}

function resourceUriFromKey(resolvedKey) {
  var index = resolvedKey.lastIndexOf(" => ");
  if (index === -1) {
    return ;
  }
  var resourceKey = resolvedKey.slice(index + 4 | 0);
  return Caml_option.some(Vscode.Uri.parse(resourceKey));
}

async function clearUnderDirectories(memento, directories) {
  var nextCache = Core__Array.reduce(Object.entries(readEntries(memento, key)), {}, (function (cache, param) {
          var resolvedKey = param[0];
          var resource = resourceUriFromKey(resolvedKey);
          var shouldRemove;
          if (resource !== undefined) {
            var resource$1 = Caml_option.valFromOption(resource);
            shouldRemove = directories.some(function (directory) {
                  return Connection__Candidate$AgdaModeVscode.isUnderDirectory({
                              TAG: "Resource",
                              _0: resource$1
                            }, directory);
                });
          } else {
            shouldRemove = false;
          }
          if (shouldRemove) {
            return cache;
          } else {
            cache[resolvedKey] = param[1];
            return cache;
          }
        }));
  return await writeEntries(memento, nextCache);
}

async function clear(memento) {
  return await writeEntries(memento, {});
}

function makeTimestampKey(username, repository) {
  return "alsReleaseCacheTimestamp_" + username + "/" + repository;
}

function makeReleasesKey(username, repository) {
  return "alsReleaseCache_" + username + "/" + repository;
}

function getTimestamp(memento, username, repository) {
  var key = makeTimestampKey(username, repository);
  return Core__Option.map(get(memento, key), (function (prim) {
                return new Date(prim);
              }));
}

async function setTimestamp(memento, username, repository, timestamp) {
  var key = makeTimestampKey(username, repository);
  return await set(memento, key, timestamp.toString());
}

function getReleases(memento, username, repository) {
  var key = makeReleasesKey(username, repository);
  return get(memento, key);
}

async function setReleases(memento, username, repository, releases) {
  var key = makeReleasesKey(username, repository);
  return await set(memento, key, releases);
}

function getCacheAgeInSecs(memento, username, repository) {
  var timestamp = getTimestamp(memento, username, repository);
  if (timestamp === undefined) {
    return ;
  }
  var now = new Date();
  var ageInMs = now.getTime() - Caml_option.valFromOption(timestamp).getTime();
  return ageInMs / 1000.0 | 0;
}

async function clear$1(memento, username, repository) {
  var timestampKey = makeTimestampKey(username, repository);
  var releasesKey = makeReleasesKey(username, repository);
  await set(memento, timestampKey, undefined);
  return await set(memento, releasesKey, undefined);
}

var key$1 = "preferredCandidate";

function get$2(memento) {
  return get(memento, key$1);
}

async function set$1(memento, path) {
  return await set(memento, key$1, path);
}

async function clear$2(memento) {
  return await set$1(memento, undefined);
}

var key$2 = "selectedChannel";

function get$3(memento) {
  return get(memento, key$2);
}

async function set$2(memento, channel) {
  return await set(memento, key$2, channel);
}

async function clear$3(memento) {
  return await set(memento, key$2, undefined);
}

var Module_ResolvedMetadata = {
  kindToString: kindToString,
  entries: entries,
  get: get$1,
  setKind: setKind,
  setError: setError,
  clearUnderDirectories: clearUnderDirectories,
  clear: clear
};

var Module_ALSReleaseCache = {
  getTimestamp: getTimestamp,
  setTimestamp: setTimestamp,
  getReleases: getReleases,
  setReleases: setReleases,
  getCacheAgeInSecs: getCacheAgeInSecs,
  clear: clear$1
};

var Module_PreferredCandidate = {
  get: get$2,
  set: set$1,
  clear: clear$2
};

var Module_SelectedChannel = {
  get: get$3,
  set: set$2,
  clear: clear$3
};

var Module = {
  make: make,
  toString: toString,
  ResolvedMetadata: Module_ResolvedMetadata,
  ALSReleaseCache: Module_ALSReleaseCache,
  PreferredCandidate: Module_PreferredCandidate,
  SelectedChannel: Module_SelectedChannel
};

var ResolvedMetadata = Module_ResolvedMetadata;

var ALSReleaseCache = Module_ALSReleaseCache;

var PreferredCandidate = Module_PreferredCandidate;

var SelectedChannel = Module_SelectedChannel;

exports.Any = Any;
exports.Module = Module;
exports.make = make;
exports.toString = toString;
exports.ResolvedMetadata = ResolvedMetadata;
exports.ALSReleaseCache = ALSReleaseCache;
exports.PreferredCandidate = PreferredCandidate;
exports.SelectedChannel = SelectedChannel;
/* vscode Not a pure module */
