// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Caml_option = require("rescript/lib/js/caml_option.js");
var Core__Array = require("@rescript/core/lib/js/src/Core__Array.bs.js");
var Core__Option = require("@rescript/core/lib/js/src/Core__Option.bs.js");
var Connection__Candidate$AgdaModeVscode = require("./Connection/Connection__Candidate.bs.js");

var Any = {};

function make(memento) {
  if (memento !== undefined) {
    return {
            TAG: "Memento",
            _0: Caml_option.valFromOption(memento)
          };
  } else {
    return {
            TAG: "Mock",
            _0: {}
          };
  }
}

function get(context, key) {
  if (context.TAG === "Memento") {
    return context._0.get(key);
  } else {
    return context._0[key];
  }
}

function getWithDefault(context, key, defaultValue) {
  if (context.TAG === "Memento") {
    return context._0.get(key, defaultValue);
  }
  var value = context._0[key];
  if (value !== undefined) {
    return Caml_option.valFromOption(value);
  } else {
    return defaultValue;
  }
}

function set(context, key, value) {
  if (context.TAG === "Memento") {
    return context._0.update(key, value);
  } else {
    return Promise.resolve((context._0[key] = value, undefined));
  }
}

function toString(context) {
  if (context.TAG === "Memento") {
    var context$1 = context._0;
    var entries = context$1.keys().map(function (key) {
          var value = context$1.get(key);
          if (value !== undefined) {
            return key + ": " + Caml_option.valFromOption(value);
          } else {
            return key + ": None";
          }
        });
    return "Memento: {\n" + entries.join("\n") + "}";
  }
  var entries$1 = Object.entries(context._0).map(function (param) {
        return param[0] + ": " + String(param[1]);
      });
  return "Mock: {\n" + entries$1.join("\n") + "}";
}

function endpointToString(endpoint) {
  if (typeof endpoint !== "object") {
    return "Unknown";
  }
  if (endpoint.TAG === "Agda") {
    var version = endpoint._0;
    if (version !== undefined) {
      return "Agda v" + version;
    } else {
      return "Agda (version unknown)";
    }
  }
  var match = endpoint._0;
  if (match !== undefined) {
    return "Agda v" + match[1] + " Language Server v" + match[0];
  } else {
    return "Agda Language Server (version unknown)";
  }
}

var key = "endpointVersion";

function entries(memento) {
  if (memento.TAG === "Memento") {
    return memento._0.get(key, {});
  }
  var value = memento._0[key];
  if (value !== undefined) {
    return Caml_option.valFromOption(value);
  } else {
    return {};
  }
}

function get$1(memento, candidateKey) {
  var cache = getWithDefault(memento, key, {});
  return cache[candidateKey];
}

function lookupCandidateInCache(cache, candidateKey) {
  var entry = cache[candidateKey];
  if (entry !== undefined) {
    return [
            candidateKey,
            entry
          ];
  }
  var candidate = Connection__Candidate$AgdaModeVscode.make(candidateKey);
  return Core__Array.findMap(Object.entries(cache), (function (param) {
                var existingKey = param[0];
                if (Connection__Candidate$AgdaModeVscode.equal(Connection__Candidate$AgdaModeVscode.make(existingKey), candidate)) {
                  return [
                          existingKey,
                          param[1]
                        ];
                }
                
              }));
}

function lookupCandidate(memento, candidateKey) {
  var cache = getWithDefault(memento, key, {});
  return lookupCandidateInCache(cache, candidateKey);
}

async function setVersion(memento, candidateKey, endpoint) {
  var cache = getWithDefault(memento, key, {});
  var entry_timestamp = new Date();
  var entry = {
    endpoint: endpoint,
    timestamp: entry_timestamp,
    error: undefined
  };
  cache[candidateKey] = entry;
  return await set(memento, key, cache);
}

async function setError(memento, candidateKey, error) {
  var cache = getWithDefault(memento, key, {});
  var existingEntry = cache[candidateKey];
  var existingEndpoint = existingEntry !== undefined ? existingEntry.endpoint : "Unknown";
  var entry_timestamp = new Date();
  var entry_error = error;
  var entry = {
    endpoint: existingEndpoint,
    timestamp: entry_timestamp,
    error: entry_error
  };
  cache[candidateKey] = entry;
  return await set(memento, key, cache);
}

async function updateCandidate(memento, candidateKey, makeEntry) {
  var cache = getWithDefault(memento, key, {});
  var match = lookupCandidateInCache(cache, candidateKey);
  var match$1 = match !== undefined ? [
      match[0],
      match[1]
    ] : [
      candidateKey,
      undefined
    ];
  var entry = makeEntry(match$1[1]);
  cache[match$1[0]] = entry;
  return await set(memento, key, cache);
}

async function setVersionForCandidate(memento, candidateKey, endpoint) {
  return await updateCandidate(memento, candidateKey, (function (_existingEntry) {
                return {
                        endpoint: endpoint,
                        timestamp: new Date(),
                        error: undefined
                      };
              }));
}

async function setErrorForCandidate(memento, candidateKey, error) {
  return await updateCandidate(memento, candidateKey, (function (existingEntry) {
                var endpoint = existingEntry !== undefined ? existingEntry.endpoint : "Unknown";
                return {
                        endpoint: endpoint,
                        timestamp: new Date(),
                        error: error
                      };
              }));
}

async function syncWithPaths(memento, discoveredEndpoints) {
  var cache = getWithDefault(memento, key, {});
  var newCache = {};
  Object.entries(discoveredEndpoints).forEach(function (param) {
        var discoveredEndpoint = param[1];
        var path = param[0];
        var existingEntry = cache[path];
        if (existingEntry !== undefined) {
          var match = existingEntry.endpoint;
          var updatedEndpoint;
          updatedEndpoint = typeof match !== "object" && discoveredEndpoint !== "Unknown" ? discoveredEndpoint : match;
          var updatedEntry_timestamp = new Date();
          var updatedEntry_error = existingEntry.error;
          var updatedEntry = {
            endpoint: updatedEndpoint,
            timestamp: updatedEntry_timestamp,
            error: updatedEntry_error
          };
          newCache[path] = updatedEntry;
          return ;
        }
        var entry_timestamp = new Date();
        var entry = {
          endpoint: discoveredEndpoint,
          timestamp: entry_timestamp,
          error: undefined
        };
        newCache[path] = entry;
      });
  return await set(memento, key, newCache);
}

async function clear(memento) {
  return await set(memento, key, {});
}

var key$1 = "resolvedMetadata";

function entries$1(memento) {
  if (memento.TAG === "Memento") {
    return memento._0.get(key$1, {});
  }
  var value = memento._0[key$1];
  if (value !== undefined) {
    return Caml_option.valFromOption(value);
  } else {
    return {};
  }
}

function get$2(memento, resolved) {
  var cache = getWithDefault(memento, key$1, {});
  return cache[Connection__Candidate$AgdaModeVscode.Resolved.toString(resolved)];
}

async function setVersion$1(memento, resolved, endpoint) {
  var cache = getWithDefault(memento, key$1, {});
  var entry_timestamp = new Date();
  var entry = {
    endpoint: endpoint,
    timestamp: entry_timestamp,
    error: undefined
  };
  cache[Connection__Candidate$AgdaModeVscode.Resolved.toString(resolved)] = entry;
  return await set(memento, key$1, cache);
}

async function setError$1(memento, resolved, error) {
  var cache = getWithDefault(memento, key$1, {});
  var resolvedKey = Connection__Candidate$AgdaModeVscode.Resolved.toString(resolved);
  var existingEntry = cache[resolvedKey];
  var existingEndpoint = existingEntry !== undefined ? existingEntry.endpoint : "Unknown";
  var entry_timestamp = new Date();
  var entry_error = error;
  var entry = {
    endpoint: existingEndpoint,
    timestamp: entry_timestamp,
    error: entry_error
  };
  cache[resolvedKey] = entry;
  return await set(memento, key$1, cache);
}

async function clear$1(memento) {
  return await set(memento, key$1, {});
}

function makeTimestampKey(username, repository) {
  return "alsReleaseCacheTimestamp_" + username + "/" + repository;
}

function makeReleasesKey(username, repository) {
  return "alsReleaseCache_" + username + "/" + repository;
}

function getTimestamp(memento, username, repository) {
  var key = makeTimestampKey(username, repository);
  return Core__Option.map(get(memento, key), (function (prim) {
                return new Date(prim);
              }));
}

async function setTimestamp(memento, username, repository, timestamp) {
  var key = makeTimestampKey(username, repository);
  return await set(memento, key, timestamp.toString());
}

function getReleases(memento, username, repository) {
  var key = makeReleasesKey(username, repository);
  return get(memento, key);
}

async function setReleases(memento, username, repository, releases) {
  var key = makeReleasesKey(username, repository);
  return await set(memento, key, releases);
}

function getCacheAgeInSecs(memento, username, repository) {
  var timestamp = getTimestamp(memento, username, repository);
  if (timestamp === undefined) {
    return ;
  }
  var now = new Date();
  var ageInMs = now.getTime() - Caml_option.valFromOption(timestamp).getTime();
  return ageInMs / 1000.0 | 0;
}

async function clear$2(memento, username, repository) {
  var timestampKey = makeTimestampKey(username, repository);
  var releasesKey = makeReleasesKey(username, repository);
  await set(memento, timestampKey, undefined);
  return await set(memento, releasesKey, undefined);
}

var key$2 = "pickedConnection";

function get$3(memento) {
  return get(memento, key$2);
}

async function set$1(memento, path) {
  return await set(memento, key$2, path);
}

async function clear$3(memento) {
  return await set$1(memento, undefined);
}

var key$3 = "selectedChannel";

function get$4(memento) {
  return get(memento, key$3);
}

async function set$2(memento, channel) {
  return await set(memento, key$3, channel);
}

async function clear$4(memento) {
  return await set(memento, key$3, undefined);
}

var Module_Endpoints = {
  endpointToString: endpointToString,
  entries: entries,
  get: get$1,
  lookupCandidate: lookupCandidate,
  setVersion: setVersion,
  setError: setError,
  setVersionForCandidate: setVersionForCandidate,
  setErrorForCandidate: setErrorForCandidate,
  syncWithPaths: syncWithPaths,
  clear: clear
};

var Module_ResolvedMetadata = {
  entries: entries$1,
  get: get$2,
  setVersion: setVersion$1,
  setError: setError$1,
  clear: clear$1
};

var Module_ALSReleaseCache = {
  getTimestamp: getTimestamp,
  setTimestamp: setTimestamp,
  getReleases: getReleases,
  setReleases: setReleases,
  getCacheAgeInSecs: getCacheAgeInSecs,
  clear: clear$2
};

var Module_PickedConnection = {
  get: get$3,
  set: set$1,
  clear: clear$3
};

var Module_SelectedChannel = {
  get: get$4,
  set: set$2,
  clear: clear$4
};

var Module = {
  make: make,
  toString: toString,
  Endpoints: Module_Endpoints,
  ResolvedMetadata: Module_ResolvedMetadata,
  ALSReleaseCache: Module_ALSReleaseCache,
  PickedConnection: Module_PickedConnection,
  SelectedChannel: Module_SelectedChannel
};

var Endpoints = Module_Endpoints;

var ResolvedMetadata = Module_ResolvedMetadata;

var ALSReleaseCache = Module_ALSReleaseCache;

var PickedConnection = Module_PickedConnection;

var SelectedChannel = Module_SelectedChannel;

exports.Any = Any;
exports.Module = Module;
exports.make = make;
exports.toString = toString;
exports.Endpoints = Endpoints;
exports.ResolvedMetadata = ResolvedMetadata;
exports.ALSReleaseCache = ALSReleaseCache;
exports.PickedConnection = PickedConnection;
exports.SelectedChannel = SelectedChannel;
/* Connection__Candidate-AgdaModeVscode Not a pure module */
