// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Vscode = require("vscode");
var Caml_obj = require("bs-platform/lib/js/caml_obj.js");
var Json_decode = require("@glennsl/bs-json/lib/js/src/Json_decode.bs.js");
var Json_encode = require("@glennsl/bs-json/lib/js/src/Json_encode.bs.js");

function toPosition($$document, offset) {
  return $$document.positionAt(offset);
}

function fromPosition($$document, position) {
  return $$document.offsetAt(position);
}

function encode(prim) {
  return prim;
}

var Offset = {
  toPosition: toPosition,
  fromPosition: fromPosition,
  decode: Json_decode.$$int,
  encode: encode
};

function toRange($$document, interval) {
  return new Vscode.Range($$document.positionAt(interval[0]), $$document.positionAt(interval[1]));
}

function fromRange($$document, range) {
  return [
          $$document.offsetAt(range.start),
          $$document.offsetAt(range.end)
        ];
}

function contains(interval, offset) {
  if (Caml_obj.caml_lessequal(interval[0], offset)) {
    return Caml_obj.caml_lessequal(offset, interval[1]);
  } else {
    return false;
  }
}

function decode(param) {
  return Json_decode.pair(Json_decode.$$int, Json_decode.$$int, param);
}

function encode$1(param) {
  return Json_encode.pair((function (prim) {
                return prim;
              }), (function (prim) {
                return prim;
              }), param);
}

var Interval = {
  toRange: toRange,
  fromRange: fromRange,
  contains: contains,
  decode: decode,
  encode: encode$1
};

exports.Offset = Offset;
exports.Interval = Interval;
/* vscode Not a pure module */
