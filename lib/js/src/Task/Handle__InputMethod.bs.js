// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Curry = require("bs-platform/lib/js/curry.js");
var $$Promise = require("reason-promise/lib/js/src/js/promise.bs.js");
var Belt_List = require("bs-platform/lib/js/belt_List.js");
var Belt_Array = require("bs-platform/lib/js/belt_Array.js");
var Caml_option = require("bs-platform/lib/js/caml_option.js");
var IM$AgdaModeVscode = require("../InputMethod/IM.bs.js");
var Util$AgdaModeVscode = require("../Util/Util.bs.js");
var Editor$AgdaModeVscode = require("../Editor.bs.js");

function handle(state, output) {
  var handle$1 = function (kind) {
    if (typeof kind === "number") {
      if (kind === /* Activate */0) {
        return $$Promise.resolved({
                    hd: {
                      TAG: 2,
                      _0: {
                        TAG: 2,
                        _0: /* Activate */0,
                        [Symbol.for("name")]: "InputMethod"
                      },
                      [Symbol.for("name")]: "ViewEvent"
                    },
                    tl: /* [] */0
                  });
      } else {
        return $$Promise.resolved({
                    hd: {
                      TAG: 2,
                      _0: {
                        TAG: 2,
                        _0: /* Deactivate */1,
                        [Symbol.for("name")]: "InputMethod"
                      },
                      [Symbol.for("name")]: "ViewEvent"
                    },
                    tl: /* [] */0
                  });
      }
    }
    if (!kind.TAG) {
      return $$Promise.resolved({
                  hd: {
                    TAG: 2,
                    _0: {
                      TAG: 2,
                      _0: {
                        _0: kind._0,
                        _1: kind._1,
                        _2: kind._2,
                        [Symbol.for("name")]: "Update"
                      },
                      [Symbol.for("name")]: "InputMethod"
                    },
                    [Symbol.for("name")]: "ViewEvent"
                  },
                  tl: /* [] */0
                });
    }
    var resolve = kind._1;
    var $$document = state.editor.document;
    var replacements = Belt_Array.map(kind._0, (function (param) {
            var range = IM$AgdaModeVscode.fromInterval($$document, param[0]);
            return [
                    range,
                    param[1]
                  ];
          }));
    return $$Promise.map(Editor$AgdaModeVscode.$$Text.batchReplace($$document, replacements), (function (param) {
                  Curry._1(resolve, undefined);
                  return /* [] */0;
                }));
  };
  return $$Promise.map(Util$AgdaModeVscode.oneByOne(Belt_Array.map(output, handle$1)), Belt_List.concatMany);
}

function runAndHandle(state, action) {
  return handle(state, IM$AgdaModeVscode.run(state.editorIM, Caml_option.some(state.editor), action));
}

function activate(state) {
  var $$document = state.editor.document;
  var intervals = Belt_Array.map(Editor$AgdaModeVscode.$$Selection.getMany(state.editor), (function (param) {
          return IM$AgdaModeVscode.toInterval($$document, param);
        }));
  return runAndHandle(state, {
              TAG: 0,
              _0: intervals,
              [Symbol.for("name")]: "Activate"
            });
}

function deactivate(state) {
  return runAndHandle(state, /* Deactivate */0);
}

var EditorIM = {
  handle: handle,
  runAndHandle: runAndHandle,
  activate: activate,
  deactivate: deactivate
};

var previous = {
  contents: ""
};

function handle$1(output) {
  var handle$2 = function (kind) {
    if (typeof kind === "number") {
      if (kind === /* Activate */0) {
        return {
                hd: {
                  TAG: 2,
                  _0: {
                    TAG: 2,
                    _0: /* Activate */0,
                    [Symbol.for("name")]: "InputMethod"
                  },
                  [Symbol.for("name")]: "ViewEvent"
                },
                tl: {
                  hd: {
                    TAG: 2,
                    _0: {
                      TAG: 1,
                      _0: previous.contents,
                      [Symbol.for("name")]: "PromptIMUpdate"
                    },
                    [Symbol.for("name")]: "ViewEvent"
                  },
                  tl: /* [] */0
                }
              };
      } else {
        return {
                hd: {
                  TAG: 2,
                  _0: {
                    TAG: 2,
                    _0: /* Deactivate */1,
                    [Symbol.for("name")]: "InputMethod"
                  },
                  [Symbol.for("name")]: "ViewEvent"
                },
                tl: /* [] */0
              };
      }
    }
    if (!kind.TAG) {
      return {
              hd: {
                TAG: 2,
                _0: {
                  TAG: 2,
                  _0: {
                    _0: kind._0,
                    _1: kind._1,
                    _2: kind._2,
                    [Symbol.for("name")]: "Update"
                  },
                  [Symbol.for("name")]: "InputMethod"
                },
                [Symbol.for("name")]: "ViewEvent"
              },
              tl: /* [] */0
            };
    }
    Curry._1(kind._1, undefined);
    var replaced = {
      contents: previous.contents
    };
    var delta = {
      contents: 0
    };
    var replace = function (param) {
      var t = param[1];
      var match = param[0];
      var end_ = match[1];
      var start = match[0];
      replaced.contents = replaced.contents.slice(0, delta.contents + start | 0) + t + replaced.contents.slice(delta.contents + end_ | 0);
      delta.contents = (delta.contents + t.length | 0) - (end_ - start | 0) | 0;
      
    };
    Belt_Array.forEach(kind._0, replace);
    previous.contents = replaced.contents;
    return {
            hd: {
              TAG: 2,
              _0: {
                TAG: 1,
                _0: replaced.contents,
                [Symbol.for("name")]: "PromptIMUpdate"
              },
              [Symbol.for("name")]: "ViewEvent"
            },
            tl: /* [] */0
          };
  };
  return Belt_List.concatMany(Belt_Array.map(output, handle$2));
}

function runAndHandle$1(state, action) {
  return $$Promise.resolved(handle$1(IM$AgdaModeVscode.run(state.promptIM, undefined, action)));
}

function change(state, next) {
  var deviseChange = function (previous, next) {
    var inputLength = next.length;
    var init = function (s) {
      return s.substring(0, s.length - 1 | 0);
    };
    var last = function (s) {
      return s.substring(s.length - 1 | 0);
    };
    if (init(next) === previous) {
      return {
              TAG: 1,
              _0: [{
                  offset: inputLength - 1 | 0,
                  insertedText: last(next),
                  replacedTextLength: 0
                }],
              [Symbol.for("name")]: "Change"
            };
    } else if (next === init(previous)) {
      return {
              TAG: 1,
              _0: [{
                  offset: inputLength,
                  insertedText: "",
                  replacedTextLength: 1
                }],
              [Symbol.for("name")]: "Change"
            };
    } else {
      return /* Deactivate */0;
    }
  };
  var input = deviseChange(previous.contents, next);
  var output = IM$AgdaModeVscode.run(state.promptIM, undefined, input);
  previous.contents = next;
  return $$Promise.resolved(handle$1(output));
}

function insertChar(state, $$char) {
  return change(state, previous.contents + $$char);
}

function activate$1(state, input) {
  var cursorOffset = input.length - 1 | 0;
  var input$1 = input.substring(0, cursorOffset);
  previous.contents = input$1;
  return runAndHandle$1(state, {
              TAG: 0,
              _0: [[
                  cursorOffset,
                  cursorOffset
                ]],
              [Symbol.for("name")]: "Activate"
            });
}

function deactivate$1(state) {
  return runAndHandle$1(state, /* Deactivate */0);
}

var PromptIM = {
  previous: previous,
  handle: handle$1,
  runAndHandle: runAndHandle$1,
  change: change,
  insertChar: insertChar,
  activate: activate$1,
  deactivate: deactivate$1
};

function isActivated(state) {
  if (IM$AgdaModeVscode.isActivated(state.editorIM)) {
    return /* Editor */0;
  } else if (IM$AgdaModeVscode.isActivated(state.promptIM)) {
    return /* Prompt */1;
  } else {
    return /* None */2;
  }
}

function deactivate$2(state) {
  var match = isActivated(state);
  switch (match) {
    case /* Editor */0 :
        return runAndHandle(state, /* Deactivate */0);
    case /* Prompt */1 :
        return runAndHandle$1(state, /* Deactivate */0);
    case /* None */2 :
        return $$Promise.resolved(/* [] */0);
    
  }
}

function activateEditorIM(state) {
  var match = isActivated(state);
  switch (match) {
    case /* Editor */0 :
        Belt_Array.forEach(Editor$AgdaModeVscode.Cursor.getMany(state.editor), (function (point) {
                Editor$AgdaModeVscode.$$Text.insert(state.editor.document, point, "\\");
                
              }));
        return runAndHandle(state, /* Deactivate */0);
    case /* Prompt */1 :
        return $$Promise.flatMap(runAndHandle$1(state, /* Deactivate */0), (function (tasks1) {
                      return $$Promise.map(activate(state), (function (tasks2) {
                                    return Belt_List.concat(tasks1, tasks2);
                                  }));
                    }));
    case /* None */2 :
        return activate(state);
    
  }
}

function shouldActivatePromptIM(input) {
  return input.endsWith("\\");
}

function activatePromptIM(state, input) {
  var match = isActivated(state);
  switch (match) {
    case /* Editor */0 :
        if (input.endsWith("\\")) {
          return $$Promise.flatMap(runAndHandle(state, /* Deactivate */0), (function (tasks1) {
                        return $$Promise.map(activate$1(state, input), (function (tasks2) {
                                      return Belt_List.concat(tasks1, tasks2);
                                    }));
                      }));
        } else {
          return $$Promise.resolved({
                      hd: {
                        TAG: 2,
                        _0: {
                          TAG: 1,
                          _0: input,
                          [Symbol.for("name")]: "PromptIMUpdate"
                        },
                        [Symbol.for("name")]: "ViewEvent"
                      },
                      tl: /* [] */0
                    });
        }
    case /* Prompt */1 :
        return change(state, input);
    case /* None */2 :
        if (input.endsWith("\\")) {
          return activate$1(state, input);
        } else {
          return $$Promise.resolved({
                      hd: {
                        TAG: 2,
                        _0: {
                          TAG: 1,
                          _0: input,
                          [Symbol.for("name")]: "PromptIMUpdate"
                        },
                        [Symbol.for("name")]: "ViewEvent"
                      },
                      tl: /* [] */0
                    });
        }
    
  }
}

function select(state, offset) {
  var match = isActivated(state);
  switch (match) {
    case /* Editor */0 :
        return runAndHandle(state, {
                    TAG: 2,
                    _0: offset,
                    [Symbol.for("name")]: "Select"
                  });
    case /* Prompt */1 :
        return runAndHandle$1(state, {
                    TAG: 2,
                    _0: offset,
                    [Symbol.for("name")]: "Select"
                  });
    case /* None */2 :
        return $$Promise.resolved(/* [] */0);
    
  }
}

function chooseSymbol(state, symbol) {
  var match = isActivated(state);
  switch (match) {
    case /* Editor */0 :
        return $$Promise.flatMap(runAndHandle(state, {
                        TAG: 3,
                        _0: {
                          _0: symbol,
                          [Symbol.for("name")]: "ChooseSymbol"
                        },
                        [Symbol.for("name")]: "Candidate"
                      }), (function (tasks1) {
                      return $$Promise.map(deactivate$2(state), (function (tasks2) {
                                    return Belt_List.concat(tasks1, tasks2);
                                  }));
                    }));
    case /* Prompt */1 :
        return $$Promise.flatMap(runAndHandle$1(state, {
                        TAG: 3,
                        _0: {
                          _0: symbol,
                          [Symbol.for("name")]: "ChooseSymbol"
                        },
                        [Symbol.for("name")]: "Candidate"
                      }), (function (tasks1) {
                      return $$Promise.map(deactivate$2(state), (function (tasks2) {
                                    return Belt_List.concat(tasks1, tasks2);
                                  }));
                    }));
    case /* None */2 :
        return $$Promise.resolved(/* [] */0);
    
  }
}

function insertChar$1(state, $$char) {
  var match = isActivated(state);
  switch (match) {
    case /* Editor */0 :
        var $$char$1 = $$char.charAt(0);
        var positions = Editor$AgdaModeVscode.Cursor.getMany(state.editor);
        var $$document = state.editor.document;
        return $$Promise.map(Editor$AgdaModeVscode.$$Text.batchInsert($$document, positions, $$char$1), (function (param) {
                      Editor$AgdaModeVscode.focus($$document);
                      return /* [] */0;
                    }));
    case /* Prompt */1 :
        return insertChar(state, $$char);
    case /* None */2 :
        return $$Promise.resolved(/* [] */0);
    
  }
}

function moveUp(state) {
  var match = isActivated(state);
  switch (match) {
    case /* Editor */0 :
        return runAndHandle(state, {
                    TAG: 3,
                    _0: /* BrowseUp */0,
                    [Symbol.for("name")]: "Candidate"
                  });
    case /* Prompt */1 :
        return runAndHandle$1(state, {
                    TAG: 3,
                    _0: /* BrowseUp */0,
                    [Symbol.for("name")]: "Candidate"
                  });
    case /* None */2 :
        return $$Promise.resolved(/* [] */0);
    
  }
}

function moveDown(state) {
  var match = isActivated(state);
  switch (match) {
    case /* Editor */0 :
        return runAndHandle(state, {
                    TAG: 3,
                    _0: /* BrowseDown */1,
                    [Symbol.for("name")]: "Candidate"
                  });
    case /* Prompt */1 :
        return runAndHandle$1(state, {
                    TAG: 3,
                    _0: /* BrowseDown */1,
                    [Symbol.for("name")]: "Candidate"
                  });
    case /* None */2 :
        return $$Promise.resolved(/* [] */0);
    
  }
}

function moveLeft(state) {
  var match = isActivated(state);
  switch (match) {
    case /* Editor */0 :
        return runAndHandle(state, {
                    TAG: 3,
                    _0: /* BrowseLeft */2,
                    [Symbol.for("name")]: "Candidate"
                  });
    case /* Prompt */1 :
        return runAndHandle$1(state, {
                    TAG: 3,
                    _0: /* BrowseLeft */2,
                    [Symbol.for("name")]: "Candidate"
                  });
    case /* None */2 :
        return $$Promise.resolved(/* [] */0);
    
  }
}

function moveRight(state) {
  var match = isActivated(state);
  switch (match) {
    case /* Editor */0 :
        return runAndHandle(state, {
                    TAG: 3,
                    _0: /* BrowseRight */3,
                    [Symbol.for("name")]: "Candidate"
                  });
    case /* Prompt */1 :
        return runAndHandle$1(state, {
                    TAG: 3,
                    _0: /* BrowseRight */3,
                    [Symbol.for("name")]: "Candidate"
                  });
    case /* None */2 :
        return $$Promise.resolved(/* [] */0);
    
  }
}

function handle$2(x) {
  if (typeof x === "number") {
    switch (x) {
      case /* Activate */0 :
          return {
                  hd: {
                    TAG: 8,
                    _0: activateEditorIM,
                    [Symbol.for("name")]: "WithStateP"
                  },
                  tl: /* [] */0
                };
      case /* BrowseUp */1 :
          return {
                  hd: {
                    TAG: 8,
                    _0: moveUp,
                    [Symbol.for("name")]: "WithStateP"
                  },
                  tl: /* [] */0
                };
      case /* BrowseRight */2 :
          return {
                  hd: {
                    TAG: 8,
                    _0: moveRight,
                    [Symbol.for("name")]: "WithStateP"
                  },
                  tl: /* [] */0
                };
      case /* BrowseDown */3 :
          return {
                  hd: {
                    TAG: 8,
                    _0: moveDown,
                    [Symbol.for("name")]: "WithStateP"
                  },
                  tl: /* [] */0
                };
      case /* BrowseLeft */4 :
          return {
                  hd: {
                    TAG: 8,
                    _0: moveLeft,
                    [Symbol.for("name")]: "WithStateP"
                  },
                  tl: /* [] */0
                };
      
    }
  } else {
    var $$char = x._0;
    return {
            hd: {
              TAG: 8,
              _0: (function (state) {
                  return insertChar$1(state, $$char);
                }),
              [Symbol.for("name")]: "WithStateP"
            },
            tl: /* [] */0
          };
  }
}

exports.EditorIM = EditorIM;
exports.PromptIM = PromptIM;
exports.isActivated = isActivated;
exports.deactivate = deactivate$2;
exports.activateEditorIM = activateEditorIM;
exports.shouldActivatePromptIM = shouldActivatePromptIM;
exports.activatePromptIM = activatePromptIM;
exports.select = select;
exports.chooseSymbol = chooseSymbol;
exports.insertChar = insertChar$1;
exports.moveUp = moveUp;
exports.moveDown = moveDown;
exports.moveLeft = moveLeft;
exports.moveRight = moveRight;
exports.handle = handle$2;
/* Promise Not a pure module */
