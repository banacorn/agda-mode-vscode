// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Caml = require("rescript/lib/js/caml.js");
var Vscode = require("vscode");
var Belt_Array = require("rescript/lib/js/belt_Array.js");
var Core__List = require("@rescript/core/lib/js/src/Core__List.bs.js");
var Caml_option = require("rescript/lib/js/caml_option.js");
var Core__Array = require("@rescript/core/lib/js/src/Core__Array.bs.js");
var Core__Option = require("@rescript/core/lib/js/src/Core__Option.bs.js");
var Util$AgdaModeVscode = require("./Util/Util.bs.js");
var Editor$AgdaModeVscode = require("./Editor.bs.js");
var AVLTree$AgdaModeVscode = require("./Util/AVLTree.bs.js");
var Resource$AgdaModeVscode = require("./Resource.bs.js");
var BinarySearchTree = require("@datastructures-js/binary-search-tree");

function decorate(editor, start, end, index) {
  var $$document = editor.document;
  var backgroundRange = new Vscode.Range($$document.positionAt(start), $$document.positionAt(end));
  var background = Editor$AgdaModeVscode.Decoration.highlightBackground(editor, "editor.selectionHighlightBackground", [backgroundRange]);
  var indexText = String(index);
  var indexRange = new Vscode.Range($$document.positionAt(start), $$document.positionAt(end - 2 | 0));
  var index$1 = Editor$AgdaModeVscode.Decoration.overlayText(editor, "editorLightBulb.foreground", indexText, indexRange);
  return [
          background,
          index$1
        ];
}

function make(editor, start, end, index) {
  var isQuestionMark = (start + 1 | 0) === end;
  return {
          index: index,
          start: start,
          end: end,
          decoration: isQuestionMark ? undefined : decorate(editor, start, end, index)
        };
}

function toString(goal) {
  return "#" + String(goal.index) + " [" + String(goal.start) + "-" + String(goal.end) + ")";
}

function makeInnerRange(goal, $$document) {
  return new Vscode.Range($$document.positionAt(goal.start + 2 | 0), $$document.positionAt(goal.end - 2 | 0));
}

function makeOuterRange(goal, $$document) {
  return new Vscode.Range($$document.positionAt(goal.start), $$document.positionAt(goal.end));
}

function make$1() {
  return {
          goals: new Map(),
          goalsWithoutIndices: new Map(),
          positions: new BinarySearchTree.BinarySearchTree(),
          isBusy: undefined,
          recentlyCaseSplited: undefined,
          goalPositionsFromRefine: []
        };
}

function size(self) {
  return self.goals.size;
}

function destroyGoal(self, goal) {
  Core__Option.forEach(goal.decoration, (function (param) {
          Editor$AgdaModeVscode.Decoration.destroy(param[0]);
          Editor$AgdaModeVscode.Decoration.destroy(param[1]);
        }));
  self.goals.delete(goal.index);
  self.positions.remove(goal.start);
}

function destroyGoalByIndex(self, index) {
  var goal = self.goals.get(index);
  if (goal !== undefined) {
    return destroyGoal(self, goal);
  }
  
}

function isBusy(self) {
  return Core__Option.isSome(self.isBusy);
}

function setBusy(self) {
  var match = self.isBusy;
  if (match !== undefined) {
    return ;
  } else {
    self.isBusy = Caml_option.some(Resource$AgdaModeVscode.make());
    return ;
  }
}

function waitUntilNotBusy(self) {
  var resource = self.isBusy;
  if (resource !== undefined) {
    return Resource$AgdaModeVscode.get(Caml_option.valFromOption(resource));
  } else {
    return Promise.resolve();
  }
}

function serialize(self) {
  return Array.from(self.goals.values()).toSorted(function (x, y) {
                return Caml.int_compare(x.index, y.index);
              }).map(toString);
}

function read(goal, $$document) {
  var innerRange = makeInnerRange(goal, $$document);
  return Editor$AgdaModeVscode.$$Text.get($$document, innerRange).trim();
}

function getGoalByIndex(self, index) {
  return Core__Option.map(self.goals.get(index), (function (goal) {
                return {
                        index: goal.index,
                        indexString: goal.index.toString(),
                        start: goal.start,
                        end: goal.end
                      };
              }));
}

async function modify(self, $$document, index, f) {
  var goal = self.goals.get(index);
  if (goal === undefined) {
    return ;
  }
  var innerRange = makeInnerRange(goal, $$document);
  var goalContent = read(goal, $$document);
  await Editor$AgdaModeVscode.$$Text.replace($$document, innerRange, " " + f(goalContent) + " ");
}

async function removeBoundaryAndDestroy(self, $$document, index) {
  var goal = self.goals.get(index);
  if (goal === undefined) {
    return true;
  }
  var outerRange = makeOuterRange(goal, $$document);
  var content = read(goal, $$document);
  if (await Editor$AgdaModeVscode.$$Text.replace($$document, outerRange, content)) {
    destroyGoal(self, goal);
    return true;
  } else {
    return false;
  }
}

function getGoalAtCursor(self, editor) {
  var $$document = editor.document;
  var cursorOffset = $$document.offsetAt(Editor$AgdaModeVscode.Cursor.get(editor));
  return Core__Option.flatMap(AVLTree$AgdaModeVscode.lowerBound(self.positions, cursorOffset), (function (index) {
                var goal = self.goals.get(index);
                if (goal !== undefined && cursorOffset >= goal.start && cursorOffset < goal.end) {
                  return {
                          index: goal.index,
                          indexString: goal.index.toString(),
                          start: goal.start,
                          end: goal.end
                        };
                }
                
              }));
}

function clear(self) {
  Array.from(self.goals.values()).forEach(function (goal) {
        Core__Option.forEach(goal.decoration, (function (param) {
                Editor$AgdaModeVscode.Decoration.destroy(param[0]);
                Editor$AgdaModeVscode.Decoration.destroy(param[1]);
              }));
      });
  self.goals = new Map();
  self.positions = new BinarySearchTree.BinarySearchTree();
}

function updateGoalPositionByIndex(self, editor, index, deltaStart, deltaEnd) {
  var goal = self.goals.get(index);
  if (goal !== undefined) {
    var newStart = goal.start + deltaStart | 0;
    var newEnd = goal.end + deltaEnd | 0;
    self.positions.remove(goal.start);
    var wasQuestionMark = (goal.start + 1 | 0) === goal.end;
    var isQuestionMark = (newStart + 1 | 0) === newEnd;
    var isQuestionMarkExpansion = wasQuestionMark && !isQuestionMark;
    var updatedGoal = isQuestionMarkExpansion ? (destroyGoal(self, goal), make(editor, newStart, newEnd, goal.index)) : ({
          index: goal.index,
          start: newStart,
          end: newEnd,
          decoration: goal.decoration
        });
    self.positions.insert(newStart, updatedGoal.index);
    self.goals.set(updatedGoal.index, updatedGoal);
    return ;
  }
  
}

function boundaryToString(x) {
  if (typeof x !== "object") {
    if (x === "Damaged") {
      return "Damaged";
    } else {
      return "Unknown";
    }
  } else {
    return "Intact(" + String(x._0) + ")";
  }
}

function toString$1(x) {
  if (x.TAG === "IsQuestionMark") {
    return "QM " + String(x._0);
  } else {
    return "Hole " + boundaryToString(x._0) + " " + boundaryToString(x._1);
  }
}

function toString$2(map) {
  return Array.from(map.entries()).map(function (param) {
                return "#" + param[0].toString() + ": " + toString$1(param[1]);
              }).join("\n");
}

function addLeftBoundary(map, index, delta) {
  var match = map.get(index);
  if (match !== undefined) {
    if (match.TAG === "IsQuestionMark") {
      return map;
    }
    var tmp = match._0;
    if (typeof tmp === "object") {
      return map;
    }
    if (tmp === "Damaged") {
      return map;
    }
    map.set(index, {
          TAG: "IsHole",
          _0: {
            TAG: "Intact",
            _0: delta,
            [Symbol.for("name")]: "Intact"
          },
          _1: match._1,
          [Symbol.for("name")]: "IsHole"
        });
    return map;
  } else {
    map.set(index, {
          TAG: "IsHole",
          _0: {
            TAG: "Intact",
            _0: delta,
            [Symbol.for("name")]: "Intact"
          },
          _1: "Unknown",
          [Symbol.for("name")]: "IsHole"
        });
    return map;
  }
}

function markLeftBoundaryDamaged(map, index) {
  var match = map.get(index);
  if (match !== undefined) {
    if (match.TAG === "IsQuestionMark") {
      console.log("Cannot mark left boundary damaged for a question mark");
      return map;
    }
    map.set(index, {
          TAG: "IsHole",
          _0: "Damaged",
          _1: match._1,
          [Symbol.for("name")]: "IsHole"
        });
    return map;
  }
  map.set(index, {
        TAG: "IsHole",
        _0: "Damaged",
        _1: "Unknown",
        [Symbol.for("name")]: "IsHole"
      });
  return map;
}

function addRightBoundary(map, index, delta) {
  var match = map.get(index);
  if (match !== undefined) {
    if (match.TAG === "IsQuestionMark") {
      return map;
    }
    var tmp = match._1;
    if (typeof tmp === "object") {
      return map;
    }
    if (tmp === "Damaged") {
      return map;
    }
    map.set(index, {
          TAG: "IsHole",
          _0: match._0,
          _1: {
            TAG: "Intact",
            _0: delta,
            [Symbol.for("name")]: "Intact"
          },
          [Symbol.for("name")]: "IsHole"
        });
    return map;
  } else {
    map.set(index, {
          TAG: "IsHole",
          _0: "Unknown",
          _1: {
            TAG: "Intact",
            _0: delta,
            [Symbol.for("name")]: "Intact"
          },
          [Symbol.for("name")]: "IsHole"
        });
    return map;
  }
}

function markRightBoundaryDamaged(map, index) {
  var match = map.get(index);
  if (match !== undefined) {
    if (match.TAG === "IsQuestionMark") {
      console.log("Cannot mark right boundary damaged for a question mark");
      return map;
    }
    map.set(index, {
          TAG: "IsHole",
          _0: match._0,
          _1: "Damaged",
          [Symbol.for("name")]: "IsHole"
        });
    return map;
  }
  map.set(index, {
        TAG: "IsHole",
        _0: "Unknown",
        _1: "Damaged",
        [Symbol.for("name")]: "IsHole"
      });
  return map;
}

function addQuestionMark(map, index, offset) {
  var match = map.get(index);
  if (match !== undefined) {
    return map;
  } else {
    map.set(index, {
          TAG: "IsQuestionMark",
          _0: offset,
          [Symbol.for("name")]: "IsQuestionMark"
        });
    return map;
  }
}

function goalToParts(goal) {
  if ((goal.start + 1 | 0) === goal.end) {
    return [[
              goal.index,
              goal.start,
              "QuestionMark"
            ]];
  } else {
    return [
            [
              goal.index,
              goal.start,
              "LeftBoundary"
            ],
            [
              goal.index,
              goal.end - 2 | 0,
              "RightBoundary"
            ]
          ];
  }
}

function caseAnalysis(removalStart, removalEnd, start, end) {
  if (removalStart < start) {
    if (removalEnd <= start) {
      return "Case1";
    } else if (removalEnd <= end) {
      return "Case2";
    } else {
      return "Case3";
    }
  } else if (removalEnd <= end) {
    return "Case4";
  } else if (removalStart < end) {
    return "Case5";
  } else {
    return "Case6";
  }
}

async function checkAllParts(self, editor, changes) {
  console.log(" ==== Checking all parts ====");
  var $$document = editor.document;
  var changes$1 = Core__List.fromArray(changes);
  var go = function (_accMap, _accDeltaBeforePart, _accDeltaAfterPart, _parts, _changes) {
    while(true) {
      var changes = _changes;
      var parts = _parts;
      var accDeltaAfterPart = _accDeltaAfterPart;
      var accDeltaBeforePart = _accDeltaBeforePart;
      var accMap = _accMap;
      if (!parts) {
        return accMap;
      }
      var parts$1 = parts.tl;
      var match = parts.hd;
      var part = match[2];
      var start = match[1];
      var index = match[0];
      if (changes) {
        var changes$1 = changes.tl;
        var change = changes.hd;
        var removalStart = change.offset;
        var removalEnd = change.offset + change.removed | 0;
        var delta = change.inserted - change.removed | 0;
        var end;
        switch (part) {
          case "LeftBoundary" :
          case "RightBoundary" :
              end = start + 2 | 0;
              break;
          case "QuestionMark" :
              end = start + 1 | 0;
              break;
          
        }
        var match$1 = caseAnalysis(removalStart, removalEnd, start, end);
        switch (match$1) {
          case "Case1" :
              _changes = changes$1;
              _parts = {
                hd: [
                  index,
                  start,
                  part
                ],
                tl: parts$1
              };
              _accDeltaAfterPart = accDeltaAfterPart + delta | 0;
              _accDeltaBeforePart = accDeltaBeforePart + delta | 0;
              continue ;
          case "Case2" :
              var tmp;
              switch (part) {
                case "LeftBoundary" :
                    tmp = markLeftBoundaryDamaged(accMap, index);
                    break;
                case "RightBoundary" :
                    tmp = markRightBoundaryDamaged(accMap, index);
                    break;
                case "QuestionMark" :
                    tmp = accMap;
                    break;
                
              }
              _changes = changes$1;
              _parts = {
                hd: [
                  index,
                  start,
                  part
                ],
                tl: parts$1
              };
              _accDeltaAfterPart = accDeltaAfterPart + delta | 0;
              _accMap = tmp;
              continue ;
          case "Case3" :
              var tmp$1;
              switch (part) {
                case "LeftBoundary" :
                    tmp$1 = markLeftBoundaryDamaged(accMap, index);
                    break;
                case "RightBoundary" :
                    tmp$1 = markRightBoundaryDamaged(accMap, index);
                    break;
                case "QuestionMark" :
                    tmp$1 = accMap;
                    break;
                
              }
              _changes = {
                hd: change,
                tl: changes$1
              };
              _parts = parts$1;
              _accMap = tmp$1;
              continue ;
          case "Case4" :
              switch (part) {
                case "LeftBoundary" :
                case "RightBoundary" :
                    break;
                case "QuestionMark" :
                    _changes = changes$1;
                    _parts = parts$1;
                    _accDeltaAfterPart = accDeltaAfterPart + delta | 0;
                    _accDeltaBeforePart = accDeltaAfterPart + delta | 0;
                    _accMap = addRightBoundary(addLeftBoundary(accMap, index, accDeltaBeforePart), index, accDeltaBeforePart + 6 | 0);
                    continue ;
                
              }
              _changes = changes$1;
              _parts = {
                hd: [
                  index,
                  start,
                  part
                ],
                tl: parts$1
              };
              _accDeltaAfterPart = accDeltaAfterPart + delta | 0;
              _accMap = markLeftBoundaryDamaged(accMap, index);
              continue ;
          case "Case5" :
              var tmp$2;
              switch (part) {
                case "LeftBoundary" :
                    tmp$2 = markLeftBoundaryDamaged(accMap, index);
                    break;
                case "RightBoundary" :
                    tmp$2 = markRightBoundaryDamaged(accMap, index);
                    break;
                case "QuestionMark" :
                    tmp$2 = accMap;
                    break;
                
              }
              _changes = {
                hd: change,
                tl: changes$1
              };
              _parts = parts$1;
              _accMap = tmp$2;
              continue ;
          case "Case6" :
              var tmp$3;
              switch (part) {
                case "LeftBoundary" :
                    tmp$3 = addLeftBoundary(accMap, index, accDeltaBeforePart);
                    break;
                case "RightBoundary" :
                    tmp$3 = addRightBoundary(accMap, index, accDeltaBeforePart);
                    break;
                case "QuestionMark" :
                    tmp$3 = addQuestionMark(accMap, index, start + accDeltaBeforePart | 0);
                    break;
                
              }
              _changes = {
                hd: change,
                tl: changes$1
              };
              _parts = parts$1;
              _accDeltaBeforePart = accDeltaAfterPart;
              _accMap = tmp$3;
              continue ;
          
        }
      } else {
        var tmp$4;
        switch (part) {
          case "LeftBoundary" :
              tmp$4 = addLeftBoundary(accMap, index, accDeltaBeforePart);
              break;
          case "RightBoundary" :
              tmp$4 = addRightBoundary(accMap, index, accDeltaBeforePart);
              break;
          case "QuestionMark" :
              tmp$4 = addQuestionMark(accMap, index, start + accDeltaBeforePart | 0);
              break;
          
        }
        _parts = parts$1;
        _accDeltaBeforePart = accDeltaAfterPart;
        _accMap = tmp$4;
        continue ;
      }
    };
  };
  var parts = Core__List.fromArray(Core__Array.filterMap(AVLTree$AgdaModeVscode.toArray(self.positions), (function (index) {
                return Core__Option.map(self.goals.get(index), goalToParts);
              })).flat());
  console.log("Parts: " + Util$AgdaModeVscode.Pretty.list(Core__List.map(parts, (function (param) {
                  var start = param[1];
                  var tmp;
                  switch (param[2]) {
                    case "LeftBoundary" :
                        tmp = "{! " + start.toString();
                        break;
                    case "RightBoundary" :
                        tmp = "!} " + start.toString();
                        break;
                    case "QuestionMark" :
                        tmp = "? " + start.toString();
                        break;
                    
                  }
                  return "#" + param[0].toString() + " " + tmp;
                }))));
  var map = go(new Map(), 0, 0, parts, changes$1);
  console.log("States: " + toString$2(map));
  var rewrites = Core__Array.filterMap(Array.from(map.entries()), (function (param) {
          var state = param[1];
          var index = param[0];
          if (state.TAG === "IsQuestionMark") {
            var offset = state._0;
            return [
                    new Vscode.Range($$document.positionAt(offset), $$document.positionAt(offset + 1 | 0)),
                    "{!   !}"
                  ];
          }
          var a = state._0;
          if (typeof a !== "object") {
            a === "Damaged";
          } else {
            var b = state._1;
            if (typeof b !== "object") {
              b === "Damaged";
            } else {
              updateGoalPositionByIndex(self, editor, index, a._0, b._0);
              return ;
            }
          }
          console.log("Destroying goal #" + index.toString());
          destroyGoalByIndex(self, index);
        }));
  console.log("Rewrites: " + rewrites.map(function (param) {
              return "Rewrite(" + Editor$AgdaModeVscode.$$Range.toString(param[0]) + ", \"" + param[1] + "\")";
            }).join(", "));
  if (rewrites.length !== 0) {
    setBusy(self);
    await Editor$AgdaModeVscode.$$Text.batchReplace($$document, rewrites);
    return ;
  } else {
    var resource = self.isBusy;
    if (resource !== undefined) {
      self.isBusy = undefined;
      return Resource$AgdaModeVscode.set(Caml_option.valFromOption(resource), undefined);
    } else {
      return ;
    }
  }
}

async function resetGoalIndices(self, editor, indices) {
  clear(self);
  Array.from(self.goalsWithoutIndices.entries()).forEach(function (param, i) {
        var start = param[0];
        var index = indices[i];
        if (index === undefined) {
          return ;
        }
        var goal = make(editor, start, param[1], index);
        self.goals.set(index, goal);
        self.positions.insert(start, index);
      });
  self.goalsWithoutIndices = new Map();
  return await checkAllParts(self, editor, []);
}

function addGoalPositions(self, positions) {
  positions.forEach(function (param) {
        self.goalsWithoutIndices.set(param[0], param[1]);
      });
}

function parseGoalPositionsFromRefine(raw) {
  var goalQuestionMark = /([\s\(\{\_\;\.\\\"@]|^)(\?)([\s\)\}\_\;\.\\\"@]|$)/gm;
  var chunks = raw.split(goalQuestionMark);
  return Core__Array.reduce(chunks, [
                [],
                0
              ], (function (param, chunk) {
                  var i = param[1];
                  var offsets = param[0];
                  if (chunk !== undefined) {
                    if (chunk === "?") {
                      return [
                              Belt_Array.concatMany([
                                    offsets,
                                    [[
                                        i,
                                        i + 1 | 0
                                      ]]
                                  ]),
                              i + 1 | 0
                            ];
                    } else {
                      return [
                              offsets,
                              i + chunk.length | 0
                            ];
                    }
                  } else {
                    return [
                            offsets,
                            i
                          ];
                  }
                }))[0];
}

function jmupToGoal(editor, goal) {
  var $$document = editor.document;
  var spaceInsideBoundaries = (goal.end - goal.start | 0) - 4 | 0;
  var offset = spaceInsideBoundaries === 0 ? goal.start + 2 | 0 : goal.start + 3 | 0;
  var position = $$document.positionAt(offset);
  Editor$AgdaModeVscode.Cursor.set(editor, position);
}

function jmupToTheNextGoal(self, editor) {
  var $$document = editor.document;
  var cursorOffset = $$document.offsetAt(Editor$AgdaModeVscode.Cursor.get(editor));
  var index = AVLTree$AgdaModeVscode.upperBound(self.positions, cursorOffset);
  var goal = index !== undefined ? self.goals.get(index) : self.goals.get(0);
  if (goal !== undefined) {
    return jmupToGoal(editor, goal);
  }
  
}

function jmupToThePreviousGoal(self, editor) {
  var $$document = editor.document;
  var cursorOffset = $$document.offsetAt(Editor$AgdaModeVscode.Cursor.get(editor));
  var index = AVLTree$AgdaModeVscode.lowerBound(self.positions, cursorOffset);
  var goal;
  if (index !== undefined) {
    var goal$1 = self.goals.get(index);
    if (goal$1 !== undefined) {
      if (cursorOffset >= goal$1.start && cursorOffset < goal$1.end) {
        var previousIndex = index === 0 ? self.goals.size - 1 | 0 : index - 1 | 0;
        goal = self.goals.get(previousIndex);
      } else {
        goal = goal$1;
      }
    } else {
      goal = undefined;
    }
  } else {
    goal = self.goals.get(self.goals.size - 1 | 0);
  }
  if (goal !== undefined) {
    return jmupToGoal(editor, goal);
  }
  
}

function markAsCaseSplited(self, goal) {
  self.recentlyCaseSplited = goal;
}

function getRecentlyCaseSplited(self) {
  return Core__Option.map(self.recentlyCaseSplited, (function (goal) {
                return {
                        index: goal.index,
                        indexString: goal.index.toString(),
                        start: goal.start,
                        end: goal.end
                      };
              }));
}

var Module = {
  make: make$1,
  size: size,
  resetGoalIndices: resetGoalIndices,
  addGoalPositions: addGoalPositions,
  parseGoalPositionsFromRefine: parseGoalPositionsFromRefine,
  destroyGoalByIndex: destroyGoalByIndex,
  scanAllGoals: checkAllParts,
  getGoalByIndex: getGoalByIndex,
  modify: modify,
  removeBoundaryAndDestroy: removeBoundaryAndDestroy,
  getGoalAtCursor: getGoalAtCursor,
  jmupToTheNextGoal: jmupToTheNextGoal,
  jmupToThePreviousGoal: jmupToThePreviousGoal,
  isBusy: isBusy,
  waitUntilNotBusy: waitUntilNotBusy,
  markAsCaseSplited: markAsCaseSplited,
  getRecentlyCaseSplited: getRecentlyCaseSplited,
  serialize: serialize
};

var scanAllGoals = checkAllParts;

exports.Module = Module;
exports.make = make$1;
exports.size = size;
exports.resetGoalIndices = resetGoalIndices;
exports.addGoalPositions = addGoalPositions;
exports.parseGoalPositionsFromRefine = parseGoalPositionsFromRefine;
exports.destroyGoalByIndex = destroyGoalByIndex;
exports.scanAllGoals = scanAllGoals;
exports.getGoalByIndex = getGoalByIndex;
exports.modify = modify;
exports.removeBoundaryAndDestroy = removeBoundaryAndDestroy;
exports.getGoalAtCursor = getGoalAtCursor;
exports.jmupToTheNextGoal = jmupToTheNextGoal;
exports.jmupToThePreviousGoal = jmupToThePreviousGoal;
exports.isBusy = isBusy;
exports.waitUntilNotBusy = waitUntilNotBusy;
exports.markAsCaseSplited = markAsCaseSplited;
exports.getRecentlyCaseSplited = getRecentlyCaseSplited;
exports.serialize = serialize;
/* vscode Not a pure module */
