// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Js_dict = require("bs-platform/lib/js/js_dict.js");
var $$Promise = require("reason-promise/lib/js/src/js/promise.bs.js");
var Belt_Option = require("bs-platform/lib/js/belt_Option.js");
var Parser$AgdaModeVscode = require("./Parser.bs.js");
var Dispatcher$AgdaModeVscode = require("./Task/Dispatcher.bs.js");

var dict = {};

function get(fileName) {
  return Js_dict.get(dict, fileName);
}

function getByEditor(editor) {
  return Js_dict.get(dict, Parser$AgdaModeVscode.filepath(editor.document.fileName));
}

function add(fileName, dispatcher) {
  var match = Js_dict.get(dict, fileName);
  if (match !== undefined) {
    return ;
  } else {
    dict[fileName] = dispatcher;
    return ;
  }
}

function rename(oldName, newName) {
  var delete_ = (function (dict, key) {delete dict[key]});
  return Belt_Option.forEach(Js_dict.get(dict, oldName), (function (dispatcher) {
                console.log("[ states ][ rename ]", oldName, newName);
                delete_(dict, oldName);
                return add(newName, dispatcher);
              }));
}

function remove(fileName) {
  var delete_ = (function (dict, key) {delete dict[key]});
  return delete_(dict, fileName);
}

function destroy(fileName) {
  var dispatcher = Js_dict.get(dict, fileName);
  if (dispatcher !== undefined) {
    remove(fileName);
    return Dispatcher$AgdaModeVscode.destroy(dispatcher);
  } else {
    return $$Promise.resolved(undefined);
  }
}

function forceDestroy(fileName) {
  var dispatcher = Js_dict.get(dict, fileName);
  if (dispatcher !== undefined) {
    remove(fileName);
    return Dispatcher$AgdaModeVscode.forceDestroy(dispatcher);
  } else {
    return $$Promise.resolved(undefined);
  }
}

function contains(fileName) {
  return Belt_Option.isSome(Js_dict.get(dict, fileName));
}

exports.dict = dict;
exports.get = get;
exports.getByEditor = getByEditor;
exports.add = add;
exports.rename = rename;
exports.remove = remove;
exports.destroy = destroy;
exports.forceDestroy = forceDestroy;
exports.contains = contains;
/* Promise Not a pure module */
