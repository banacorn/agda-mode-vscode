// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Main$AgdaModeVscode = require("./Main.bs.js");
var Connection__DevALS$AgdaModeVscode = require("../Connection/Download/Connection__DevALS.bs.js");
var Connection__Download$AgdaModeVscode = require("../Connection/Download/Connection__Download.bs.js");
var Connection__Download__Trace$AgdaModeVscode = require("../Connection/Download/Connection__Download__Trace.bs.js");
var Connection__Download__ManagedStorage$AgdaModeVscode = require("../Connection/Download/Connection__Download__ManagedStorage.bs.js");

function determinePlatform() {
  return Promise.resolve({
              TAG: "Ok",
              _0: "Web"
            });
}

function findCommand(_command, _timeoutOpt) {
  return Promise.resolve({
              TAG: "Error",
              _0: "NotFound"
            });
}

async function alreadyDownloaded(globalStorageUri) {
  return await Connection__Download__ManagedStorage$AgdaModeVscode.findAnyWasmDownloaded(globalStorageUri);
}

function resolveDownloadChannel(channel, _useCache) {
  return async function (memento, globalStorageUri, _platform) {
    if (channel === "LatestALS") {
      return {
              TAG: "Error",
              _0: "CannotFindCompatibleALSRelease"
            };
    }
    var repo = Connection__DevALS$AgdaModeVscode.makeRepo(globalStorageUri);
    var error = await Connection__Download$AgdaModeVscode.getReleaseManifestFromGitHub(memento, repo, _useCache);
    if (error.TAG === "Ok") {
      return Connection__DevALS$AgdaModeVscode.toDownloadOrder(error._0, "Web");
    } else {
      return {
              TAG: "Error",
              _0: error._0
            };
    }
  };
}

function download(globalStorageUri, channel, traceOpt) {
  var trace = traceOpt !== undefined ? traceOpt : Connection__Download__Trace$AgdaModeVscode.noop;
  return Connection__Download$AgdaModeVscode.download(globalStorageUri, channel, trace, undefined, undefined);
}

function askUserAboutDownloadPolicy() {
  return Promise.resolve("Yes");
}

var Web = {
  determinePlatform: determinePlatform,
  findCommand: findCommand,
  alreadyDownloaded: alreadyDownloaded,
  resolveDownloadChannel: resolveDownloadChannel,
  download: download,
  askUserAboutDownloadPolicy: askUserAboutDownloadPolicy
};

function make() {
  return Web;
}

function activate(context) {
  return Main$AgdaModeVscode.activate(Web, context);
}

var deactivate = Main$AgdaModeVscode.deactivate;

exports.Web = Web;
exports.make = make;
exports.activate = activate;
exports.deactivate = deactivate;
/* Main-AgdaModeVscode Not a pure module */
