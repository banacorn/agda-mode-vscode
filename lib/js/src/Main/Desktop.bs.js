// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Vscode = require("vscode");
var Nodefs = require("node:fs");
var Belt_Array = require("rescript/lib/js/belt_Array.js");
var Core__Option = require("@rescript/core/lib/js/src/Core__Option.bs.js");
var FS$AgdaModeVscode = require("../FS.bs.js");
var OS$AgdaModeVscode = require("../Util/OS.bs.js");
var Main$AgdaModeVscode = require("./Main.bs.js");
var Util$AgdaModeVscode = require("../Util/Util.bs.js");
var Config$AgdaModeVscode = require("../Config.bs.js");
var Connection__DevALS$AgdaModeVscode = require("../Connection/Download/Connection__DevALS.bs.js");
var Connection__Command$AgdaModeVscode = require("../Connection/Resolver/Connection__Command.bs.js");
var Connection__Download$AgdaModeVscode = require("../Connection/Download/Connection__Download.bs.js");
var Connection__Hardcoded$AgdaModeVscode = require("../Connection/Download/Connection__Hardcoded.bs.js");
var Connection__LatestALS$AgdaModeVscode = require("../Connection/Download/Connection__LatestALS.bs.js");
var Connection__Download__Trace$AgdaModeVscode = require("../Connection/Download/Connection__Download__Trace.bs.js");
var Connection__Download__Platform$AgdaModeVscode = require("../Connection/Download/Connection__Download__Platform.bs.js");

async function alreadyDownloaded(globalStorageUri, channel) {
  var findNestedDownloaded = async function (rootUri, fileName) {
    var entries = await FS$AgdaModeVscode.readDirectory(rootUri);
    if (entries.TAG !== "Ok") {
      return ;
    }
    var sortedEntries = entries._0.toSorted(function (param, param$1) {
          var b = param$1[0];
          var a = param[0];
          if (a > b) {
            return -1;
          } else if (a < b) {
            return 1;
          } else {
            return 0;
          }
        });
    var loop = async function (i) {
      if (i >= sortedEntries.length) {
        return ;
      }
      var match = Belt_Array.getExn(sortedEntries, i);
      if (match[1] !== 2) {
        return await loop(i + 1 | 0);
      }
      var candidateUri = Vscode.Uri.joinPath(rootUri, match[0], fileName);
      var match$1 = await FS$AgdaModeVscode.stat(candidateUri);
      if (match$1.TAG === "Ok") {
        return candidateUri.fsPath;
      } else {
        return await loop(i + 1 | 0);
      }
    };
    return await loop(0);
  };
  switch (channel) {
    case "LatestALS" :
        var uri = Vscode.Uri.joinPath(globalStorageUri, "latest-als", "als");
        var match = await FS$AgdaModeVscode.stat(uri);
        if (match.TAG === "Ok") {
          Util$AgdaModeVscode.log("[ debug ] alreadyDownloaded LatestALS: found", uri.fsPath);
          return uri.fsPath;
        }
        Util$AgdaModeVscode.log("[ debug ] alreadyDownloaded LatestALS: not found", uri.fsPath);
        return ;
    case "DevALS" :
        var alsUri = Vscode.Uri.joinPath(globalStorageUri, "dev-als", "als");
        var match$1 = await FS$AgdaModeVscode.stat(alsUri);
        if (match$1.TAG === "Ok") {
          Util$AgdaModeVscode.log("[ debug ] alreadyDownloaded DevALS: found", alsUri.fsPath);
          return alsUri.fsPath;
        }
        var path = await findNestedDownloaded(Vscode.Uri.joinPath(globalStorageUri, "dev-als"), "als");
        if (path !== undefined) {
          Util$AgdaModeVscode.log("[ debug ] alreadyDownloaded DevALS: nested found", path);
          return path;
        } else {
          Util$AgdaModeVscode.log("[ debug ] alreadyDownloaded DevALS: not found", alsUri.fsPath);
          return ;
        }
    case "Hardcoded" :
        var alsUri$1 = Vscode.Uri.joinPath(globalStorageUri, "hardcoded-als", "als");
        var match$2 = await FS$AgdaModeVscode.stat(alsUri$1);
        if (match$2.TAG === "Ok") {
          var alsPath = alsUri$1.fsPath;
          Util$AgdaModeVscode.log("[ debug ] alreadyDownloaded Hardcoded: native found", alsPath);
          if (OS$AgdaModeVscode.onUnix) {
            await Nodefs.promises.chmod(alsPath, 484);
          }
          return alsPath;
        }
        Util$AgdaModeVscode.log("[ debug ] alreadyDownloaded Hardcoded: native not found", alsUri$1.fsPath);
        var wasmUri = Vscode.Uri.joinPath(globalStorageUri, "hardcoded-als", "als.wasm");
        var match$3 = await FS$AgdaModeVscode.stat(wasmUri);
        if (match$3.TAG === "Ok") {
          Util$AgdaModeVscode.log("[ debug ] alreadyDownloaded Hardcoded: wasm found", wasmUri.toString());
          return wasmUri.toString();
        }
        Util$AgdaModeVscode.log("[ debug ] alreadyDownloaded Hardcoded: nothing found", "");
        return ;
    
  }
}

function resolveDownloadChannel(channel, useCache) {
  return async function (memento, globalStorageUri, platform) {
    switch (channel) {
      case "LatestALS" :
      case "DevALS" :
          break;
      case "Hardcoded" :
          var url = Connection__Hardcoded$AgdaModeVscode.nativeUrlForPlatform(platform);
          if (url !== undefined) {
            return {
                    TAG: "Ok",
                    _0: {
                      TAG: "FromURL",
                      _0: "Hardcoded",
                      _1: url,
                      _2: "hardcoded-als"
                    }
                  };
          } else {
            return {
                    TAG: "Error",
                    _0: "CannotFindCompatibleALSRelease"
                  };
          }
      
    }
    var repo;
    switch (channel) {
      case "DevALS" :
          repo = Connection__DevALS$AgdaModeVscode.makeRepo(globalStorageUri);
          break;
      case "LatestALS" :
      case "Hardcoded" :
          repo = Connection__LatestALS$AgdaModeVscode.makeRepo(globalStorageUri);
          break;
      
    }
    var toDownloadOrder;
    switch (channel) {
      case "LatestALS" :
          toDownloadOrder = (function (__x) {
              return Connection__LatestALS$AgdaModeVscode.toDownloadOrder(__x, platform);
            });
          break;
      case "DevALS" :
          toDownloadOrder = (function (__x) {
              return Connection__DevALS$AgdaModeVscode.toDownloadOrder(__x, platform);
            });
          break;
      case "Hardcoded" :
          toDownloadOrder = (function (__x) {
              return Connection__LatestALS$AgdaModeVscode.toDownloadOrder(__x, platform);
            });
          break;
      
    }
    var error = await Connection__Download$AgdaModeVscode.getReleaseManifestFromGitHub(memento, repo, useCache);
    if (error.TAG === "Ok") {
      return toDownloadOrder(error._0);
    } else {
      return {
              TAG: "Error",
              _0: error._0
            };
    }
  };
}

function download(globalStorageUri, channel, traceOpt) {
  var trace = traceOpt !== undefined ? traceOpt : Connection__Download__Trace$AgdaModeVscode.noop;
  return Connection__Download$AgdaModeVscode.download(globalStorageUri, channel, trace, undefined, undefined);
}

async function askUserAboutDownloadPolicy() {
  var result = await Vscode.window.showWarningMessage("Cannot find Agda or Agda Language Server", {
        modal: true,
        detail: "Do you want to download and install the latest Agda Language Server?"
      }, Config$AgdaModeVscode.Connection.DownloadPolicy.toString("Yes"), Config$AgdaModeVscode.Connection.DownloadPolicy.toString("No"));
  return Core__Option.mapOr(result, "No", Config$AgdaModeVscode.Connection.DownloadPolicy.fromString);
}

var Desktop = {
  determinePlatform: Connection__Download__Platform$AgdaModeVscode.determine,
  findCommand: Connection__Command$AgdaModeVscode.search,
  alreadyDownloaded: alreadyDownloaded,
  resolveDownloadChannel: resolveDownloadChannel,
  download: download,
  askUserAboutDownloadPolicy: askUserAboutDownloadPolicy
};

function make() {
  return Desktop;
}

function activate(context) {
  return Main$AgdaModeVscode.activate(Desktop, context);
}

var deactivate = Main$AgdaModeVscode.deactivate;

exports.Desktop = Desktop;
exports.make = make;
exports.activate = activate;
exports.deactivate = deactivate;
/* vscode Not a pure module */
