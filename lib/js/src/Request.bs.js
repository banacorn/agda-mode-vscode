// Generated by BUCKLESCRIPT, PLEASE EDIT WITH CARE
'use strict';

var Curry = require("bs-platform/lib/js/curry.js");
var Belt_Array = require("bs-platform/lib/js/belt_Array.js");
var Caml_splice_call = require("bs-platform/lib/js/caml_splice_call.js");
var Goal$AgdaModeVscode = require("./Goal.bs.js");
var Util$AgdaModeVscode = require("./Util/Util.bs.js");
var Parser$AgdaModeVscode = require("./Parser.bs.js");
var Command$AgdaModeVscode = require("./Command.bs.js");

function Impl(Editor) {
  var Goal = Goal$AgdaModeVscode.Impl(Editor);
  var encode = function (editor, version, filepath, libraryPath, highlightingMethod, request) {
    libraryPath.unshift(".");
    var libraryPath$1 = Caml_splice_call.spliceObjApply(", ", "concat", [Belt_Array.map(libraryPath, (function (x) {
                  return "\"" + (Parser$AgdaModeVscode.filepath(x) + "\"");
                }))]);
    var highlightingMethod$1 = highlightingMethod ? "Direct" : "Indirect";
    var commonPart = function (level) {
      var level$1;
      switch (level) {
        case /* None */0 :
            level$1 = "None";
            break;
        case /* NonInteractive */1 :
            level$1 = "NonInteractive";
            break;
        case /* Interactive */2 :
            level$1 = "Interactive";
            break;
        
      }
      return "IOTCM \"" + (filepath + ("\" " + (level$1 + (" " + highlightingMethod$1))));
    };
    var buildRange = function (goal) {
      if (Util$AgdaModeVscode.Version.gte(version, "2.5.1")) {
        return Curry._4(Goal.buildHaskellRange, editor, goal, false, filepath);
      } else {
        return Curry._4(Goal.buildHaskellRange, editor, goal, true, filepath);
      }
    };
    if (typeof request === "number") {
      if (Util$AgdaModeVscode.Version.gte(version, "2.5.0")) {
        return commonPart(/* NonInteractive */1) + ("( Cmd_load \"" + (String(filepath) + "\" [] )"));
      } else {
        return commonPart(/* NonInteractive */1) + ("( Cmd_load \"" + (String(filepath) + ("\" [" + (String(libraryPath$1) + "] )"))));
      }
    }
    switch (request.tag | 0) {
      case /* Auto */0 :
          var goal = request[0];
          var index = String(goal.index);
          var content = Curry._2(Goal.getContent, goal, editor);
          var range = buildRange(goal);
          if (Util$AgdaModeVscode.Version.gte(version, "2.6.0.1")) {
            return commonPart(/* NonInteractive */1) + ("( Cmd_autoOne " + (String(index) + (" " + (String(range) + (" \"" + (String(content) + "\" )"))))));
          } else {
            return commonPart(/* NonInteractive */1) + ("( Cmd_auto " + (String(index) + (" " + (String(range) + (" \"" + (String(content) + "\" )"))))));
          }
      case /* InferType */1 :
          var index$1 = String(request[2].index);
          var normalization = Command$AgdaModeVscode.Normalization.toString(request[0]);
          var content$1 = Parser$AgdaModeVscode.userInput(request[1]);
          return commonPart(/* NonInteractive */1) + ("( Cmd_infer " + (String(normalization) + (" " + (String(index$1) + (" noRange \"" + (String(content$1) + "\" )"))))));
      case /* InferTypeGlobal */2 :
          var normalization$1 = Command$AgdaModeVscode.Normalization.toString(request[0]);
          var content$2 = Parser$AgdaModeVscode.userInput(request[1]);
          return commonPart(/* None */0) + ("( Cmd_infer_toplevel " + (String(normalization$1) + (" \"" + (String(content$2) + "\" )"))));
      case /* GoalType */3 :
          var index$2 = String(request[1].index);
          var normalization$2 = Command$AgdaModeVscode.Normalization.toString(request[0]);
          return commonPart(/* NonInteractive */1) + ("( Cmd_goal_type " + (String(normalization$2) + (" " + (String(index$2) + " noRange \"\" )"))));
      
    }
  };
  return {
          Goal: Goal,
          encode: encode
        };
}

exports.Impl = Impl;
/* Goal-AgdaModeVscode Not a pure module */
